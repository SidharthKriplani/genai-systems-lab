# GSL — THE plan (single source of truth)

> One doc. Work top-down. History at the bottom. Build log detail in `GSL_MASTER_PLAN.md`.

## TARGET (Revision 2 — bold restructure, 2026-07-03)
Frames collapse to **content by learner-verb + an MSL-consistent personal strip**. Concepts (Learn) is the ONE learning home; Company Tracks is the ONE company home; Build gets a real interview simulator.

**Content frames:** Learn · Build · Interview
**Personal strip (mirror MSL exactly):** Home · Profile · My Progress · Review · My Tracks · Leaderboard · Start Here · Plans & Access · Resources · About

- **Learn** = Foundations · Ground Truth · **Domain Labs** (Retrieval / Agents / Evaluation / Production — each = Learn modules + Lab + Case Chains) · **Playground sandboxes** · **Prompt Engineering**. (Practice frame dissolves INTO Learn. Playground + Domain Labs live here.)
- **Agents = ONE entry** (Agent Lab is the home; the old "agentshub" duplicate row is dropped, route kept).
- **Build** = **Design Studio** (System Design + Take-home MERGED into a staged interview simulator w/ rubric) · **Code Labs** (+ more skeletons: RAG pipeline, orchestrator, eval harness, guardrails).
- **Interview** = **Question Bank** (w/ company logos) · **Speaking & Mock** · **Company Tracks** (absorbs Questions-by-company + Interview Signal + Company Scenarios).
- **Me strip reverts** to the flat MSL-style personal list above (no "Me hub").

## THE FIX LIST — Revision 2 worklist (build ALL in one shot)
- [x] **R1. Frames → Learn/Build/Interview.** DONE (Wave-1 nav). Practice ("do") frame dissolved; NAV_SECTIONS = Learn/Build/Interview. Under Learn: Foundations, Ground Truth, "Domain Labs" header → Retrieval / Agent Lab / Evaluation / Production, "Sandboxes & drills" header → Playground + Prompt Engineering (routes to `concepts` gym via `route:"concepts"`, note: no dedicated PE route id) + Code Drills(soon), "Sister labs" header → Python·DSA / SQL link-outs. TAB_FRAME repointed all former-"do" ids → "know".
- [x] **R2. Agents unify** — DONE. Single "Agent Lab" row (`id: agents`) under Domain Labs. `agentshub` row dropped; its route/render/VALID_VIEWS/#hash intact; TAB_FRAME maps `agentshub → know`.
- [x] **R3. Personal strip → mirror MSL** — DONE. NAV_TRACK = Home · Profile · My Progress(`progress`) · Review · My Tracks · Leaderboard · Start Here · Plans & Access(`plans`) · Resources · About — exact MSL order. `me` hub dropped from nav (route/#me kept). Leaderboard surfaced. Start Here already existed (`StartHere.jsx`, route live) — surfaced. Resources built NEW (`src/Resources.jsx`, GSL dark theme: curated external refs + in-lab jump-offs + sister labs) and fully wired (lazy + VALID_VIEWS + render + hash + GUEST_ALLOWED_TABS + TAB_TITLES + ALL_TABS). Footer duplicate About row removed (route kept).
- [x] **R4. Cut AI Product Judgment** — DONE (nav + salvage). `aipm` row removed from nav; route/render/#aipm intact; TAB_FRAME maps `aipm → know`. **R4-salvage DONE (Wave 4):** Launch Checklist surfaced in Production hub via **reuse-by-import** — `LaunchChecklist` now a named export from `AIPM.jsx` (single source; AIPM stays archived-but-routable), rendered in `ProductionHub.jsx` as a new "Pre-Ship Readiness" section (between the tradeoff card and Key Concepts). No new data file needed; nothing deleted.
- [x] **R5. Build → Design Studio** — DONE (Wave-3). `Career.jsx`: `SystemDesignInterview` rebuilt into `DesignStudio`, a 5-stage staged simulator (Clarify → High-level → Deep-dive → Scale → Defend) with a stage stepper + prev/next nav, ending in a staff-level rubric self-assessment (requirements / components / key tradeoff / bottleneck / judgment). Reuses existing content: the 6 `SYSTEM_DESIGN_PROMPTS` are the scenario bank; the old component-checklist IS stage 2 (coverage grading preserved); each prompt got a new `studio` block (clarify Qs w/ good-vs-skip rationale, deep-dive staff tradeoff, 10x bottleneck, defend-prompt + model answer) authored per scenario. Take-home became "Take-home Mode" (same `TakeHomeChallenge`, unchanged content incl. the 4 free-text scenarios + rubric). Route/id `career`, module ids `sysdesign`/`takehome`, localStorage key `genai_visited_career` all preserved. Surface relabeled "Design Studio" (welcome + nav group). No deferrals — full staged rebuild landed.
- [x] **R6. More Code Lab skeletons** — DONE (Wave-3). `codeLabsData.js`: added 4 roadmap skeletons w/ `status:"upcoming"` + an `outline[]` of planned step titles (real intro: scenario/whatYouBuild/prereqs, no authored steps) — `rag-pipeline-min`, `multi-agent-orchestrator-min`, `eval-harness-min`, `guardrails-moderation-min`. Documented the skeleton schema in the file header; degrades safely on a pre-`status` renderer (valid id/title/intro, empty `steps`). NOTE for the Code Labs browser owner: a small `status:"upcoming"` branch is needed to render `outline[]` as a roadmap + suppress completion (out of my 2-file scope).
- [x] **R7. Company Tracks = the one company home** — DONE (Wave-2). Added an "Everything for this company" 3-tile section in `CompanyTracks.jsx` detail pane: Questions → `onNavigateTo({tab:"preplab", mode:"companyprep"})`, Interview intel → `mode:"intexp"`, Spoken scenarios → sets `localStorage gsl-fluency-initial="cases"` then navigates `#fluency` (FluencyApp reads+clears on mount). Removed **"Questions by company"** (`companyprep`) from `PREPLAB_SIDEBAR` and **"Company scenarios"** (`cases`) from Fluency's registry. Both components + data KEPT in-file; `companyprep`/`intexp` render branches reachable via PrepLab `initialMode`, `cases` via the localStorage handoff. Interview Signal (`intexp`) also stays a PrepLab sidebar mode. **DEFERRED-note:** company-name filtering not wired into `intexp` (it filters by tier/role) — the tile opens the surface, per "else just open the surface." Routes/hashes/keys intact.
- [x] **R8. Question Bank company logos** — DONE (Wave-2). PrepLab landing header shows 3 logos (OpenAI/Anthropic/Google via `CompanyLogo`) + "+12 more · prep by company →", linked to Company Tracks via `onNavigate("company-tracks")`.
- [x] **R9. Interview Sprint → rename "Cheatsheet".** DONE (nav-visible part). It's a PrepLab-internal mode (`PREPLAB_SIDEBAR`, id `sprint`) — relabeled display "Interview Sprint" → "Cheatsheet", id kept. Nav shows PrepLab as "Question Bank" (unchanged). Wave 2 owns any deeper PrepLab mode-registry cleanup.
- [x] **R10. Cut Interview Strategy** — DONE (Wave-2). Removed `jdprep` ("Interview Strategy") from `PREPLAB_SIDEBAR`. Component `InterviewPrepMode` + its `mode==="jdprep"` render branch KEPT in-file (reachable via `initialMode`/stale hash); data intact. Rationale: thin JD-template; readiness "work next" covers it.
- [x] **R11. Browse All → dropdowns** — DONE (Wave-2). `BrowseMode` topic + difficulty pill rows → dark-theme `<select>` dropdowns (`BROWSE_SELECT_STYLE`). Same filter state/behavior; scenarios still excluded.
- [x] **R12. Mock Interview == Timed Drills → merge** — DONE (Wave-2). Named **"Mock Interview"**. Folded `DRILLS` (Timed Drills bank) into the Mock Interview pool via `MERGED_INTERVIEW_QUESTIONS` (DRILLS mapped: `level`→`difficulty`, `topic:"Drill"`). Dropped the `drills` registry row; `TimedDrills` component KEPT in-file (archived, unused). Stale `drills` hash/state → falls back to `<MockInterview/>`.
- [x] **R13. Cut Flashcards; Phrase Bank → absorb into Speak** — DONE (Wave-2). Removed `flashcards` and `phrases` from Fluency's registry (`FlashcardMode`/`PhraseBank` + `FLASHCARDS`/`PHRASES` KEPT in-file). Phrase Bank's weak→strong value ABSORBED into Speak: new `StrongerPhrasingTab` in `SpeakMode.jsx` (6 high-impact upgrades, reveal-on-click) surfaced on the Speak select screen. Stale `phrases`/`flashcards` hash → falls back to `<SpeakMode/>`.
- [x] **R14. Prompts mode removed from Fluency** — DONE (Wave-2). Dropped `prompts` (and merged `challenges`) from Fluency's registry; `PromptModeMerged` + datasets KEPT in-file. Home for prompt-engineering is the Learn gym (routes `concepts`, per R1). Stale `prompts`/`challenges` hash → falls back to `<MockInterview/>`.
- [x] **R15. Speak filters → dropdowns** — DONE (Wave-2). `SpeakMode.jsx` topic + difficulty pill rows → dark-theme `<select>` (`SPEAK_SELECT_STYLE`); question-count moved inline. Same filter behavior.
- [x] **R16. Readiness Check — light enhance** — DONE (Wave-2). Results screen weakest-area block now shows the per-area score ("You scored X/Y here — start here.") + a **"Work on your weakest area → <route>"** deep-link button (`TOPIC_WEAK_ROUTE` maps each topic → a GSL hash view; navigates via `window.location.hash` since FluencyApp gets no `onNavigate`).

## Content verdicts
Strong & KEEP: Agent Lab, PrepLab bank, Foundations (23 core + 4 niche tracks now filled), Speak, Company Tracks (now the hub), Code Labs.
Cut: AI Product Judgment (salvage Launch Checklist), Interview Strategy, Flashcards, System Design + Take-home as-is (→ Design Studio), Phrase Bank standalone.
Merge: Mock=Timed Drills, company content 4→1, prompts→Prompt-Eng foundation, Agent Lab/Agents.

## Log
| Date | Done |
|---|---|
| 2026-07-03 | Rev-1 IA (#1-9) shipped: INTERVIEW frame; Learn/Practice/Build/Interview/Me; dissolved BY DOMAIN; retired/trimmed AIPM; company signposts; widget/MCP dedupe; Fluency/Agent-Lab tighten; 4 niche tracks filled (20 modules). |
| 2026-07-03 | Rev-2 restructure authored (this list). Executing all R1-R16 in one shot. |
| 2026-07-03 | **Wave-3 Build/Design-Studio shipped (R5,R6):** `Career.jsx` System Design + Take-home MERGED into flagship **Design Studio** — a 5-stage staged AI-system-design simulator (`DesignStudio`): Clarify (pick architecture-shaping Qs, reveal the good ones) → High-level (the old component-checklist, coverage grading preserved, as stage 2) → Deep-dive (staff tradeoff on the highest-leverage component) → Scale (what breaks at 10x + the bottleneck) → Defend (judgment prompt + model answer), closing on a 5-point staff rubric self-grade. Reused all 6 `SYSTEM_DESIGN_PROMPTS` as the scenario bank + authored a new `studio` block per prompt; Take-home kept as "Take-home Mode" (content unchanged). `codeLabsData.js` gains 4 roadmap skeletons (`status:"upcoming"` + `outline[]`, real intros): RAG pipeline, multi-agent orchestrator, eval harness, guardrails/moderation — schema documented in header, degrades safely. Route `career` + ids + `genai_visited_career` key preserved. esbuild 2/2 OK. NOTE: Code Labs browser needs a small `status:"upcoming"` render branch to show outlines as roadmap (out of the 2-file scope; flagged for that surface's owner). |
| 2026-07-03 | **Wave-2 PrepLab/Fluency/CompanyTracks shipped (R7,R8,R10,R11,R12,R13,R14,R15,R16):** CompanyTracks now the ONE company home — "Everything for this company" tiles deep-link to Questions (`companyprep`)/Interview intel (`intexp`)/Spoken scenarios (Fluency `cases` via `gsl-fluency-initial` handoff). Retired standalone modes from registries (kept components+data in-file): PrepLab `companyprep`+`jdprep`; Fluency `cases`,`drills`,`phrases`,`flashcards`,`prompts`. PrepLab landing gets company logos (→ Company Tracks). Browse-All + Speak filters → dark `<select>` dropdowns. Timed Drills folded into Mock Interview (`MERGED_INTERVIEW_QUESTIONS`). Phrase Bank absorbed into Speak (`StrongerPhrasingTab`). Readiness Check adds per-area score + "work on weakest area →" deep-link. Files: `src/PrepLab.jsx`, `src/Fluency.jsx`, `src/CompanyTracks.jsx`, `src/SpeakMode.jsx`. esbuild 4/4 OK. **App.jsx untouched** (still resolves — no removed routes/props). DEFERRED: `intexp` company-name filtering (opens surface instead). |
| 2026-07-03 | **Wave-4 R4-salvage shipped:** Launch Checklist salvaged from cut AIPM surface into the Production hub via reuse-by-import — `LaunchChecklist` made a named export in `AIPM.jsx` (single source of truth; AIPM default export + `#aipm` route/render untouched, still archived-but-routable), imported + rendered in `ProductionHub.jsx` as a new additive "Pre-Ship Readiness — the checklist before you ship an LLM feature" section (placed after the tradeoff card, before Key Concepts). No new data file (reuse over data-copy); hard-deleted nothing; routes/hashes/localStorage preserved; GSL dark theme. esbuild: `AIPM.jsx` + `ProductionHub.jsx` both OK. |
| 2026-07-03 | **Wave-1 NAV surgery shipped (R1,R2,R3,R4-nav,R9,R10-nav):** NAV_SECTIONS → Learn/Build/Interview (Practice dissolved into Learn w/ Domain Labs + Playground + Prompt Engineering + sister-lab links); Agents unified to one "Agent Lab" row (agentshub route kept); NAV_TRACK reverted to flat MSL strip (Home·Profile·My Progress·Review·My Tracks·Leaderboard·Start Here·Plans & Access·Resources·About), `me` hub dropped from nav (route kept); new `src/Resources.jsx` built + wired; `aipm` cut from nav (route kept); PrepLab "Interview Sprint"→"Cheatsheet" (id `sprint` kept). All routes/hashes/keys/VALID_VIEWS preserved. Full esbuild sweep: 95/95 files OK. DEFERRED: R4-salvage(Launch Checklist→Production) → Wave 4; R10-removal(Interview Strategy mode) → Wave 2. |
