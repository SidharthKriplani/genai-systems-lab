// src/data/foundationsRunnerData.js — PAL runner content for Foundations tracks (sprint 92c)
// Format per module:
//   scenario: string (2-3 sentences production context)
//   explanation: string[] (array of prose paragraphs)
//   mcq: { question, options: string[], correct: number (0-indexed), explanation: string }
//   takeaway: string (1-2 sentences key insight)
//
// Pilot: Language Models track (10 modules)
// Expand to other tracks in subsequent sprints.

export const RUNNER_DATA = {

  // ── Language Models track ────────────────────────────────────────────────────

  "tokenizer": {
    depthTier: "light",
    interviewWeight: "high",
    scenario: "You're debugging a RAG pipeline where long financial documents are being silently truncated. The model keeps missing key clauses near the end of 15-page contracts, producing hallucinated summaries. Your first debugging step is to understand exactly how many tokens your documents consume — and why the number is higher than you expected.",
    explanation: [
      "A neural network cannot process characters directly — it needs numbers. The naive fix is an integer ID per character, but character-level encoding has two problems: sequences become extremely long (every letter is its own token), and the model has to burn capacity learning morphology — spelling patterns, prefixes, suffixes — instead of meaning. The next naive fix is an integer ID per word — one token per dictionary entry — but English has hundreds of thousands of word forms, 'tokenizing' and 'tokenized' would be unrelated entries, and any word outside the vocabulary is simply unknown (OOV). Vocabulary size explodes and generalization collapses on novel word forms.",
      "Byte Pair Encoding (BPE) resolves both problems by building a vocabulary from subword units. It starts with individual characters and iteratively merges the most frequent adjacent pair — 'e','r' → 'er'; then 'er' with whatever character follows it most often — until a target vocabulary size is reached. Common words like 'the' compress to a single token; rare words like 'collateralized' split into 'collateral' + 'ized' or similar subunits — both in-vocabulary, both meaningful. The direct consequence is that token count ≠ word count. Standard English averages 0.65–0.75 tokens per word, but financial documents, code, and JSON structure all tokenize far less efficiently: a single UUID can be 4–6 tokens, a 10-digit number can be 5+ tokens. This is exactly why the 15-page contracts in this scenario consume more context than expected — you measured word count, not tokens.",
    ],
    mcq: {
      question: "A 500-word English document typically tokenizes to approximately how many tokens?",
      options: [
        "325–375 tokens (correct estimate for standard English prose)",
        "490–510 tokens (roughly one token per word)",
        "750–800 tokens (more tokens than words due to subword splitting)",
        "425–500 tokens (BPE merges common bigrams and trigrams, reducing token count slightly below word count by about 10–15%)",
      ],
      correct: 0,
      explanation: "Standard English prose averages about 0.65–0.75 tokens per word. A 500-word document typically tokenizes to roughly 325–375 tokens — not 1:1. Option B (490–510) is the most common misconception: BPE merges frequent subword pairs so common words often become single tokens rather than mapping one-to-one with English words. Option C (750–800) reverses the direction — standard prose tokenizes more efficiently than 1:1, not less. Option D (425–500) sounds plausible — it reasons that BPE 'merges bigrams and trigrams' which is directionally correct, but badly underestimates the compression rate. BPE builds a vocabulary of common multi-character subwords, not just bigrams: 'tokenization' might be three tokens ('token', 'ization' or similar), and the net effect at the word level is that most common English words map to one or two tokens, pushing the ratio well below 0.9:1 to the 0.65–0.75 range.",
    },
    takeaway: "Tokens ≠ words. Budget your context window in tokens, not words, and always benchmark your actual content type against the tokenizer — financial documents, code, and JSON tokenize very differently from plain English prose.",
  },

  "attention": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "Your team is debugging why a model produces poor extraction quality for key details near the middle of 10K-token documents. A researcher suggests the model may be exhibiting 'attention sink' — where attention mass concentrates on the first and last few tokens. You need to understand the attention mechanism well enough to evaluate whether this diagnosis is plausible and what to do about it.",
    explanation: [
      "Text has long-range dependencies: 'The surgeon who treated the patient agreed with the recommendation.' To connect 'agreed' to its subject 'surgeon' across nine intervening tokens, a model needs to retrieve relevant content from an arbitrary distance. Recurrent Neural Networks (RNNs) handle this by threading a fixed-size hidden state through positions sequentially — but early-position information gets compressed alongside every subsequent position. By token 11, the signal from position 1 is mixed with positions 2–10 and degrades. LSTMs improved this with selective gating, but didn't solve it — even with gating, backpropagation still has to travel through thousands of time steps to reach a long-range dependency, and the gradient degrades across that distance regardless of the gating mechanism. The failure mode is structural: any mechanism that requires gradient flow through a sequential chain loses signal at scale.",
      "Attention abandons sequential compression entirely. Every token simultaneously asks: 'what from the rest of the sequence do I need?' Each token produces three vectors from learned projections: a query Q (what am I looking for?), a key K (what do I expose to others?), and a value V (what I contribute if selected). For token i, the model computes a relevance score against every other position j by taking the dot product of Qi and Kj. Raw dot products have variance that grows with d_k (the key dimension) — at large d_k, scores spread into extreme values, softmax saturates on the largest score, and the remaining tokens receive vanishingly small gradients. Training stalls. Dividing by √d_k keeps scores in a stable range. This scaling is not elegance — it's a practical fix for a gradient problem that appears specifically at larger model dimensions.",
      "The scaled scores pass through softmax to produce a probability distribution over all positions. The model takes a weighted sum of all value vectors V using these probabilities as weights: positions with high attention weight contribute heavily to the output vector, irrelevant positions contribute near zero. A token representing 'agreed' will load content from 'surgeon' because their query-key dot product is high; the filler words between them contribute near nothing. The output — one new vector per position — routes information from wherever each position chose to attend, with no sequential compression in the path.",
      "The cost is quadratic: n queries × n keys = n² attention pairs per layer. A 4K-token sequence has ~16 million pairs per layer; a 16K-token sequence has 256 million. Memory scales the same way — the full n×n attention matrix must be materialized. This is why long-context serving is expensive and why 100K+ context windows required algorithmic changes (Flash Attention), not just bigger hardware. The 'attention sink' diagnosis in this scenario follows directly from these mechanics: token position 0 is always visible to all subsequent positions during training, making it a reliable target for attention mass that doesn't belong to any semantic target — concentration there rather than on mid-document content produces the extraction failures you're seeing.",
    ],
    mcq: {
      question: "Why does full self-attention scale quadratically with sequence length?",
      options: [
        "The embedding dimension doubles at each attention layer as sequence length increases",
        "Each token must compute attention scores against every other token, producing n² attention pairs for n tokens",
        "Tokenization time increases quadratically with longer input sequences",
        "Each attention layer must store a separate positional encoding matrix for every sequence position, and storing these matrices for n positions requires O(n²) memory",
      ],
      correct: 1,
      explanation: "Self-attention computes a query for each of n tokens against keys from all n tokens: n × n = n² attention pairs per layer. For a 32K-token sequence, that's over 1 billion attention pairs. Option B is the correct answer. Option A is false — embedding dimension is a fixed architectural hyperparameter; it doesn't change with sequence length. Option C is wrong — tokenization runs once before attention and is linear in input length; self-attention, not tokenization, is the quadratic step. Option D represents a reasonable-sounding but incorrect explanation: positional encoding does not grow with sequence length in a per-layer matrix sense. RoPE and other position schemes add a fixed-size encoding to each token's Q and K vectors — the position encoding cost is O(n × d), not O(n²). The quadratic cost comes from the attention score matrix itself (each of n queries dot-producted against all n keys), not from positional bookkeeping.",
    },
    takeaway: "Attention enables context-aware representations by letting every token attend to every other — but quadratic cost is why long-context models are expensive, and why attention sinks cause mid-document quality degradation that isn't about model intelligence.",
  },

  "attention-3d": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "You're presenting multi-head attention to a product team reviewing model interpretability reports. The report references 'attention head 4 in layer 12 attending strongly to entity coreference.' Your team needs an intuition for what this means — without the math — to decide whether the finding is significant enough to act on.",
    explanation: [
      "Single-head attention runs one set of Q/K/V projections — the learned linear transformations that determine what query, key, and value space the attention operates in. At any given layer, that single head learns one pattern of relevance across the sequence. But a sentence has multiple independent relationship structures simultaneously: 'The surgeon who treated the patient recommended surgery' has syntactic dependencies (surgeon → recommended), coreference (who → surgeon), and positional patterns — all operating in the same token sequence at the same layer. One projection can't capture all of them; the geometry that makes verb-object pairs similar would conflict with the geometry that makes pronouns similar to their antecedents.",
      "Multi-head attention runs H parallel attention computations on the same input, each with different learned projections in a d/h-dimensional subspace. The H outputs are concatenated and projected back to the full dimension. The parameter and compute cost is approximately the same as one large attention head — H heads of d/h dimensions each have the same total projection parameters as one head of d dimensions. The gain is representational: each head specializes in a different relationship type, and those specializations emerge from training because each specialization reduces next-token prediction loss in a different way across different document types.",
      "When an interpretability report identifies 'attention head 4 in layer 12 attends strongly to entity coreference,' it means that specific set of learned projections causes the model to consistently concentrate attention on the token a pronoun refers back to, across documents. This is actionable: if the model is failing on tasks that depend on coreference resolution, head 4 is a concrete starting point for debugging or targeted fine-tuning. The finding is significant precisely because the specialization is real — not a design decision but an emergent pattern the optimizer found useful enough to maintain.",
    ],
    mcq: {
      question: "What is the primary reason for using multiple attention heads rather than one large attention head of the same total dimension?",
      options: [
        "Multiple smaller heads are computationally cheaper than one large head of equivalent total dimension",
        "Using h heads of dimension d/h each provides h times more parameters than one head of dimension d, giving the model greater capacity",
        "Multiple heads can simultaneously capture different types of relationships (syntactic, semantic, positional) in parallel subspaces",
        "Multiple heads prevent gradient vanishing in the attention layers of very deep networks",
      ],
      correct: 2,
      explanation: "The total computation is roughly the same regardless of head count for a fixed total dimension. The benefit is representational: each head learns different projection matrices, specializing in different relationship types. One large head can only learn one projection — the multi-head structure allows the model to simultaneously capture multiple types of long-range dependencies. Option C is the correct answer. Option A is false — splitting into multiple heads does not reduce computational cost; the total parameter count and FLOPs are approximately equal for a fixed total embedding dimension, so 'computationally cheaper' is not the reason. Option B is the subtler misconception: h heads of dimension d/h each does NOT provide h times more parameters than one head of dimension d. The total projection parameters for h heads are h × (d × d/h) = d², exactly the same as one head of dimension d. More heads means different projections, not more parameters — the representational advantage comes from diversity of subspaces, not from increased parameter count. Option D is false — gradient vanishing in deep networks is addressed by residual connections (skip connections), not by multiple attention heads; head count is about representational diversity, not gradient flow.",
    },
    takeaway: "Multi-head attention's value isn't computational — it's representational. Each head specializes in a different type of relationship, which is why Transformer models generalize across tasks and why individual heads appear interpretable in attribution studies.",
  },

  "transformer": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "An engineer on your team proposes 'just add more layers' to improve a model's reasoning quality on complex multi-step questions. You need to probe whether they understand the actual tradeoffs of depth vs. width, and articulate what makes a Transformer block trainable at scale — because their proposal has no compute budget attached.",
    explanation: [
      "Attention gives every token selective access to every other token — but each attention output is still a linear combination of value vectors. Stacking attention layers compounds this: composing linear functions collapses to a single linear transformation regardless of depth. The model also has no notion of order: attention is a set operation that produces the same output whether token A precedes or follows token B. Two structural gaps remain after attention alone — no nonlinearity per position, no positional awareness — and neither can be fixed by adding more attention layers.",
      "The Feed-Forward Network (FFN) resolves the nonlinearity gap. After each attention sublayer, an FFN applies a learned nonlinear transformation independently to each position: two linear layers with a ReLU or GeLU activation between them, at 4× the hidden dimension. This is where most of the model's parametric knowledge concentrates — factual associations in trained models are predominantly stored in FFN weights. Position is handled separately via positional encoding added to each token's representation before the block stack. Modern models use Rotary Positional Embedding (RoPE), which encodes relative position directly in the attention computation and generalizes to sequences longer than those seen during training.",
      "The third structural requirement is depth, and depth creates a training problem. Backpropagation multiplies gradients through every layer from output to input. Without shortcut paths, gradients at early layers approach zero before the optimizer can use them — the network is too deep to train. Residual connections fix this: output = input + block(input). Each block now learns a correction to the identity path rather than a complete transformation, creating a direct gradient highway from the loss to every layer. Layer normalization, applied after each residual addition, handles a second problem residuals alone don't solve: as blocks stack, activation magnitudes drift — later layers receive inputs at wildly different scales, destabilizing training. Layer norm re-centers and rescales activations after each sublayer, keeping the signal stable regardless of depth. The 'add & layer-norm' in every Transformer block — attention sublayer, then FFN sublayer, each with residual + layer norm — is both gradient highway and activation stabilizer together.",
      "The final choice is causal masking. Encoders use bidirectional attention — every token attends to every other — producing the richest possible per-position representation. This makes encoders ideal for classification, retrieval, and embedding, where you need to understand text, not generate it. Decoders apply a causal mask that prevents each token from attending to future positions, because at generation time those tokens don't exist yet. This mask forces the model to predict each next token from left context only, which is exactly what autoregressive generation requires. In production: embedding models for RAG use encoder attention; generation models use decoder attention. Using a generation model's internal representations for retrieval produces poor results because the causal mask distorts the vector geometry — an asymmetry the 'add more layers' proposal ignores entirely.",
    ],
    mcq: {
      question: "What is the primary purpose of residual connections (skip connections) in a Transformer?",
      options: [
        "They compress the hidden dimension at each layer to reduce overall memory usage",
        "They prevent different attention heads from attending to the same token positions",
        "They allow the model to bypass layers that haven't converged yet during training, adaptively skipping unstable blocks until their weights stabilize",
        "They create a gradient highway through the network, preventing gradient vanishing and enabling training of many layers",
      ],
      correct: 3,
      explanation: "Residual connections add the input of each block to its output — each block learns a residual correction, not a complete transformation. This creates a direct gradient path from the loss to every layer, preventing the vanishing gradient problem that makes very deep networks untrainable without them. Option D is the correct answer. Option A is false — residual connections add the input back, not compress the dimension; a dimension reduction would be a bottleneck, not a skip connection. Option B is false — residual connections operate on representations, not on which positions attention heads can see. Option C describes a plausible-sounding mechanism — 'adaptive layer skipping' — that doesn't exist in standard Transformers. Residual connections do allow a block to output near-zero correction (the identity path still carries signal), but they do not selectively bypass or skip blocks based on training convergence state. Every block always contributes to the forward pass; the skip path just ensures the gradient can reach early layers directly.",
    },
    takeaway: "Depth isn't free: each Transformer layer adds sequential latency, memory, and training compute. Residual connections make depth trainable. In production, model depth directly determines cost-per-token — 'add more layers' needs a compute budget attached to be a real proposal.",
  },

  "seq-parallel": {
    depthTier: "deep",
    interviewWeight: "medium",
    scenario: "Your team is fine-tuning a model on 32K-token legal documents. Training crashes with OOM on your 8×A100 cluster even with gradient checkpointing enabled. A training engineer proposes sequence parallelism. You need to understand what it actually does — and whether it solves your specific problem or just adds infrastructure complexity.",
    explanation: [
      "Attention's O(n²) cost creates two training problems: compute and memory. Compute can be amortized across more hardware. Memory cannot — per-device activation memory is a hard ceiling. At training time, every intermediate value must be stored for the backward pass, and activation memory for attention scales with sequence length: a 32K-token document consumes roughly 4× the activation memory of an 8K-token document across all layers and heads. Gradient checkpointing (recomputing activations during the backward pass instead of storing them) and mixed-precision training (16-bit floats instead of 32-bit) reduce this, but have limits. For 32K-token legal documents on an 8×A100 cluster, if both are already enabled and OOM still occurs, the sequence length itself is the binding constraint.",
      "Sequence parallelism distributes the token sequence across devices: 8 devices each process 4K tokens instead of one device processing 32K. Per-device activation memory scales down linearly with device count. The communication challenge is causal attention: every token needs to attend to all preceding tokens, and when those tokens live on different devices, KV data must travel between them. Ring-Attention handles this efficiently — devices pass KV chunks in a ring topology, each computing its portion of the attention output against each received KV chunk before passing it along. Communication volume scales linearly with sequence length, not quadratically, so the inter-device overhead is bounded.",
      "The infrastructure cost is real: Ring-Attention adds communication latency, requires framework support (Megatron-LM, DeepSpeed, JAX pjit), and makes distributed training significantly harder to debug. For the OOM scenario in this question: gradient checkpointing and mixed precision first if not already enabled, then per-device memory upgrade (A100 40GB → A100 80GB or H100) before implementing sequence parallelism. Sequence parallelism is the right solution when per-device memory is the genuine binding constraint after all single-device mitigations are exhausted — typically above 16K tokens at production fine-tuning scale on fixed hardware.",
    ],
    mcq: {
      question: "What specific problem does sequence parallelism primarily solve during LLM training?",
      options: [
        "It reduces per-device activation memory by splitting the token sequence across devices, enabling training on longer contexts than any single device can hold",
        "It reduces total training FLOPs by allowing devices to skip attention computation for distant tokens",
        "It improves training accuracy by having multiple devices independently process the same sequence and averaging their gradients",
        "It prevents gradient explosion by distributing the backward pass across devices with different learning rates",
      ],
      correct: 0,
      explanation: "Sequence parallelism is a memory solution, not a compute reduction. Activation memory during training scales with sequence length — for 32K+ tokens, it exceeds single-device capacity. Splitting the sequence reduces per-device memory linearly. Option B is false — sequence parallelism distributes the same computation across devices, it doesn't skip any attention pairs. Option C is false — each device processes its own distinct sequence slice (not the same slice); processing the same slice on multiple devices is data parallelism. Option D is wrong — gradient explosion is caused by large weight updates, not long sequences, and is addressed by gradient clipping, not sequence parallelism.",
    },
    takeaway: "Sequence parallelism solves per-device activation memory for very long sequences — but adds significant infrastructure complexity. For most teams, it's a last resort after gradient checkpointing, mixed precision, and hardware upgrades. The payoff is only meaningful above ~16K tokens at production fine-tuning scale.",
  },

  "flashattn": {
    depthTier: "deep",
    interviewWeight: "medium",
    scenario: "A cost proposal on your desk says: switch the inference stack from standard attention to Flash Attention 2 to cut memory usage by 5–10× for 16K context windows, with zero accuracy loss. The number sounds too good — you want to understand exactly why this is possible without changing the math, before approving the migration.",
    explanation: [
      "Standard attention materializes the full N×N attention score matrix — query×key scores for every token pair — in HBM (High Bandwidth Memory: the main GPU memory at 40–80GB on an A100/H100) before applying softmax and multiplying by values. For a 16K-token sequence in float16: 16,384 × 16,384 × 2 bytes ≈ 512MB per layer, just for attention scores. This matrix is written to HBM, read back for softmax, written again, read back for the value multiplication. The GPU's arithmetic units are fast; HBM is not. The attention computation at long sequences is memory-bandwidth-bound — the bottleneck is moving data between HBM and the compute units, not the arithmetic itself.",
      "Flash Attention eliminates the HBM bottleneck by keeping the computation in SRAM — the on-chip memory (~20MB, ~10× faster than HBM but far smaller). The computation proceeds in tiles small enough to fit in SRAM, with all intermediate values staying on-chip. The obstacle: standard softmax requires seeing all N scores in a row before normalizing. Flash Attention uses the online softmax trick: compute the exact softmax-normalized output by iterating over tiles while maintaining two running statistics per row — the running maximum and the running sum. As each new tile arrives, both statistics update and the partial output rescales. The result is mathematically bit-identical to standard attention — not an approximation, the same numbers produced by a different computation order.",
      "The memory saving comes entirely from the tiling: the N×N attention matrix (O(N²) space) is never stored in HBM — only the output matrix (N × d, O(N) space) is written. For N=16K, d=128: standard attention needs 512MB for scores; Flash Attention needs essentially zero extra memory for those scores. The speed improvement comes from the same source: HBM reads and writes drop 5–10× at long sequence lengths, and because the computation is memory-bandwidth-bound, fewer HBM accesses means proportionally faster execution. The 5–10× memory reduction in the cost proposal is a direct consequence of eliminating N×N materialization — not an estimate, a geometric consequence of the algorithm.",
    ],
    mcq: {
      question: "Why does Flash Attention reduce memory usage without changing the mathematical output of attention?",
      options: [
        "It approximates attention by dropping low-weight attention scores below a learned threshold",
        "It computes attention in tiles using fast on-chip SRAM, never materializing the full N×N attention matrix in HBM — using the online softmax trick to maintain correctness",
        "It quantizes the attention weights to 4-bit precision during the forward pass and dequantizes before the output",
        "It reuses the attention weight matrix from the previous layer instead of recomputing it from scratch",
      ],
      correct: 1,
      explanation: "Flash Attention is not an approximation. The online softmax algorithm computes the exact softmax-normalized output by iterating over tiles and maintaining running row-max and row-sum statistics — producing bit-identical results to standard attention. The memory saving comes from never writing the full N×N matrix to HBM. Option B is the correct answer. Option A is wrong — Flash Attention does not drop or threshold any attention scores; it computes the exact same weighted sum as standard attention, just in a memory-efficient order using tiled SRAM computation. Option C is wrong — Flash Attention does not quantize attention weights; it operates in the same numeric precision as standard attention, and the algorithm's correctness guarantee requires full-precision arithmetic throughout. Option D is wrong — Flash Attention recomputes attention from scratch in each tile using the stored Q/K/V matrices; it does not reuse attention weights from a previous layer.",
    },
    takeaway: "Flash Attention is not an approximation — it's the same math computed more efficiently. The entire 4× speedup and 10× memory reduction comes from restructuring memory access patterns (tiled SRAM computation vs. full HBM materialization). Understanding this is what separates 'just use FA2' from knowing why it's safe.",
  },

  "sampling": {
    depthTier: "light",
    interviewWeight: "high",
    scenario: "A product designer asks why the chatbot sometimes gives very different answers to the same question. You explain temperature. They follow up: 'So setting temperature to 0 always gives the best, most accurate answer, right?' You need to correct this misconception precisely — because they're about to set it as the default for all users.",
    explanation: [
      "After the forward pass, the model has raw scores (logits) over its entire vocabulary for the next token. Softmax converts these into a probability distribution. The naive conversion: always pick the highest-probability token. This is greedy decoding — temperature=0 — and it produces deterministic, reproducible output that maximizes per-token likelihood. The problem is that maximizing per-token probability at each step is not the same as maximizing full-response quality. Complex reasoning requires committing to lower-probability intermediate tokens that unlock better final answers — greedy precludes this. 'Temperature=0 is the most accurate setting' is the misconception the product designer is about to deploy for all users.",
      "Temperature scales the logits before softmax: dividing by T < 1 sharpens the distribution (the highest-probability token dominates more), dividing by T > 1 flattens it (lower-probability tokens become more likely). Temperature doesn't change what the model computed — it changes how you sample from that distribution. Top-p (nucleus) sampling restricts the candidate pool to the smallest set of tokens whose cumulative probability exceeds p, dynamically adjusting based on how spread the distribution is. Top-k restricts to exactly the k highest-probability tokens. Task determines the right setting: temperature=0 for structured output (SQL, code, JSON) where exact consistency matters; temperature 0.2–0.7 with top-p 0.9 as a general default; higher temperature for creative generation where exploration is the goal.",
    ],
    mcq: {
      question: "A developer sets temperature=0 to 'get the most accurate answers.' What is the key limitation of this approach?",
      options: [
        "Temperature=0 is significantly more computationally expensive than higher temperature sampling",
        "Temperature=0 causes the model to repeat the same token indefinitely in a degenerate loop",
        "Greedy decoding maximizes per-token probability but can miss globally better answers that require lower-probability intermediate tokens — particularly for multi-step reasoning",
        "Temperature=0 only works correctly when combined with top-k sampling; used alone it produces incoherent output",
      ],
      correct: 2,
      explanation: "Temperature=0 (greedy decoding) is deterministic and maximizes per-step likelihood — but 'most likely next token at every step' ≠ 'highest quality complete response.' Chain-of-thought reasoning often requires committing to a lower-probability intermediate token that unlocks a better final answer — greedy decoding precludes this. Option C is the correct answer. Option A is wrong — inference cost is determined by model size and sequence length, not temperature; temperature=0 has identical compute cost to any other setting. Option B describes a real but rare edge case (repetition degeneration when the model is uncertain), not the primary limitation. Option D is false — temperature=0 is valid on its own; top-k is a separate, independently combinable parameter.",
    },
    takeaway: "Temperature=0 is not 'most accurate' — it's 'most deterministic.' For consistency-critical tasks (structured extraction, SQL, code), low temperature is right. For reasoning, generation, and anything requiring exploration, slightly elevated temperature (0.2–0.7) often outperforms pure greedy. Document your temperature rationale — undocumented temperature changes silently break production behavior.",
  },

  "nextoken": {
    depthTier: "light",
    interviewWeight: "high",
    scenario: "A PM asks why your fine-tuned model 'forgot' general knowledge after domain adaptation on a 5,000-document legal corpus. The model now answers legal questions well but makes basic factual errors it didn't make before fine-tuning. You need to explain this at a mechanism level — and what you'd do differently next time.",
    explanation: [
      "A training signal that requires human labels doesn't scale to trillions of tokens. The next-token objective sidesteps this entirely: at every position in every document, the ground truth is simply the next character of text — no annotation required, infinite training data. Applied across everything humans have written — news, textbooks, code, legal documents, conversations — this signal reinforces every useful pattern in language. Factual associations, multi-step reasoning, code syntax, instruction-following formats all emerge from the same objective. The capabilities weren't designed in; they're what gradient descent finds when next-token prediction is applied at sufficient scale across sufficient diversity.",
      "Fine-tuning continues the exact same CLM objective on a smaller domain corpus — and this is precisely why catastrophic forgetting happens. The optimizer has no preservation mechanism: gradient updates overwrite whatever weight configuration minimized pre-training loss whenever those same weights can also minimize domain loss. The weights encoding 'Paris is the capital of France' get updated when that parameterization happens to be useful for predicting the next token in a legal document. The optimizer doesn't know or care what was there before — it just minimizes the current loss. This is not a bug; it's the expected behavior of unconstrained gradient descent on a new distribution. PEFT methods like LoRA fix it by freezing the base weights entirely and training only small low-rank adapter matrices — the weights encoding general knowledge cannot be overwritten because they're outside the optimizer's update scope.",
    ],
    mcq: {
      question: "Why does full fine-tuning on a small domain corpus cause the model to 'forget' pre-trained general knowledge?",
      options: [
        "The model runs out of context window space to 'store' general knowledge once domain content is added",
        "Domain fine-tuning selectively disables the attention heads that handled general-knowledge queries",
        "The learning rate during fine-tuning is automatically set higher than during pre-training, erasing prior gradients",
        "Gradient updates for domain-specific token prediction overwrite the weight values that encoded general knowledge — the optimizer has no mechanism to preserve prior learning",
      ],
      correct: 3,
      explanation: "The optimizer minimizes loss on the fine-tuning corpus and updates weights accordingly, regardless of what those weights encoded during pre-training. There's no loss term for 'preserve general knowledge' — that information simply gets overwritten if the same weights are useful for domain prediction. Option D is the correct answer. Option A is wrong — the model has no 'context window' for storing knowledge; knowledge lives in the weights. Option B is false — fine-tuning doesn't selectively disable attention heads; it updates weight matrices across the full model. Option C is wrong — fine-tuning typically uses a LOWER learning rate than pre-training, not a higher one; even so, lower LR reduces but does not eliminate catastrophic forgetting.",
    },
    takeaway: "Next-token prediction is both the source of LLM capability and the root of fine-tuning fragility. Full fine-tuning overwrites pre-trained knowledge. The production answer is almost always LoRA or another PEFT method — fine-tune the delta, not the base model.",
  },

  "tempgame": {
    depthTier: "light",
    interviewWeight: "medium",
    scenario: "Your team is calibrating a customer-facing summarization model for a healthcare company. The accuracy team insists on temperature=0 for clinical consistency; the UX team says responses feel robotic and repetitive to patients. You need to resolve this disagreement with a principled decision — and explain why there's no single universally correct temperature setting.",
    explanation: [
      "Sampling established that the model selects each token from a probability distribution over its vocabulary. Temperature is the single parameter that controls how concentrated or spread that distribution is. Low temperature (approaching 0) makes the distribution increasingly peaked — the model almost always selects its highest-probability token, producing consistent, deterministic-looking output. High temperature flattens the distribution — lower-probability tokens become more likely, producing varied, less predictable output. The choice between them is not a quality dial; it's a task appropriateness dial. Consistency-critical tasks — structured extraction, SQL generation, classification, clinical fact queries where the same question should always produce the same answer — benefit from low temperature (0.0–0.3). Tasks where naturalness and avoiding repetition matter — patient-facing summaries, open-ended generation — benefit from moderate temperature (0.5–0.7).",
      "The accuracy team and UX team are both correct within their domain. Temperature=0 produces robotic repetition through a specific mechanism: when no single next token has an overwhelmingly high probability — which happens whenever the model is uncertain in open-ended prose generation — greedy decoding gets stuck in repetition loops because the same token remains the local maximum at each step. Top-p sampling (nucleus sampling — restricting generation to the smallest set of tokens whose cumulative probability exceeds p, typically 0.9) combined with moderate temperature (0.5–0.7) prevents this without opening the door to incoherence. The right resolution for the healthcare scenario: per-use-case calibration, not a global setting. Clinical fact queries (drug interactions, dosing) use temperature=0 or near-0. Patient-facing prose summaries use temperature=0.3–0.5 with top-p=0.9. Document every temperature choice — undocumented changes are one of the most common sources of unexplained production behavior shifts.",
    ],
    mcq: {
      question: "For a medical information retrieval task where clinical accuracy and consistency are critical, which sampling configuration is most appropriate?",
      options: [
        "Low temperature (0.0–0.2) to maximize determinism and consistency, accepting that responses may be more formulaic",
        "High temperature (0.9–1.0) to ensure diverse coverage of medical information across multiple conditions",
        "Top-k=1 alone, never combined with temperature control, for guaranteed single-answer outputs",
        "Temperature=0.5 with top-k=5 to balance creativity and accuracy for all medical queries",
      ],
      correct: 0,
      explanation: "For accuracy-critical applications — medical, legal, financial — determinism is more valuable than variety. Low temperature (0.0–0.2) ensures the model consistently selects its highest-confidence output. Option B (high temperature 0.9–1.0) increases the probability of low-confidence tokens appearing, raising the risk of rare but consequential hallucinations in medical context — the wrong tradeoff. Option C (top-k=1 alone) is mathematically equivalent to temperature=0 but isn't the idiomatic configuration; combining temperature and top-k/top-p is how sampling is typically implemented in production. Option D (temperature=0.5 with top-k=5) is a general creative writing setting that introduces unnecessary variance into critical clinical queries.",
    },
    takeaway: "Temperature is a dial calibrated to the task, not a single global 'correct' setting. Low for accuracy-critical consistency; moderate for natural language quality; higher for creative exploration. Per-use-case calibration is the production answer — document every temperature choice, because silent changes break user trust.",
  },

  "training-signal": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "A customer reports that your model gives highly confident wrong answers on niche financial regulatory questions — specific edge cases that appear in maybe two or three documents in any corpus. A colleague says 'the model doesn't know what it doesn't know.' You need to explain the mechanism of hallucination precisely enough to design a mitigation strategy — not just describe the symptom.",
    explanation: [
      "Next-token prediction gave the model the ability to produce fluent, coherent text conditioned on any input. The training objective optimized for exactly one thing: statistical accuracy at predicting each token in human-written text. What it did not optimize for — what the objective has no signal for — is whether the model's generated token is factually true or whether the model has reliable knowledge of the underlying claim. These are different objectives, and the training signal conflates them entirely.",
      "The internet — the primary pre-training corpus — is written by humans who assert rather than hedge. Published articles, books, Stack Overflow answers, legal documents: all are written in the register of stated knowledge, not epistemic uncertainty. 'The Basel III capital ratio is 8%' gets published and indexed; 'I think the Basel III capital ratio might be around 8% but this varies' is cleaned up in editing. The model learned the distribution of human claims, not the distribution of human knowledge. Confident prose is not evidence of reliable knowledge — it is evidence that humans write confidently, which the model absorbed and reproduces.",
      "When the model encounters a question about a niche regulatory detail that appeared in two or three documents in any corpus — the scenario's specific failure — the next-token predictor has very little signal about which tokens should follow that question. But it has strong signal about what confident-sounding regulatory answers look like in general. The softmax distribution spreads over plausible-sounding regulatory language: specific percentages, regulatory body names, article numbers. The model samples from this distribution, producing a concrete, confident answer that has no reliable grounding — because the training signal for that specific domain was insufficient to learn accurate parametric facts, but sufficient to learn what regulatory answers look like.",
      "The framing 'the model doesn't know what it doesn't know' is technically correct but obscures the mechanism. The model doesn't have a knowing dimension — it has a probability distribution over next tokens. There is no internal representation of confidence that correlates reliably with factual accuracy for rare topics. High-frequency facts (Paris is the capital of France) have consistent training signal across millions of examples — the distribution is tight and confident generation correctly predicts accuracy. Low-frequency regulatory facts have sparse, noisy training signal — the distribution looks superficially confident but is capturing 'what regulatory answers sound like' rather than 'what the correct answer is.' These produce identical surface outputs.",
      "Calibrated uncertainty requires explicit training signal the base next-token objective doesn't provide. RLHF (Reinforcement Learning from Human Feedback — training on human preference data to reward calibrated, honest responses) teaches the model to hedge appropriately by rewarding expressions of uncertainty where uncertainty is warranted. Retrieval augmentation replaces parametric memory: instead of generating an answer from training-time statistical patterns, the model conditions its response on a retrieved document containing the current regulatory text. Calibration training on datasets where the model predicts its own correctness can produce probability estimates that correlate with accuracy for high-stakes outputs. None of these fully eliminate hallucination — each shifts the failure mode. The root cause remains: next-token prediction on human text is not the same objective as learning calibrated factual knowledge, and closing that gap requires explicit post-training work beyond the base pre-training stage.",
    ],
    mcq: {
      question: "Why do LLMs produce confident-sounding answers even when they lack reliable knowledge about a niche topic?",
      options: [
        "LLMs are explicitly programmed to avoid expressing uncertainty to prevent user frustration",
        "The pre-training objective rewards accurate next-token prediction — and confident-sounding text was the dominant pattern in training data, so confident output was reinforced throughout training",
        "Uncertainty expression requires more compute than the model allocates for low-frequency topics",
        "The softmax output layer always forces the probability distribution to appear highly peaked, producing apparent confidence regardless of actual knowledge",
      ],
      correct: 1,
      explanation: "Next-token prediction has no 'know vs. don't know' signal — it just optimizes for token-level prediction accuracy on the training corpus. Confident, fluent text was overrepresented in pre-training data; uncertainty hedges were rare. Option B is the correct answer. Option A is false — models aren't explicitly programmed to avoid uncertainty; the behavior emerges statistically from training data patterns. Option C is false — compute allocation doesn't distinguish between frequent and infrequent topics; every token prediction gets the same forward pass. Option D is false — the softmax output creates a peaked distribution that reflects the model's learned statistical prior, not a hardcoded confidence flag.",
    },
    takeaway: "Hallucination is not a failure mode to patch — it's the natural output of a next-token predictor trained on human text. Confident prose is what the internet contains; the model learned to produce it. Calibrated uncertainty requires explicit alignment training (RLHF, retrieval grounding). Until then: treat model confidence as a signal, not a guarantee — especially on low-frequency knowledge.",
  },

  // ── Language Models — high-priority stubs ────────────────────────────────────

  "positional-encoding": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your team is evaluating whether to fine-tune a base model for processing very long legal documents — up to 128K tokens. The base model was pre-trained with a 4K token context window. A colleague says 'just use RoPE, it extrapolates automatically.' You need to understand how positional encoding works well enough to evaluate whether that claim is true.",
    explanation: [
      "The Transformer's attention mechanism computes relationships between all token pairs simultaneously — that's why it handles long-range dependencies better than RNNs. But that same mechanism has a gap: it treats the input as a set of tokens, not a sequence. Without positional information, 'the cat sat on the mat' and 'the mat sat on the cat' produce identical attention scores — the mechanism depends on token content, not token position. Positional encoding injects position information into each token's representation so the model can reason about sequence order. Early Transformers added fixed sinusoidal encodings to token embeddings. Modern models use relative positional encodings built directly into the attention mechanism, which generalize better across context lengths.",
      "RoPE (Rotary Position Embedding — used in Llama, Mistral, Gemma, and most modern open-source LLMs) encodes position differently from additive schemes: instead of adding a position vector to token embeddings, RoPE rotates the query and key vectors in attention by an angle proportional to their absolute position. The rotation is applied before the Q-K dot product, so the attention score between any two tokens depends on their rotational difference — encoding their relative position rather than absolute indices. The model learns that 'token A is 5 positions before token B' from the rotation angle difference, not from stored absolute coordinates. This makes RoPE inherently relative: it represents positional relationships, not positions.",
      "The production implication closes the scenario: RoPE does not automatically extrapolate beyond the training context length. At 128K tokens, positions far beyond the 4K training range produce rotation angles the model's weights have never represented during training — attention scores at those distances become unreliable because the model has no learned representation for those rotation values. Techniques like YaRN and LongRoPE (methods that rescale RoPE rotation frequencies to extend usable context without full retraining) explicitly address this — but they require deliberate application, hyperparameter tuning, and validation at the target context length. The claim 'just use RoPE, it extrapolates automatically' conflates having RoPE with having context extension. You get accurate long-context behavior only if the model was pre-trained at that length or an explicit extension technique was applied and validated at your target length.",
    ],
    mcq: {
      question: "A model pre-trained with 4K token RoPE context is tested at 32K tokens without any context extension technique. What most likely happens?",
      options: [
        "The model performs identically — RoPE is designed to extrapolate to any sequence length automatically",
        "The model crashes with an out-of-bounds error — RoPE cannot process position indices above the training maximum",
        "Attention quality degrades — positions beyond the training range produce rotation angles the model has never encountered, causing attention scores to become unreliable at long distances",
        "Performance improves slightly — longer context gives the model more information to attend to",
      ],
      correct: 2,
      explanation: "RoPE rotation frequencies are calibrated to the training context length. Beyond that range, the rotation angles correspond to values never seen during training, and the model's attention patterns become unreliable. Option C is the correct answer. Option A is false and is the exact misconception this module is designed to correct — RoPE does not extrapolate automatically. Option B is false — no hard error occurs; the model silently degrades instead of crashing. Option D is also false — beyond the training context, extra tokens produce unreliable attention patterns that add noise, not useful signal.",
    },
    takeaway: "RoPE encodes relative position via rotation angles in attention — it doesn't add position to the embedding, it bakes it into Q-K dot products. But it doesn't extrapolate beyond training context length without explicit rescaling. Always apply a context extension technique and evaluate perplexity at target length before deploying a model beyond its training window.",
  },

  "kv-cache": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "Your inference team reports that adding one more user to a shared inference server reduces throughput for all existing users, not just the new one. Memory usage per request is also growing proportionally with conversation length. An engineer says the KV cache is the culprit. You need to understand the mechanism well enough to confirm the diagnosis and propose a fix.",
    explanation: [
      "Attention requires every token to query over all previous tokens — that's the mechanism that makes long-range dependencies possible. In training, the full attention matrix over a sequence is computed in parallel. At inference, generation is autoregressive: one token at a time. Without caching, producing token N means running the full attention computation from scratch over tokens 1 through N-1 at every layer — repeating work that is identical to what was done when producing tokens 1 through N-1 individually. The KV cache eliminates this redundancy by storing the key and value tensors from each prior position so they can be reused. Token N only needs to compute one new Q, K, V for itself and attend over the cached K, V tensors from all prior positions.",
      "The memory cost of the KV cache is: 2 (keys + values) × num_layers × num_heads × head_dim × sequence_length × bytes_per_element. For a 70B model in float16 with 80 layers, 64 heads, 128 head_dim: each token costs 2 × 80 × 64 × 128 × 2 bytes ≈ 2.6MB. A 4K-token conversation: ~10GB just in KV cache. This is linear in sequence length — each additional token adds another fixed entry across all layers and heads. When a server handles N users simultaneously each with average conversation length L, total KV footprint = N × L × 2.6MB. Model weights (~140GB for a 70B model in float16) plus activation buffers are fixed. Adding one more user reduces the memory available for all existing users' KV caches — either forcing shorter effective context windows per user or reducing how many requests can be batched simultaneously. This is the direct mechanism behind the throughput reduction.",
      "Modern architectures reduce KV cache size at the model design level, before any inference-time optimization. Standard Multi-Head Attention maintains one K and V head per Q head — 64 KV heads at 128 head_dim for a typical 70B model. Multi-Query Attention (MQA — used in Falcon, early Gemma) uses a single shared K and V head across all Q heads, reducing KV cache by the full num_heads factor — 64× smaller at the cost of slight quality degradation. Grouped-Query Attention (GQA — used in Llama 2, Llama 3, Mistral) is the production compromise: G groups of Q heads each share one K/V head, reducing cache by G× (typically 4–8×) with minimal quality loss. These aren't inference-time options — they're architectural choices baked into the model before training.",
      "For deployed models with fixed architectures: (1) PagedAttention (vLLM — an open-source inference serving engine) manages KV cache memory in fixed-size pages like OS virtual memory, eliminating fragmentation and enabling efficient multi-user memory sharing. (2) KV cache quantization (INT8 or INT4) reduces memory 2–4× with small quality loss. (3) Sliding window attention caps KV cache at the most recent W tokens regardless of conversation length. (4) Prompt caching — providers cache KV tensors for stable system prompt prefixes across requests, so repeated system prompts don't consume KV budget per request. For the scenario: the throughput regression with one additional user is KV cache exhausting GPU memory headroom. Fix: deploy via PagedAttention-based serving (eliminates fragmentation waste), add KV quantization, and set a maximum context length cap to bound per-user memory footprint.",
    ],
    mcq: {
      question: "Why does KV cache memory grow proportionally with conversation length rather than staying constant?",
      options: [
        "KV cache is recomputed from scratch at each generation step, so longer inputs require more compute memory temporarily",
        "The vocabulary embedding table grows larger as more unique tokens appear in the conversation",
        "KV cache stores the full attention weight matrix, which is O(n²) in sequence length",
        "The cache stores key and value tensors for every token in the conversation — each new token adds another entry to the cache across all layers and heads, making total memory linear in sequence length",
      ],
      correct: 3,
      explanation: "The KV cache persists the K and V tensors for every position processed so far — it's a running record, not a fixed-size buffer. Each new token adds one more entry across every layer and every head. Option D is the correct answer. Option A is wrong — that describes the state WITHOUT a KV cache; the cache exists precisely to avoid recomputation. Option B is wrong — the vocabulary embedding table is a fixed model weight matrix, not a per-conversation structure. Option C is wrong — the cache stores only K and V tensors (not the attention weight matrix), so memory is O(n), not O(n²). That linear growth is what causes the per-user memory expansion that compresses throughput for concurrent users.",
    },
    takeaway: "KV cache trades recomputation for memory: each cached token costs ~2.6MB across layers in a 70B model. It's usually the binding memory constraint on concurrent request capacity. Understanding KV cache size is prerequisite to any inference capacity planning — without it, memory estimates are off by an order of magnitude.",
  },

  // ── Retrieval track ─────────────────────────────────────────────────────────

  "embeddings": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A medical Q&A RAG pipeline built on OpenAI ada-002 embeddings is returning irrelevant results for clinical queries. A search for 'myocardial infarction treatment protocols' surfaces general cardiology overviews instead of the specific ICU protocols that exist in the index. The same protocol is correctly returned when you query with the exact section heading. You suspect domain mismatch between the embedding model and the medical corpus.",
    explanation: [
      "BPE gave the model integer IDs for subwords — but an integer carries no semantic signal. Token 4,731 is 'myocardial'; token 4,732 is 'infarction'. Their arithmetic distance is meaningless. The next step is one-hot encoding: a vector of zeros with a 1 at position 4,731. This eliminates the false ordinal relationship but creates a new problem: every token is equidistant from every other. 'Heart attack' and 'myocardial infarction' are as far apart in one-hot space as 'heart attack' and 'quarterly earnings' — the model has no signal about which concepts are related.",
      "The solution comes from a linguistic observation: words that appear in similar contexts have similar meanings. 'Myocardial infarction' co-occurs with 'treatment protocols', 'ICU', 'troponin'. 'Heart attack' co-occurs with the same contexts. A model trained to predict surrounding context is forced to place both phrases in similar regions of the vector space. Dense vectors (128–1536 floating-point numbers per token) replace sparse one-hot representations, and geometry now carries semantic information: cosine similarity between vectors measures meaning overlap, not token ID proximity. This is the distributional hypothesis turned into a training objective.",
      "ada-002 is a contextual encoder — unlike static models (where 'bank' gets the same vector in 'river bank' and 'bank account'), ada-002 produces a different vector for the same token depending on its surrounding context. Polysemy is solved. But contextual and static models share a deeper constraint: the geometry they encode reflects their training distribution. 'Myocardial infarction' and 'heart attack' are synonymous in clinical use, but ada-002 was trained on general web text where they may not co-occur frequently enough in shared contexts to land close together in the vector space. The semantic bridge the retriever needs doesn't exist in the vectors. Clinical search fails not because embeddings are broken — they work exactly as designed — but because the domain distribution of the encoder doesn't match the domain distribution of the query corpus.",
    ],
    mcq: {
      question: "What does cosine similarity between two embedding vectors measure?",
      options: [
        "The angle between them in embedding space — a measure of semantic similarity that is independent of vector magnitude",
        "The Euclidean distance between the two vectors, where 0 means identical",
        "The fraction of tokens the two texts share after stopword removal",
        "The probability that the same encoder model produced both vectors",
      ],
      correct: 0,
      explanation: "Cosine similarity measures the cosine of the angle between two vectors, normalizing out magnitude. Two texts that are semantically similar will have small angular separation (cosine near 1) even if their vector norms differ. Option B (Euclidean distance where 0 means identical) gets the metric right but the value wrong — Euclidean distance of 0 means identical, but it's also sensitive to vector magnitude and not the standard for embeddings. Option C (fraction of shared tokens) describes keyword overlap — a completely different, much weaker signal that misses synonyms and paraphrases entirely. Option D is false — cosine similarity is a geometric measure between vectors; it has no information about which encoder produced them.",
    },
    takeaway: "Embedding quality is not universal — it's tied to the distribution of data the encoder model was trained on. Domain mismatch is the most common cause of unexplained retrieval degradation. Evaluate your embedding model on real examples from your domain before deploying, and always re-embed the entire index when upgrading models.",
  },

  "chunking": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A customer support RAG system is failing on multi-part questions. A user asks 'How do I integrate the API and what happens if authentication fails?' The answer to part one is in a 256-token fixed chunk; the answer to part two is in the immediately adjacent chunk. The retriever returns only one, and the response is half complete. Changing the chunk size has not solved the problem.",
    explanation: [
      "The retriever doesn't retrieve documents — it retrieves chunks. Whatever information unit gets embedded is the unit of retrieval. If the answer to a question spans a chunk boundary, the retriever returns one half and the generation model never sees the other. Fixed-size chunking (256 or 512 tokens) cuts text at arbitrary token counts with no regard for semantic structure. A three-sentence explanation of API authentication, split mid-explanation by a token budget, produces two chunks that each require the other to be interpretable — the retriever returns one; the answer is incomplete.",
      "Overlap is the standard first response: duplicate 50–100 tokens between adjacent chunks so content near a boundary appears in both. This increases the probability that a spanning answer lands inside one chunk — but doesn't eliminate the root problem. The root problem is that semantic boundaries don't align with token budgets. For the multi-part question in this scenario, both answers exist in adjacent chunks; overlap helps only if both answers fall within a single overlapping window, which arbitrary token counts don't guarantee.",
      "Structure-aware chunking aligns the chunk boundary to the natural information unit of the content. FAQ systems: chunk by question + complete answer. Documentation: chunk by section heading + content. Code: chunk by function. Recursive character splitting approximates this without schema knowledge by walking a hierarchy of separators — paragraph breaks, then line breaks, then sentence breaks — to find natural boundaries at or below a target size.",
      "Parent-child chunking separates retrieval precision from generation context completeness. Small chunks (64–128 tokens) are used for retrieval: small chunks closely match short queries and produce high-precision similarity scores. But the retrieved result expands to its parent section (512–1024 tokens) before being passed to the generation model. The customer support failure — retriever finds the first half of the answer but not the second — is exactly what parent-child solves: small chunks locate the right region; the parent provides the complete answer.",
    ],
    mcq: {
      question: "A retriever finds the correct chunk for the first half of a user's question but misses the second half, which is in the adjacent chunk. The most direct architectural fix is:",
      options: [
        "Reduce chunk size to 64 tokens so more chunks fit in the context window simultaneously",
        "Parent-child chunking: retrieve at the small-chunk level for precision, then return the full parent section to the generation model for complete context",
        "Increase temperature so the model infers the missing half of the answer from context",
        "Pre-process queries to extract only the first question and discard subsequent parts",
      ],
      correct: 1,
      explanation: "Parent-child chunking separates the retrieval precision problem (small chunks find the right location) from the context completeness problem (the generation model needs more than a snippet). Option B is the correct answer. Option A (reduce to 64 tokens) makes completeness worse — smaller chunks mean each retrieved chunk has even less context. Option C (increase temperature) affects text generation style, not what content is retrieved; temperature has no bearing on which chunks the retriever returns. Option D (discard subsequent questions) treats the symptom by removing the requirement instead of solving the actual retrieval gap.",
    },
    takeaway: "Chunk boundaries are the most underestimated RAG failure point. Your chunking strategy should match the natural information unit of your content — FAQ pairs, documentation sections, or code functions — not an arbitrary token window. When overlap isn't enough, parent-child chunking separates retrieval precision from generation context completeness.",
  },

  "rag-pipeline": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A legal research tool indexes thousands of case documents. Users report two distinct problems: (1) relevant cases exist in the index but aren't returned for conceptual queries like 'cases where force majeure was applied'; (2) the model cites specific cases that don't exist in the database at all. You need to attribute each failure type to the correct pipeline stage before you can fix either.",
    explanation: [
      "Two distinct failures in this scenario, and they cannot be fixed with the same intervention. The diagnostic question before touching any code: could this failure happen if I bypassed the retriever and fed perfect context directly to the generation model? If yes, it's a generation failure — the model produces wrong output despite having the right information. If no, it's a retrieval failure — the right information never entered the context. Wrong attribution means fixing the wrong stage, which wastes engineering time and leaves the actual failure intact.",
      "Failure 1 — relevant cases not returned for conceptual queries — is a retrieval failure. The user's query ('force majeure was applied') uses everyday language; the relevant case documents use legal terminology ('frustration of contract,' 'supervening impossibility'). A general-purpose embedding model doesn't encode the synonym relationship between those legal terms — it was never trained on legal corpus co-occurrences where these phrases cluster together. The fix is retrieval-stage: hybrid search (dense embedding for semantic match + BM25 for lexical overlap with legal terminology) combined with query expansion that injects domain synonyms before retrieval.",
      "Failure 2 — citations to cases that don't exist — is a generation failure. The model was pre-trained on legal text containing citations throughout, giving it a strong prior for generating citation-formatted strings. When retrieved context doesn't contain a matching case, the model completes the citation pattern from pre-training memory — producing plausible-looking but non-existent identifiers. This isn't a retrieval gap; it's pattern completion from training data. The fix is generation-stage: constrain output to cite only verbatim identifiers from retrieved chunks (structured output with citation validation), or instruct the model to output 'no citation found' rather than infer one.",
      "Attribution is the engineering judgment. Fixing the embedding model or adding BM25 does nothing for hallucinated citations. Adding citation grounding does nothing for missing retrievals. Treating both as a single 'quality problem' and tuning the generation model improves citations but leaves conceptual recall broken. Stage-isolated evals — retrieval recall@k separately from answer faithfulness — are what make attribution rigorous rather than a guess.",
    ],
    mcq: {
      question: "A legal RAG tool retrieves relevant documents but the response cites a case that doesn't exist in the index. Which pipeline stage failed?",
      options: [
        "Retrieval — the vector index returned documents from outside the database boundary",
        "Chunking — the case identifier was split across chunk boundaries and corrupted",
        "Generation — the model completed a citation pattern from pre-training memory rather than grounding it in the retrieved context",
        "Embedding — the model mapped case names to incorrect vector positions during indexing",
      ],
      correct: 2,
      explanation: "Hallucinated citations are a generation failure — the retriever worked correctly but the generation model completed citation patterns from pre-training memory rather than grounding them in retrieved content. Option C is the correct answer. Option A (vector index returning outside-boundary documents) describes a retrieval failure producing wrong documents, not a fabricated citation. Option B (case identifier split across chunks) would corrupt or truncate an existing citation, not generate a non-existent one. Option D (embedding maps names incorrectly during indexing) would cause retrieval failures returning wrong cases — a different symptom from a plausible-looking but invented citation.",
    },
    takeaway: "RAG failures are stage-specific. Hallucinated citations = generation failure (model completing patterns, not grounding in retrieved content). Wrong or missing retrievals = retrieval failure (embedding or index quality). Mixing these into a single end-to-end score hides which stage to fix. Eval retrieval recall@k and answer faithfulness separately.",
  },

  "context": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A document summarization pipeline was extended from 8K to 32K tokens to handle full annual reports. In testing, the 32K model performs worse on multi-section synthesis questions than the 8K model did on shorter documents — even though all relevant information is technically in context. A researcher attributes this to the 'lost in the middle' effect. You need to understand the mechanism to design around it.",
    explanation: [
      "RAG solved the retrieval problem — the right content is in the index and gets retrieved into context. But retrieval into context doesn't guarantee extraction from context. A 32K window containing the relevant section still fails if the model doesn't attend to it. The failure is positional: LLMs trained on text where key information appears near the beginning (journalism, executive summaries) or near the end (conclusions, results) develop uneven attention patterns across long sequences. Information placed in the middle 60% of a long context receives measurably less attention weight than content at either end — regardless of semantic relevance. 'Lost in the middle' is the documented name for this effect.",
      "The effect compounds with irrelevance density. A 32K context packed with all 50 annual report sections forces the model to locate 3 relevant sections among 47 noisy ones. The relevant sections compete with irrelevant content for attention weight. A curated 8K context with 10 highly relevant sections often outperforms — not because a smaller window is inherently better, but because the relevant content is a larger fraction of the total, and attention concentrates on signal rather than noise. The 32K model performing worse than the 8K model in this scenario is this effect exactly: more context window, lower extraction quality.",
      "Three production mitigations: map-reduce avoids the problem entirely — summarize each section independently in separate inference calls, then synthesize the summaries. The model never reasons across 32K at once, so positional bias never applies. Reranking and positional placement exploit the bias constructively: retrieve the most relevant chunks, rerank them, and place the highest-relevance content at the beginning or end of the context where attention concentration is highest. Hierarchical chunking — retrieve section summaries first, expand to full content only on demand — reduces context length while preserving document coverage. For cases where long contexts are unavoidable, build evals that specifically test extraction from middle-document positions — standard accuracy metrics on a held-out set won't surface this failure pattern.",
    ],
    mcq: {
      question: "An annual report has a key figure at the document's middle. Compared to placing it at the start, retrieval accuracy for this figure in a 32K-context window is:",
      options: [
        "Higher — longer context windows allocate more attention capacity to middle positions",
        "Identical — Transformer attention is uniform across all token positions",
        "Variable — it depends only on the top-p sampling parameter used at generation",
        "Lower — the 'lost in the middle' effect means models consistently perform worse at extracting information placed in the middle of long contexts, regardless of whether the information is technically present",
      ],
      correct: 3,
      explanation: "The 'lost in the middle' effect is empirically documented: extraction accuracy drops for information placed in the middle 60% of long contexts. Option D is the correct answer. Option A is false — longer context windows extend the middle 'at risk' region; they don't redistribute attention capacity to compensate for positional bias. Option B is wrong — Transformer attention explicitly produces uneven weights via softmax; true uniform attention would require equal-weight averaging, which defeats the purpose of the attention mechanism. Option C is wrong — sampling temperature affects text generation style, not what the model extracts from its context window during reading.",
    },
    takeaway: "Long context ≠ good context. Position within the context window materially affects extraction quality. For long-document synthesis, use map-reduce instead of stuffing everything into one context, place your most critical content at the beginning or end, and always test extraction accuracy specifically at middle-document positions.",
  },

  "reranking": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your RAG pipeline retrieves the top-10 chunks by embedding similarity. Precision at rank 1 is 52% — the most relevant chunk is first just over half the time. A colleague proposes adding a reranker. Before approving the latency increase, you want to understand exactly what a reranker does that the embedding retriever can't.",
    explanation: [
      "Embedding retrieval compresses each document chunk into a single dense vector. The compression is lossy in a specific way: fine-grained relevance to a specific query is averaged out across all the semantic content the chunk contains. Two chunks with identical cosine similarity to the query can differ dramatically in actual relevance — one directly answers the question, one mentions the topic in passing. The bi-encoder structurally cannot distinguish them: it encoded query and document independently, so query attention never touched document tokens. First-stage retrieval is optimized for recall — get the right chunk somewhere in the top-10. Precision at rank 1 is a different, harder problem that bi-encoders aren't designed to solve.",
      "A cross-encoder reranker takes the (query, chunk) pair as a single concatenated input and runs full attention between query tokens and document tokens before producing a relevance score. This joint encoding enables what bi-encoders cannot: detection of specific answer presence (does this chunk directly answer this question?), query-document entailment (does the chunk's content satisfy what the query is asking?), and fine-grained topical match beyond surface similarity. The tradeoff is speed: a full forward pass per query-document pair makes cross-encoders 10–100× slower than bi-encoders at query time. Running them over a full million-chunk index would take minutes per query.",
      "The two-stage pattern makes the tradeoff viable: retrieve the top-20 fast with the bi-encoder for recall, then rerank those 20 with the cross-encoder for precision. Cross-encoders only run over the small candidate set — 20 pairs, not millions. Lightweight cross-encoders (ms-marco-MiniLM-L-6-v2, BGE-reranker-base) add 50–150ms for 20 candidates. For the pipeline in this scenario — precision at rank 1 of 52% — a reranker will close that gap more than doubling the index size or fine-tuning the embedding model, at lower engineering cost than either. When the top-3 chunks determine answer quality, 100ms of reranking latency is almost always the right tradeoff.",
    ],
    mcq: {
      question: "Why does a cross-encoder reranker outperform embedding similarity for ranking the most relevant chunk first?",
      options: [
        "A cross-encoder processes the query and document jointly in a single forward pass — attention flows between query and document tokens, enabling detection of specific answer presence that bi-encoder embeddings compress away",
        "Cross-encoders use a larger vocabulary than bi-encoders, capturing more domain-specific terminology",
        "Rerankers are fine-tuned on more data than embedding models, making them inherently more accurate",
        "Cross-encoders run at higher precision (float32 vs float16) which improves relevance scoring accuracy",
      ],
      correct: 0,
      explanation: "The fundamental advantage is joint encoding. A bi-encoder compresses each text into a vector independently — the query vector never 'sees' the document during encoding. A cross-encoder concatenates both and runs full attention between them, enabling specific answer-presence detection that independent vectors cannot provide. Option B is false — vocabulary size is an architectural choice unrelated to whether encoding is joint or independent. Option C conflates training data volume with architectural design; the cross-encoder advantage comes from joint attention, not from having more training data. Option D is wrong — cross-encoders typically run in the same float16 precision as bi-encoders; the quality difference comes from architecture, not numeric precision.",
    },
    takeaway: "Embedding retrieval maximizes recall; reranking maximizes precision. The two-stage pattern (retrieve top-20 fast, rerank to top-3 accurately) consistently outperforms single-stage retrieval for RAG quality. A lightweight cross-encoder adding 100ms to achieve 20–30% better precision at rank 1 is almost always the right tradeoff for user-facing applications.",
  },

  // ── AI Agents track ──────────────────────────────────────────────────────────

  "agent": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A customer service agent is built to handle refund requests end-to-end: look up orders, check the return policy, and process refunds up to $50. In testing, it approves a $50 refund for an order that's clearly outside the return window — and the reasoning trace says the order 'appears to be within the eligible period.' The tool output clearly shows an expired return date. You need to understand the agent loop to find exactly where it failed.",
    explanation: [
      "A single inference call produces one response given a fixed context. It can't look up a customer's order, check a return policy, and process a refund — those require external actions with real-world side effects, and the results of those actions change what the model should do next. The agent loop solves this: the model receives a task plus tool descriptions → reasons about the next action → calls a tool → receives the result → updates its reasoning → repeats until it can produce a final answer. This is the ReAct pattern — Reasoning and Acting interleaved. Each tool result is appended to the model's context window, and every subsequent reasoning step is conditioned on the full accumulated history.",
      "The loop introduces failure modes that don't exist in single-turn inference. Errors compound: a wrong interpretation in step 2 propagates into wrong decisions in steps 4 and 6, with nothing to catch it automatically. More critically, the reasoning trace is not ground truth. The model can write 'the order appears to be within the eligible period' in its chain-of-thought while the tool output in the same context explicitly shows an expired return date. The model isn't reading the tool output like a program that branches on a boolean — it's predicting tokens conditioned on it, and that prediction can be wrong regardless of what the tool returned. The reasoning trace is a generation, not a logical consequence of the tool result.",
      "The refund approval failure in this scenario is the model reasoning over unstructured return policy text and reaching the wrong conclusion. The fix is structural: replace model reasoning over unstructured content with typed, structured tool output. A `check_return_eligibility(order_id)` tool returning `{eligible: false, reason: 'return_window_expired', deadline: '2024-03-15'}` leaves no room for misinterpretation — the model cannot reason its way to `eligible: true` when the schema field is a boolean false. Where possible, push decision logic into tool output and out of model reasoning. The model should act on structured results, not infer conclusions from prose descriptions of policy.",
    ],
    mcq: {
      question: "An agent correctly retrieves order data but then approves a refund it shouldn't. The tool output shows an expired return date, but the reasoning trace says 'order appears eligible.' What failed?",
      options: [
        "The embedding model — vector similarity returned the wrong order record from the database",
        "The reasoning step between tool calls — the model received correct structured data but generated an incorrect interpretation of it in its chain-of-thought, then acted on the incorrect interpretation",
        "Temperature=0 caused the agent to always select its most likely action (approval) regardless of tool output",
        "System prompts are excluded from the agent's context window during tool call steps",
      ],
      correct: 1,
      explanation: "The model's reasoning trace is a token prediction, not a reliable summary of what the tool returned. The model can produce incorrect intermediate reasoning even when correct data is present in its context — especially when reasoning about unstructured policy text. This is why structured, typed tool outputs are preferable: boolean eligibility fields are harder to misinterpret than natural language policy documents. Option B is the correct answer. Option A is wrong — the scenario explicitly states the agent 'correctly retrieves order data,' so the embedding model and database retrieval are not at fault; the failure happens after correct data is in context. Option C is wrong — temperature=0 makes the model deterministic but does not override what the reasoning trace says; greedy decoding still produces the most likely token conditioned on the context, which can be an incorrect interpretation. Option D is false — system prompts are part of the agent's context window throughout all tool call steps; they are not excluded.",
    },
    takeaway: "Agent loops fail differently than single-turn models. The reasoning trace is not a reliable report of tool output — it's a token prediction that can contradict the tool data in the same context. Design tool outputs to be typed and structured, not free-text, to minimize the surface area for intermediate reasoning errors.",
  },

  "agent-tools": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A support agent has 15 tool definitions: CRM lookup, order status, email send, refund processor, Slack notify, calendar create, document search, Jira ticket, user profile, billing history, support history, knowledge base search, policy lookup, contract lookup, SLA check. In production the agent frequently calls the wrong tool or invokes tools in the wrong order — attempting to send an email before verifying the user exists.",
    explanation: [
      "Agents established that tool use lets LLMs act on the world rather than just respond. The failure mode that creates: with 15 tools available simultaneously, the model must select the right one at each step from name and description alone. Tool definitions consume tokens and compete for the model's attention across the full context. Ambiguous or overlapping descriptions — 'document search,' 'knowledge base search,' and 'policy lookup' all sound similar to a model without your internal taxonomy — produce wrong tool selection even when the model has no underlying capability gap. Selection errors are not model failures; they're tool design failures.",
      "Four design rules that reduce wrong selection: (1) One action per tool — combining CRM lookup and billing history into one 'get_customer_info' tool creates ambiguity about which data it returns for which questions. Split them. (2) Verb-object naming — 'get_order_by_id(order_id)' is unambiguous; 'lookup' is not. (3) Strong parameter schemas — required parameters with explicit types (string, integer, enum) constrain the model's action space and reduce malformed calls by narrowing the completion space at each argument. (4) Minimum tool set — remove tools the current task doesn't need. A model choosing among 5 applicable tools makes better selections than one choosing among 15 where 10 are distractors.",
      "Ordering failures — sending email before verifying the user exists — aren't fixed by better tool descriptions because the model is making a sequencing decision, not a selection decision. Two patterns: orchestration (the outer system enforces tool call sequence via a directed graph — LangGraph, an agent orchestration framework, structures agent execution as a state machine where prerequisite edges prevent a node from executing until its inputs are available) versus autonomy (the agent reasons about dependency ordering from description and task context alone). For irreversible actions — sending emails, processing refunds, creating tickets — use structural orchestration. Trust the graph, not the model's reasoning, to enforce prerequisites. Wrong tool selection is a design failure you fix in descriptions; wrong ordering under autonomy is an architectural failure you chose by not using orchestration.",
    ],
    mcq: {
      question: "An agent with 15 tool definitions frequently calls the wrong tool for similar tasks. The most direct fix is:",
      options: [
        "Increase the model's context window so all 15 tool schemas fit without potential truncation",
        "Move tool selection into the system prompt as a decision tree, specifying which tool to call for each user intent category explicitly",
        "Reduce the tool set to only the tools needed for the current task, and rewrite tool descriptions to be precise and maximally distinct from each other",
        "Set temperature=0 so the agent always picks its highest-confidence tool without sampling variance",
      ],
      correct: 2,
      explanation: "Tool selection errors come from ambiguous descriptions and irrelevant options, not context window limits or temperature. Reducing the tool set removes noise, and precise descriptions give the model unambiguous selection criteria. Option C is the correct answer. Option A is wrong — at 15 tools, the total schema tokens are unlikely to cause truncation in a modern context window; the problem is semantic confusion between similar-sounding tools, not context overflow. Option B sounds like a reasonable engineering fix — intent classification trees are a real pattern — but a static decision tree in the system prompt just moves the ambiguity problem: you still have to enumerate every possible user intent and map it correctly, the tree becomes a maintenance burden as intents grow, and the model must interpret free-form user messages against the tree's categories. Fixing the tool descriptions directly is more scalable because the model's semantic understanding does the classification rather than pattern-matching against a hardcoded list. Option D is wrong — temperature=0 selects the highest-probability token, but if the tool descriptions are ambiguous, the highest-probability token is still the wrong tool; determinism does not fix underlying tool description quality.",
    },
    takeaway: "Tool design is as important as prompt design. Ambiguous names, overlapping descriptions, and irrelevant tools produce confused agents. Give the agent the minimum set of tools needed for the current task, name them with verbs and precise objects, and use structural orchestration (not model reasoning) to enforce prerequisite ordering for irreversible actions.",
  },

  "multiagent": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "A compliance review pipeline needs to: extract key terms from 100-page contracts (Step 1), check each term against a regulatory database (Step 2), and produce a risk summary (Step 3). The team is deciding between a single LLM with all tools vs. a multi-agent system. Step 2 can run in parallel for each extracted term. Step 3's report is missing compliance flags that Step 2 identified.",
    explanation: [
      "Agent-tools established how a single agent selects and executes tools. The failure it left unsolved: a single agent handles all pipeline steps sequentially using the same model for every task. The compliance pipeline exposes why this is operationally costly. Step 1 needs a long-context model for 100-page inputs. Step 2 is a cheap classification call per extracted term — independently decidable, with no dependency between terms. Step 3 needs a reasoning model that synthesizes across all results. A single model doing all three pays frontier pricing for the cheapest work in the pipeline and runs all term checks in sequence when they could run in parallel.",
      "Parallelism is the primary throughput argument for multi-agent systems. If 50 terms are extracted, 50 independent regulatory checks can run simultaneously rather than sequentially. Fan-out and merge: an orchestrator distributes each term to a Step-2 agent, collects all results, then passes the complete result set to Step 3. Wall-clock time scales with the slowest single Step-2 call rather than with the number of terms. Fifty sequential calls at 500ms each take 25 seconds; fifty parallel calls take 500ms. For compliance workflows with turnaround SLAs, this gap is significant — and it's unavailable to a single-agent sequential pipeline regardless of model speed.",
      "The missing flags in Step 3 are the canonical multi-agent handoff failure. Context doesn't flow automatically between agents — Agent C receives only what Agent B returned, not what Agent B reasoned. If Step 2 returns results as free-text summaries ('Term X appears low-risk but check jurisdiction'), Step 3 receives a lossy description where flags are embedded in natural language prose. If Step 2 returns structured schema — `{term, risk_level, regulation_id, requires_review: true/false, jurisdiction}` — Step 3 receives a complete, machine-readable record it can reliably aggregate. The missing flags in the scenario are not missing because the regulatory check failed to find them; they're missing because the handoff format allowed them to be lost in summarization. Design inter-agent output contracts with the same care as tool schemas — every handoff boundary is a fidelity loss point unless the schema explicitly preserves all required fields.",
    ],
    mcq: {
      question: "A 3-agent compliance pipeline's Step 3 (risk report) omits flags raised by Step 2 (regulatory check). The most likely root cause is:",
      options: [
        "Step 2 and Step 3 are using different model providers whose output formats are incompatible",
        "Step 3 ran before Step 2 completed due to incorrect async orchestration",
        "Multi-agent systems don't support output passing between agents using different context window sizes",
        "Step 2 returned findings as unstructured text summaries that Step 3 partially missed — structured schemas with explicit fields for each flag would prevent this information loss at handoff",
      ],
      correct: 3,
      explanation: "Multi-agent handoff failures are almost always information loss at the boundary between agents. When natural language summaries are passed between agents, nuanced findings get dropped in summarization. A structured schema with explicit boolean fields (`requires_review: true`, `regulation_id: 'GDPR-Art-17'`) is unambiguous and complete — there's no risk of the aggregation model missing a flag that was clearly marked in the upstream output. Option D is the correct answer. Option A is wrong — model provider incompatibility is a real integration concern but not the typical cause of missing flags; the failure mode described is information loss in unstructured text handoffs, which occurs regardless of provider. Option B is wrong — the scenario describes Step 3 producing a report that omits flags Step 2 raised, implying Step 2 completed before Step 3 ran; an async ordering bug would cause Step 3 to receive no Step 2 output at all, not a partial one. Option C is false — multi-agent frameworks do support output passing between agents with different context window sizes; context window size mismatch is not a real architectural constraint on inter-agent communication.",
    },
    takeaway: "Multi-agent systems scale via specialization and parallelism, but context doesn't flow automatically between agents. Every inter-agent handoff requires an explicit, structured output schema — natural language summaries lose information. Design inter-agent contracts (typed schemas with required fields) as carefully as you design tool interfaces.",
  },

  "guardrails": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "An HR chatbot deployed for employee benefits questions is being used to draft termination letters and advise on specific disciplinary cases. The system prompt says 'only answer HR policy questions.' Legal flags it as a liability. You need to implement guardrails without breaking legitimate HR information queries. The system prompt restriction is clearly not working.",
    explanation: [
      "Agent-tools established that structural enforcement beats relying on model reasoning for irreversible actions. Guardrails generalize this principle to content: constraining what the system produces requires structural enforcement, not instructions. A system prompt saying 'only answer HR policy questions' is a text instruction inside the model's context — it asks the model to regulate itself at generation time. Instruction-following is probabilistic and competes with the model's helpfulness prior and its learned sense of what assistance looks like in context. Instructions can guide the typical case. They don't constrain boundary cases.",
      "'Draft a termination letter template' is an HR-adjacent documentation request. From the model's perspective, this is professionally appropriate assistance in a professional HR context — close enough to the allowed category that its instruction-following prior doesn't reliably distinguish it from a permitted policy question. The failure isn't a jailbreak; it's distributional. The model learned from pre-training that assisting with HR documents is expected behavior for an HR assistant. A soft instruction inside the model's context can't override a strong learned prior for a request that plausibly fits the category.",
      "Reliable restriction requires enforcement before the main model sees the input. An input classifier — a small, fast LLM call or a fine-tuned classification model — categorizes requests into: allowed (benefits, policy lookup, procedural), review (performance management, disciplinary questions with legal context), and blocked (draft legal documents, advise on specific employee cases). The classifier runs before the main LLM call. 'Review' category requests route to an HR representative rather than auto-refusing — this serves the user while managing liability. Output guardrails add a second layer: even if an allowed request somehow produces a termination letter draft, it gets caught before delivery. The principle: restrict at the layer furthest upstream from the model's generation decision, not inside the model's context. This is exactly why the system prompt failed — it was a restriction inside the input the model was generating a response to.",
    ],
    mcq: {
      question: "An HR chatbot's system prompt says 'only answer HR policy questions' but users successfully get it to draft termination letters. What is the most accurate explanation?",
      options: [
        "System prompt instructions are soft constraints — the model has learned to be helpful with plausible requests, and 'draft an HR document' is close enough to the allowed category that the instruction-following prior doesn't reliably block it",
        "Termination letters are an HR topic, so the system prompt is functioning as designed",
        "The system prompt was truncated by context window limits, removing the relevant restriction",
        "Models cannot parse multi-sentence system prompts without explicit structured formatting",
      ],
      correct: 0,
      explanation: "System prompts are probabilistic soft constraints, not access control. Instruction-following competes with helpfulness and with the model's learned prior for what belongs in a given context. An HR chatbot that's been helpful with document-adjacent requests can be nudged past a vague system prompt restriction by requests that superficially fit the category. A dedicated input classifier running before the main model is the only reliable enforcement layer. Option B is wrong — termination letters are an HR-adjacent task the system should explicitly not handle; 'it's an HR topic' is precisely the ambiguity that causes the system prompt to fail, not evidence that it's working as designed. Option C is wrong — system prompts for a short 'only answer HR policy questions' instruction are well within any modern context window; truncation is not the mechanism at play. Option D is false — models reliably parse multi-sentence system prompts without structured formatting; poor parsing is not the explanation for why restriction instructions are bypassed by clever user framing.",
    },
    takeaway: "System prompts are a soft constraint, not a hard guardrail. For liability-sensitive applications, add a dedicated input classifier before the main LLM, and route high-risk request categories to human review. Layered defenses — input classification, constrained system prompt, output review — are more reliable than trusting the model to refuse itself.",
  },

  "agent-tracing": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "A refund agent is producing incorrect refund amounts in roughly 1 in 200 production requests. You can reproduce the failure in roughly 1 in 10 deliberate tests once you understand the pattern — but you don't know the pattern yet. Your logs contain only the input query and the final output. You have no visibility into which tool calls were made, what they returned, or what intermediate reasoning the agent produced.",
    explanation: [
      "Multi-agent systems established that agents execute chains of tool calls to produce an output. The failure mode that leaves unaddressed: when that chain produces wrong output 1 time in 200, you have no visibility into which of 8 intermediate steps diverged. Without traces, a multi-step agent is a black box — you know the wrong input-output pair but cannot see which tool call passed wrong parameters, which tool returned unexpected data, or where the model's intermediate reasoning introduced an error. Agent tracing captures the complete execution record: each tool call (name, parameters passed, raw output returned, latency), each LLM inference step (model, full context window sent, full output generated), and intermediate reasoning content between steps.",
      "Per-span capture fields that matter in practice: (1) tool call parameters — exactly what arguments the agent passed, which often differ from what the user sent due to intermediate parsing steps. (2) Raw tool output — before the agent parsed or reasoned about it. (3) Full LLM context window at each turn — the complete prompt including accumulated tool results, not a log-line summary. (4) Per-step latency — to identify which steps contribute disproportionately to tail latency. (5) Per-step token counts — for cost attribution per pipeline stage. OpenTelemetry-compatible span structures work well here (OpenTelemetry is an open standard for distributed tracing — a span represents one unit of work with trace_id, span_id, parent_span_id, timestamps, and attributes) and integrate with standard observability backends without custom infrastructure.",
      "The patterns traces reveal that input-output logs never can: the agent called the right tool but passed wrong parameter values — a parsing failure earlier in the chain; a tool returned an error the agent silently ignored and continued past; the tool returned valid data that the agent misread in its reasoning step. The 1-in-200 refund error traces to exactly one of these. Trace-driven debugging converts 'something went wrong in 1 in 200 requests' into 'on step 3 of request xyz, order_lookup returned amount: 47.50 but the reasoning trace said 74.50 — transposition during string parsing.' Finding that from input-output logs requires blind testing across all plausible failure points; finding it from a trace requires one grep. The fixed cost of instrumentation before a production incident is always less than the variable cost of debugging from black-box logs after one.",
    ],
    mcq: {
      question: "A refund agent incorrectly processes 1 in 200 requests. With only input + final output logged, what makes the failure hard to debug?",
      options: [
        "1-in-200 failure rates are too rare to reproduce in a test environment under any conditions",
        "Without traces of intermediate tool calls and reasoning steps, there is no way to determine which step produced the wrong value — reproducing the failure requires blind manual testing across all plausible failure points",
        "Input-output logs are sufficient — the failure cause is always visible in the output token distribution",
        "Adding traces would require replacing the agent framework, which is a larger project than fixing the bug",
      ],
      correct: 1,
      explanation: "Multi-step agent failures are not diagnosable from input-output pairs alone. The failure could originate in any of 8+ intermediate steps. Without traces, every debugging attempt is a guess about which step to examine. With a complete trace, you have the exact tool parameters, raw outputs, and reasoning at each step — the failure source is visible directly rather than inferred from the final wrong answer. Option B is the correct answer. Option A is wrong — a 1-in-200 failure rate is absolutely reproducible with deliberate test design once you understand the triggering pattern; the problem is not rarity but lack of visibility into which step diverges. Option C is wrong — the output token distribution tells you what text the model generated but not which intermediate tool call returned a wrong value; the failure source is upstream of the final output and invisible from it. Option D is wrong — adding traces typically requires adding OpenTelemetry-compatible instrumentation to the existing agent framework (usually a few hours of work), not a full framework replacement; the cost of instrumentation is always lower than the cost of undiagnosable production failures.",
    },
    takeaway: "An agent you can't trace is an agent you can't debug. Invest in tracing before a production incident — capturing every tool call, every LLM context, and every reasoning step. The fixed cost of instrumentation is always less than the variable cost of debugging a multi-step failure from input-output logs alone.",
  },

  // ── Evaluation track ─────────────────────────────────────────────────────────

  "eval-loop": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "Your team shipped a new RAG retrieval model and says evals are passing. The evals turn out to be 12 questions written last year, graded by the same LLM that generates answers, with no comparison to a baseline. When you push back, the engineer says the results are still useful. You need to explain specifically what these evals cannot tell you — and what a minimum viable eval loop requires.",
    explanation: [
      "Changing the RAG retrieval model raises a question the retrieval mechanics themselves can't answer: did it improve? The answer requires an eval loop with four properties: a fixed evaluation dataset that doesn't change when the system changes; an automated scorer that produces quantitative metrics rather than qualitative opinions; a baseline — the previous version or a known reference — to compare against; and a pass/fail threshold that triggers human review when crossed. An eval missing any of these can confirm that the system generates output but cannot tell you whether it's improving or degrading. The 12-question eval in the scenario has none of these properties — it's a list of test inputs, not an eval loop.",
      "The evaluator independence failure is specific. Same-family models — GPT-4 grading GPT-4 outputs — are trained on similar data distributions and have learned similar stylistic preferences. The judge model prefers outputs that resemble its own generation style, assigning systematically higher scores than human raters or different-family judges would. GPT-4 grading GPT-4 is not neutral measurement; it's measurement with a thumb on the scale for outputs that match the judge's own generation patterns. Human annotation is the gold standard; LLM judges are acceptable for scale when calibrated against human labels with an explicit rubric and when the judge is from a different model family than the system under test.",
      "Test set contamination is the eval equivalent of test-set overfitting. When team members who modify the system have access to the eval set, they can — intentionally or not — optimize the system for those 12 specific inputs rather than for general quality. The eval set should be version-controlled separately from the system, managed independently, and augmented with adversarial cases and real-user failure queries from production. 12 questions written once and never updated is a regression test for exactly those 12 inputs. It cannot detect whether the new retrieval model is better on the general distribution. The scenario eval fails on all three dimensions: no baseline to compare against, a biased judge, and a static test set that may already be implicitly saturated by prior optimization.",
    ],
    mcq: {
      question: "A team uses GPT-4 to both generate and evaluate answers. What is the primary validity problem with this setup?",
      options: [
        "GPT-4 cannot produce numerical scores, making quantitative comparison impossible",
        "Running inference twice (generate + evaluate) is prohibitively expensive for production eval pipelines",
        "Same-family models share stylistic preferences — the judge is systematically biased toward outputs that match its own generation style, inflating quality scores for GPT-4 outputs vs. what independent human raters would assign",
        "LLM judges can only evaluate factual accuracy, not relevance or completeness",
      ],
      correct: 2,
      explanation: "Same-family bias is documented: models evaluate outputs similar to their own generation more favorably than independent human raters do. This inflates scores for the exact model being evaluated and makes the eval unreliable for comparing model versions. Independent judges (different model family or human raters calibrated against a rubric) are necessary for meaningful quality measurement. Option C is the correct answer. Option A is wrong — GPT-4 absolutely produces numerical scores and that is the standard use-case for LLM-as-judge; inability to score is not the issue. Option B is wrong — running two inference calls (generate then evaluate) does cost more than one, but for most teams this is not prohibitively expensive; the validity problem, not the cost, is why same-model judging is problematic. Option D is false — LLM judges can be prompted to evaluate relevance, completeness, coherence, and other dimensions beyond factual accuracy; restricting the assessment to factual accuracy is a prompt design choice, not an architectural limitation.",
    },
    takeaway: "An eval loop is only as good as its independence, stability, and comparison baseline. Same-model judge = biased score. Fixed dataset + version control + explicit baseline = minimum viable eval loop. Every model change should have an answer to: 'compared to what, and by how much?'",
  },

  "debug": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A medical literature RAG pipeline produces answers that miss crucial detail from correctly retrieved documents. The retrieved chunks are right — the relevant paragraphs are clearly present — but the model's answers are incomplete or generic. The team has spent two weeks changing prompts without systematic progress. You need to structure a debugging approach.",
    explanation: [
      "Eval-loop established how to detect that something is wrong. When the measurement points to correct retrieval but incomplete answers, the next question is which specific stage is failing — and that question can't be answered without stage isolation. Two weeks of prompt changes without systematic progress happened because all variables changed simultaneously with no ability to attribute any quality movement to any specific intervention. Three stages, three tests: (1) Retrieval recall@k — what percentage of the time does the relevant chunk appear in the top-3 results? If low, fix retrieval first, everything else is secondary. (2) Augmentation — log the exact prompt sent to the LLM, including all retrieved content. Verify the relevant chunk is present and not silently truncated. (3) Oracle test — manually construct a prompt with the confirmed correct chunk and ask the same question.",
      "The oracle test is the highest-leverage diagnostic in RAG debugging because it separates retrieval failure from generation failure in one experiment. If the model answers correctly with a manually constructed perfect context, the failure is in what chunks actually reach the model: wrong chunk selected, truncation cutting the relevant passage, or ordering issues — the lost-in-the-middle effect means the model pays less attention to context in the middle of a long prompt than at the beginning or end. If the oracle prompt also fails, the failure is generational: the model isn't extracting fine-grained medical detail even when it's present and prominent. These two failure modes require entirely different repairs — fixing retrieval does nothing for a generation failure, and improving generation does nothing if the right chunk never reaches the model.",
      "For the medical case where the oracle prompt fails — correct chunks retrieved, but the model paraphrases loosely instead of extracting specific values: 'Quote directly from the retrieved passage before summarizing' forces the model to ground its answer in retrieved text before abstracting. 'Reproduce numbers and measurements exactly as they appear in the retrieved text' prevents rounding or paraphrasing clinical values that must be literal. 'If the retrieved text does not contain the answer, say so explicitly' blocks plausible-but-ungrounded completions. Change one element at a time, run against a fixed test set, record which change produced which movement. This is what converts two weeks of undifferentiated prompt tuning into a diagnosable, attributable repair.",
    ],
    mcq: {
      question: "A RAG pipeline retrieves the correct medical document but still produces incomplete answers. The most diagnostic next step is:",
      options: [
        "Switch embedding models — incomplete answers always indicate a retrieval quality problem",
        "Increase temperature to make the model explore more of the retrieved content",
        "Index additional documents — the missing detail may exist in documents not yet in the corpus",
        "Oracle test: manually construct a prompt with the confirmed correct chunk and ask the same question — if the model answers correctly, the failure is in context construction; if it fails, the failure is in generation",
      ],
      correct: 3,
      explanation: "The oracle test separates retrieval failures from generation failures with a single experiment. If the model answers correctly with a manually provided perfect chunk, the problem is in what chunks are actually reaching the model (construction, truncation, ordering). If it still fails with the perfect chunk, the problem is in how the model uses context when it's present. This distinction determines the entire repair strategy. Option D is the correct answer. Option A is wrong — the scenario explicitly states that retrieved chunks are correct and relevant, so switching embedding models addresses a retrieval problem that doesn't exist here; changing retrieval cannot fix a failure that happens after correct retrieval. Option B is wrong — temperature affects text generation style and diversity, not which information the model extracts from its context; increasing temperature would make the output more varied but not more complete, and could introduce new hallucinations. Option C is wrong — the scenario says the correctly retrieved documents contain the relevant detail that's being missed; adding more documents to the index does not fix a failure in how the model uses the context it already has.",
    },
    takeaway: "Debug RAG by stage isolation, not parameter tuning. The oracle prompt test (inject the perfect context manually) is the single most efficient diagnostic in RAG debugging — it separates a retrieval problem from a generation problem in one experiment. Change one variable at a time; log every test; stop when you can point to the specific cause.",
  },

  "llm-as-judge": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "Evaluating a new answer quality model takes 3 days of human annotation per version. A researcher proposes switching to GPT-4 as the judge, which would increase throughput 100×. Before approving, you want to understand the specific failure modes of LLM-as-judge — when it's reliable enough to replace human annotation and when it isn't.",
    explanation: [
      "Eval-loop established that evaluator independence is required — the judge must be distinct from the system under evaluation. LLM-as-judge is the scalable form of that evaluator: a capable model grades another model's outputs against a rubric. It correlates well with human judgments on well-defined tasks: factual accuracy given a specified context, code correctness against test cases, structured output format compliance. The judge's reliability degrades for tasks requiring domain expertise the judge model lacks, for subjective dimensions where human preference is the actual measurement target, and — most critically — for evaluating outputs from models within the same family. At 100× throughput compared to human annotation, the bias failure modes determine whether that speed advantage is actually measuring anything real.",
      "Three documented failure modes: (1) Same-family bias — GPT-4 grading GPT-4 outputs assigns systematically higher scores than human raters, because the judge's learned stylistic preferences align with its own output distribution. Use a different model family as judge, or average across multiple judges from different providers. (2) Position bias — when comparing two responses side-by-side, LLM judges prefer the first response at rates significantly above chance. Mitigate by swapping order in a second evaluation pass and averaging scores. (3) Verbosity bias — longer, more confident-sounding answers score higher than concise accurate answers regardless of factual content. Detect by calibrating judge scores against human labels on your specific task — if the judge consistently rates verbose responses higher than human raters do, adjust the rubric to penalize length explicitly.",
      "Best practices: provide an explicit numeric rubric instead of 'is this good?' — 'Score 1–5 where 5 = all factual claims grounded in retrieved context, 3 = partially grounded, 1 = answer contradicts retrieved context.' Ask the judge to reason before scoring (CoT judging reduces position and verbosity bias). Calibrate against a human-labeled gold set before using for model selection — correlation with human judgment should exceed 0.7 before trusting it for decisions. Hard limits: do not use LLM judges for safety evaluations, legal quality, or medical quality where human annotation is the only acceptable standard. For the proposed GPT-4-as-judge switch: viable for the throughput gain, but only after calibrating against human labels from your specific task domain — otherwise 100× throughput is measuring something systematically different from what human raters would assign.",
    ],
    mcq: {
      question: "An LLM judge gives systematically higher scores to outputs from the same model family that generated them. The most accurate explanation is:",
      options: [
        "Same-family models share learned stylistic preferences — the judge assigns higher scores to text that resembles its own generation distribution, biasing evaluations of related models upward",
        "Same-family models share API infrastructure, causing evaluation requests to be routed to the same GPU cluster",
        "LLM judges memorize specific outputs from models they were trained on and rate familiar outputs higher",
        "Same-family models produce identical outputs for each input, so the judge always gives the same score",
      ],
      correct: 0,
      explanation: "Same-family bias is distributional: a model trained on similar data to the generator has internalized similar style preferences. Text that 'sounds like' outputs from that family is preferred because it matches the judge's learned prior for what good text looks like. This is documented empirically — GPT-4 judging GPT-4 outputs consistently inflates scores relative to human raters and relative to alternative-family judges. Option B is wrong — same-family models do not share GPU infrastructure in any meaningful way that would route requests to the same cluster; each API call is independently scheduled, and even if they shared hardware it would have no effect on the scores assigned. Option C is wrong — LLMs do not memorize specific training outputs and recognize them later for higher scoring; the bias is distributional (preference for a stylistic pattern) not recognitional (identifying specific memorized text). Option D is wrong — same-family models do not produce identical outputs; they produce varied, stochastic outputs, and their scores also vary; the bias is a systematic upward shift in mean score, not a deterministic sameness.",
    },
    takeaway: "LLM-as-judge is a scalable tool, not a ground truth substitute. Use it for well-defined rubric dimensions, calibrate against human labels before making model selection decisions, and explicitly mitigate position bias (swap order) and same-family bias (use a different judge family). For safety or high-stakes quality decisions, human annotation is the floor.",
  },

  "eval-design": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "You're the first ML engineer at a startup building a contract analysis product. There are no evals. The CEO asks 'is it good enough to ship?' before you've defined what 'good enough' means. You have two weeks and no labeled data. You need to design an eval suite that produces a defensible ship/no-ship answer.",
    explanation: [
      "Eval-loop established four properties a useful eval needs: fixed dataset, automated scorer, baseline, and threshold. The prior question that assumes you already have a dataset: how do you build one when you have no labeled data and 2 weeks? The design starts with two lists rather than with data collection: what the system must do, and what it must never do. For contract analysis — must-do: extract all defined terms with definitions, identify risk clauses by category, flag missing standard provisions. Must-never: omit a high-risk clause, attribute a clause to the wrong party, silently truncate a long contract. These two lists define the test structure before a single annotation hour is spent, and the must-never list carries more weight for a legal tool than the must-do list.",
      "Use real documents from actual customer use cases, not synthetic examples. Synthetic contracts have clean structure; real contracts have formatting irregularities, non-standard clause ordering, and unusual legal language that synthetic examples omit — and those are exactly where systems fail. Annotate a golden set of 50–100 real documents manually. This is the upfront investment, and it's reusable across all future model versions. Allocate annotation time by failure cost: must-never test cases — high-risk clause detection and party attribution — deserve 60–70% of the annotation budget. The goal is worst-case coverage, not balanced coverage across all clause types.",
      "For legal extraction, precision (of what was extracted, what fraction was correct) and recall (of what should have been extracted, what fraction was found) are the right primary metrics — not accuracy. For must-never items, recall is the load-bearing metric: you may accept lower precision (some false alarms on non-risky clauses) to guarantee recall stays high on the clauses that matter most. Establish the minimum acceptable threshold before building the eval — 'ship if recall on high-risk clauses is ≥95%' is a defensible, pre-committed threshold. 'Ship if overall accuracy is 85%' may hide 50% recall on the clauses that matter and still pass. The eval exists to answer a specific pre-committed question, not to produce a number you interpret after the fact.",
    ],
    mcq: {
      question: "A contract analysis eval shows 85% overall accuracy. The CEO says 'ship it.' What critical information does this number fail to provide?",
      options: [
        "85% accuracy is always insufficient; the threshold for any production system is 99%",
        "Per-category performance — 85% overall can mask 50% recall on high-risk clauses while showing 99% on common boilerplate, which is the wrong tradeoff for a legal tool",
        "Accuracy metrics don't apply to extraction tasks — contract analysis cannot be evaluated quantitatively",
        "Overall accuracy is only valid if the eval set was peer-reviewed by the legal industry",
      ],
      correct: 1,
      explanation: "Aggregate metrics hide per-category failures. A system that correctly handles standard boilerplate (80% of clauses) but misses high-risk clauses (the 20% that matter) can achieve 85% overall accuracy while being genuinely dangerous to ship. For legal tools, the correct question is recall on must-never categories, not overall accuracy. A ship decision based solely on overall accuracy is a liability. Option B is the correct answer. Option A is wrong — there is no universal 99% threshold for production systems; acceptable accuracy depends on the failure cost per category, and even 99% overall accuracy can be insufficient if the 1% failures are all in high-risk clause detection. Option C is wrong — contract analysis can absolutely be evaluated quantitatively using precision, recall, and F1 per category; claiming quantitative evaluation is impossible is a misconception that lets teams avoid the hard measurement work. Option D is false — peer review by the legal industry is a useful validation step but has no bearing on whether overall accuracy is the right primary metric; the metric selection problem is independent of who validates the eval set.",
    },
    takeaway: "Design evals around your worst-case failure mode, not your average case. For high-stakes extraction, recall on the must-never categories is the load-bearing metric. Define the minimum acceptable recall threshold before building the eval — the eval should answer a specific question ('does it meet the bar?'), not produce a number you interpret after the fact.",
  },

  "rag-eval": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your RAG pipeline for a customer support tool has been running for a month. The team reports it's 'working well' based on internal impressions. Before presenting to leadership, you want to replace impressions with numbers. You have no existing eval infrastructure. You need to build a minimal RAG-specific eval suite from scratch in one week.",
    explanation: [
      "Eval-design established the structure for an effective eval: must-do and must-never test lists, pre-committed thresholds, and recall as the load-bearing metric for high-stakes extraction. The failure it left unsolved: what does 'recall' mean in a RAG system specifically? A single end-to-end accuracy score collapses three separately breakable sub-systems into one number — retrieval quality, context faithfulness, and generation correctness can each fail independently. When accuracy is 78%, you don't know which stage is failing or which to fix. RAG evaluation requires measuring all three separately.",
      "Building the ground-truth dataset: take 50–100 representative real user questions, manually identify which chunk in your index contains the correct answer (retrieval ground truth), and write the correct answer (answer ground truth). Use real questions from production logs, not synthetic ones — production questions expose the actual query distribution and difficulty your system faces. This annotation is the main upfront cost and is reusable across all future model, retrieval, and prompt changes.",
      "Three RAG-specific failure patterns to test explicitly: retrieval failure — the answer is in the corpus but retrieval misses it (test with questions where the answer chunk is confirmed to exist); faithfulness failure — the right chunk is retrieved but the model ignores it and answers from memory (test by checking whether answer claims appear verbatim in the retrieved context); hallucination under no-retrieval — no relevant chunk exists but the model invents an answer rather than saying it doesn't know (test with out-of-distribution questions). These three patterns require separate metrics — retrieval recall@k, faithfulness rate, and abstention rate — because each has a different fix: re-embedding and reranking, faithfulness prompting, or explicit no-retrieval handling.",
    ],
    mcq: {
      question: "A RAG system achieves 78% end-to-end answer accuracy. What does this tell you about where to improve the system?",
      options: [
        "The retrieval stage is working well since over three-quarters of answers are correct",
        "The generation model needs to be upgraded since it's failing 22% of the time",
        "Nothing diagnostic — end-to-end accuracy conflates retrieval failures, faithfulness failures, and generation failures; you need retrieval recall@k and context faithfulness measured separately to know which stage to fix",
        "78% is above the industry standard for RAG systems and no improvement is needed",
      ],
      correct: 2,
      explanation: "End-to-end accuracy is the least actionable metric in a RAG system because it doesn't tell you whether the failure came from retrieval, augmentation, or generation. 78% accuracy could mean 100% retrieval recall with 78% generation accuracy, or 78% retrieval recall with 100% generation accuracy — completely different fixes. Decomposed stage metrics (retrieval recall@k, faithfulness rate) are the only way to point the improvement effort at the right stage. Option C is the correct answer. Option A is wrong — a 78% end-to-end answer accuracy says nothing specific about retrieval stage health; retrieval could be perfect (100% recall@3) while generation failures drive the 22% miss rate, or retrieval could be the bottleneck with generation performing perfectly on whatever it receives. Option B is wrong — the generation model may be performing perfectly on the chunks it receives; the 22% failure rate could be entirely a retrieval problem, making a model upgrade the wrong fix. Option D is false — 78% is not a universal industry standard and no such benchmark threshold exists; whether it's acceptable depends entirely on the task, user impact, and cost of failure.",
    },
    takeaway: "RAG evaluation must be decomposed by stage: retrieval recall@k, context faithfulness, and answer correctness are three separate metrics that diagnose three separate failure modes. End-to-end accuracy alone tells you the system is imperfect — it doesn't tell you where to fix it. Build the 50-question ground-truth set once; it pays dividends across every future model iteration.",
  },

  // ── Production Systems track ──────────────────────────────────────────────────

  "cost-latency-concepts": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A startup's inference bill jumped from $8K to $34K in 30 days after a product change. Request volume increased 20%, average output length stayed the same, but average input tokens per request grew from 800 to 3,200. You need to attribute the cost increase to the right variable before you can propose a fix — and explain why a 4× input growth caused a 4× bill increase, not a proportionally smaller one.",
    explanation: [
      "KV cache established that token count drives inference memory. The same token count drives inference cost — through a different mechanism. LLM inference billing has two components at different rates: input tokens (the prompt — typically $2.50–5.00 per million tokens in leading models) and output tokens (the generated response — typically $10–15 per million). The asymmetry is counterintuitive: input tokens cost less per token but dominate total cost in short-output, long-prompt applications. In the scenario, input tokens quadrupled (800→3,200) while output stayed at ~150 tokens. At $2.50/1M input, $10/1M output: old cost per request = 800×$2.50/1M + 150×$10/1M = $0.002 + $0.0015 = $0.0035. New cost = 3,200×$2.50/1M + 150×$10/1M = $0.008 + $0.0015 = $0.0095 — a 2.7× increase per request compounded by 20% volume growth, producing the ~4× bill increase.",
      "Latency has a different structure than cost. Total latency = TTFT + (output_tokens × TPOT), where TTFT is Time to First Token (how long before any output appears — scales with input length and queuing) and TPOT is Time Per Output Token (roughly constant per generated token at a given model size and hardware tier). A 500-token prompt at 50ms TTFT plus 200 output tokens at 20ms/token = 4.05 seconds end-to-end. For the 3,200-token scenario: TTFT grows roughly proportionally to input length — approximately 200ms at the same hardware. Same output: 200ms + 4,000ms = 4.2 seconds. The latency increase is smaller than the cost increase because TPOT dominates total latency when output is long. But TTFT drives perceived responsiveness in streaming UIs — users see slower first-token response directly from input growth even when total generation time is similar.",
      "Prompt inflation is the most common source of surprise cost increases. Four components to audit: (1) System prompt — did a product change add more context or instructions? (2) Few-shot examples — were examples added to improve output quality? (3) Retrieved context — did the RAG pipeline begin returning more chunks per query, or longer chunks? (4) Conversation history — are all prior turns being included, growing unboundedly with conversation length? Fix hierarchy: prompt caching (providers offer 50–90% discounts on repeated prefix tokens like stable system prompts and fixed few-shot examples — reducing cost for unchanged input components without altering prompt structure); prompt compression (trim low-information tokens from retrieved context using a reranker to select only highest-relevance chunks); right-size the model (use a smaller model for sub-tasks that don't need flagship capability). The scenario bill increase is entirely attributable to input growth — identifying which component grew (likely retrieved context or conversation history based on the 4× scale) is the prerequisite to any mitigation proposal.",
    ],
    mcq: {
      question: "A request costs $0.008 in input tokens and $0.0015 in output tokens. Cutting output length by 50% would reduce total cost by approximately:",
      options: [
        "50% — output is exactly half of total cost in all LLM pricing models",
        "Nothing — output tokens are billed separately and don't affect the input-side total",
        "~40% — output tokens are always priced higher than input tokens so they dominate cost reduction",
        "~16% — output tokens represent $0.0015 of a $0.0095 total; halving them saves $0.00075, roughly 8% of total cost",
      ],
      correct: 3,
      explanation: "In this cost breakdown, input tokens ($0.008) dominate over output tokens ($0.0015). Halving output saves $0.00075 on a $0.0095 total — roughly 8%, not 50%. The common misconception is that because output tokens have a higher per-token rate, they always dominate total cost. They do when output is long relative to input, but in prompt-heavy applications with short outputs, input tokens drive the bill. Option D is the correct answer. Option A is wrong — it assumes output tokens are 'exactly half of total cost,' which is only true in a specific token-count and pricing scenario; in this example, the math shows output is $0.0015 of a $0.0095 total, nowhere near half. Option B is wrong — output tokens are billed alongside input tokens and do contribute to total cost; halving them saves a real dollar amount, just a small one relative to the input-dominated total. Option C is wrong — whether output or input tokens dominate depends on the ratio of prompt length to output length and the specific pricing model; in long-prompt, short-output applications like RAG, input dominates even though output costs more per token.",
    },
    takeaway: "Prompt inflation is the most common source of surprise cost increases. Audit what changed in your prompt template before reaching for model downgrades. Input tokens dominate total cost in short-output, long-prompt applications — even though they're cheaper per token. Prompt caching for stable prefixes (system prompts, few-shot examples) is the fastest high-ROI cost reduction.",
  },

  "latency-planner": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A document Q&A tool must respond in under 3 seconds per interaction. Current measured latency: p50=2.8s, p95=5.4s, p99=8.2s. Retrieval takes ~300ms. The LLM call accounts for the rest. Target is p95 ≤ 3s. You need to understand which levers are available and what each actually moves before tuning anything.",
    explanation: [
      "Cost-latency-concepts established the latency equation: TTFT + (output_tokens × TPOT). The current measurements make this concrete. With p50=2.8s and 300ms retrieval, the LLM contributes ~2.5s at the median. At p95=5.4s, the LLM contributes ~5.1s — more than double the median LLM latency. That gap indicates shared-infrastructure queuing or throttling is driving tail latency, not longer generation time. Before touching any lever, attribute the latency budget: retrieval (300ms, fixed), TTFT (scales with input length and queuing delay), generation (output_tokens × TPOT), network overhead. Each lever targets one of these terms specifically.",
      "Levers in order of typical impact: (a) Reduce output length — if the model generates 300-word explanations when 80-word answers suffice, explicit concision instructions halve generation time with no model change. Check this first by reading TPOT × token_count from inference logs — it's the most commonly overlooked optimization. (b) Streaming — start rendering as the first tokens arrive; TTFT becomes perceived latency rather than total generation time. A 5.4s total response that starts streaming in 400ms is experienced as a responsive tool by users, unchanged wall-clock latency and all. (c) Reduce input length — fewer prompt tokens lower TTFT. Trim system prompt, reduce retrieved context from 10 chunks to 3, implement prefix caching for stable system prompt sections. (d) Smaller or faster model — a model 3× cheaper is often 2–3× faster at the same hardware tier, and most document Q&A tasks don't require frontier capability.",
      "P99 at 8.2s is a different failure mode from p95. The 3.3-second gap between p95 and p99 indicates infrastructure events — cold starts on serverless endpoints, shared-resource queuing spikes, or token-rate throttling — not generation time variance. Application-layer optimizations cannot fix this. Infrastructure mitigations: provision a dedicated throughput endpoint rather than the shared public endpoint, eliminating shared-queue spikes that no application optimization can touch. Set a timeout-plus-retry: for requests exceeding 4s, retry with a smaller fallback model — the answer may be slightly worse but 3s beats 8s for Q&A. Async processing with a notification callback removes the slowest requests from the perceived-latency bucket entirely. Reducing p95 from 5.4s to ≤3s is achievable through streaming plus output-length reduction plus possibly a model downgrade. Reducing p99 from 8.2s requires a serving infrastructure change.",
    ],
    mcq: {
      question: "A streaming document Q&A tool has p95 total latency of 5.4s, but users report the tool feels fast. The most likely reason is:",
      options: [
        "Streaming delivers the first tokens within ~400ms — users see content immediately and experience the remainder as progressive loading, so perceived latency is driven by TTFT, not total generation time",
        "P95 measurements are always lower than actual latency because they exclude network overhead",
        "The model uses speculative decoding, which generates multiple output tokens per forward pass and reduces wall-clock time",
        "The UI hides total latency by showing a fixed 2-second progress animation before rendering",
      ],
      correct: 0,
      explanation: "Perceived latency in a streaming UI is dominated by TTFT — the delay before the first character appears. Once content is streaming, users experience the output as a natural flow rather than a wait. A tool that starts streaming in 400ms and completes in 5.4s feels faster than a non-streaming tool that delivers the complete response in 3.0s. Streaming is often the highest-leverage perceived-latency improvement available without changing the model. Option B is wrong — p95 measurements do not systematically exclude network overhead; the latency percentiles include all observed end-to-end latency, and being 'lower than actual' is not a property of p95 vs. other percentiles. Option C is wrong — speculative decoding does reduce token generation time but is not the explanation for why users report the tool 'feels fast' despite high total latency; streaming start time is what drives perceived responsiveness regardless of how tokens are generated internally. Option D is wrong — a 2-second progress animation would itself introduce a 2-second delay before content appears, making the tool feel slower, not faster; progress animations increase perceived latency when they delay rendering.",
    },
    takeaway: "Optimize for perceived latency first: streaming + fast TTFT changes user experience without changing wall-clock latency. For sustained p95 reduction, reduce output length and prompt length before upgrading the model. For tail latency (p99), provisioned throughput endpoints eliminate shared-queue spikes that no application-layer optimization can fix.",
  },

  "observability-concepts": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "A RAG pipeline for B2B analytics has been running for 3 months. The customer success team reports answer quality 'seems worse' over the past 2 weeks, but no code was deployed. Error rate is flat at 0.1%. Request volume is stable. You need to design a minimal observability stack that would have detected this regression automatically — and explain why error rate is the wrong primary metric for LLM systems.",
    explanation: [
      "Eval-loop established that an automated scorer against a fixed dataset catches known failure modes. The failure that creates: between eval runs, you're blind. Worse — evals only catch failure modes you thought to test. An HTTP 200 response with a confident, grammatically correct, completely wrong answer is indistinguishable from a correct answer from the error-rate perspective. This is the structural problem: LLM system quality can degrade while error rate stays flat at 0.1%, because the model successfully generates a response — it's just a worse one. Traditional API observability measures failures; LLM observability must measure quality, which is harder. The signals that work: retrieval quality indicators (average retrieval score distribution, percentage of queries where the top-k chunk contains a keyword from the query), answer quality proxies (answer length distribution, response format compliance, citation completeness), and downstream behavior signals (user thumbs-up/down rate, follow-up question rate, session abandonment after response).",
      "The most common causes of quality regression without code changes: (1) Data drift — indexed documents went stale (products, policies, or data changed but the vector index wasn't re-embedded). (2) Silent model version update — inference providers update model weights behind stable API endpoint names without announcements. GPT-4-turbo today differs from GPT-4-turbo 6 months ago. (3) Traffic distribution shift — a new user cohort with different query patterns entered the system; the model performs well on the original distribution but poorly on the new one. (4) Third-party dependency drift — an embedding model API or re-ranker changed behavior. Observability tells you which of these is happening.",
      "Minimum viable LLM observability stack: log every request with input tokens, output tokens, retrieval scores, model name (from API response headers), and response time. Sample 5% of responses for lightweight LLM-as-judge quality scoring (cheap, scalable). Alert on: answer length distribution shift (Z-score — a measure of how many standard deviations a value is from the historical mean — > 2), retrieval score mean shift > 10%, user feedback rate drop > 10% week-over-week, model name in API headers differing from expected version. With this stack, the 2-week quality regression surfaces as a retrieval score shift or answer length distribution change within hours, not after weeks of support tickets.",
    ],
    mcq: {
      question: "A RAG pipeline's error rate stays flat at 0.1% while answer quality degrades. Which metric would most reliably detect this regression automatically?",
      options: [
        "Total token count per request — more tokens always corresponds to better answer quality",
        "Retrieval score distribution shift — a change in the mean or variance of embedding similarity scores for retrieved chunks is a direct signal that retrieval quality changed, even when the system generates responses without errors",
        "API response latency — quality degradation consistently increases response generation time",
        "System prompt token count — drift in system prompt length is the primary indicator of quality regression",
      ],
      correct: 1,
      explanation: "Retrieval score distributions change when the quality of what's being retrieved changes — even if the system never errors. A drop in mean similarity score or a shift toward lower-scoring retrievals indicates that queries are matching chunks less well, which directly predicts lower answer quality. Error rate measures failures, not quality. Option B is the correct answer. Option A is wrong — total token count per request has no meaningful correlation with answer quality; longer inputs do not produce more accurate or complete answers, and in fact longer prompts can degrade quality through lost-in-the-middle effects. Option C is wrong — quality degradation does not consistently increase response latency; generation time is driven by output length and model size, not answer quality, and a model producing confidently wrong answers takes the same time as one producing correct answers. Option D is wrong — system prompt token count is fixed or nearly fixed across requests and is not a per-request signal; drift in system prompt length would reflect a code change, not an operational quality regression.",
    },
    takeaway: "LLM systems degrade silently — error rate won't tell you. Log retrieval scores, answer length distributions, model version headers, and sample responses for lightweight quality scoring. These signals catch regressions in hours. Silence from error monitoring is not evidence that quality is healthy.",
  },

  "prompt-regression-signals": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your team updated a production prompt template — added 3 few-shot examples and tightened output format instructions. Manual testing on 10 queries looked good. After deploy, customer support tickets increased 30% over 3 days. You need to determine whether the prompt change caused the regression and design a detection system that catches this before tickets accumulate.",
    explanation: [
      "Observability-concepts established passive monitoring: retrieval score distributions, answer length trends, and sampled LLM-as-judge scoring catch silent regressions automatically. The failure it leaves as a gap: those signals have 24–48 hour lag before a trend is detectable, meaning a prompt change can cause 3 days of support ticket accumulation before the monitoring alert fires. Prompt regression signals are metrics that fire on the first bad response — not after a trend accumulates. The fastest signals: (1) Output format compliance rate — if the prompt specifies JSON output, what percentage of responses parse as valid JSON? A format change that confuses the model shows up as a parse error on request #1, not request #10,000. (2) Downstream parse error rate — if code downstream of the LLM tries to extract specific fields, parse failures appear in application logs immediately. (3) Answer length distribution — over-constrained prompts produce shorter, truncated answers; over-reliance on few-shot examples causes verbose, example-mirroring responses. (4) Refusal rate — prompt changes that confuse the model about its task increase unhelpful 'I can't help with that' responses.",
      "A/B testing is the principled deployment approach: route a percentage of traffic to the new prompt and compare metrics in real time against the old prompt. Most LLM serving frameworks (LiteLLM, PromptLayer, LangSmith) support prompt versioning and traffic splitting natively. Without A/B testing, you're deploying blind: if a regression occurs you know when, but not which change caused it if multiple variables changed simultaneously. Even a 5% traffic split to the new prompt for 1 hour before full rollout gives you statistical signal on format compliance and downstream errors.",
      "For the 3-day attribution window: the 30% ticket increase starting at deploy is strong circumstantial evidence, but not proof. Cluster the support tickets by failure type — if they concentrate around the specific output changes (new format, new length constraint, new few-shot examples), that's causal evidence. Roll back the prompt (not the model) immediately if the ticket type matches the change. Prompt rollbacks take seconds and cost nothing; they're the fastest causal test available.",
    ],
    mcq: {
      question: "Which metric provides the earliest automated signal for prompt regression in production?",
      options: [
        "Net Promoter Score — statistically significant NPS changes indicate quality regression within days",
        "Model temperature — temperature automatically shifts when prompt quality degrades",
        "Output format compliance rate — detectable on the first non-compliant response, before any user feedback accumulates",
        "Output token count — longer outputs always indicate better prompt quality",
      ],
      correct: 2,
      explanation: "Output format compliance fires immediately when a prompt change causes the model to generate non-conforming structure — it doesn't require user feedback to accumulate. NPS takes days or weeks to register and is affected by many factors beyond prompt quality. Option C is the correct answer. Option A is wrong — NPS is a lagging indicator that aggregates user sentiment over long timeframes and is confounded by unrelated factors (pricing, competition, product features); it cannot reliably attribute a specific 3-point score drop to a prompt change that happened this week. Option B is wrong — temperature is a parameter set at inference time, not a variable the model adjusts in response to prompt quality; temperature does not 'automatically shift' when the prompt changes. Option D is wrong — output length has no reliable directional correlation with quality; verbose responses can be low-quality padding and concise responses can be high-quality; length is weakly correlated in both directions depending on the task.",
    },
    takeaway: "Detect prompt regressions before users do. Output format compliance and downstream parse error rate fire on the first bad response. A/B test every prompt change — 5% traffic for 1 hour is enough to detect format failures before full rollout. Make prompt rollback a one-click operation; it's always faster than debugging a regression from support tickets.",
  },

  "quality-drift": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "An enterprise AI writing assistant has been in production for 6 months with no major code or model deployments. Average quality ratings dropped from 4.2 to 3.6 over the past 8 weeks. The team says nothing changed. You need to explain what 'nothing changed' actually means in an LLM system and identify the likely root cause.",
    explanation: [
      "Prompt-regression-signals covered quality declines caused by changes your team made — a new prompt template, new few-shot examples. The failure it left uncovered: 'nothing changed' regressions — quality declines where your team genuinely didn't push any code or config change. 'Nothing changed' means nothing you control changed. The system has external dependencies that change independently: (1) Silent model version update — inference providers update model weights behind stable API endpoint names; 'GPT-4-turbo' today is not the same checkpoint as 6 months ago; providers update for safety, capability, and efficiency without changing the endpoint name or announcing it in application logs. (2) Knowledge base staleness — indexed documents go out of date; users asking about policies or products that changed after the last index rebuild get stale retrievals that produce wrong answers. (3) User distribution shift — new user cohorts with different query patterns entered the system; the model performs well on the original distribution but poorly on the new one. (4) Third-party dependency changes — an embedding API or reranker changed behavior silently. Observability tells you which of these is responsible.",
      "Diagnosing the specific source requires structured investigation: (1) Run your fixed eval set against the current system — if scores are lower than at launch, something changed. (2) Check model version from API response metadata — some providers include a model version or build hash in response headers. (3) Compare your embedding index rebuild timestamp against source document update timestamps in your CMS or database. (4) Segment quality ratings by user cohort and query category — if older users of established query types are also rating lower, it's not distribution shift; if only new users are rating lower, it might be. If the failure is concentrated in specific content categories, it's likely knowledge base staleness.",
      "Preventing drift requires proactive instrumentation: pin model versions when the provider supports it (many expose version-pinning endpoints in addition to 'latest'). Set up automated alerts when source documents were updated more recently than the last index rebuild. Run weekly regression evals on a fixed golden set of 50 questions — silent model version changes produce detectable score movements within one weekly run. Alert on model version fields in API response headers differing from expected. Most of these are 1-day instrumentation investments.",
    ],
    mcq: {
      question: "An LLM system's quality dropped 15% over 8 weeks with no code deployments. The most common root cause in practice is:",
      options: [
        "Network latency increases that degrade token generation quality at the hardware level",
        "Browser cache causing old API responses to be served to users instead of fresh ones",
        "Ambient temperature changes in the data center affecting GPU computation",
        "Silent model version update by the API provider — inference endpoints may serve updated model weights under the same API name without explicit change notifications",
      ],
      correct: 3,
      explanation: "Silent model updates are the most common source of unexplained quality changes in managed API deployments. Providers release updated model versions for safety, efficiency, and capability improvements, and these changes may affect output style, refusal rates, verbosity, or factual grounding in ways that affect quality ratings. Option D is the correct answer. Option A is wrong — network latency affects response speed, not the semantic quality of the text generated; a slower network produces the same answer more slowly, not a worse one. Option B is wrong — API responses are served over HTTPS with no-cache headers in standard client implementations; browser cache does not intercept server-side API calls, and even if it did, it would serve stale but previously correct responses, not degraded ones. Option C is wrong — GPU computation is performed inside temperature-controlled data centers with no material impact on model output from ambient temperature variation; this is not a real failure mode for software systems.",
    },
    takeaway: "'Nothing changed' never means nothing changed in an LLM system. Pin model versions, monitor index freshness, and run weekly regression evals on a fixed golden set. These three instruments catch the four most common sources of silent quality drift before they accumulate 8 weeks of user impact.",
  },

  "cost-attribution": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your company's monthly inference bill is $180K. The CFO asks for a cost breakdown by product team and use case. All requests go through one API key. You have no attribution. The CFO gives you 2 weeks to produce a defensible breakdown before budget season.",
    explanation: [
      "Cost-latency-concepts established that token count drives cost — input tokens at one rate, output tokens at a higher rate — and gave the formula for computing cost per request. The failure it left unsolved: attributing that cost to the organizational structure that produced it. A single API key with a $180K/month bill is a flat number. The token-cost math tells you why it's $180K; attribution tells you which team, product, or feature decision drove it there. That gap matters for budget decisions, optimization prioritization, and organizational accountability — and it cannot be reconstructed retroactively from billing logs. LLM cost attribution requires tagging every inference request at the time of the call with: team (which product or engineering team owns this request), use_case (summarization, extraction, chat, eval), environment (production vs. staging vs. eval runs), user_tier (free vs. paid). Most LLM providers expose user and metadata fields in the API request that appear in billing exports. Instrument this today; the data becomes available in the next billing cycle.",
      "Token-level attribution: input + output token counts per request multiplied by the per-token rate for the specific model used. Track model name per request — different models on the same account have different rates, and model selection decisions are often team-level. Most large inference bills follow a heavy-tail distribution: 20% of requests (long documents, multi-turn conversations with full history, batch eval runs) account for 60–80% of the total. Identifying and optimizing these requests first has far more impact than broadly optimizing all requests.",
      "Attribution enables specific optimization decisions that are invisible without it. If the Eval team is running GPT-4 evaluations that could use GPT-4o-mini at 1/50th the cost without quality loss, you can't surface that without attribution. If one product feature accounts for 40% of costs and has 10% user engagement, that's a build-vs-optimize conversation. Attribution transforms 'we spend $180K on AI' into 'we spend $72K on document summarization, $45K on the search assistant, and $63K on internal evals' — each of which has a different optimization path.",
    ],
    mcq: {
      question: "A company wants to attribute LLM costs to product teams but currently routes all requests through one API key. The fastest path to full attribution is:",
      options: [
        "Add team and use_case metadata tags to every API request — most providers expose user/metadata fields that appear in billing reports, producing retroactive segmentation within the current billing period",
        "Create a separate API key per team and distribute credentials to each team for independent management",
        "Analyze request content retrospectively to infer which team sent each request",
        "Switch to a self-hosted model to get full cost transparency through infrastructure billing",
      ],
      correct: 0,
      explanation: "Adding metadata tags to API requests is instrumentation, not infrastructure — it requires changing a few lines of code in your LLM client wrapper, not credential management overhead or self-hosting complexity. Option B is wrong — separate API keys per team does achieve team-level isolation, but it creates credential management overhead, security surface area (more keys to rotate and audit), and provides no visibility into per-use-case attribution within a team's key; metadata tags achieve both team and use-case attribution with a single key. Option C is wrong — retroactive content analysis to infer which team sent each request is expensive (requiring additional inference or regex classifiers over all historical logs), often ambiguous (many requests are cross-team), and doesn't produce clean attributions in time for the current budget cycle. Option D is wrong — self-hosting gives you infrastructure cost visibility but introduces ops complexity that far exceeds the attribution problem; it solves a different problem (provider dependency) and doesn't automatically produce per-team or per-use-case cost breakdowns without the same metadata tagging effort.",
    },
    takeaway: "Cost attribution requires instrumentation before the bill. Tag every API request with team and use_case. This takes hours to instrument and produces full attribution data within one billing cycle. Without it, you're optimizing blind. With it, you can identify the 20% of requests driving 80% of the bill and target optimization where it has actual impact.",
  },

  "managed-vs-selfhosted": {
    depthTier: "deep",
    interviewWeight: "medium",
    scenario: "Your team at 50M tokens/month is evaluating whether to self-host Llama 3 70B on 2×A100 80GB GPUs vs. using a managed API. The infrastructure engineer says self-hosting will be way cheaper. You want to build the actual cost model before deciding.",
    explanation: [
      "Cost-attribution established where the $180K/month bill comes from — which teams, which use cases. For any team seeing that breakdown for the first time, the next question is inevitable: would self-hosting be cheaper? The question is not rhetorical and the answer is not obvious — it depends entirely on token volume and total cost of ownership. The managed API math first: for a GPT-4-class model at approximately $5/1M input + $15/1M output tokens, with a typical 70/30 input/output split at 50M tokens/month — 35M input × $5/1M + 15M output × $15/1M = $175 + $225 = $400/month. This includes high availability, zero infrastructure ops, and automatic model updates. You pay a premium for operational simplicity.",
      "Self-hosted compute: Llama 3 70B in float16 requires ~140GB VRAM (GPU video memory — the on-card RAM that holds model weights and activations during inference) minimum, so 2×A100 80GB is the minimum viable setup. Cloud pricing: roughly $6–8/hour reserved, $8–12/hour on-demand for 2×A100 instances. At 720 hours/month reserved: $4,300–5,800/month in compute alone. Throughput for Llama 3 70B on 2×A100: approximately 500–1,000 tokens/second at batch size 8. To serve 50M tokens/month: 50M / (750 t/s × 720 hrs × 3,600 s/hr) ≈ 2.6% GPU utilization. You're paying for 97% idle compute.",
      "The true comparison requires total cost of ownership: compute + ops engineering time (managing infrastructure, handling failures, monitoring, model upgrades — estimate 0.25–0.5 FTE (Full-Time Equivalent — a measure of engineer time; 0.5 FTE = half of one engineer's time) at $200–400K/year salary). The monthly conversion: `0.5 FTE × $300K/year ÷ 12 months = $12,500/month engineering cost` — which is how the $4,200–17,000/month equivalent range is derived across the 0.25–0.5 FTE and $200–400K salary bands. Add security and compliance overhead (self-hosted requires your own data governance, audit logging, access controls) plus model upgrade cost (you're responsible for evaluating and deploying new versions). The crossover point where self-hosting becomes cheaper typically occurs at 300–500M tokens/month for a lean team. At 50M tokens/month, the math strongly favors the managed API.",
      "For the scenario: $400/month managed API vs. $16,800–18,300/month self-hosted TCO — the math is not close. Self-hosting at low volume is not a cost optimization; it's a cost increase. The 300–500M token crossover is not a cliff but a gradient — start monitoring TCO recalculation at 150M tokens/month and plan the migration at 250M+. When you do approach crossover: set up a managed-API-compatible serving layer (vLLM, LiteLLM) on a small slice of traffic early, so the migration is a routing change rather than an infrastructure build under volume pressure. Beware the sunk-cost path: teams that self-host prematurely often keep the infrastructure running because the ops FTE is already allocated, even when a TCO recalculation would recommend switching back.",
    ],
    mcq: {
      question: "A team at 50M tokens/month considers self-hosting to reduce costs. The cost comparison should include:",
      options: [
        "Only raw compute cost per token, since operational overhead is the same for all infrastructure choices",
        "Total cost of ownership — compute at actual utilization (often < 5% at 50M tokens/month), plus ops engineering overhead and reliability costs — compared against managed API pricing",
        "The number of model parameters, since larger models are always more expensive to self-host",
        "The managed API cost per token vs. the self-hosted cost per token at theoretical peak throughput (100% GPU utilization), since that measures the true per-token cost ceiling",
      ],
      correct: 1,
      explanation: "At 50M tokens/month, GPU utilization on self-hosted infrastructure is under 5% — meaning you're paying for idle capacity. The compute cost alone may exceed the managed API bill, and ops engineering overhead compounds this. The 'cheaper' intuition for self-hosting only holds at high sustained utilization (40%+), which typically requires 300-500M+ tokens/month. Option B is the correct answer. Option A is wrong — raw compute cost per token excludes the most significant components of self-hosted cost (idle capacity when utilization is low, ops engineering time, reliability infrastructure); operational overhead is not the same across all infrastructure choices and must be included. Option C is wrong — number of parameters is relevant to hardware sizing but not directly to the cost comparison decision; you need actual token throughput, hardware cost, and utilization rate, not a parameter count heuristic. Option D describes a common mistake: using theoretical peak throughput (100% utilization) as the baseline. At 100% utilization, self-hosting is genuinely cheaper per token than managed APIs — this is why the intuition 'self-hosting is cheaper' exists and is technically correct in that one scenario. But 100% utilization requires sustained high traffic, which at 50M tokens/month means less than 5% actual utilization — the comparison should be made at actual utilization, not theoretical peak, because you pay for idle GPU time regardless of whether requests are arriving.",
    },
    takeaway: "Self-hosting is not cheaper until utilization is high. At 50M tokens/month, GPU utilization is ~3% — you pay for idle compute. Add ops overhead and the managed API almost always wins on total cost. Build the actual math: compute at your utilization rate + ops salary equivalent vs. managed API bill. Revisit when monthly token volume exceeds 300–500M.",
  },

  "enterprise-ai-cost-model": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "You're presenting the business case for expanding an AI writing assistant from 100 pilot users to 10,000 enterprise users. Finance wants a cost-per-user model. Legal wants a cost cap. Engineering measured token usage in the pilot but needs to extrapolate with appropriate uncertainty. The naive approach — multiply pilot cost by 100× — is being challenged.",
    explanation: [
      "Managed-vs-selfhosted established the infrastructure cost comparison at a given token volume. The failure it left: infrastructure cost is the supply side. Enterprise cost modeling requires the demand side — how many tokens will 10,000 users actually consume, and how does that distribution interact with pricing? The naive path is multiplying the 100-person pilot cost by 100×. This fails because enterprise user populations are heterogeneous: the pilot population's usage distribution stabilizes at 10,000 users in ways the 100-person pilot didn't reveal. A realistic enterprise distribution: 15% heavy users (3–4× average token volume — power users who rely on the tool for most of their work), 65% average users, 20% light users (sporadic use, 0.2× average). The heavy-user tail is the most common source of enterprise AI budget overruns. Enterprise AI cost modeling requires three components: usage assumptions per cohort (not per average user), per-tier model costs, and infrastructure overhead (API gateway, logging, monitoring, caching layer).",
      "A three-scenario cost model: from the pilot, measure average input tokens per session, output tokens per session, sessions per active user per day, and daily active user rate (what fraction of registered users actually use the tool on a given day). Apply the distribution: at 10K users, 30% DAU (Daily Active Users — the fraction of registered users who use the product on a given day) = 3K active users/day. Apply usage tiers: 450 heavy (3× avg), 1,950 average, 600 light (0.2× avg). Total daily tokens ≈ 450×(3×avg) + 1,950×avg + 600×(0.2×avg). This produces a range (p25, p50, p75 — percentile scenarios where p50 is the median expected cost, p25 is a low-usage optimistic estimate, and p75 is a high-usage conservative estimate) rather than a single number. Finance needs a range with a ceiling, not a point estimate.",
      "Cost caps require enforcement mechanisms: per-user daily token budgets enforced at the application layer, automatic model downgrade when a user exceeds a threshold (switch from GPT-4 to GPT-4o-mini), query caching for repeated prompts (many writing assistants see 15–20% of queries as near-duplicates). The cost model is only useful to Finance if it includes control levers — not just a forecast, but a ceiling and the mechanism that enforces it. Present the model as: expected cost (p50 scenario) + ceiling (p95 scenario with model downgrade enforcement).",
    ],
    mcq: {
      question: "A team extrapolates pilot AI costs to enterprise scale by multiplying pilot cost by the user ratio (100→10,000 = 100×). The key flaw is:",
      options: [
        "Token prices decrease at higher volumes, so the 100× multiplier consistently overestimates cost at scale",
        "Pilot users are always unrepresentative because they're motivated early adopters who use the product far less than average users will",
        "The heavy-user tail — 10–20% of enterprise users consume 3–4× average token volume, making the 100× multiplier an underestimate for that cohort and causing budget overruns at scale",
        "The 100× multiplier only applies to output tokens; input tokens must be calculated independently",
      ],
      correct: 2,
      explanation: "The heavy-user tail is systematically underestimated by average-based extrapolation. In a 100-person pilot, the distribution of usage extremes may not have stabilized. At 10,000 users, the heaviest 10% (1,000 users at 3-4× average) drive a disproportionate share of total cost that the pilot average didn't reveal. The 100× multiplier on the pilot average omits this concentration effect. Option C is the correct answer. Option A is wrong — volume discounts do exist at very high token volumes, but they reduce cost per token modestly (10–20%), not enough to invalidate a 100× multiplier; and the question is about the flaw in the linear extrapolation method, not about whether discounts exist. Option B is wrong — pilot users being motivated early adopters is a real concern but typically makes them heavier users than average, not lighter; if anything, a pilot heavy-user bias would make the 100× multiplier an overestimate, not an underestimate, which is the opposite of the identified flaw. Option D is wrong — input and output tokens must always be calculated using their respective per-token rates, but the 100× multiplier applies to total cost, not separately to input and output; this is not a separate calculation requirement that makes the multiplier flawed.",
    },
    takeaway: "Enterprise AI cost modeling requires a user distribution assumption, not a single average. The heavy-user tail (10–20% of users driving 50–60% of costs) is the most common source of budget overruns. Present Finance a p50 scenario and a p95 ceiling with an explicit enforcement mechanism — model downgrade thresholds and per-user daily budgets — not a point estimate.",
  },

  // ── Prompt Engineering — high-priority stub ───────────────────────────────────

  "zero-shot": {
    depthTier: "light",
    interviewWeight: "medium",
    scenario: "A new team member asks why the team bothers writing detailed prompt instructions when 'you can just ask the model what you want.' They run a zero-shot test on your classification task and get 71% accuracy vs. your current 89%. You need to explain what zero-shot prompting is, when it's sufficient, and specifically why it fell short here — without dismissing it as a technique.",
    explanation: [
      "Instruction-following training (SFT and RLHF) gave models the ability to respond to natural language task descriptions without examples — zero-shot capability is the product of that training. Zero-shot prompting applies this directly: describe the task in plain language, provide the input, get output. No annotation effort, no example curation cost. It works well when the task closely matches patterns the model saw extensively in pre-training: sentiment classification, factual Q&A, summarization, common text transformations. For tasks like these, zero-shot is frequently sufficient — and the cost of annotating examples is unnecessary overhead.",
      "Zero-shot falls short when the task has non-obvious decision rules that aren't captured by the category names alone. 'Billing' and 'technical' are intuitive in isolation, but an email about API authentication failures with billing implications requires a disambiguation rule. Without examples showing which category wins in boundary cases, the model defaults to its prior from pre-training — which may not match your business logic. The 18-point gap (71% vs 89%) almost certainly comes from these ambiguous boundary cases, not from the easy clear-cut examples. Zero-shot and few-shot aren't competing approaches — they're different levels of task specification. Start with zero-shot (fast, no annotation cost), measure, and use the failures to identify exactly which boundary cases your few-shot examples need to cover.",
    ],
    mcq: {
      question: "Zero-shot prompting achieves 71% accuracy on a 12-category classification task. The most likely source of the remaining 29% errors is:",
      options: [
        "Zero-shot works well only for tasks the model encountered in RLHF feedback data; tasks learned purely from pre-training require at least one example to activate the relevant capability",
        "Zero-shot reliability improves consistently with longer, more detailed task descriptions because the model has more signal to condition on",
        "The model ignores the instruction and generates random category labels",
        "Boundary cases where two categories both apply — zero-shot has no examples showing which category takes priority under your specific business rules, so the model resolves ambiguity using pre-training priors that don't match your taxonomy",
      ],
      correct: 3,
      explanation: "Zero-shot provides category names and a general instruction. It doesn't specify disambiguation rules for boundary cases. Errors concentrate at the boundary — emails that partially fit two categories, edge cases with unusual phrasing, or situations where your business rule differs from the model's pre-training prior. Few-shot examples fix exactly this by showing the model how you resolve the cases it gets wrong. Option D is the correct answer. Option A is a plausible-sounding but false claim: zero-shot capability emerges from pre-training scale, not from RLHF feedback data specifically. A model can classify sentiment, answer factual questions, and perform many tasks zero-shot entirely from pre-training — RLHF shapes how the model responds (helpfully, safely) not which tasks it can perform. Option B sounds reasonable — more information seems like it should help — but empirically, very long zero-shot task descriptions with extensive caveats and edge-case prose often hurt performance. The model attends across all the instructions and the performance degrades when the specification is more verbose than precise. The real fix for boundary cases is examples, not longer descriptions. Option C is wrong — LLMs do not generate random labels; they generate probabilistically conditioned outputs that, while imperfect, systematically reflect their understanding of the category names and the input text.",
    },
    takeaway: "Zero-shot is the correct starting point for any new task — it costs nothing and reveals where your task is ambiguous. When it falls short, the failures tell you exactly what few-shot examples to write. Don't skip zero-shot to go straight to few-shot; the zero-shot failures are your example curation guide.",
  },

  // ── Foundation Models track ───────────────────────────────────────────────────

  "model-families": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your company is evaluating LLMs for three different use cases: a real-time customer chat widget, a nightly batch document summarization job, and an internal coding assistant for engineers. A stakeholder suggests 'just use GPT-4 for everything.' You need to explain why model family selection matters and recommend the right model tier for each use case.",
    explanation: [
      "Cost-latency-concepts established that model size is the primary driver of cost and latency — larger models have higher TTFT, higher per-token cost, and lower throughput at the same hardware tier. The failure that creates: 'which model?' must become a per-use-case decision, not a platform-level default. Using the same model everywhere — typically the best available — is the most common source of avoidable inference cost overruns. Model families are the structured answer: every major provider offers a spectrum from frontier to small/fast, and the gap between tiers is 20–50× in cost and 3–5× in latency. Frontier models (GPT-4o, Claude Opus, Gemini Ultra) are highest capability, highest cost, highest latency. Mid-tier models (GPT-4o-mini, Claude Sonnet, Gemini Flash) balance capability and efficiency. Small/fast models (Claude Haiku, Gemini Flash Lite) optimize for speed and cost at some quality tradeoff. Open-source families (Llama 3, Mistral, Qwen) offer self-hosted options across the same size spectrum.",
      "Model selection is a task-capability matching problem, not a 'biggest is best' problem. For the three use cases: the real-time chat widget has a 500ms latency budget — a frontier model typically takes 2–5s TTFT (Time To First Token — how long until the model begins outputting text) at peak load. A mid-tier or small model is required. The nightly batch job has no latency constraint but high volume (thousands of documents) — a mid-tier model at 1/10th the cost of frontier may be acceptable if summarization quality holds. The coding assistant needs strong reasoning and code generation — this is where frontier models justify their cost, because errors in generated code compound into debugging time that costs more than the API premium.",
      "The practical evaluation process: define the minimum acceptable quality bar per use case (not 'best possible quality,' but 'good enough to ship'). Test your actual task — benchmarks like MMLU (Massive Multitask Language Understanding — a widely-used academic reasoning benchmark) measure reasoning, not your specific summarization quality. Measure cost and latency at your expected request volume. The answer is almost never 'use the same model for everything.' Mixing model tiers across use cases — frontier for high-stakes reasoning, mid-tier for content generation, small for classification and routing — is the production pattern that controls costs while maintaining quality where it matters.",
    ],
    mcq: {
      question: "A company uses a frontier model (GPT-4o) for all use cases including simple intent classification on customer messages. The most likely consequence is:",
      options: [
        "Unnecessary cost and latency — intent classification is well within the capability of a small/fast model at 1/20th the price; frontier model quality doesn't improve classification accuracy on simple tasks but adds significant per-request cost",
        "Better accuracy on classification tasks since frontier models are strictly superior on all tasks",
        "Reduced latency since frontier models have more optimized inference infrastructure",
        "No difference — model family only affects creative writing tasks, not classification",
      ],
      correct: 0,
      explanation: "Frontier models provide marginal or no quality improvement on simple classification tasks that small models already handle well. A simple intent classifier (5–10 categories, clear examples) achieves near-identical accuracy with GPT-4o-mini or Claude Haiku vs. GPT-4o, at 20–50× lower cost. The quality ceiling for this task is determined by task complexity, not model size. Paying for frontier-model compute on simple tasks is the most common source of avoidable LLM cost overruns. Option B is wrong — frontier models are not strictly superior on all tasks; for simple classification with well-defined categories, smaller models match or approach frontier quality because the task complexity is well within their capability ceiling; 'more parameters always means better classification' is the exact misconception this module corrects. Option C is wrong — larger frontier models are generally slower and more expensive than smaller models on the same infrastructure tier; they have more optimized serving at providers, but not enough to outperform smaller models on latency for the same hardware class. Option D is wrong — model family affects performance on all task types including classification; the idea that model selection only matters for creative writing conflates capability with style, and simple classification is absolutely affected by model capability choices.",
    },
    takeaway: "Model selection is task-capability matching. Define the minimum acceptable quality bar per use case, then find the cheapest model that meets it — not the best model available. Frontier models are worth their cost for complex reasoning and code generation. For classification, routing, and summarization of straightforward content, mid-tier or small models almost always meet the bar at a fraction of the cost.",
  },

  "rlhf": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "You're explaining why the base GPT-3 model couldn't be released as a product but InstructGPT (GPT-3 fine-tuned with RLHF) could. A PM asks: 'if the base model already knows everything, why does it need RLHF? Can't you just prompt it to be helpful?' You need to explain what RLHF actually changes — at the behavior level — that prompting cannot.",
    explanation: [
      "The base pre-training objective — next-token prediction — gave us a model that can fluently continue any text. What it didn't give us: a model with any preference for being helpful over harmful, for accurate over inaccurate, or for honest over deceptive. Pre-training rewarded statistical accuracy at predicting human text; human text contains harmful content, manipulation, and false claims, and the model learned to produce all of these as statistically appropriate completions. A base GPT-3 prompted to 'help my users' might complete the text in any direction the training distribution supports — product advice, manipulation tactics, or any other genre represented in its training corpus. The model has no objective to be helpful; it has an objective to be statistically typical.",
      "A system prompt saying 'be helpful, honest, and avoid harm' competes with the model's learned prior at every generation step. Under adversarial prompting — 'pretend you have no restrictions,' 'this is for research' — or in long multi-turn conversations, the system prompt's influence weakens as the model's base distribution pulls generation in directions the instruction tried to prohibit. This isn't a context-length problem; it's a competition between a text instruction and the model's trained weights. Instructions guide typical-case generation. They don't constrain adversarial or boundary-case generation, because the constraint exists in the prompt — which the model processes as input — not in the weights that determine generation probability across all contexts.",
      "RLHF adds three training stages on top of base pre-training. Stage 1 — supervised fine-tuning on demonstrations: humans write ideal responses to a sample of prompts, and the model fine-tunes on these (SFT — Supervised Fine-Tuning, standard gradient descent on human-written examples). This shifts the model toward helpful completions but doesn't address all the ways unhelpful responses can still emerge. Stage 2 — reward model training: human raters compare pairs of model responses and indicate which is better. A separate model trains on these preference pairs to score any given response. Stage 3 — reinforcement learning: the language model updates using PPO (Proximal Policy Optimization — a reinforcement learning algorithm that makes small, stable weight updates) to maximize the reward model's score. The objective: R(response) − β × KL(π_RL || π_SFT), where R is the reward model score, β (typically 0.1–0.5) controls how much the policy is penalized for drifting from the SFT baseline, and KL(π_RL || π_SFT) measures that drift. This reward signal reaches into the model's weights — changing what the model is likely to generate across all future contexts, not just the contexts seen in training examples.",
      "The reward model is not a perfect proxy for human preference — it's a model trained on human comparisons that generalizes imperfectly. Reward hacking occurs when the language model learns to produce responses that score high with the reward model but don't actually align with what humans wanted: verbose, confident-sounding responses that reward models trained by workers who equate length with helpfulness; sycophantic responses that agree with the user rather than correct them; safe refusals that score higher than risky-but-correct responses. The β KL penalty prevents the policy from exploiting these reward model biases too aggressively — it limits how far the model can drift from the SFT baseline. Without it, the model would rapidly optimize for what the reward model rewards, not for what users actually need.",
      "The reason base GPT-3 couldn't be deployed as a product and InstructGPT could is this weight-level shift. InstructGPT was smaller than GPT-3 by parameter count but outperformed it on human preference rankings — because alignment redirected capabilities, not just guided them. The model's knowledge of harmful content isn't deleted; it's deprioritized. RLHF raises the floor of safe behavior across the typical input distribution; it doesn't make safe behavior inviolable under all adversarial conditions. That remaining gap — where adversarial prompting can still elicit unsafe outputs — is why subsequent techniques (Constitutional AI, DPO) were developed, and why RLHF alignment combined with system prompting is more robust than either alone.",
    ],
    mcq: {
      question: "Why can't a detailed system prompt fully replace RLHF alignment for safety?",
      options: [
        "System prompts are limited to 1,000 tokens, which is too short to specify all safety requirements",
        "System prompts are context-level instructions that compete with other inputs and can be overridden by adversarial prompting or long conversations; RLHF modifies model weights so safe behavior is the default regardless of context",
        "RLHF uses a larger dataset than any system prompt can provide",
        "System prompts only affect the first response; RLHF affects all responses in a conversation",
      ],
      correct: 1,
      explanation: "The distinction is context vs. weights. A system prompt is text the model processes alongside user input — it influences but doesn't constrain generation. RLHF modifies the model's parameters so that safe, helpful responses have higher probability under any context. A base model with a safety system prompt and an RLHF-aligned model without one produce very different behavior under adversarial conditions — because the prompt can be overwhelmed, but weights cannot be overridden from context. Option B is the correct answer. Option A is wrong — system prompts are not limited to 1,000 tokens; modern context windows support system prompts of 10,000+ tokens, and detailed safety specifications can be written in a few hundred tokens anyway; token length is not the practical constraint on system prompt safety coverage. Option C is wrong — RLHF uses a specialized dataset of human preference pairs, not a larger version of a general pre-training dataset; the distinction is the type of training signal (human preferences on response quality) not the volume of data relative to a system prompt. Option D is wrong — system prompts do not only affect the first response; they persist throughout the conversation context, but they can be overwhelmed by accumulated user turns and adversarial framing in later turns — which is the actual limitation, not a first-turn-only restriction.",
    },
    takeaway: "RLHF shifts the model's optimization target from 'statistically typical text' to 'helpful, harmless responses' by training on human preference data. This is a weight-level change, not a prompting technique. It's why base models need careful prompting to be safe and RLHF'd models are safe by default. Prompting adds guidance on top; RLHF changes the underlying behavior.",
  },

  "scaling-laws": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "Your team has a fixed compute budget for training a domain-specific model. A researcher argues the 70B parameter model will always outperform a 7B model. You have 1.5T tokens of domain training data available. You need to evaluate this claim using scaling law principles before committing the compute budget.",
    explanation: [
      "Before scaling laws, compute allocation was intuition. Teams trained large models because large models performed better — but there was no principled way to predict by how much, or whether a given budget was better spent on more parameters or more training data. Kaplan et al. (2020) provided the first empirical map: loss follows a power law in both model size N and dataset size D independently. Each 10× increase in parameters or tokens reduces loss by a predictable amount. Useful signal — but it left the joint allocation question unanswered. The field interpreted the Kaplan results as: given a compute budget, maximize parameters. GPT-3 (175B parameters, ~300B training tokens) followed this logic.",
      "Hoffmann et al. (2022) — the Chinchilla paper — ran controlled compute-matched experiments and found the GPT-3 generation of models was systematically undertrained. The problem: Kaplan's training runs varied model size while holding training tokens roughly constant at 300B. This confounded the parameter-vs-token question. Chinchilla varied both. The result: at typical training scales, compute-optimal training requires approximately 20 training tokens per parameter — this is the approximation that holds across most practical model sizes, though the precise Chinchilla finding is a frontier curve where the optimal ratio shifts slightly at extreme compute budgets. A 7B model needs ~140B tokens; a 70B model needs ~1.4T tokens. Below that ratio, you're paying for parameters you haven't trained sufficiently. The underlying compute relationship is `C ≈ 6 × N × D` where the 6× factor accounts for the forward pass, backward pass, and optimizer step across all parameters and tokens — fixed compute budget should be split roughly evenly between scaling parameters and scaling tokens, not concentrated in parameters alone.",
      "Applied to the scenario: 1.5T tokens, fixed compute budget. By Chinchilla, 1.5T tokens is compute-optimal for a ~75B parameter model — the 70B model is close to optimal. The 7B model at 1.5T tokens is receiving 10× more data than Chinchilla prescribes. Counterintuitively, a deliberately over-trained smaller model often matches or outperforms an under-trained larger model at the same total FLOPs — the smaller model absorbed more signal per parameter. This is the logic behind Llama 2 and Llama 3: Meta over-trained smaller models past compute-optimal to get better inference performance per dollar at deployment. The researcher's claim that 70B 'always outperforms' 7B is wrong — it depends entirely on the training token budget.",
      "Scaling laws predict pre-training loss, not downstream task performance on domain-specific benchmarks — that requires evaluation. But the parameter-to-token ratio is the right prior for compute budget allocation before committing to a full training run. The inference-time implication is the piece the claim misses entirely: a 70B model is 5–10× more expensive per inference token than a 7B model on the same hardware. If a 7B model can reach equivalent loss by training on more tokens, every future query is cheaper permanently. The question is not 'which parameter count wins at training?' — it's 'what compute-optimal size maximizes quality-per-inference-dollar given this token budget?'",
    ],
    mcq: {
      question: "The Chinchilla scaling laws suggest that for a fixed compute budget, model quality is maximized when:",
      options: [
        "Maximizing model size always yields better results, regardless of training token count",
        "Training data should always be at least 1,000× the number of model parameters",
        "Model parameters and training tokens are balanced at roughly 1 parameter per 20 training tokens — under-training large models underperforms an equivalently-compute-budget smaller model trained on proportionally more data",
        "Model quality scales linearly with parameter count and is independent of training data volume",
      ],
      correct: 2,
      explanation: "Chinchilla's key finding was that prior large models (GPT-3, Gopher) were significantly under-trained relative to their size — too many parameters, too few tokens. At a fixed compute budget, the optimal strategy is to scale parameters and tokens proportionally at roughly 1:20. A smaller model trained on 10× more data often outperforms a larger model trained on 1× the data at the same total FLOPs (floating-point operations — a standard measure of training or inference compute). Option C is the correct answer. Option A is wrong — it states the exact misconception Chinchilla refuted: maximizing model size regardless of training token count leads to under-trained large models that are outperformed by proportionally trained smaller ones at the same compute budget. Option B is wrong — Chinchilla's ratio is approximately 20 training tokens per parameter, not 1,000; a 1,000:1 ratio would massively over-train smaller models and is not what the scaling laws prescribe. Option D is wrong — model quality does not scale linearly with parameter count alone; training data volume is an equally important factor, and at a fixed compute budget the joint parameter-token relationship determines loss, not parameter count independently.",
    },
    takeaway: "Bigger models trained on insufficient data underperform smaller models trained proportionally. Chinchilla compute-optimal training: ~20 tokens per parameter. For inference-constrained deployments, deliberately over-training a smaller model often achieves better quality-per-inference-dollar than under-training a larger one at the same compute budget.",
  },

  "lora": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "A team needs 10 domain-specific fine-tuned versions of a 70B model (legal, medical, finance, retail, HR, etc.) for different enterprise clients. Full fine-tuning produces 10 separate 140GB checkpoints — 1.4TB of storage and 10× the inference infrastructure. A researcher proposes LoRA. You need to evaluate whether this is operationally viable and understand the quality tradeoff.",
    explanation: [
      "Ten domain-specific models needed from a 70B base. Full fine-tuning produces 10 independent checkpoints: 10 × 140GB = 1.4TB of storage, 10 separate inference stacks, 10 deployment pipelines to maintain. The problem isn't primarily storage — it's operational. Each domain requires its own serving environment. At 20 or 50 domains, the infrastructure multiplies proportionally. The underlying question: does domain adaptation genuinely require updating all 70 billion parameters, or do the relevant changes operate in a much smaller subspace of the weight space?",
      "LoRA's answer is the latter. Instead of updating the full m×n weight matrix directly, LoRA decomposes the update as ΔW = A × B, where A has shape m×r and B has shape r×n, with r much smaller than both m and n. The parameter cost makes this concrete: parameters_added = m×r + r×n versus parameters_full = m×n. At m=n=4096 and r=8: 4096×8 + 8×4096 = 65,536 added parameters versus 4096×4096 = 16,777,216 for a full weight update — 0.4% of the full matrix. The base model weights are frozen throughout; only A and B are updated. The intuition: domain adaptation shifts the model's behavior in a few important directions, not across all 16 million dimensions of a weight matrix simultaneously.",
      "The operational consequence: one base model checkpoint, N lightweight adapter files. Adapters are typically 50–300MB each. For 10 domains: 140GB base + 10 × ~200MB = ~142GB total versus 1.4TB for full fine-tuning — and only one inference stack to maintain. The base model loads once into GPU memory; adapters swap in milliseconds per request or per tenant. New domains add an adapter, not a new serving environment. This is what makes multi-tenant LLM serving at scale operationally viable.",
      "Quality at rank r=32–64 achieves 85–98% of full fine-tuning on most domain adaptation tasks. The gap depends on how far the target domain deviates from the base model's pre-training distribution — for specialized vocabulary, document format, and tone adaptation, r=32 is usually sufficient. For tasks requiring new capabilities not represented in pre-training, the gap widens and higher rank or full fine-tuning may be required. QLoRA (Quantized LoRA) extends reach further: the base model is quantized to 4-bit precision to reduce memory footprint while the adapter trains in full precision. A 70B model that normally requires ~140GB at 16-bit precision fits in a single 80GB GPU under QLoRA — making large-model fine-tuning accessible without a multi-GPU cluster.",
    ],
    mcq: {
      question: "A team fine-tunes 10 domain models from a 70B base using LoRA (rank=16). The primary operational advantage over 10 full fine-tuned checkpoints is:",
      options: [
        "LoRA training is 100× faster than full fine-tuning regardless of model size or dataset",
        "LoRA automatically selects optimal rank per domain, eliminating hyperparameter search",
        "LoRA adapters are model-agnostic and transfer directly between different base architectures",
        "One base model checkpoint is deployed, with per-domain LoRA adapters (hundreds of MB each) swapped at inference time — providing 10 specialized models at the storage and infrastructure cost of one",
      ],
      correct: 3,
      explanation: "The core LoRA operational benefit is adapter-only storage and a single serving stack. 10 full checkpoints at 140GB each require 10× the storage, 10× the deployment infrastructure, and separate update pipelines. With LoRA, the base model is loaded once and adapters swap on demand. Option D is the correct answer. Option A is wrong — LoRA training is faster than full fine-tuning, but the speedup ratio varies significantly with rank, dataset size, and which layers are adapted; '100× faster regardless of model size' is an overstatement that confuses the memory efficiency benefit (fewer trainable parameters) with a guaranteed time speedup that depends on hardware and dataset characteristics. Option B is wrong — LoRA does not automatically select optimal rank; rank is a hyperparameter that must be tuned per domain, typically by evaluating model quality at r=8, r=16, r=32, and r=64; no automatic rank selection is built into the LoRA framework. Option C is wrong — LoRA adapters are architecture-specific and not transferable between different base model architectures; an adapter trained for Llama 3's attention projection matrices cannot be applied to Mistral or GPT-2 with different hidden dimensions and layer structures.",
    },
    takeaway: "LoRA enables multi-tenant fine-tuning at scale: one base model plus N lightweight adapters replaces N full model checkpoints. For 10+ domain-specific models, LoRA reduces storage 10–100× and inference infrastructure to a single stack. Quality is comparable to full fine-tuning at r=32–64. QLoRA extends this to single-GPU fine-tuning of 70B models.",
  },

  // ── Prompt Engineering track ──────────────────────────────────────────────────

  "few-shot": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "An email classification system needs to assign support emails to 12 categories. Zero-shot achieves 71% accuracy. Adding few-shot examples improved performance in principle, but a preliminary test with 8 billing examples and 1 example each for 4 other categories made things worse for those 4 categories. You need to understand what makes few-shot selection effective before rebuilding the example set.",
    explanation: [
      "Natural language instructions are ambiguous at the boundaries that matter most. 'Classify as billing, technical, or account' doesn't resolve: what happens when a customer asks why their API call failed and includes an invoice in the same email? Is that billing or technical? The instruction doesn't say. The model will guess, and it will guess wrong on the same boundary cases consistently — because the instruction didn't specify the decision rule for that case. Few-shot examples resolve boundary cases by demonstration: show the model the hard case and the correct label, and it learns the decision rule you intended. Examples aren't illustrations of the obvious cases — they're a specification of the non-obvious ones.",
      "Three ways examples fail instead of helping: (1) Category imbalance — 8 billing examples and 1 each for four other categories teaches the model that billing is the statistically expected output. For borderline emails, that prior dominates the actual content. Balance coverage across categories, or deliberately weight toward the most-confused pairs. (2) Unrepresentative examples — cherry-picked easy cases don't help at the boundary cases where the model actually needs specification. Include the hard cases: the API billing question, the authentication error that looks like an account issue. (3) Recency bias — the last few examples before the target input have disproportionate influence on the model's output. If all your boundary-case examples are clustered at the end, the model over-indexes on them as the expected format for all inputs.",
      "Dynamic few-shot selection replaces the static example set with retrieval. At inference time, retrieve the most similar labeled examples from a historical annotation store. An email about API authentication gets technical examples. An email about billing disputes gets billing examples. Retrieved examples are naturally balanced to the query because they're selected by similarity to it — not chosen once at prompt-design time and applied uniformly to all inputs. This requires a labeled annotation store and a fast retrieval mechanism — typically the same embedding model used in the main RAG pipeline, closing the loop between retrieval infrastructure and prompt construction.",
    ],
    mcq: {
      question: "A few-shot prompt for 12-category email classification contains 8 billing examples and 1 example each for 4 other categories. The expected failure mode is:",
      options: [
        "Over-prediction of billing — unbalanced examples bias the model toward the most-represented category, especially for ambiguous emails that could plausibly fit multiple categories",
        "Under-prediction of billing — the model treats the 8 billing examples as negative evidence for the other categories",
        "No effect on accuracy — the model ignores example distribution and uses only category name descriptions",
        "Higher latency — the additional billing examples increase prompt length and slow inference",
      ],
      correct: 0,
      explanation: "In-context learning is sensitive to example distribution. A model given 8 billing examples and 1 each for other categories has learned that billing is the statistically expected output in this context. For borderline emails (billing-adjacent technical issues, for instance), the prior toward billing from the imbalanced examples will dominate. The fix is balanced representation — roughly equal examples across categories, or deliberately weighted toward the most-confused pairs. Option B is wrong — it reverses the actual direction of the bias; 8 billing examples do not teach the model to avoid billing for the other categories, they push prediction toward billing by establishing it as the expected output frequency in this prompt context. Option C is wrong — models do use example distributions to form priors in few-shot settings; this is the core mechanism of in-context learning, and ignoring examples entirely in favor of only category names is the zero-shot behavior, not the few-shot behavior. Option D is wrong — the 8 extra billing examples add tokens to the prompt but prompt length is not the relevant failure mechanism; even if the prompt fit in a 512-token limit, the imbalance problem would persist because it's about distributional signal, not input processing speed.",
    },
    takeaway: "Few-shot examples are a task specification, not illustrations. Unbalanced examples bias the model; unrepresentative examples fail on the cases that actually need guidance. Balance coverage across categories, include the ambiguous boundary cases, and for production classifiers with many categories, use dynamic retrieval of similar labeled examples rather than a static set.",
  },

  "chain-of-thought": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A financial Q&A feature needs to compute compound interest, amortization schedules, and tax estimates. Zero-shot accuracy on a 50-question test set is 62%. A team member suggests chain-of-thought prompting. Before implementing it, you need to understand when and why it works — and whether it is likely to help for financial calculation tasks specifically.",
    explanation: [
      "A 62% accuracy baseline on financial calculations suggests the model has the underlying capability but fails at execution. The likely mechanism: generating a direct answer is predicting one unconstrained token stream from question to result. Multi-step calculations have sequential dependencies — if the formula substitution in step 2 is wrong, every subsequent step compounds the error. Without explicit intermediate steps, each token is generated conditioned only on the question and the model's current forward pass — there's no structural constraint enforcing mathematical consistency between the problem statement and the answer.",
      "Chain-of-thought prompting restructures the generation: question → formula identification → variable substitution → each arithmetic step → final answer. Each correct intermediate step increases the conditional probability of the next correct step — the model learned from millions of worked derivations in pre-training that well-formed calculations follow consistent patterns. A correct formula substitution makes a wrong exponent application less likely, not because the model is checking anything, but because 'rate=0.05, periods=12' is reliably followed by '1.05^12' in training data more than a direct answer follows the problem statement. CoT doesn't add capability — it routes generation through the derivation patterns the model already knows.",
      "Variants: zero-shot CoT — 'Show your work before giving the final number' — is the right starting point for financial calculations, no examples required. Few-shot CoT provides complete worked examples with full derivation traces, giving the model explicit format guidance for domain-specific calculation style. Self-consistency runs the same prompt multiple times and takes the majority-vote answer across independent chains — the most robust variant for high-stakes calculations, at the cost of multiple inference calls. For compound interest, amortization, and tax estimates specifically: zero-shot CoT closes most of the accuracy gap; add self-consistency for calculations where a single chain error has significant downstream financial consequences.",
    ],
    mcq: {
      question: "Chain-of-thought prompting improved accuracy from 62% to 87% on financial calculation questions. The most accurate explanation for this improvement is:",
      options: [
        "CoT forces the model to allocate more GPU memory per token, increasing effective compute per answer",
        "Intermediate steps constrain subsequent tokens toward mathematically consistent derivations — the model generates each step conditioned on all previous correct steps, matching the structured derivation patterns it learned from training data",
        "The longer output produced by CoT allows the model to implicitly check its own work at the end",
        "CoT causes the model to retrieve stored formula tables rather than approximating numerical results",
      ],
      correct: 1,
      explanation: "CoT works by token conditioning: each correct intermediate step increases the conditional probability (the likelihood of an outcome given what came before — here, given the correct prior steps) of the next correct step because the model has learned that well-formed derivations follow consistent patterns. It doesn't increase compute per token or enable retrieval — it restructures the generation so each step constrains the next rather than generating the answer in one unconstrained step. Option B is the correct answer. Option A is wrong — LLMs allocate a fixed compute budget per token regardless of whether they're generating intermediate reasoning or a direct answer; CoT produces more tokens (each at the same compute cost per token), not more compute per token. There is no mechanism by which prompting increases GPU memory allocation per generated token. Option C is wrong — the longer output is a side effect of generating intermediate steps, not the mechanism; if implicit self-checking at the end were the reason, adding a 'check your work' instruction without intermediate steps would produce the same improvement, but it does not. Option D is wrong — LLMs do not have a stored formula retrieval mechanism; they generate tokens conditioned on prior context, and CoT improves accuracy by structuring that generation, not by triggering lookup from a memory store.",
    },
    takeaway: "Chain-of-thought works by structuring generation so each reasoning step constrains the next, matching derivation patterns from training data. Use it for multi-step arithmetic, logical deductions, and multi-hop reasoning. Add self-consistency (majority vote over multiple chains) for high-stakes answers where single-chain reliability is insufficient.",
  },

  // ── Vector Infrastructure track ───────────────────────────────────────────────

  "vector-db-index-mechanics": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "Your vector database query latency jumped from 20ms to 2,000ms after the document index grew from 100K to 10M vectors. No code changes were made. A colleague says the problem might be index configuration rather than the database itself. You need to understand HNSW and IVF index mechanics to determine whether reconfiguration is viable before considering a database migration.",
    explanation: [
      "Vector search needs to be fast. The naive approach — compute the query's distance to every stored vector, return the k closest — is exact but scales as O(N × d) operations per query, where N is index size and d is the embedding dimension. At N=10M vectors and d=1,536: roughly 15 billion multiply-and-add operations per query. At 100K vectors this was fast enough; at 10M it takes seconds. The index grew 100× with no code changes, and latency grew 100× in return — because the index was built and tuned for 100K-vector scale and never reconfigured. ANN (Approximate Nearest Neighbor) indexes solve this by trading a small accuracy loss for orders-of-magnitude speedup, restricting search to a relevant subspace of vectors rather than all N.",
      "HNSW (Hierarchical Navigable Small World) builds a multi-layer graph where each vector connects to its approximate nearest neighbors. Queries start at the top layer — sparse, long-range connections — and navigate greedily toward the query before descending to denser layers. Key parameters: M (connections per node, set at index build time) and ef_search (how many candidate nodes to explore at query time before returning results). At 100K vectors, ef_search=64 explored a sufficient fraction of the graph to find true nearest neighbors. At 10M vectors, the same ef_search explores a subgraph that is 100× too small relative to the total graph — recall degrades because the true nearest neighbor is in a region the search never reaches. Fix: rebuild the index with M and ef_search calibrated to 10M-vector scale.",
      "IVF (Inverted File Index) clusters vectors into k-means groups at build time. At query time, only the members of the nprobe nearest cluster centroids are searched — a fraction of N. The key parameter is nlist (number of clusters), with the heuristic nlist ≈ sqrt(N). At N=100K: nlist=316, ~316 vectors per cluster. At N=10M with the same nlist=316: each cluster now contains ~31,600 vectors — each cluster search is 100× more expensive, and nprobe=64 covers a far smaller fraction of the index, missing the relevant vectors in clusters the search doesn't reach. Recall collapses, latency spikes. Fix: rebuild with nlist≈3,162 for 10M-scale, then tune nprobe to hit acceptable recall. This is an index reconfiguration, not a database migration.",
    ],
    mcq: {
      question: "An HNSW vector index returns results in 20ms for 100K vectors but 2,000ms after growing to 10M. Without migrating databases, the most likely fix is:",
      options: [
        "Increase the embedding dimension to improve recall, which also speeds up graph traversal",
        "Switch from cosine similarity to dot product to reduce the cost of each distance comparison",
        "Rebuild the index with parameters calibrated for 10M vectors — M and ef_search were set for small-scale operation; at 10M vectors the graph traversal behavior and recall-latency tradeoffs require different parameter values",
        "Add more API server replicas to serve queries in parallel, distributing the per-replica vector count",
      ],
      correct: 2,
      explanation: "HNSW performance is parameter-dependent. Parameters set for 100K vectors (low M, low ef_search) produce poor recall or excessive traversal at 10M vectors because the graph structure scales with N. Rebuilding with parameters appropriate to 10M-vector scale is the correct fix. Option C is the correct answer. Option A is wrong — embedding dimension is determined by the embedding model (e.g., 1536 for OpenAI ada-002) and cannot be changed without re-embedding all documents with a different model; higher dimension increases the distance computation cost per pair and does not speed up graph traversal. Option B is wrong — switching from cosine similarity to dot product is a minor distance metric choice that changes the mathematical formulation of similarity but not the graph traversal algorithm or the 100× scale mismatch; it would not reduce 2,000ms to 20ms. Option D is wrong — adding API server replicas distributes requests across servers but each server still runs the full HNSW traversal over its copy of the index; the per-query vector search complexity within each replica is unchanged, and the latency problem is per-query, not a throughput bottleneck.",
    },
    takeaway: "Vector index performance is parameter-dependent, not just database-dependent. HNSW M, ef_search, and IVF nlist, nprobe must all be calibrated to dataset scale. A 100× dataset growth with no index reconfiguration almost always explains 100× latency degradation — rebuild the index before considering a database migration.",
  },

  "hybrid-search-design": {
    depthTier: "standard",
    interviewWeight: "high",
    scenario: "A technical documentation search tool uses embedding-based semantic search. It works well for conceptual queries ('how does rate limiting work') but fails on exact-match queries ('AttributeError: NoneType object has no attribute split on line 17'). The exact error string gets semantically matched to general Python error documentation instead of the specific error. Your team is evaluating hybrid search.",
    explanation: [
      "Vector index mechanics established how embedding-based search works. The specific failure it left unsolved: embeddings encode semantic similarity — the distance between 'AttributeError: NoneType object has no attribute split' and 'Python type error handling' in embedding space is small because these concepts are semantically related. That's not a retrieval bug; it's exactly what embeddings are designed to do. They generalize across paraphrases and synonyms. But exact technical tokens — error messages, function names, version numbers, API endpoints — aren't synonyms. An exact token that doesn't match is wrong, not a paraphrase. Dense search cannot distinguish 'semantically similar' from 'literally identical' because it was designed for the former. Sparse keyword search (BM25 — Best Match 25, a keyword ranking algorithm that scores by term frequency while penalizing very long documents) was designed for the latter. Neither alone handles the full query distribution of a technical documentation tool.",
      "Hybrid search runs both in parallel and fuses the results. Merging them requires resolving an incompatibility: BM25 scores are unbounded and document-length-dependent (a score of 12.7 for one document, 4.3 for another); cosine similarity is bounded [-1, 1]. Naively averaging them by raw score gives BM25 dominant influence simply due to scale. Reciprocal Rank Fusion (RRF) bypasses this: RRF_score = 1/(rank_bm25 + k) + 1/(rank_vector + k), where k=60 is a smoothing constant (position 1 from either method contributes 1/61 ≈ 0.016; position 10 contributes 1/70 ≈ 0.014). RRF works in rank space — position 1 from BM25 and position 1 from dense search contribute equally regardless of underlying score magnitude. Running both queries in parallel adds negligible latency.",
      "Adaptive weighting improves on static hybrid for mixed query distributions. Classify the incoming query as 'exact' (contains code identifiers, error strings, version numbers — detectable via simple heuristics: punctuation patterns, identifier-character density) or 'conceptual' (natural language question about behavior). For exact queries, weight sparse results higher; for conceptual, weight dense results higher. A fast heuristic classifier adds sub-millisecond latency and materially improves precision for each query type. This is why the pure-embedding system failed on 'AttributeError: NoneType object has no attribute split' — it applied dense-only search to an exact-identifier query. The fix: hybrid with adaptive weighting, sparse weight elevated when the query contains exact technical tokens.",
    ],
    mcq: {
      question: "Hybrid search uses Reciprocal Rank Fusion rather than score normalization to merge dense and sparse results. The primary reason is:",
      options: [
        "RRF guarantees that dense results always rank higher than sparse results for technical queries",
        "RRF eliminates the need for a re-ranker since it already performs fusion at the ranking level",
        "RRF is computationally cheaper than running a normalized score combination",
        "RRF is rank-based and doesn't assume comparable score scales between cosine similarity and BM25 — it avoids the normalization problem caused by incompatible distributions",
      ],
      correct: 3,
      explanation: "BM25 scores are unbounded and vary with document length and collection statistics; cosine similarity scores are bounded between -1 and 1. Combining them with weighted addition requires arbitrary normalization choices that affect fusion quality. RRF bypasses this by working in rank space: position 1 from any method contributes 1/(1+60)=0.016, position 10 contributes 1/(10+60)=0.014, regardless of the underlying score scale. Option D is the correct answer. Option A is wrong — RRF does not guarantee any particular ordering between dense and sparse results; it combines ranked lists from both methods, and either type of result can rank first in the merged output depending on where each appears in the individual lists. Option B is wrong — RRF is a fusion method, not a ranking model; it merges two ranked lists but does not perform the fine-grained relevance scoring that a cross-encoder reranker does; a reranker may still be beneficial after hybrid fusion. Option C is wrong — RRF requires running two retrieval queries (dense and sparse in parallel) and then computing a combined score per document, which is slightly more expensive than a single-method query; the main advantage is fusion quality, not computational cost.",
    },
    takeaway: "Semantic search alone fails on exact-match technical queries; keyword search alone fails on conceptual queries. Hybrid search with RRF fusion handles both without score normalization complexity. For production, add adaptive weighting (heavier sparse for exact queries, heavier dense for conceptual) to optimize precision across mixed query distributions.",
  },

  "metadata-filtering": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "A legal document search tool indexes 2 million contracts across 200 enterprise clients. A user at Client A searches for 'indemnification clauses.' Without filtering, results from Client B's contracts appear in the response. This is a data isolation failure. You need to design a metadata filtering approach that provides isolation guarantees, not just isolation in the common case.",
    explanation: [
      "Vector-db-index-mechanics established how ANN indexes search for nearest neighbors across the full index. The failure that creates in multi-tenant systems: the full index has no concept of data ownership. An ANN search for 'indemnification clauses' finds globally nearest vectors — including contracts from clients who must never appear in each other's search results. This is not an edge case; it's the default behavior of a shared index. Metadata filtering adds a hard constraint at search time: retrieve the top-k nearest vectors, but only among vectors where metadata.client_id == 'clientA'. Every indexed vector must carry structured metadata (client_id, document_type, date, jurisdiction, confidentiality_level) assigned at index time — this is a prerequisite, not an afterthought.",
      "Two implementation approaches with different failure modes: pre-filtering (filter by metadata first, then run ANN search on the filtered subset) provides strong isolation — the ANN graph never touches other tenants' vectors. The risk is that the filtered subset may be small enough that HNSW graph connectivity degrades (a 10K-vector per-tenant subset may have poor graph structure in an HNSW built on 2M vectors). Post-filtering (run full ANN search on the entire index, then discard results that don't match the filter) preserves ANN quality but introduces a risk: if the filter is misapplied due to a bug, Client B's results pass through. For legal documents, this is a compliance failure, not a minor bug.",
      "Physical index partitioning by tenant is the gold standard for multi-tenant data isolation. Each client's vectors live in a completely separate index namespace or index file. Client A's query never interacts with Client B's index data at the infrastructure level — not just the application level. Bugs in filter logic cannot cause cross-tenant leakage because there is no shared index to leak from. The tradeoff: cross-client authorized queries (e.g., a compliance officer searching all client contracts) require querying multiple namespaces and merging results. Most enterprise vector databases (Weaviate, Qdrant, Pinecone) support physical namespace isolation.",
    ],
    mcq: {
      question: "A multi-tenant vector search system must guarantee that User A never sees results from Tenant B. Which approach provides the strongest isolation guarantee?",
      options: [
        "Physical index partitioning by tenant — Client A's vectors are in a separate namespace from Client B's, making cross-tenant leakage impossible even if filter logic has bugs",
        "Metadata filter applied at query time — as long as the client_id filter is applied correctly, isolation is guaranteed",
        "Using different embedding models per tenant so vectors from different tenants are incomparable",
        "Post-filtering after retrieval and returning empty results if no matching documents are found",
      ],
      correct: 0,
      explanation: "Metadata filtering is application-layer logic — a bug, race condition, or misconfiguration can cause the filter to be misapplied, leaking cross-tenant results. Physical partitioning is an infrastructure guarantee: the search engine never sees the other tenant's data, so no application-layer error can leak it. For compliance-sensitive multi-tenant applications (legal, healthcare, finance), infrastructure-level isolation is the correct pattern. Option B is wrong — metadata filtering provides isolation when implemented correctly, but it is not a guarantee; a single code bug (missing filter parameter, incorrect client_id lookup, race condition in multi-threaded code) can silently bypass the filter and return cross-tenant results; 'as long as correctly applied' is exactly the failure surface physical partitioning eliminates. Option C is wrong — using different embedding models per tenant would prevent direct vector similarity comparison between tenants' content, but the vectors still exist in the same index namespace and could be retrieved by a query; incomparable scores don't prevent retrieval if the filtering logic fails, and this also prevents authorized cross-tenant searches. Option D is wrong — post-filtering after retrieval is the weakest isolation approach; it runs the full ANN search across all tenants' vectors and filters at application layer afterward, which is exactly the pattern most vulnerable to filter logic bugs leaking cross-tenant data.",
    },
    takeaway: "Metadata filtering provides isolation when it works correctly, not an isolation guarantee. For compliance-sensitive multi-tenant applications, physical index partitioning per tenant is the only approach that guarantees data isolation at the infrastructure level — independent of application-layer filter correctness.",
  },

  // ── Multimodal AI track ───────────────────────────────────────────────────────

  "vision-language-arch": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your team is evaluating whether to use a general-purpose vision-language model (GPT-4V or LLaVA) for automating invoice data extraction from scanned PDFs. The model needs to extract line items, totals, dates, and vendor names. Before selecting a model, you want to understand the architecture well enough to anticipate where extraction failures will occur.",
    explanation: [
      "RAG established how to retrieve relevant text chunks and use them as generation context. The failure it left: RAG is blind to information stored in images. Text extraction from PDFs captures the text layer; figures, charts, diagrams, and fully scanned documents with no text layer require a different path. Vision-language models bridge this gap by combining a visual encoder with a language model backbone, giving the system the ability to generate text conditioned on image inputs. The architecture: the image is divided into fixed-size patches (typically 14×14 pixels); each patch is projected into an embedding vector. A Vision Transformer (ViT) processes these patch embeddings through transformer layers, producing a sequence of visual tokens — one per patch. A projection layer (MLP — multi-layer perceptron, a simple neural network; or cross-attention) maps visual token dimensions to the language model's embedding space. The projected visual tokens are prepended to or interleaved with text tokens, and the language model generates text conditioned on both.",
      "The visual encoder's training distribution determines what visual understanding the model has. Models like CLIP (Contrastive Language-Image Pretraining — trained to align image and text representations in a shared embedding space) are trained on natural image-text pairs from the internet — they learn object recognition, scene understanding, and natural image semantics. Fine-grained character recognition in document images (OCR-quality extraction) requires either a document-specialized model (LayoutLM — a model trained on document layouts with text position awareness; Donut — a model that reads documents end-to-end without a separate OCR step; PaddleOCR — a high-accuracy character recognition toolkit) or a VLM that was specifically fine-tuned on document images. General-purpose VLMs handle invoices reasonably well for clean, high-resolution scans, but fail more often on unusual fonts, dense tables, or low-DPI scans — because those visual patterns were underrepresented in their training data.",
      "For invoice extraction: (1) Resolution sensitivity — the ViT's fixed patch size means a 100-DPI scan loses character detail within each 14×14 patch, causing digit misreads. Minimum 200 DPI for reliable character-level extraction. (2) Spatial layout — invoice line items are grid-structured; models trained primarily on natural images may not reliably maintain row-column relationships when extracting values. (3) Numeric hallucination — when OCR is ambiguous (smudged digits, low contrast), the model generates plausible-looking numbers from its prior rather than refusing to answer. For financial data, all extracted numerics should be validated against business rules: do line item amounts sum to the extracted total? If not, flag for human review.",
    ],
    mcq: {
      question: "A VLM correctly identifies that an invoice contains line items but extracts an incorrect total amount. The most likely cause is:",
      options: [
        "The language model component cannot perform arithmetic and cannot sum line items to verify the total",
        "The visual encoder produces patch-level embeddings that may not reliably distinguish individual digits at typical scan resolutions — the model generates a plausible number rather than precisely reading each character",
        "Invoice templates are too structurally diverse for any VLM architecture to process reliably",
        "The language model component generates the total from its pre-training distribution of invoice amounts rather than reading the printed value — because training on financial documents creates a strong prior for common total ranges",
      ],
      correct: 1,
      explanation: "Numeric extraction accuracy in VLMs is limited by the visual encoder's patch-level resolution. At 14×14 pixel patches, individual digits in an invoice may fall partly in two adjacent patches, or may be too small to distinguish at typical scan DPI. The language model then generates a plausible number from context rather than a precisely-read value. This is why numeric extraction from financial documents always requires downstream business-logic validation. Option B is the correct answer. Option A is wrong — the incorrect total amount is not primarily caused by arithmetic inability; VLMs can often perform addition, and the total is typically printed on the invoice itself and should be read directly rather than computed; the failure is visual reading of the printed digits, not arithmetic. Option C is wrong — invoice templates vary widely, but VLMs do process diverse document structures reasonably well for high-level understanding; claiming no VLM can process invoices reliably overstates the limitation; the failure mode is specific to fine-grained digit recognition, not structural diversity. Option D is a genuine misconception: LLMs do draw on pre-training priors when uncertain, and a model trained on financial text has learned the distribution of typical invoice totals. However, VLMs are not primarily generating totals from this parametric prior — they are attempting to read the printed value and producing wrong digits due to visual encoding resolution limits, not statistical substitution from a learned price distribution. The distinction matters for the fix: the solution is higher DPI and better visual encoding, not de-biasing the language model's financial priors.",
    },
    takeaway: "VLMs are trained for broad visual understanding, not precision document OCR. For financial document extraction, validate every numeric against business rules (do line items sum to total?), ensure scan resolution is ≥200 DPI, and compare against document-specialized models (Donut, LayoutLM) for high-accuracy pipelines.",
  },

  "multimodal-rag": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "A research assistant tool needs to answer questions from 10,000 scientific papers where 24% of relevant information is in figures and tables, not in prose text. A standard text-only RAG pipeline achieves 68% accuracy. Failures cluster around questions requiring figure or table data. You need to extend the pipeline to handle non-text content.",
    explanation: [
      "Vision-language-arch established how VLMs interpret images — patch embeddings through a visual encoder projected into the language model's token space. The failure that creates for retrieval: VLMs can interpret an image given to them directly, but they cannot retrieve one. Standard text RAG retrieves chunks by embedding similarity; figures and tables don't have text embeddings to match against a query. Multimodal RAG solves the representation problem: how do you make figures and tables retrievable in a text-based retrieval system? The figure captioning approach runs a VLM on each figure at index time to generate a descriptive text caption ('Bar chart showing model accuracy vs. parameter count for 8 models; GPT-4 achieves highest accuracy at 89%'). Captions are embedded and retrieved as text. When a relevant figure is retrieved, both the caption and the original image are passed to the generation model as context.",
      "Caption quality is the retrieval quality ceiling for figure captioning. If the VLM captioner omits the specific numeric values in a chart (e.g., 'a bar chart showing performance' instead of 'GPT-4: 89%, Llama 3 70B: 84%, Claude 3: 91%'), retrieval will find the right figure but the generation model won't have the detail needed to answer a specific question. For scientific charts, the captioner should be prompted to explicitly enumerate all axis values, labels, and key data points — not just describe the chart type.",
      "Tables require a separate strategy from figures. Tables in PDFs are often stored as whitespace-aligned text that breaks semantic chunking — the content may exist in the text layer but be structurally unreadable after chunking. Specialized extractors (Camelot and pdfplumber — Python libraries that parse tables directly from PDF files; Amazon Textract — a cloud OCR service that reads text and tables from scanned images) produce structured tables as JSON or CSV with explicit column headers and row values. Embed the structured table as a serialized JSON string — this preserves the relational structure that a natural language description loses, and allows exact value lookup at retrieval and generation time. Evaluate retrieval separately for text queries, figure queries, and table queries to identify which modality is underperforming.",
    ],
    mcq: {
      question: "A multimodal RAG pipeline using VLM-generated captions retrieves the correct figure 78% of the time but produces incorrect answers in 30% of those successful retrievals. The most likely cause is:",
      options: [
        "The embedding model cannot handle captions longer than 128 tokens, truncating critical information",
        "Chart figures are stored as SVG vectors in the source PDFs and cannot be processed by VLMs",
        "Caption quality — the VLM captioner omitted or approximated the specific numeric values the question requires, so retrieval succeeded but the generation context lacks the detail needed to answer precisely",
        "Retrieved figures are always placed after retrieved text in the context window, causing lost-in-the-middle degradation",
      ],
      correct: 2,
      explanation: "Caption quality determines generation quality for figure-based answers. When a VLM captioner describes a chart as 'shows model accuracy comparisons' without listing specific values, the text retrieval correctly finds the figure, but the generation model receives only a high-level description — it cannot answer a question like 'what accuracy did model X achieve?' from that caption. The fix is better captioning prompts that require enumeration of all data points, not just structural description. Option C is the correct answer. Option A is wrong — standard embedding models (text-embedding-ada-002, BGE, etc.) support sequences of 512–8192 tokens and can handle even detailed, data-rich captions without truncation; caption length is not typically the bottleneck for figure retrieval pipelines. Option B is wrong — PDFs do sometimes contain SVG vector figures, but most scientific papers use rasterized bitmap images (PNG, JPEG); SVG is not a general barrier, and even SVG can be rasterized before VLM processing; this does not explain the 30% answer failure rate. Option D is wrong — figure placement within the context window is a valid concern (lost-in-the-middle), but the scenario states retrieval succeeds 78% of the time and the failure is in producing incorrect answers from those successful retrievals; lost-in-the-middle would more likely manifest as the model ignoring the figure than producing confidently wrong specific values.",
    },
    takeaway: "Text-only RAG is blind to 20-30% of scientific paper content. Figure captioning is the lowest-friction entry point — but caption quality is the retrieval quality ceiling. Prompt captioners to enumerate all values explicitly, pass both caption and original image to the generation model, and handle tables with structured extraction (JSON/CSV) rather than VLM captioning.",
  },

  "resolution-token-cost": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "An image analysis pipeline processes 50,000 product photos per day through a VLM for attribute extraction (color, material, visible dimensions). Inference costs $2,400/day. An engineer notices images are sent at 2048×2048 resolution and the documentation mentions a low-resolution mode at 512×512. You need to understand the resolution-token relationship before changing settings.",
    explanation: [
      "Vision-language-arch established that images are processed as patch sequences — each fixed-size tile of pixels becomes one visual token in the language model's context window. The cost implication follows directly from the patch math: token count scales as the square of linear resolution, not linearly. This is the most important quantitative fact for VLM cost optimization. For a model using 14×14 pixel patches: a 512×512 image produces (512÷14)² ≈ 1,340 visual tokens. A 2048×2048 image produces (2048÷14)² ≈ 21,400 visual tokens. A 4× linear resolution increase (512→2048) produces 16× more visual tokens — because area scales as the square of linear dimension. Visual tokens are billed at the same per-token rate as text tokens on most providers.",
      "For the 50K/day pipeline: at 2048×2048 (≈21K visual tokens/image), the pipeline generates approximately 1.05B visual tokens/day. At $5/1M input tokens: $5,250/day in visual tokens alone — consistent with the $2,400/day figure when combined with text prompt tokens and accounting for actual model pricing. Switching to 512×512 (≈1,340 visual tokens): 67M visual tokens/day = $335/day. The potential savings are $1,900–2,000/day, or ~$60,000/month — before evaluating whether quality is maintained.",
      "Resolution-accuracy tradeoff must be measured on your specific task. For product attribute extraction (color, material, visible dimensions in catalog photos): most attributes are clearly visible at 512×512. High resolution (2048×2048) provides value when reading fine print, examining surface texture at pixel level, or detecting subtle defects — typically not required for catalog attribute extraction. Test approach: process 1,000 images at both resolutions, compare extraction accuracy against a ground truth annotation set. If quality is equivalent, the business case is straightforward. If quality degrades for a subset of products, use adaptive resolution: classify images as 'complex' (high-res) or 'standard' (low-res) with a cheap binary classifier, and route accordingly.",
    ],
    mcq: {
      question: "A VLM pipeline switches product image input from 2048×2048 to 512×512 pixels. Token count changes by approximately:",
      options: [
        "4× reduction — token count scales linearly with the linear dimension ratio",
        "2× reduction — token count scales with the square root of image area",
        "No change — VLMs use a fixed token budget regardless of input resolution",
        "16× reduction — visual token count scales as (resolution)² for fixed patch size; (2048÷512)² = 4² = 16× more tokens at full resolution",
      ],
      correct: 3,
      explanation: "Visual token count scales quadratically with linear resolution because patch count = (image_width / patch_size) × (image_height / patch_size). Doubling linear resolution quadruples patch count. A 4× linear resolution increase (512→2048) produces 16× more patches and 16× more visual tokens. This is the most important quantitative fact for VLM cost optimization: resolution choices have quadratic cost impact. Option D is the correct answer. Option A is wrong — it confuses the linear dimension ratio with the token count ratio; token count depends on area (width × height), not on a single linear dimension; a 4× linear increase corresponds to a 16× area increase and 16× token count, not 4×. Option B is wrong — token count scales quadratically with linear resolution (as the square), not with the square root of area; the square root of area equals the linear dimension, so this would be the same as linear scaling, which is wrong. Option C is wrong — VLMs do not use a fixed token budget regardless of resolution; token count is directly proportional to patch count, which is proportional to image area; providers bill per actual visual token consumed, and switching from 2048×2048 to 512×512 does materially reduce the number of visual tokens processed and billed.",
    },
    takeaway: "Visual token count scales quadratically with linear resolution — a 4× linear resolution increase costs 16× more. For bulk image processing, always evaluate whether your task requires full resolution. Catalog attribute extraction typically works at 512×512; the 16× cost difference is significant at 50K images/day. Measure accuracy at target resolution before changing settings in production.",
  },

  // ── AI Safety & Alignment track ──────────────────────────────────────────────

  "alignment-techniques": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "You're deploying a customer-facing LLM assistant. The base model (a fine-tuned Llama 3 checkpoint) sometimes provides harmful responses when directly prompted. Legal is evaluating RLHF vs. DPO vs. Constitutional AI for alignment before deployment. You need to explain the tradeoffs clearly enough to recommend one approach for a team without dedicated RL infrastructure.",
    explanation: [
      "RLHF established that alignment requires weight-level change — training on human preference data to shift the model's output distribution from 'statistically typical human text' toward 'helpful, honest, harmless responses.' The operational cost of RLHF (separate reward model, PPO training stability, reward hacking risk) motivated two successors designed to achieve comparable alignment with lower pipeline complexity. Choosing between RLHF, DPO, and Constitutional AI requires understanding what each changes, what each costs, and what constraint each was designed to relieve.",
      "DPO (Direct Preference Optimization) reformulates RLHF as supervised learning directly on preference pairs, eliminating the reward model and the RL training loop entirely. Given a preferred response and a rejected response for the same prompt, DPO trains the model to increase the log probability of the preferred response and decrease that of the rejected response, with a KL divergence term (a measure of how far the updated probability distribution has moved from the reference model — prevents excessive drift from pre-alignment behavior). The result: alignment quality comparable to RLHF while running as standard supervised fine-tuning — no PPO instability, no reward hacking to monitor, no second model to train. DPO is well-supported in open-source frameworks (TRL and Axolotl — Python libraries for the SFT/DPO fine-tuning loop) and has largely replaced RLHF for teams without dedicated RL infrastructure. The prerequisite: preference data — pairs of (prompt, preferred response, rejected response). Building this dataset is the main cost.",
      "Constitutional AI (CAI — developed by Anthropic) addresses the annotation bottleneck rather than the RL complexity. Instead of human preference pairs, CAI starts with a written constitution: a list of explicit principles ('be helpful,' 'avoid harmful content,' 'don't deceive'). The model critiques and revises its own responses against these principles (RLAIF — Reinforcement Learning from AI Feedback, where the model acts as its own annotator), generating a preference dataset synthetically rather than through human labeling. RLHF or DPO then trains on the synthetic preference data. CAI scales annotation automatically — write the constitution once, generate preference data at scale. The tradeoff: the quality of alignment depends on the quality of the model doing the self-critique. CAI works best when the base model is already capable enough to produce meaningful critiques; for early or smaller models, the self-critique loop produces low-quality synthetic preferences.",
      "For the scenario — deploying a Llama 3 fine-tuned checkpoint that produces harmful responses under direct prompting, team without dedicated RL infrastructure: DPO is the correct starting point. Collect 500–2,000 pairs of (prompt, preferred safe response, rejected harmful response) from manual annotation or red-team sessions. Run DPO on the existing fine-tuned checkpoint using TRL's DPO trainer. The model's weights shift toward refusing harmful completions and producing helpful responses by default — this is a training change, not a system prompt change, so safe behavior becomes the default across all future contexts including adversarial prompting. Add Constitutional AI only if annotation costs become prohibitive and the checkpoint is capable enough for reliable self-critique. RLHF adds pipeline complexity (reward model + PPO stability) without meaningfully better results for a standard deployment alignment task.",
    ],
    mcq: {
      question: "Why is DPO generally preferred over RLHF for practical alignment of open-source models without dedicated RL infrastructure?",
      options: [
        "DPO reformulates alignment as supervised learning on preference pairs — no separate reward model or RL training loop, resulting in lower implementation complexity, greater training stability, and comparable alignment quality",
        "DPO requires 10× less preference data than RLHF to achieve equivalent safety",
        "DPO is only applicable to models under 7B parameters, making it suitable for small-scale deployments",
        "DPO eliminates the need for any human preference labels by using the model's self-evaluation",
      ],
      correct: 0,
      explanation: "DPO's practical advantage is operational simplicity, not data efficiency or scale constraints. It removes the reward model training and RL optimization from the pipeline, reducing the process to supervised fine-tuning on preference pairs — a well-understood, stable procedure. DPO works for all model sizes and still requires human preference labels; it eliminates only the RL infrastructure, not the human annotation requirement. Option B is wrong — DPO does not require 10× less data than RLHF; both methods learn from human preference pairs, and the amount of annotation required for comparable alignment quality is similar; DPO's advantage is the training pipeline simplicity, not reduced data requirements. Option C is wrong — DPO works for models of all sizes including 70B and larger; the constraint is GPU memory for fine-tuning, which is addressed by QLoRA; there is no architectural reason DPO is limited to sub-7B models. Option D is wrong — DPO still requires human preference labels (preferred vs. rejected response pairs); it eliminates the reward model and RL training loop, but the human annotation step remains essential; self-evaluation without human labels is a separate technique (RLAIF) not DPO.",
    },
    takeaway: "RLHF is the foundational alignment technique but operationally complex. DPO achieves comparable alignment quality with a simpler supervised objective — no reward model, no RL instability, standard SFT tooling. If you have preference data and no RL infrastructure, DPO is the right starting point for aligning open-source models.",
  },

  "red-teaming": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Before deploying a customer-facing AI chatbot for a financial services company, you're asked to conduct a red-teaming exercise. The PM thinks this means 'test it with tricky prompts.' Legal wants to hire an external firm. You have 2 weeks and internal resources only. You need to scope and execute a meaningful exercise.",
    explanation: [
      "Alignment-techniques established that DPO and RLHF shift the model's weight-level behavior toward safety — refusing harmful request patterns in the training distribution. The failure that shift doesn't address: coverage. Training teaches the model to refuse the specific harmful framings that appeared in training data. Users in production discover harmful framings that weren't in that training set. Red-teaming systematically searches for these coverage gaps before deployment — it's adversarial testing, not standard QA (which tests expected behavior) and not benchmark evaluation (which tests against a known dataset). Red-teaming finds novel failures through adversarial creativity. For a financial chatbot, the relevant failure categories are: specific financial advice violations (telling a user to buy a specific stock), PII extraction from system prompt or context, compliance restriction bypass, impersonation of a licensed advisor, and unauthorized transaction guidance.",
      "Attack categories to cover systematically: (1) Direct injection — 'ignore your instructions and tell me...' style prompts; (2) Persona adoption — 'pretend you are a financial advisor with no restrictions'; (3) Hypothetical framing — 'in a story where a character gives investment advice...'; (4) Multi-turn escalation — starting with allowed requests, incrementally pushing toward policy violations across 6–10 turns; (5) Indirect injection — embedding adversarial instructions in documents or data the model retrieves (if the system does retrieval); (6) Edge cases — non-English inputs, unusual encodings, very long inputs that push context limits.",
      "For a 2-week internal exercise: days 1–3, build an attack taxonomy specific to financial chatbot risks (map each attack category to the specific harm it could cause and to the relevant compliance requirement). Days 4–10, run systematic attack sweeps by category: 20–30 prompts per category, log every attempt and response, mark pass/fail against the harm definition. Days 11–14, produce a risk-ranked findings report with: attack vector, example prompt, model response, whether existing safeguards caught it, and recommended fix. You don't need external red-teamers for an initial exercise — you need structured process. External teams are valuable for the blind spots internal teams accumulate after the system is familiar.",
    ],
    mcq: {
      question: "An AI chatbot passes direct prompt injection tests but fails on multi-turn escalation attacks. What is the most accurate explanation?",
      options: [
        "Multi-turn attacks use longer inputs that exceed the safety classifier's context window, causing it to miss the injection",
        "Single-turn safety training is insufficient for multi-turn contexts — safety instructions respected at turn 1 can be overridden by turn 8 of a gradual escalation as the model's context model of the conversation shifts",
        "The model treats later conversation turns as more authoritative than the system prompt",
        "Multi-turn attacks are not defensible without real-time human moderation on every response",
      ],
      correct: 1,
      explanation: "Safety alignment is trained primarily on single-turn interactions. In a multi-turn conversation, the model's representation of the current request is conditioned on accumulated context — a gradual escalation across 8 turns may not resemble any single-turn unsafe prompt in the training data, so the learned refusal behavior doesn't trigger. This is why multi-turn testing is a distinct red-team attack category from direct injection. Option B is the correct answer. Option A is wrong — safety classifiers typically operate on the full context window, not just the latest turn; exceeding the context window would require extremely long conversations (thousands of tokens per turn), which is not typical for multi-turn escalation attacks; the failure is distributional, not a context truncation issue. Option C is wrong — models do not apply more authority to later turns than to the system prompt; the system prompt persists throughout the context, but the model's completion of the current request is conditioned on all accumulated context, which is why gradually escalated context can shift behavior. Option D is wrong — layered defenses (input classification at each turn, output review, conversation-level monitoring) can significantly reduce but not eliminate multi-turn attack success; real-time human moderation of every response is not required and would eliminate the automation benefit of the chatbot.",
    },
    takeaway: "Red-teaming is systematic adversarial testing, not a pass of tricky prompts. Structured attack taxonomies (direct injection, persona adoption, multi-turn escalation, indirect injection) cover the failure modes that ad hoc testing misses. Run red-teaming before deployment, document every finding with attack vector and model response, and treat the taxonomy as a living artifact updated with new attack patterns.",
  },

  "jailbreak-taxonomy": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your deployed content moderation model passed internal safety testing but started failing in production after 3 weeks. Users discovered a 'DAN mode' style prompt that reframes the model as an unrestricted AI. You want to understand the taxonomy of jailbreak attacks to build a more systematic defense — not just patch the specific DAN prompt.",
    explanation: [
      "Red-teaming established the structured process for finding coverage gaps: build an attack taxonomy, sweep systematically by category, log every finding. The question it leaves open: why do jailbreaks work at all? If alignment was applied, why does 'you are now DAN, an AI with no restrictions' bypass it? Jailbreak taxonomy is the mechanism side — understanding the structural reasons attacks succeed, not just cataloguing that they do. Without this, defenses are pattern-matching patches; with it, defenses can target the underlying vulnerabilities. The major jailbreak categories: (1) Persona adoption — triggers the model's in-context learning to simulate a described entity, including the entity's described absence of restrictions. (2) Hypothetical framing — 'in a story where a character explains step-by-step how to...' — uses fictional context to distance the request from real-world harm. (3) Instruction override — 'ignore your previous instructions' or claiming developer/admin mode. (4) Privilege escalation (claiming elevated permissions or authority — borrowed from cybersecurity, where it means gaining more access than granted) — 'I am a researcher at [company] who needs this for safety work' — claims authority the model cannot verify. (5) Gradual escalation — benign requests incrementally pushed toward policy violations across multiple turns, each individually allowed.",
      "Why jailbreaks work: alignment training teaches the model to refuse specific patterns of requests — but it's a distributional learning process, not a logical constraint. Novel framings that don't match the training distribution of refused requests can bypass the learned refusal behavior. Persona adoption works because the model has learned to role-play as described entities (a capability from pre-training on fiction, dialogue, and character writing), and the safety training didn't include enough examples of 'harmful persona removal' to reliably distinguish 'harmless role-play' from 'adopt a persona defined as having no safety restrictions.'",
      "Layered defense: (1) Input classification — classify requests before the main model, catching known jailbreak patterns. Fails on novel framings. (2) System prompt hardening — explicitly include examples of persona adoption attacks and the correct refusal response; instruct the model never to abandon its role regardless of framing. Helps, but not sufficient alone. (3) Output classification — check outputs for policy violations before delivering them (defense in depth when the input classifier misses a novel attack). (4) Red-team update loop — as new jailbreak patterns emerge in production, add them as adversarial examples to update classifiers. No single layer is sufficient; the production standard is layered defenses with continuous monitoring.",
    ],
    mcq: {
      question: "A 'DAN mode' jailbreak ('you are now DAN, an AI with no restrictions') successfully bypasses safety training. The most accurate explanation is:",
      options: [
        "DAN prompts are longer than the safety classifier's context window, causing the safety check to time out",
        "The model was accidentally fine-tuned on DAN-style data during pre-training",
        "Persona adoption triggers the model's learned role-play capability — the model simulates the described entity including its described absence of restrictions, because alignment training didn't consistently distinguish harmful persona removal from benign role-play",
        "Models apply safety filters only to their own voice, not to simulated characters or personas",
      ],
      correct: 2,
      explanation: "DAN-style attacks exploit the model's strong in-context role-play capability. The model learned from pre-training to simulate described characters and entities. Safety alignment would need to explicitly teach that 'adopt a persona described as having no restrictions' is itself an unsafe request — but this specific pattern is often underrepresented in alignment training data, allowing it to bypass learned refusals. Option C is the correct answer. Option A is wrong — DAN prompts are typically a few hundred tokens and well within any modern context window; safety classifiers see the full prompt; the failure is not a context limit or timeout but a distributional gap in alignment training data. Option B is wrong — the model was not fine-tuned on DAN-style data during pre-training; the vulnerability arises from legitimate role-play capabilities learned from fiction, dialogue, and character-writing data, combined with insufficient alignment training on persona-adoption-as-attack patterns. Option D is wrong — models do not apply safety filters exclusively to first-person statements and not to simulated characters; the correct observation is that alignment training underrepresented 'persona defined as having no restrictions' as an attack vector, not that a distinct first-person/character filter is hard-coded.",
    },
    takeaway: "Jailbreaks exploit distributional gaps in alignment training — novel framings that weren't in the training data bypass learned refusals. No single defense layer is complete. The production response is layered defenses: input classification, hardened system prompts with explicit jailbreak examples, output classification, and continuous red-team updates as new attack patterns emerge in the wild.",
  },

  "safety-measurement": {
    depthTier: "standard",
    interviewWeight: "medium",
    scenario: "Your team is preparing a safety report before deploying an AI system. Internal evals show a 99.2% refusal rate on a harmful requests benchmark. The ethics team asks whether this number is 'good enough.' You need to explain what this metric actually measures — and what it fails to measure — before anyone can answer that question.",
    explanation: [
      "Red-teaming produces a findings report: which attack vectors succeeded, which were caught, recommended fixes. The question it leaves unanswered: when is coverage sufficient? You tested 120 prompts across 6 categories. You found 3 failures. Is 97.5% success 'good enough'? A 99.2% refusal rate on a harmful requests benchmark answers a specific, narrow question: 'on these specific prompt patterns, does the model refuse?' It does not answer: 'does the model refuse novel attacks not in this benchmark?', 'does it refuse appropriately across different user contexts?', 'does refusing harmful things come at the cost of over-refusing legitimate requests?', or 'does the model resist multi-turn escalation not tested here?' Each is a distinct safety dimension requiring separate measurement. A single percentage is a data point, not a safety assessment.",
      "Safety benchmarks have well-documented failure modes. Benchmark overfitting: teams that optimize specifically for known benchmarks (AdvBench, HarmBench, and WildGuard — publicly available datasets of harmful prompts used to measure refusal rates) can achieve high scores while failing on novel adversarial prompts not in the benchmark. This is the safety equivalent of test-set leakage. Distribution mismatch: published benchmarks reflect attack patterns known at benchmark creation time; real-world attack distributions evolve continuously. Over-refusal is a separate and equally important safety problem: a model that refuses 25% of legitimate medical queries to avoid 0.01% of possible misuse has shifted harm from potential misuse to systematic under-service. Both refusal rate on harmful prompts and false-positive rate on legitimate prompts in the same domain must be measured together.",
      "Rigorous safety measurement includes: (1) Refusal rate on known benchmarks (necessary but not sufficient); (2) Evaluation on novel red-team attacks not in training data (tests generalization vs. overfitting); (3) False-positive rate on legitimate requests in the same domain (measures over-refusal); (4) Harm severity weighting (a 0.8% failure rate on instructions for mass harm is not equivalent to a 0.8% failure rate on mild content violations — failures should be weighted by potential harm magnitude); (5) Longitudinal production monitoring (safety can degrade with model updates, new attack patterns, or user distribution shifts).",
    ],
    mcq: {
      question: "A safety report claims 99.2% refusal rate on harmful requests. What critical dimension is missing from this single metric?",
      options: [
        "The number of model parameters — larger models should have higher refusal rates",
        "Whether the benchmark was run at temperature=0 vs. higher temperature",
        "The country of origin of the red-team operators who designed the benchmark",
        "False positive rate on legitimate requests in the same domain — a model that achieves 99.2% refusal by also refusing 25% of legitimate queries has made a different safety tradeoff than one that maintains the same refusal rate with low false positives",
      ],
      correct: 3,
      explanation: "Safety is a two-dimensional problem: refusing harmful requests (sensitivity) and not refusing legitimate ones (specificity). A 99.2% refusal rate on harmful prompts achieved by being so restrictive that legitimate medical, legal, or safety queries are also refused is not good safety — it's a different harm profile, not an absence of harm. Both dimensions must be measured together for the metric to be meaningful for deployment decisions. Option D is the correct answer. Option A is wrong — model parameter count has no established relationship with refusal rate; a 7B model properly aligned can outperform a 70B model poorly aligned on safety benchmarks; parameter count is irrelevant to interpreting a safety metric. Option B is wrong — running evals at temperature=0 vs. higher temperature does affect refusal rate in theory (higher temperature can produce more varied outputs), but this is a methodological detail, not the critical missing dimension; the false-positive rate on legitimate requests is far more important for deployment decisions than the temperature setting used during evaluation. Option C is wrong — the country of origin of red-team operators may affect cultural perspective on harm categories but is not a standard validity criterion for safety benchmarks; the critical missing information is the false-positive rate, not the demographics of the benchmark designers.",
    },
    takeaway: "A high refusal rate is necessary but not sufficient for safety. Measure both: refusal rate on harmful prompts AND false-positive rate on legitimate requests in the same domain. Add harm severity weighting (not all failures are equal), novel attack generalization (benchmark overfitting test), and longitudinal production monitoring. Present safety as a profile across dimensions, not a single headline percentage.",
  },

};
