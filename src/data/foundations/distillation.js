// Foundations module — Knowledge Distillation. RUNNER_DATA fragment spread into
// src/data/foundationsRunnerData.js. Keep the export name RUNNER_DISTILLATION.
export const RUNNER_DISTILLATION = {
  "distillation": {
    depthTier: "deep",
    interviewWeight: "high",
    scenario: "Your 70B model hits the quality bar on a customer-support classification and drafting task, but it costs $8 per million tokens and adds 900ms of latency you can't ship. Product needs something ~10× cheaper and faster that keeps most of the quality. A smaller base model trained from scratch on your labeled data plateaus 6 points below the 70B. Someone proposes knowledge distillation — you need to explain what it actually buys you, and why it can beat training the small model directly.",
    explanation: [
      "Start from what a labeled dataset actually gives a small model. A hard label is one-hot: for an email that is really 'billing', the target is [billing=1, technical=0, account=0, spam=0, ...]. Cross-entropy against that one-hot target tells the model exactly one thing — the correct class — and asserts that every other class is equally and maximally wrong. But that's false. A billing dispute is much closer to an account-access complaint than to spam. The one-hot target throws that similarity structure away. A small model trained from scratch has to rediscover which classes are near which others purely from co-occurrence in its limited data, and with limited capacity it often can't. That is why the from-scratch small model plateaus below the big one: not only fewer parameters, but a poorer training signal.",
      "Knowledge distillation replaces (or blends) the one-hot target with the teacher's full output distribution — the probabilities the large model assigns across ALL classes/tokens. This is the core idea. Instead of 'the answer is billing,' the target becomes the teacher's soft judgment: 'billing 0.72, account 0.21, technical 0.06, spam 0.001, ...'. Geoff Hinton called the information hiding in those small non-top probabilities dark knowledge. The teacher saying account=0.21 (not 0.001) tells the student that billing and account are confusable neighbors; the teacher saying spam≈0.001 tells the student spam is nowhere near. A hard label carries roughly log2(num_classes) bits about this example; the soft distribution carries a graded similarity signal over every class at once. The student learns the teacher's geometry of the problem, not just its verdict.",
      "The objective. Distillation trains the student to match the teacher's distribution, measured by KL divergence — the same quantity from the training-signal module, the extra bits you pay to encode samples from p (teacher) using the student's code q. Minimizing KL(teacher‖student) over the soft targets pulls the student's whole distribution toward the teacher's, including the ranking of the non-top classes. In practice you blend two terms: L = α · T² · KL(soft_teacher ‖ soft_student) + (1−α) · CE(hard_label, student). The hard-label cross-entropy keeps the student anchored to ground truth where labels exist; the KL term transfers the dark knowledge. A typical α is 0.5–0.9 — the soft signal usually does most of the work.",
      { type: "illustration", label: "Temperature exposes the dark knowledge (and the T² fix)", content: `Teacher logits z for one example (4 classes):  [4.0, 3.0, -1.0, -3.0]

Softmax at T=1 (softmax(z)):
  billing 0.708 | account 0.260 | technical 0.005 | spam 0.0006
  -> account 0.26 is visible but technical/spam are ~0: the near-neighbor
     signal is faint, and gradients from those tiny probs are negligible.

Softmax at T=3 (softmax(z / T)):
  billing 0.452 | account 0.324 | technical 0.121 | spam 0.062
  -> the RELATIVE structure is now exposed: billing > account > technical > spam.
     "spam is far, technical is closer, account is a real neighbor" is now
     a strong training signal the student can actually fit.

Why T raises T=1 -> T=3? Softening flattens the distribution so the ratios
between small classes carry gradient. You apply the SAME temperature T to the
student's logits during training, then softmax both.

The T^2 detail: soft targets from softmax(z/T) shrink each logit's gradient by
~1/T^2 (a 1/T from the softened student output, a 1/T from the softened target).
Multiplying the distillation loss by T^2 rescales it back so the soft-target
gradients stay comparable in magnitude to the hard-label CE gradients when you
blend them. Drop the T^2 and the soft term is silently down-weighted ~9x at T=3.` },
      "Temperature, precisely. At inference T=1 you want a peaked distribution. But for transfer you deliberately raise the temperature (T>1) on BOTH teacher and student logits before the softmax. A higher T flattens both distributions, magnifying the relative probabilities of the non-top classes — exactly the dark knowledge you want to move. Without softening, technical=0.005 and spam=0.0006 are numerically tiny and produce almost no gradient, so the neighbor structure never transfers; the student effectively falls back to learning near-hard labels. The catch is that softening by T shrinks the gradient magnitude of the soft term by ~1/T², so the standard recipe multiplies the distillation loss by T² to keep it on the same scale as the hard-label term — otherwise the soft signal gets silently down-weighted and you've thrown away the thing you turned temperature up to capture.",
      "Variants, at a naming level. (1) Response-based / logit distillation: match the teacher's output distribution — everything above. Simplest, most general, and all you need if you only have the teacher's outputs. (2) Feature-based: also match intermediate representations — the student is trained so its hidden layers mirror the teacher's (this is how DistilBERT hits ~97% of BERT's GLUE score at ~40% fewer parameters). Needs white-box access and a projection to align dimensions. (3) Relation-based: match relationships BETWEEN examples (e.g. pairwise distances in representation space) rather than per-example outputs. (4) Sequence-level distillation for autoregressive LLMs: matching per-token distributions is weak because errors compound over a generation; instead you train the student on the teacher's actual generated sequences (often the teacher's beam/greedy output), so the student learns to reproduce whole trajectories the teacher would produce.",
      "The modern LLM pattern is (4) taken to its logical end: use a strong teacher to GENERATE the training data. You don't need the teacher's logits at all — you sample outputs, or richer, ask the teacher for step-by-step rationales, and fine-tune a smaller model on that synthetic corpus. Alpaca distilled instruction-following from a stronger model's generations; Orca went further by training on the teacher's reasoning traces, not just final answers; the Phi and Gemma lines lean heavily on high-quality (often teacher-generated / filtered) data to punch far above their parameter count. Mechanistically this is still distillation — you're transferring the teacher's behavior through its outputs — but it works black-box, over any API, with no access to logits or weights. The soft-label/KL machinery and the synthetic-data machinery are the same idea (copy the teacher's output distribution) at two different levels of access.",
      "The decision: distill vs quantize vs prune vs just use a smaller base. These attack different axes. Quantization keeps the SAME architecture and shrinks the numeric precision of the weights (fp16 -> int8/int4) — cheap, no training run, but it only takes you so small before quality falls off a cliff, and it can't change the model's capability. Pruning removes weights/heads/channels, usually needing a fine-tune to recover. Distillation is the expensive-but-powerful option: it requires a training run and access to a teacher (logits OR just generations), but it can produce a genuinely smaller architecture that retains quality a same-size model trained from scratch cannot reach — because it inherits the teacher's learned similarity structure. Crucially they COMPOSE and are usually stacked: distill the 70B down to an 8B student to recover most of the quality, THEN quantize that 8B to int4 for deployment. Distillation buys quality-per-parameter; quantization buys bytes-per-parameter.",
      "Limits, stated plainly so you don't oversell it. First, the student rarely exceeds the teacher on the distilled distribution — you are copying the teacher's judgments, including its mistakes and biases; distillation transfers a ceiling, it doesn't raise one. (Self-distillation and ensemble-teacher tricks nibble at this but don't overturn it.) Second, coverage bounds transfer: the student only learns the teacher's behavior on the inputs you actually distill over. If your distillation data doesn't cover a slice of the input space, the student has no signal there and will diverge from the teacher exactly where you didn't look — a narrow synthetic set gives a narrow student. Third, capacity gaps are real: if the student is far too small relative to the task, no amount of soft labels closes the gap — it will plateau below the teacher no matter how good the signal, because it lacks the representational room to hold the teacher's function. When a student plateaus well below its teacher, the first two suspects are always (a) too little capacity or (b) distillation data that doesn't cover the failing region — not the temperature or α.",
    ],
    mcqs: [
      {
        question: "Distilling a 70B teacher into a small classifier, an engineer trains the student purely on the teacher's argmax (hard) predictions instead of its full softened output distribution. Why does this throw away most of distillation's benefit?",
        options: [
          "Argmax predictions are lower precision (int8) than logits, so the student loses numerical accuracy during training",
          "The teacher's non-top probabilities encode similarity structure — 'dark knowledge' about which classes are near-neighbors — and collapsing to argmax discards that graded signal, leaving the student with a one-hot target no richer than the original labels",
          "Argmax predictions cannot be used with KL divergence, so the student is forced to use a weaker loss function",
          "Using the teacher's argmax makes the student slower to converge because it must recompute the softmax itself",
        ],
        correct: 1,
        explanation: "The whole point of distillation is the teacher's full distribution. When the teacher says billing 0.72 / account 0.21 / spam 0.001, the 0.21 vs 0.001 gap tells the student that account is a confusable neighbor and spam is far — similarity structure a one-hot label can't express. Collapsing to argmax gives you back exactly a one-hot target, no richer than the original hard labels, so you've discarded the dark knowledge that lets a distilled student beat a from-scratch one. Option B is correct. Option A is wrong: argmax vs soft targets is about information content, not numeric precision; logits aren't 'higher precision labels.' Option C is wrong: you certainly can compute KL against a one-hot target (it reduces to cross-entropy) — the problem is the target carries no neighbor information, not that KL is unavailable. Option D is wrong: convergence speed isn't the issue, and the student doesn't recompute the teacher's softmax.",
      },
      {
        question: "In the standard soft-label distillation recipe, both teacher and student logits are divided by a temperature T>1 before the softmax, and the distillation loss is then multiplied by T². What is temperature doing, and why the T² factor?",
        options: [
          "T sharpens the distribution to make the top class more confident; T² corrects for the sharper distribution having larger gradients",
          "T is only applied to the student to regularize it; T² compensates for the doubled number of loss terms in the blended objective",
          "T>1 softens both distributions so the relative probabilities of non-top (near-zero) classes become large enough to carry gradient — exposing the dark knowledge; softening shrinks those gradients by ~1/T², so multiplying the loss by T² rescales the soft term back to the same magnitude as the hard-label term",
          "T controls how many teacher classes are kept; T² normalizes the KL divergence so it sums to one across the vocabulary",
        ],
        correct: 2,
        explanation: "Temperature FLATTENS the distributions (T>1 is softening, not sharpening): at T=1 a near-neighbor class might sit at 0.005 and produce negligible gradient, but at T=3 it lifts to ~0.12 and the ranking billing>account>technical>spam becomes a strong, learnable signal — that's the dark knowledge transferring. But softening by T scales each logit's gradient down by roughly 1/T² (one 1/T from the softened student output, one from the softened target). Multiplying the distillation loss by T² undoes that shrinkage so, when you blend it with the un-softened hard-label cross-entropy, the two terms stay comparable and the soft signal isn't silently down-weighted (~9× at T=3). Option C is correct. Option A inverts the effect — T>1 softens, it does not sharpen — and misstates the T² rationale. Option B is wrong: T is applied to BOTH teacher and student, and T² is not about counting loss terms. Option D is wrong: T doesn't select a class count, and the softmax already normalizes the distribution; T² is a gradient-scale correction, not a normalizer.",
      },
      {
        question: "A team must ship a 70B-quality model at ~10× lower cost and latency. They're weighing distillation vs post-training quantization. Which framing is correct?",
        options: [
          "Quantization is strictly better — it achieves any size reduction distillation can, without needing a training run or teacher access",
          "They solve different axes and are usually composed: distillation trains a genuinely smaller architecture that inherits the teacher's quality-per-parameter (needs a training run + teacher outputs), while quantization shrinks bytes-per-parameter of a fixed architecture (cheap, no training) but caps how small you can go — so distill down to a smaller student, then quantize that student for deployment",
          "Distillation and quantization are mutually exclusive: quantizing a distilled model destroys the transferred dark knowledge, so you must pick one",
          "Distillation is only worthwhile when you have no labeled data, whereas quantization is the right tool whenever labels exist",
        ],
        correct: 1,
        explanation: "Distillation and quantization attack orthogonal axes. Distillation changes the ARCHITECTURE to a smaller one and, by transferring the teacher's similarity structure, keeps quality a from-scratch small model can't reach — but it costs a training run and teacher access (logits or just generations). Quantization keeps the same architecture and just lowers weight precision (fp16 -> int4): cheap, no training, but it can only go so small before quality collapses and it can't add capability. The production move is to STACK them — distill 70B -> 8B to recover quality, then quantize the 8B to int4 for serving. Option B is correct. Option A is wrong: quantization caps how small you can go and can't recover the quality-per-parameter distillation provides. Option C is wrong: distilling then quantizing is the standard, effective pipeline — they compose. Option D is wrong: distillation's value is quality-per-parameter transfer, not a fallback for missing labels; the presence of labels doesn't make quantization the right size-reduction tool.",
      },
      {
        question: "An 8B student is distilled from a 70B teacher on a synthetic corpus the teacher generated, but it plateaus 5 points below the teacher and gives obviously wrong answers on a whole category of edge-case inputs. Turning the temperature up and re-tuning α barely moves the number. What are the two most likely root causes?",
        options: [
          "The KL divergence was computed in the wrong direction, and the T² scaling was omitted",
          "The learning rate was too high and the student overfit the hard labels",
          "A capacity gap (the 8B is too small to represent the teacher's function on this task) and/or distillation-data coverage gaps (the synthetic corpus doesn't cover that edge-case region, so the student got no signal there and diverges exactly where you didn't look)",
          "The teacher itself is miscalibrated, so no student can ever match it regardless of data or size",
        ],
        correct: 2,
        explanation: "When a distilled student plateaus and fails on a specific slice, the first two suspects are capacity and coverage — not the softmax hyperparameters (which is why T and α barely help). Capacity gap: if 8B lacks the representational room to hold the teacher's function on this task, more/better soft labels can't close it; the student simply can't fit the target. Coverage gap: distillation only transfers behavior on inputs you actually distill over — a synthetic corpus that skips an edge-case region gives the student no signal there, so it diverges exactly where the data was thin. The failure being concentrated on 'a whole category of edge-case inputs' points squarely at missing coverage. Option C is correct. Option A is a real bug class but would degrade transfer globally and uniformly, not produce a clean plateau plus a localized edge-case failure. Option B (LR/overfitting) doesn't explain a category-shaped hole and would show as train/val divergence, not this pattern. Option D is wrong: the student is 5 points BELOW the teacher, so the teacher is the ceiling to reach, not the bottleneck — and 'no student can ever match it' contradicts the premise.",
      },
    ],
    takeaway: "Distillation trains a small student to copy a large teacher's full output distribution, not its argmax — the non-top probabilities ('dark knowledge') carry similarity structure a one-hot label throws away, which is why a distilled student beats a same-size model trained from scratch. Blend KL to the teacher's temperature-softened distribution (with the T² rescale) plus hard-label CE; at the LLM level this generalizes to fine-tuning on the teacher's generated data/rationales. Distillation buys quality-per-parameter and composes with quantization (distill then quantize) — but the student can't exceed the teacher, transfers only what the distillation data covers, and plateaus if it's simply too small.",
  },
};
