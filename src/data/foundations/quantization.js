// Foundations module — Quantization. RUNNER_DATA fragment spread into
// src/data/foundationsRunnerData.js. Keep the export name RUNNER_QUANTIZATION.
export const RUNNER_QUANTIZATION = {
  "quantization": {
    depthTier: "deep",
    interviewWeight: "high",
    groundUp: "Let's start with something you already know is true: a trained model is just a giant pile of numbers. Billions of them. And every one of those numbers is stored in some format that takes up a fixed number of bytes — that's a choice someone made, not a law of nature.\n\nHere's the puzzle we'll unravel. Those numbers were stored *generously* during training, because training is delicate and needs the extra precision. But you're not training anymore — you're just running the model forward to get answers. So a fair question hangs in the air: do you still need all those bytes per number once training is done? If the answer is no, you could shrink the whole model dramatically and fit it on hardware that could never hold it before.\n\nTake your time with this one. We'll work out exactly how much precision you can strip away, watch a naive attempt break in a very specific and sneaky way, and see why the *smart* methods survive where the lazy one collapses. By the end you'll be able to look at a model that passed every demo and then failed in production, and name the cause in a sentence.",
    explanation: [
      "A model's parameters are **just numbers**, and the precision you store them in is a free variable.\n\nTraining happens in fp16 or bf16 — **2 bytes per parameter**. That precision was necessary *during training* to accumulate tiny gradient updates without underflow. But at inference you're only doing a forward pass — you don't need 16 bits of dynamic range to multiply a weight by an activation.\n\nQuantization exploits exactly this: store each weight in **fewer bits**, and the model still runs — just with a lossier representation of each number. ==The core question is how much precision you can strip before behavior degrades — and the answer depends heavily on which method you use and which bits you target.==",
      "The reason to care is **pure memory arithmetic** — and it's the same arithmetic that decides which GPU a model fits on.\n\n`params × bytes_per_param = weight memory`. fp16 is 2 bytes; int8 is 1 byte; int4 is 0.5 bytes.\n\n==That linear scaling is the entire value proposition:== halve the bit-width, halve the VRAM — and a model that needed a multi-GPU node now fits on **one card**.",
      { type: "illustration", label: "VRAM by precision (70B model) and which GPU it fits", content: `Weight memory = num_params × bytes_per_param

70B model:
  fp16  (2 bytes)   →  70e9 × 2    = 140 GB   →  needs 2× A100 80GB (won't fit on one)
  int8  (1 byte)    →  70e9 × 1    =  70 GB   →  fits on 1× A100 80GB (with headroom for KV cache)
  int4  (0.5 bytes) →  70e9 × 0.5  =  35 GB   →  fits on 1× A100 40GB, or leaves ~45GB for KV/batching on 80GB

Smaller models, same math:
  13B fp16 → 26 GB   int4 → 6.5 GB  (fits a 24GB consumer 4090)
  7B  fp16 → 14 GB   int4 → 3.5 GB  (fits an 8GB laptop GPU)

Note: this is WEIGHTS only. Activations + KV cache add on top — int4 weights
are what create the headroom to actually batch requests and hold long contexts.` },
      "Quantization splits into **two families** by *when* it happens.\n\n**Post-Training Quantization (PTQ)** takes an already-trained fp16 model and compresses the weights afterward — no gradient updates, no training loop. It runs in minutes to hours on a single GPU and needs only a small calibration set. ==This is what you reach for 95% of the time.==\n\n**Quantization-Aware Training (QAT)** instead simulates the quantized forward pass *during* training or fine-tuning, so the optimizer learns weights that are robust to the rounding error. QAT recovers more quality at very low bit-widths — but costs a full training run and a labeled dataset.\n\nThe decision rule: **use PTQ** unless you're pushing below 4-bit or PTQ has measurably failed your evals. Only then is QAT's training cost justified.",
      "Within PTQ, the naive method is **round-to-nearest (RTN)** — map each weight to the closest value on a uniform int grid, scaled per-tensor or per-channel. This is what a one-line int4 flag often does, and it's exactly where the scenario's failure comes from.\n\nRTN assumes every weight is equally important and every error equally tolerable. ==Both assumptions are false.== A handful of weight channels — the ones multiplied by large-magnitude *outlier* activations that transformers reliably produce — dominate the layer's output. Rounding those channels crudely injects error that **propagates and compounds** across dozens of layers.\n\n`int8` has 256 levels of resolution, so this rarely bites — which is why int8 RTN is usually near-lossless. `int4` has only **16 levels**, so crude rounding of the salient channels is catastrophic — precisely for multi-step tasks where per-layer error accumulates over a long reasoning chain.\n\nThat's the whole scenario: the smoke test passed because single-step lookups tolerate the noise; multi-step arithmetic collapsed because **errors compound**.",
      "The smart int4 methods exist to solve exactly this.\n\n**GPTQ** quantizes weights one layer at a time and uses second-order information — an approximation of the *Hessian* of the layer's reconstruction loss — to decide the rounding. Instead of rounding each weight independently, it rounds one weight, then adjusts the remaining not-yet-quantized weights to compensate for the error it just introduced. ==It minimizes the layer's output error, not the per-weight error.==\n\n**AWQ** (Activation-aware Weight Quantization) takes a different route: it observes that a small fraction of channels are *salient* (they multiply the large activations), scales those channels **up** before quantizing so the grid resolves them finely, then scales the activations **down** to compensate — mathematically equivalent output, but the important channels survive int4.\n\nBoth keep quality near-lossless at int4 where RTN fails. **NF4** (4-bit NormalFloat, from bitsandbytes/QLoRA) uses a *non-uniform* grid whose levels match the roughly-normal distribution of weights — more points where weights are dense (near zero), fewer in the sparse tails. Information-theoretically optimal for normally-distributed data, and the standard base for QLoRA fine-tuning.",
      { type: "illustration", label: "The int8-vs-int4 quality cliff and why method choice matters", content: `Grid resolution:
  int8 → 2^8  = 256 levels   (fine grid; RTN error is small)
  int4 → 2^4  =  16 levels   (coarse grid; rounding is brutal)

Quality on domain evals (typical, relative to fp16 baseline):
  int8 RTN          ~99–100%   → near-lossless, ship it, no special method needed
  int4 RTN (naive)  ~60–85%    → smoke test passes, reasoning/arithmetic COLLAPSES
  int4 GPTQ         ~97–99%    → second-order error compensation saves it
  int4 AWQ          ~97–99%    → salient-channel protection saves it
  int4 NF4          ~97–99%    → optimal grid for weight distribution (QLoRA base)
  below 4-bit (int3/int2) → degrades fast even WITH smart methods

Why the cliff: outlier activations mean a few channels carry most of the signal.
  256 levels tolerates crude rounding of those channels.
   16 levels does not — you MUST protect them (AWQ) or compensate (GPTQ).` },
      "Weight quantization is only **one lever** — and for long-context serving it's often not the binding one.\n\nThe **KV cache** — the stored keys and values for every past token — grows linearly with sequence length and concurrency, and at long contexts it can *exceed the weights* in memory. KV-cache quantization (FP8 or int8 KV) is a **separate, orthogonal decision**: you can run int4 weights with FP8 KV, or fp16 weights with int8 KV.\n\n==It's what lets you hold a 32K-token context or batch more concurrent users on the same card *after* you've already quantized weights.== The KV cache is generated at runtime and is more sensitive than weights to precision loss in some regimes — so `FP8` (which preserves dynamic range better than int8 at the same bit count) is often preferred there.\n\nTreat **weight-bits** and **KV-bits** as two independent knobs on the memory budget.",
      "The final decision is a **three-way tradeoff** — accuracy vs. memory vs. latency — and quantization is not always the right tool.\n\nIf a smaller model (e.g. a 13B) already meets quality, *just use it* — you avoid the quantization risk entirely and often get lower latency. If you need the 70B's capability but can't afford its footprint, **distillation** (train a small model to mimic the big one) can beat quantization on latency, but costs a training pipeline and a distillation dataset. **Quantization wins** when you must preserve the *exact* trained model's behavior and only need to shrink its footprint.\n\nAnd the single most under-appreciated failure mode is the **calibration set**. PTQ methods (GPTQ/AWQ) estimate activation statistics from a small calibration corpus. If that corpus doesn't match production traffic — calibrate on generic English but serve code and multilingual queries — the quantizer protects the *wrong* channels. The model passes smoke tests on the calibration-like distribution and tanks on the real one.\n\n==Calibration-set representativeness is a correctness requirement, not a detail.==",
    ],
    scenario: "Now let's put all of that to work on a real one. A team fine-tuned a Llama-3 70B that hits every quality target, but they need to serve it on a single A100 80GB — and fp16 weights alone are 140GB, so it won't even load. An engineer quantizes it to int4 with a one-line bitsandbytes flag. It loads, smoke tests pass, it ships. A week later, support escalates: multi-step arithmetic and structured JSON generation have collapsed on real customer traffic, even though the demo looked fine.\n\nTake a moment before reading on — what could make a model pass every smoke test and then fail *specifically* on multi-step reasoning? Here's the reasoning, step by step. That one-line flag is almost certainly plain round-to-nearest, and int4 has only 16 grid levels. We saw that a handful of salient channels — the ones multiplied by transformers' large outlier activations — dominate each layer's output, and rounding those crudely injects error that **compounds across dozens of layers**. Notice how cleanly that explains the symptom: a single-fact lookup tolerates a little noise, so the smoke test passes; a multi-step arithmetic chain accumulates that error at every step until the answer is wrong. The fix follows directly from the mechanism — re-quantize with an outlier-aware method (AWQ scales up and protects the salient channels; GPTQ compensates rounding error with second-order information), and calibrate on traffic that actually looks like production. Same 70B model, same 35GB footprint — quality restored, because now the bits that matter survive.",
    keyPoints: [
      "**Precision is a free variable at inference.** Training needs fp16/bf16 for gradient accumulation; a forward pass does not. Quantization stores each weight in fewer bits — the model still runs, just lossier.",
      "**Memory scales linearly with bit-width:** `params × bytes_per_param`. fp16 (2B) → int8 (1B) → int4 (0.5B). A 70B model goes 140GB → 70GB → 35GB — which is exactly what decides the GPU it fits on.",
      "**int8 RTN is near-lossless; int4 RTN silently breaks reasoning.** 256 grid levels tolerate crude rounding of outlier-driven channels; 16 levels do not. Error compounds across layers, so multi-step tasks collapse while single-step smoke tests pass.",
      "**Smart int4 methods rescue quality by protecting salient channels.** GPTQ compensates rounding error using second-order (Hessian) info; AWQ scales up the outlier channels before rounding; NF4 uses a distribution-matched non-uniform grid. All ~97–99% vs fp16.",
      "**Weight-bits and KV-cache-bits are independent knobs.** The KV cache grows with sequence length and concurrency and can exceed the weights at long context; quantize it (FP8/int8 KV) separately to reclaim that memory.",
      "**A mismatched calibration set is a correctness bug.** PTQ estimates which channels are salient from the calibration corpus — calibrate on the real production distribution, or it protects the wrong channels.",
    ],
    recap: [
      "**Quantization = fewer bits per weight.** Training needs fp16/bf16 precision; a forward pass doesn't — store weights lossier and the model still runs.",
      "**Memory is linear in bit-width:** fp16 2B → int8 1B → int4 0.5B. 70B: 140GB → 70GB → 35GB — decides which GPU it fits on.",
      "**int8 near-lossless, int4 dangerous under naive RTN:** 256 vs 16 grid levels → outlier-driven channels rounded crudely → error compounds across layers → multi-step reasoning collapses while smoke tests pass.",
      "**Fix int4 with outlier-aware methods:** GPTQ (Hessian error compensation), AWQ (scale up salient channels), NF4 (distribution-matched grid) — all ~97–99% of fp16.",
      "**KV cache is a separate knob:** grows with context length + concurrency, can exceed weights; quantize FP8/int8 KV independently for long-context serving.",
      "**Calibrate on production-representative data** — a mismatched calibration set makes PTQ protect the wrong channels; a correctness bug, not a detail.",
    ],
    mcqs: [
      {
        question: "A 70B model was quantized to int4 with a one-line round-to-nearest (RTN) flag. Smoke tests (single-fact lookups) pass, but multi-step arithmetic and structured JSON generation collapse in production. What is the most accurate explanation?",
        options: [
          "int4 halves the byte-width used to store each weight, which effectively halves how many distinct parameters the model can represent, so multi-step reasoning degrades but simple lookups don't",
          "The smoke tests ran against the original fp16 checkpoint while production served the int4 build, so the two evaluations never tested the same weights or the same numerical behavior at all",
          "A few salient channels multiplied by large outlier activations dominate each layer's output; int4's 16-level grid rounds them crudely, and that error compounds across layers, breaking long chains",
          "int4 quantization silently disables the attention layers needed for multi-step arithmetic, while leaving the simpler layers used for single-fact lookup fully untouched and intact",
        ],
        correct: 2,
        explanation: "Option 3 is correct: transformer activations produce large-magnitude outliers, and the weight channels multiplied by those outliers carry a disproportionate share of each layer's output. int4's 16 grid levels round those salient channels crudely, and that error compounds across dozens of layers — multi-step chains accumulate it while single-step lookups tolerate the noise. Option 1 is wrong: quantization changes the PRECISION of each weight (bytes per parameter), not the NUMBER of parameters — a 70B int4 model still has 70B distinct weights. Option 2 is wrong: the smoke test and production traffic both ran against the same int4-quantized model; there's no fp16-vs-int4 mismatch in the scenario as described. Option 4 is wrong: quantization lowers precision uniformly across weights — it doesn't selectively disable attention layers, and there is no such all-or-nothing shutoff mechanism.",
      },
      {
        question: "Given the int4-RTN failure above, an engineer needs to keep the exact 70B model but restore quality at 4-bit. Which fix targets the actual mechanism?",
        options: [
          "Switch from int4 down to int3, since fewer available grid levels forces the quantizer to round each weight to a coarser but supposedly more stable value, cutting error",
          "Raise the sampling temperature at inference so the model explores alternate reasoning paths and can route around the weights that were rounded incorrectly during quantization",
          "Leave the weights at int4 RTN but raise the KV-cache precision to FP8, since multi-step reasoning state is accumulated and stored token-by-token in the KV cache",
          "Re-quantize with AWQ or GPTQ: AWQ scales up salient channels before rounding, GPTQ uses Hessian info to compensate rounding error, both preserve the channels RTN destroys",
        ],
        correct: 3,
        explanation: "Option 4 is correct: the failure is crude rounding of salient, outlier-multiplied channels. AWQ protects those channels by scaling them into a finer part of the grid before quantizing; GPTQ uses an approximation of the layer's Hessian to round one weight and then compensate with the remaining weights, minimizing the layer's output error rather than per-weight error. Both directly counter the RTN mechanism and hold quality near fp16 at int4. Option 1 is wrong and backwards — int3 is COARSER (8 levels) than int4 (16 levels); dropping below 4-bit degrades faster, it doesn't help. Option 2 is wrong — temperature changes sampling randomness, not the numerical error baked into the quantized weights; a wrong intermediate computation is wrong regardless of temperature. Option 3 is wrong — KV-cache precision is a separate lever for serving long contexts/concurrency; it does not store or repair reasoning, and it leaves the damaged int4 weights untouched.",
      },
      {
        question: "A team uses AWQ int4 and calibrates the quantizer on a corpus of generic English prose. The model passes their internal evals but degrades badly in production, where real traffic is mostly source code and multilingual queries. What is the root cause?",
        options: [
          "The calibration set was unrepresentative of production traffic, so AWQ estimated activation stats from the wrong distribution and protected the wrong salient channels",
          "AWQ's quantization method is fundamentally incompatible with code tokens and non-English scripts, so those domains degrade under AWQ no matter how the calibration set is built",
          "int4's 16-level grid cannot represent the wider token vocabulary used by code and multilingual text, so those inputs overflow the available quantization levels entirely",
          "The domain shift to code made this a QAT problem rather than a PTQ problem — no calibration set, however representative, could have prevented this regression from happening",
        ],
        correct: 0,
        explanation: "Option 1 is correct: PTQ methods like AWQ and GPTQ estimate which channels are salient from activation statistics measured on a calibration corpus. If that corpus (generic English) doesn't match production (code + multilingual), the estimated statistics are wrong, the wrong channels get protected, and the quantized model passes evals on calibration-like data while tanking on the real distribution. Calibration-set representativeness is a correctness requirement. Option 2 is wrong — AWQ works fine on code and multilingual data when calibrated on representative samples of that data; the method isn't domain-incompatible, the calibration set was mismatched. Option 3 is wrong — quantization lowers the precision of WEIGHTS; it does not restrict which tokens or scripts the model can process, and there is no per-token grid overflow mechanism. Option 4 is wrong — the fix here is fixing the calibration set (add code and multilingual samples), not necessarily switching to QAT; QAT's training cost is only justified below 4-bit or after PTQ with a proper calibration set has failed.",
      },
      {
        question: "A serving team already runs a model at int4 weights on an 80GB GPU, but at 32K-token contexts with high concurrency they hit out-of-memory errors, and weight quantization is already maxed out. Which two statements about the correct next lever are true?",
        options: [
          "The KV cache grows linearly with sequence length and concurrency, so at long contexts under load it can rival or exceed the memory used by the weights themselves",
          "Quantizing the KV cache (to FP8 or int8) is a separate, orthogonal lever from weight-bit quantization, so it can reclaim memory that shrinking the weights alone cannot",
          "KV-cache size is fixed permanently by the model's architecture at training time, so the only way to free memory at serving time is to re-quantize or distill the weights themselves",
          "Raising the weight precision from int4 back to int8 reduces total memory pressure across the deployment, giving more headroom to serve a longer context at the same concurrency",
        ],
        correct: [0, 1],
        explanation: "Options 1 and 2 are both correct: the KV cache stores keys and values for every past token, so it scales linearly with sequence length and concurrency and can exceed weight memory at long contexts (option 1) — and because KV-cache quantization is an independent knob from weight-bit quantization, it can reclaim memory that weight quantization alone already maxed out on (option 2). Option 3 is wrong — the KV cache is not fixed at serving time; it can be quantized, paged, or windowed independently of the weights. Option 4 is wrong and backwards — raising weight precision from int4 to int8 DOUBLES weight memory (0.5→1 byte/param), making the out-of-memory situation worse, not better.",
      },
    ],
    takeaway: "Quantization trades precision for memory linearly (fp16 2B → int8 1B → int4 0.5B per param), and which GPU a model fits on falls straight out of that math. int8 is near-lossless with plain rounding; int4 needs outlier-aware methods (AWQ/GPTQ/NF4) or it silently breaks multi-step reasoning while passing smoke tests. Treat weight-bits and KV-cache-bits as independent knobs, and always calibrate PTQ on a corpus that matches production traffic — a mismatched calibration set is a correctness bug, not a detail.",
  },
};
