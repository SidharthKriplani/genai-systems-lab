// Foundations module — Speculative Decoding. RUNNER_DATA fragment spread into
// src/data/foundationsRunnerData.js. Keep the export name RUNNER_SPECDECODE.
export const RUNNER_SPECDECODE = {
  "speculative-decoding": {
    depthTier: "deep",
    interviewWeight: "high",
    groundUp: "Let's start with the thing you watch every time a model streams an answer: tokens appear one after another, never all at once. That ordering isn't a UI choice — it's forced by how the model generates. And once you see *why* it's forced, a surprising opportunity opens up.\n\nHere's the puzzle this module cracks. Generation feels slow, and the obvious fix — throw more GPUs at it — doesn't help a single stream at all. Stranger still, while the model grinds out one token at a time, the expensive hardware is mostly sitting *idle*. There's spare capacity right there, unused, every single step. What if you could spend it to produce several tokens for the price of one — without changing a single word the model would have said?\n\nThat's exactly what speculative decoding does, and it sounds impossible, so we'll earn it slowly. First we'll see why one token per step is forced and why that leaves the GPU idle, then the split between *proposing* tokens cheaply and *verifying* them in parallel, then the little piece of accept/reject math that makes it provably lossless — and finally when it gives you nothing at all.",
    scenario: "Now let's put all of that to work on a real one. Your inference team ships a 70B model that generates at 30 tokens/sec on a single stream — fast enough for batch jobs, too slow for the live coding assistant where users watch every token appear. Buying more GPUs doesn't help: one stream can't be split across cards, and the GPU sits mostly idle during decode. A senior engineer proposes speculative decoding to 'make it 2–3× faster without changing the answers.' Take a moment before reading on: given everything above, is that claim believable, what sizes the speedup, and where will it do nothing? Here's the reasoning, step by step. The claim holds because single-stream decode is memory-bandwidth-bound — the GPU streams the weights to make one token while compute sits idle, so a cheap draft can propose several tokens the big model verifies in one parallel pass, and the accept/reject rule keeps every emitted token drawn from exactly the target's distribution (lossless — speed changes, answers don't). The size of the win is set by the acceptance rate α (draft–target alignment): a well-aligned draft gives ~2.5–3×, a poorly aligned one collapses to net-neutral. And it does nothing where the GPU is already busy: under high-throughput batched serving the batch already consumes the free parallelism, so the live batch-1 assistant benefits a lot while the nightly compute-saturated batch job barely moves.",
    explanation: [
      "Start with why decode is slow in the first place. Autoregressive generation is **sequential by construction**: token N cannot be computed until token N-1 exists, because N-1 is part of the input that produces N. So you run one full forward pass through the entire model — all the layers, all the weights — to produce **exactly one token**, then repeat.\n\nThe bottleneck is not arithmetic. On modern GPUs a single-stream decode step is **memory-bandwidth-bound**: the expensive part is streaming the model's weight matrices (and the KV cache) from HBM into the compute units. ==Producing one token touches roughly the same weights as producing a hundred would== — the tensor cores are mostly idle, waiting on memory.\n\nThis is the root fact that makes speculative decoding possible: **there is spare compute sitting there per step.**",
      "The insight: **separate proposing tokens from verifying them.** Keep a small, cheap DRAFT model (say a 1B model of the same family) alongside the big TARGET model (the 70B you actually want to sample from). Each round, let the draft model autoregressively generate k candidate tokens — cheaply, because it is tiny.\n\nNow the k proposed tokens are **already known strings of text**. That lets the target model process all k positions in ONE forward pass: feed the current context plus the k drafted tokens as a single sequence, and the target produces its next-token distribution at every one of those k positions **in parallel**.\n\nThis is the same reason training and prefill are fast — when the tokens are already fixed, attention over a whole span is one batched matmul, not k sequential steps. ==Verification is parallel precisely because the tokens being verified are already committed.==",
      "Now the accept/reject rule, and why it is **LOSSLESS — not an approximation**. Walk left to right through the k drafted positions. At each position you have the draft's probability q(x) for the token it proposed and the target's probability p(x) for that same token. **Accept the drafted token with probability min(1, p(x)/q(x)).** If it is accepted, move to the next position.\n\nOn the FIRST rejection, you stop, throw away all remaining drafted tokens, and resample that one position from the corrected distribution — a renormalized (p − q)⁺ — which is exactly the target's distribution conditioned on 'not the rejected draft.' This is modified rejection sampling, and ==the math guarantees the emitted token at every position is drawn from exactly p, the target model's distribution.==\n\nThe draft never gets to inject a token the target wouldn't have sampled: where the draft is over-confident (q > p) it gets down-weighted, and where it disagrees the correction resamples from the target. Net effect: the sequence of tokens you emit is distributionally identical to plain autoregressive sampling from the target — **speculative decoding changes SPEED, not WHAT the model would have produced**. (Contrast this with the sampling module's temperature/top-p, which deliberately change the output distribution — speculative decoding is the opposite: it holds the distribution fixed.)",
      { "type": "illustration", "label": "One speculative round (k=4), and where the speedup comes from", "content": "Context: \"def add(a, b):\\n    return\"\n\nDraft model proposes k=4 tokens (4 cheap draft passes):\n    [ a  +  b  \\n ]\n\nTarget model verifies ALL 4 in ONE forward pass:\n    pos1  q(a)=0.7  p(a)=0.9   accept  (p>q, keep)\n    pos2  q(+)=0.6  p(+)=0.8   accept\n    pos3  q(b)=0.8  p(b)=0.5   accept w.p. min(1, 0.5/0.8)=0.625 -> REJECT\n    pos4  ...discarded (never verified)\n\n  Accepted this round: 2 tokens (a, +)\n  Position 3 resampled from corrected target dist (p - q)+  -> e.g. \"c\"\n  Emitted: a  +  c        (all drawn from the TARGET's distribution)\n\nCost of the round: 1 target forward pass (+ 4 cheap draft passes)\nTokens produced:    3   (2 accepted + 1 resampled)\n\n  Naive decode:  3 tokens = 3 target forward passes\n  Speculative:   3 tokens = 1 target forward pass\n  -> ~3x fewer target passes THIS round.\n\nThe target forward pass is the expensive, memory-bound step.\nEvery accepted draft token is one target pass you did NOT have to run." },
      "The economics: **the speedup is governed by the acceptance rate α** — how often the draft agrees with the target. Each round costs one target forward pass regardless of how many tokens it verifies (that is the whole point — the target pass is nearly free per extra position, because it is memory-bound). So the win is the **expected number of tokens emitted per target pass**.\n\nWith per-token acceptance probability α and a proposal length of k, the expected accepted-run length before the first rejection is roughly (1 − α^(k+1)) / (1 − α), plus the one guaranteed resampled token. Concretely: propose k=4, and if α ≈ 0.75 you accept ~2.5 tokens on average, so you emit ~3.5 tokens per target pass instead of 1 — **call it a 2.5–3× wall-clock speedup** after draft overhead. If α ≈ 0.4, you accept well under one token per round on average, the draft passes are pure overhead, and ==you can end up NET NEUTRAL OR SLOWER than plain decoding.==\n\nSo α is everything, and α depends on **draft–target ALIGNMENT**: the draft must predict what the target would predict. That is why the best drafts are small models distilled from (or in the same family as) the target — a random 1B model paired with a 70B produces low α and no speedup. There is also a **k tradeoff**: bigger k means more tokens per accepted round but more wasted draft work when an early rejection discards the tail.",
      "When it helps vs. when it does not. Speculative decoding **buys back the idle compute of a memory-bandwidth-bound step**. That describes single-stream, latency-sensitive, **LOW-BATCH decode** — one user watching tokens stream, batch size 1. There, the GPU has spare compute and speculative decoding converts it into throughput: **big win**.\n\nBut in already-saturated, **HIGH-THROUGHPUT batched serving**, many independent requests are already packed into each forward pass — the GPU's compute is BUSY, the step is no longer memory-bound, and ==the 'free' parallelism speculative decoding relies on is already consumed by the batch.== Adding speculation there mostly adds draft overhead and rejected-token waste for little gain, and can reduce total throughput even as it might still trim single-request latency. So the same technique helps enormously at batch-1 and barely at all under a compute-saturated batch — the regime *is* the diagnosis.\n\n**Variants relax the two-model requirement:** self-speculative decoding drafts using the target's own early layers or skipped layers; Medusa adds extra decoding heads to the target so it proposes multiple future tokens itself; EAGLE drafts in feature space for higher α. All keep the same lossless verify-and-accept core — they only change how the k proposals are produced.\n\nThe interactive lets you turn the acceptance rate α and the proposal length k and watch tokens-per-target-pass rise, then collapse to net-neutral when the draft falls out of alignment. Then the closing scenario puts it to work on a 70B assistant that's fast enough for batch but too slow live — see if you can confirm the 2–3× claim, size it, and call where it does nothing before the reasoning does.",
    ],
    keyPoints: [
      "**Single-stream decode is memory-bandwidth-bound, not compute-bound.** One forward pass streams all the weights from HBM to produce just one token — the tensor cores sit idle, leaving spare compute to exploit.",
      "**Draft proposes, target verifies in parallel.** A tiny draft model autoregressively generates k tokens; because those tokens are already fixed, the target scores all k positions in ONE batched forward pass — same parallelism as prefill/training.",
      "**The accept/reject rule is provably lossless.** Accept each drafted token with prob min(1, p/q); on first rejection resample from the corrected (p−q)⁺ distribution. Every emitted token comes from exactly the target's distribution — speed changes, output doesn't.",
      "**Speedup is set by the acceptance rate α = draft–target alignment.** High α (well-aligned/distilled draft) → 2.5–3×; low α (α≈0.4) → draft passes become pure overhead, net-neutral or slower.",
      "**Biggest wins are low-batch, latency-sensitive decode.** Under high-throughput batched serving the GPU is already compute-saturated, the free parallelism is consumed by the batch, and speculation barely helps.",
      "**Variants change only how proposals are made:** self-speculative (early/skipped layers), Medusa (extra decoding heads), EAGLE (feature-space drafting) — all keep the same lossless verify-and-accept core.",
    ],
    recap: [
      "**Decode is sequential + memory-bound:** one target forward pass per token, GPU mostly idle — spare compute is what speculation reclaims.",
      "**Draft/verify split:** cheap draft proposes k tokens; target verifies all k in one parallel pass because the tokens are already committed.",
      "**Lossless by construction:** accept w.p. min(1,p/q), resample from (p−q)⁺ on first rejection → emitted tokens are exactly the target's distribution.",
      "**α governs the win:** α is draft–target alignment; high α → 2.5–3×, low α → overhead, net-neutral or slower. k trades tokens-per-round vs wasted tail work.",
      "**Batch regime decides value:** big win at batch-1/latency-sensitive; barely helps under compute-saturated high-throughput serving.",
      "**Variants:** self-speculative, Medusa, EAGLE — different proposal mechanisms, same lossless verify-and-accept core.",
    ],
    mcqs: [
      {
        question: "A PM worries that turning on speculative decoding will subtly change the model's answers, since a smaller draft model is now involved in generation. Is that concern valid, and why?",
        options: [
          "Yes — the draft model contributes some tokens directly, so the output is a blend of the draft and target distributions and will drift from the target's true output",
          "No — the accept/reject rule is constructed so every emitted token is drawn from exactly the target model's distribution; the draft only proposes candidates, and mismatches are corrected by resampling from the target, so the output distribution is identical",
          "Yes — speculative decoding raises the effective temperature because rejected drafts inject randomness, making outputs more diverse than plain decoding",
          "No — but only because the draft and target are the same size, so any tokens the draft contributes would have been chosen by the target anyway",
        ],
        correct: 1,
        explanation: "Option B is correct: speculative decoding is lossless by construction. Each drafted token is accepted with probability min(1, p/q) and, on the first rejection, that position is resampled from the corrected target distribution (p − q)⁺. The math guarantees every emitted token is drawn from exactly p, the target's distribution — so the output is distributionally identical to plain autoregressive sampling from the target. It changes speed, not what the model produces. Option A is wrong because the draft never contributes an un-verified token; where q > p the draft is down-weighted and disagreements are resampled from the target, so there is no 'blend.' Option C is wrong because the accept/reject rule does not change the sampling distribution or effective temperature — the emitted distribution is exactly the target's at whatever temperature the target uses. Option D is wrong because losslessness comes from the accept/reject math, not from matching model sizes; a tiny draft with a huge target is still exactly lossless (it just affects speed, via acceptance rate, not correctness).",
      },
      {
        question: "Two teams both add speculative decoding to the same 70B target. Team A gets a 2.7× speedup; Team B gets essentially none. Both propose k=4 tokens per round. What most likely explains the difference?",
        options: [
          "Team B used a larger draft model, and larger drafts are always slower than no speculation",
          "Team B's draft is poorly aligned with the target, so the acceptance rate α is low — few drafted tokens survive verification, the draft passes become pure overhead, and the win collapses toward net-neutral",
          "Team A must be running at a lower temperature, which is the only parameter that controls speculative speedup",
          "Team B forgot to disable the KV cache, which is incompatible with speculative decoding",
        ],
        correct: 1,
        explanation: "Option B is correct: the speedup is governed by the acceptance rate α, which depends on how well the draft predicts what the target would predict. A well-aligned draft (e.g. distilled from or same-family as the target) yields high α, long accepted runs, and many tokens per target forward pass. A poorly aligned draft yields low α — most drafted tokens get rejected, so each round emits close to one token while still paying for the draft passes, driving the net win to zero (or negative). Option A is wrong because draft size is a cost/alignment tradeoff, not a guaranteed loss — a slightly larger but much better-aligned draft can win big; the binding variable is α, not raw draft size. Option C is wrong because temperature does not set the speedup; α and draft alignment do, and speculative decoding is lossless at whatever temperature the target uses. Option D is wrong because speculative decoding is fully compatible with (and relies on) the KV cache; nothing needs to be disabled.",
      },
      {
        question: "A team enables speculative decoding on their high-throughput serving cluster, which already runs large batches of concurrent requests near full GPU utilization. They see almost no throughput improvement. Why?",
        options: [
          "Speculative decoding only works below a certain sequence length, and their requests exceed it",
          "Batching and speculative decoding are mathematically incompatible, so the accept/reject rule silently falls back to plain decoding",
          "Speculative decoding buys back the idle compute of a memory-bandwidth-bound step; under heavy batching the GPU is already compute-saturated, so there is little idle compute to reclaim and speculation mostly adds draft overhead and rejected-token waste",
          "The draft model is competing with the target for the same GPU memory, so the KV cache overflows and throughput drops to zero",
        ],
        correct: 2,
        explanation: "Option C is correct: single-stream decode is slow because it is memory-bandwidth-bound — the GPU streams weights from HBM to produce one token while compute sits idle, and speculative decoding converts that spare compute into extra verified tokens per pass. But large-batch serving already packs many requests into each forward pass, so the step becomes compute-bound and the GPU is busy; there is little idle compute for speculation to reclaim, and the extra draft passes plus discarded rejected tokens can even cost throughput. The technique's biggest wins are in low-batch, latency-sensitive single-stream decode. Option A is wrong because there is no fixed sequence-length cutoff that disables speculative decoding. Option B is wrong because batching and speculation are not incompatible; the real issue is that the free compute speculation needs is already consumed by the batch. Option D is wrong because the null result is a compute-saturation effect, not a memory overflow; a memory blowup would cause errors or evictions, not a quiet lack of speedup.",
      },
      {
        question: "In a speculative round the draft proposes k tokens one at a time (k sequential draft passes), yet the large target model verifies all k of them in a SINGLE forward pass. Why can the target verify k positions in one pass when it can only generate one token at a time during normal decoding?",
        options: [
          "The target uses a smaller precision (INT4) during verification, which lets it process k positions for the cost of one",
          "During verification the k proposed tokens are already known, so the target can compute its next-token distribution at all k positions in parallel in one batched pass — exactly like prefill/training; normal decoding is sequential only because each token is unknown until the previous one is produced",
          "The target skips the attention computation during verification and only runs the feed-forward layers, which are parallelizable",
          "Verification reuses the draft model's forward pass, so the target never actually runs on the k positions itself",
        ],
        correct: 1,
        explanation: "Option B is correct: autoregressive decoding is sequential only because token N is unknown until token N-1 is produced, forcing one forward pass per token. In verification the k drafted tokens are already fixed strings, so the target can feed the context plus those k tokens as one sequence and compute its next-token distribution at every position simultaneously — a single batched forward pass, the same parallelism that makes prefill and training fast. Option A is wrong because the parallel verification comes from the tokens being known, not from lowering precision; quantization is an unrelated memory optimization. Option C is wrong because the target runs full attention (and needs it — verification depends on attending over the real context); it does not skip attention. Option D is wrong because the target genuinely runs its own forward pass over the k positions to produce p(x) for the accept/reject test; it does not reuse the draft's computation.",
      },
    ],
    takeaway: "Autoregressive decode is sequential and memory-bandwidth-bound — one target forward pass per token with the GPU mostly idle. Speculative decoding has a cheap draft model propose k tokens that the target verifies in a single parallel pass, keeping accepted tokens and resampling on the first rejection; the accept/reject math makes it LOSSLESS — exactly the target's distribution, faster, not different. Speedup is set by the acceptance rate α (draft–target alignment); it wins big on low-batch single-stream latency and barely helps under already-saturated high-throughput batched serving.",
  },
};
