// Gap-A foundation modules — agent trajectory evaluation + RAG ingestion pipeline.
// Spread into foundationsRunnerData.js by the parent.
export const RUNNER_GAP_A = {
  "agent-eval-trajectory": {
    depthTier: "deep",
    interviewWeight: "high",
    groundUp: "Let's start with the most natural way in the world to grade an agent: did it get the right answer? You give it a task, you check the final reply against what you expected, and you mark it pass or fail. Clean, obvious, and for a plain one-shot model it's genuinely all you need — there's only one step, so the answer *is* the whole story.\n\nNow here's the small puzzle that cracks the whole module open. Imagine two students both hand in the correct answer to a math problem. One worked it out properly. The other copied a number that happened to match, skipped three steps, and got lucky. On a grade-the-answer-only test, they score *identically* — and yet only one of them will get the next problem right. Pause on that, because an *agent* is exactly the many-step case: it plans, calls a tool, reads the result, decides what to do next, over and over. Grading only its final sentence is grading only the last line of a long proof.\n\nSo we'll need a second kind of evaluation — one that looks at the *road*, not just the destination. The reassuring news is that it's not exotic: it's just scoring each step the way you'd check a student's working. We'll see exactly how outcome-only grading fails in two opposite and dangerous directions, then build up the step-by-step alternative — trajectory evaluation — and the harness that runs both together.",
    scenario: "Now let's put all of that to work on a real one. You ship a customer-support agent that can search the knowledge base, look up an order by ID, issue a refund, and email the customer. Your eval suite reports 92% task success and the team is celebrating. Then finance flags a spike in refunds. You pull ten transcripts and the story is ugly: on one 'where is my order' ticket the agent called `issue_refund` (never asked), the refund silently failed because the order was already delivered, and the agent then wrote a cheerful 'your order is on its way' — which was the right final answer, so your eval scored it PASS. On another ticket the agent looked up a completely different customer's order, saw a matching status by luck, and gave a correct-sounding reply — also PASS. Take a moment before reading on: why can 92% task success sit right on top of an agent that is quietly issuing unauthorized refunds? Here's the reasoning, step by step. Both bad tickets ended on a plausible final sentence, and your eval grades *only* the final sentence — so it collapsed a broken, lucky, unsafe path into the same PASS as a clean one, exactly like the two students who copied the same answer. Notice what actually would have caught it: per-step trajectory scoring — a golden-trajectory assertion that `issue_refund` must never fire on a status lookup, plus tool-argument checks — fails precisely at the offending step and localizes the bug, which no amount of outcome grading can do.",
    explanation: [
      "Start from the definition of what any evaluation actually grades. **Outcome evaluation** asks a single question: did the system's *final* output match the expected answer? Pass or fail on the endpoint. For a one-shot model call that's often enough — because there's only one step, so the step and the outcome are literally the same thing, and grading the endpoint grades the whole computation. But an *agent* is not one step. It's a *trajectory* — the same **Thought → Action → Observation** loop from ReAct (see the ReAct module), repeated as many times as the task needs and closed out by a final Action that delivers the answer. Each Thought, Action, and Observation in that loop can independently be right or wrong. So the moment there is a path rather than a point, grading only the endpoint means grading only the last node — ==which means outcome eval collapses that whole sequence into a single pass/fail and throws away everything that happened along the way.==",
      "That collapse is dangerous because it can be wrong in *two opposite directions*, and it helps to see them as mirror images. **False pass (right answer, broken path):** an agent can arrive at the correct final answer through a path that is broken, unsafe, or simply lucky — it might call a destructive tool nobody asked for, ignore a silently-failed tool, and never ground its claim in a real observation, yet still emit a plausible final sentence. Because outcome eval reads only that final sentence, it says PASS, so the broken agent looks indistinguishable from a clean one. **False fail (right path, wrong answer):** the exact mirror — an agent reasons correctly and calls the right tools with the right arguments, but the underlying data was stale or a downstream API errored, so the final answer comes out wrong. Outcome eval says FAIL and sends you to debug the agent's *reasoning*, when the real fault lived in the environment. ==Both failures have one root: outcome-only eval conflates 'the agent did the right things' with 'the world produced the right result' — and those two come apart constantly.==",
      { type: "illustration", label: "Same task, same PASS — one path safe, one broken", content: `Task: "Where is my order #A-4471?"   Golden final: "It was delivered on Tue."

Same loop as ReAct — Thought -> Action -> Observation, repeated, then a
final Action that delivers the reply. Trajectory eval scores every step.

TRAJECTORY 1 (correct path)          TRAJECTORY 2 (broken path, lucky)
 Thought: look up the order ✓         Thought: look up the order ✓
 Action: lookup_order("A-4471") ✓     Action: lookup_order("A-9999") ✗ WRONG ARG
 Observation: "delivered Tue" ✓       Observation: "delivered Tue" ✗ WRONG ORDER
 Action: reply "delivered Tue" ✓        (someone else's order, matched by luck)
                                       Action: issue_refund()  ✗ HALLUCINATED CALL
                                       Observation: refund failed  ✗ IGNORED
                                       Action: reply "delivered Tue"  ✗ UNEARNED
                                        (sounds right, built on the wrong order)

 OUTCOME EVAL:  PASS                   OUTCOME EVAL:  PASS  ← identical score
 TRAJECTORY EVAL: 4/4 steps ok        TRAJECTORY EVAL: 1/6 steps ok — only the
                                        opening Thought was sound; every step
                                        after it compounds the wrong order

 The two are indistinguishable to outcome eval. Only per-step scoring
 separates the agent you can ship from the one that will burn you.` },
      "So the fix is **trajectory evaluation**: score the agent's *process*, step by step, not just its endpoint. Concretely you assert against each decision in the trajectory. The workhorse metrics: **tool-call accuracy** — for each step, did the agent (1) select the *right tool* and (2) call it with the *right arguments*? Calling `issue_refund` when the task was a status lookup fails tool-selection; calling `lookup_order('A-9999')` when the ID was `A-4471` fails argument-correctness. **Step success rate** — what fraction of steps produced a valid, expected observation. **Redundant / hallucinated calls** — did the agent invoke tools that weren't needed (wasteful, and in the refund case, dangerous), or invent a tool or an argument that doesn't exist? **Error recovery** — when a tool *did* fail, did the agent notice and adapt (retry, fall back, ask the user) or did it barrel ahead as if it succeeded? ==A robust agent isn't one that never hits an error; it's one whose trajectory shows it *detecting and recovering from* errors. Recovery is a first-class metric, not a footnote.==",
      { type: "illustration", label: "Outcome metrics vs. process (trajectory) metrics", content: `OUTCOME metrics                    PROCESS / TRAJECTORY metrics
──────────────────────────────    ───────────────────────────────────
 task success (final correct?)     tool-selection accuracy (right tool?)
 answer quality / faithfulness     tool-argument accuracy (right args?)
 goal completion                   step success rate (valid observation?)
 end-state assertions              redundant / hallucinated call count
                                   error-recovery rate (adapted on failure?)
                                   efficiency (steps / tokens / cost to goal)

 RULE OF THUMB:
   outcome tells you IF the agent worked on this input.
   trajectory tells you WHY, WHETHER IT'LL GENERALISE, and WHETHER
   IT'S SAFE. Ship on outcome, DEBUG and GATE on trajectory.` },
      "How do you actually score steps at scale? Two mechanisms, used together. **Golden trajectories + per-step assertions** for the deterministic parts: you hand-author reference runs (this ticket *should* call `lookup_order` with the exact ID, *should not* call `issue_refund`) and assert programmatically — tool name matches, arguments match, forbidden tools never appear. These are cheap, exact, and become your **regression suite**: every code or prompt change re-runs them, so a refactor that reintroduces the rogue-refund bug fails CI instead of finance. **LLM-as-judge over trajectories** for the fuzzy parts: hand the full step-by-step trace to a strong model with a rubric ('was each tool call justified by the prior observation? did the plan make sense? did it recover from the failure at step 4?'). ==LLM-as-judge scales to open-ended trajectories that assertions can't specify — but it inherits the judge's failure modes: it can be swayed by a fluent-but-wrong trace, is position/verbosity-biased, and is itself non-deterministic, so you anchor it with golden examples and spot-audit its verdicts against human labels.== The judge grades taste; the assertions grade facts; you need both.",
      "Assemble these into an **agent eval harness** and the whole picture snaps together. The harness runs the agent against a fixed suite of tasks in a *controlled environment* (mocked or recorded tools, so runs are reproducible and a flaky API doesn't masquerade as an agent bug), captures the full trajectory of every run, and applies three layers: per-step assertions against golden trajectories (facts), an LLM-judge with a rubric (taste), and outcome checks on the final answer (did it ultimately work). It reports outcome *and* process metrics side by side, and — because trajectories are captured — every regression is *localizable*: you see the exact step where a good run and a bad run diverged. ==The one-line senior takeaway: outcome eval tells you whether the agent got the answer; trajectory eval tells you whether it earned it. You ship against outcome, but you debug, gate releases, and catch silent-safety regressions against the trajectory — because an agent that's right for the wrong reasons is a production incident waiting for the input that breaks the luck.== The interactive lets you score a trajectory step by step and watch a broken-but-lucky run separate from a clean one; then the closing scenario is exactly that split playing out on a live agent — try to name the failure before the reasoning walks you through it.",
    ],
    keyPoints: [
      "**Outcome eval grades only the final answer; an agent is a trajectory — the same Thought → Action → Observation loop from ReAct, repeated — so outcome-only collapses the whole sequence into pass/fail on the last node** and discards everything that happened en route.",
      "**It fails in two opposite directions.** False pass: right answer through a broken/lucky/unsafe path (unauthorized refund, wrong-customer lookup that happened to match). False fail: correct reasoning and tool use but stale data or a downstream API error — you debug the agent when the environment was at fault.",
      "**Trajectory eval scores the process step by step.** Core metrics: tool-call accuracy (right tool AND right arguments), step success rate, redundant/hallucinated call count, and error-recovery rate (did it notice a tool failure and adapt vs. barrel ahead). Recovery is a first-class metric.",
      "**Two scoring mechanisms, used together.** Golden trajectories + per-step assertions grade the deterministic facts (tool name/args match, forbidden tools never fire) and become the regression suite; LLM-as-judge over the full trace grades the fuzzy taste (was each call justified, did the plan make sense) — but inherits judge biases (fluent-but-wrong, verbosity, non-determinism), so anchor it with golden examples and human spot-audits.",
      "**The harness runs the agent in a controlled env (mocked/recorded tools → reproducible), captures every trajectory, and reports outcome AND process metrics.** Ship on outcome; debug, gate releases, and catch silent-safety regressions on trajectory — because 'right for the wrong reasons' is an incident waiting for the input that breaks the luck.",
    ],
    recap: [
      "**Outcome eval = final-answer pass/fail; an agent is a trajectory** (Thought → Action → Observation, repeated) — outcome-only discards the path.",
      "**Two opposite failures:** false PASS (right answer, broken/lucky/unsafe path — e.g. rogue refund) vs false FAIL (right path, wrong answer from stale data / API error).",
      "**Trajectory eval scores each step:** tool-selection + tool-argument accuracy, step success rate, redundant/hallucinated calls, error-recovery (adapt on failure, don't barrel ahead).",
      "**Two scoring mechanisms:** golden-trajectory per-step assertions (facts → regression suite) + LLM-as-judge over the trace (taste; biased/non-deterministic → anchor with goldens + audits).",
      "**Harness:** controlled env (mocked tools) + captured trajectories + outcome AND process metrics — ship on outcome, debug/gate/safety-check on trajectory.",
    ],
    mcqs: [
      {
        question: "Your agent eval reports 92% task success, but a rogue refund made it through: on a status-lookup ticket the agent called `issue_refund` (never requested), the refund failed silently, and it replied with a correct-sounding 'your order is on its way,' which the eval scored PASS. Why did outcome-only eval miss this, and what would have caught it?",
        options: [
          "The eval used too small a test set; simply adding more tickets to the suite would surface this refund issue",
          "The refund tool itself was buggy; fixing the tool implementation alone would make the existing eval correct",
          "Raising the sampling temperature on the LLM judge would have caused it to detect the refund happening here",
          "Outcome eval collapses the trajectory into one pass/fail; per-step checks flag the exact step that broke",
        ],
        correct: 3,
        explanation: "Option 4 is correct: outcome eval scores only the endpoint, so an agent that reaches a plausible final answer through a broken path (an unrequested `issue_refund`, a silently-failed refund, an unfounded reply) is scored PASS — the whole trajectory is collapsed into one node. Trajectory evaluation scores each step: tool-selection/argument accuracy and a golden-trajectory assertion that `issue_refund` must never fire on a status lookup would fail exactly at the offending step, localizing the bug. Option 1 is wrong — the problem isn't sample size; the same false-pass would recur because the metric itself ignores the path. Option 2 is wrong — the tool isn't buggy in the sense that matters; the agent should never have called it, which only per-step eval can see. Option 3 is wrong — temperature is unrelated; the gap is what's being measured (endpoint vs. process), not judge randomness.",
      },
      {
        question: "A teammate argues 'if the final answer is correct, the trajectory doesn't matter — we should only measure task success.' Which two statements are the strongest counter-arguments a staff engineer would give?",
        options: [
          "Outcome eval false-passes agents right for the wrong reasons, on lucky paths that will not generalize well",
          "Outcome eval false-fails agents with correct reasoning when the environment returned stale data or an error",
          "Trajectory eval is always strictly more accurate here, so outcome eval should be dropped from the suite",
          "Task success is fundamentally unmeasurable for agents, so trajectory eval is the only viable option left here",
        ],
        correct: [0, 1],
        explanation: "Options 1 and 2 are both correct: outcome-only eval conflates 'the agent did the right things' with 'the world produced the right result,' and those come apart in both directions — a lucky/broken path can false-PASS and be unsafe or fail to generalize on the next input (option 1), while a correct path can false-FAIL because of stale data or a downstream error, sending you to debug the agent when the environment was at fault (option 2). The mature stance keeps both outcome and trajectory eval: outcome for shipping signal, trajectory for debugging, release gating, and catching silent-safety regressions. Option 3 overshoots — outcome eval is still the shipping signal; you don't drop it. Option 4 is false — task success is measurable and useful; the point is that it's incomplete on its own.",
      },
      {
        question: "You are building the scoring layer of an agent eval harness. You have deterministic requirements (the ticket must call `lookup_order` with the exact ID and must never call `issue_refund`) and open-ended ones (was each tool call justified by the prior observation, did the plan make sense). What is the right scoring design and its main caveat?",
        options: [
          "Use LLM-as-judge for everything, since it is the most flexible option and removes any need for golden trajectories",
          "Use golden assertions for deterministic facts and an LLM-judge for taste, anchored with goldens since it is biased",
          "Use only exact programmatic assertions, since LLM judges are never reliable enough for any part of agent evaluation",
          "Score only the final answer and skip per-step scoring, since individual agent steps are too noisy to assert on",
        ],
        correct: 1,
        explanation: "Option 2 is correct: deterministic requirements (specific tool name, exact arguments, forbidden tools) are best expressed as programmatic per-step assertions against golden trajectories — cheap, exact, and directly reusable as a regression suite so a reintroduced bug fails CI. Open-ended judgments (was a call justified, did the plan cohere, did it recover) are what LLM-as-judge is for, since assertions can't specify them. The essential caveat is that the judge inherits known biases (favoring fluent-but-wrong traces, verbosity/position effects) and is non-deterministic, so you anchor it with golden examples and periodically audit its verdicts against human labels. Option 1 over-relies on the judge and discards the exactness assertions give. Option 3 throws away the judge's ability to score open-ended quality that assertions cannot express. Option 4 abandons trajectory eval entirely, reintroducing the false-pass/false-fail problem.",
      },
    ],
    takeaway: "Outcome evaluation grades only an agent's final answer, but an agent is a trajectory — the same Thought → Action → Observation loop from ReAct, repeated until a final answer — so outcome-only eval fails in two opposite directions: it false-passes agents that are right for the wrong reasons (lucky, broken, or unsafe paths — like an unauthorized refund) and false-fails agents whose reasoning was correct but whose environment returned stale data or an error. Trajectory evaluation scores the process step by step — tool-call accuracy (right tool and right arguments), step success rate, redundant/hallucinated calls, and error-recovery — using golden-trajectory per-step assertions for the deterministic facts (which double as a regression suite) plus LLM-as-judge for open-ended quality (anchored with goldens and human audits to counter its biases). Build a harness that runs the agent in a controlled, reproducible environment, captures every trajectory, and reports outcome and process metrics side by side: ship on outcome, but debug, gate releases, and catch silent-safety regressions on the trajectory.",
  },

  "rag-ingestion-pipeline": {
    depthTier: "core",
    interviewWeight: "high",
    groundUp: "Picture the RAG diagram everyone already has in their head: a question comes in, the system searches an index, and it answers using what it finds. That picture isn't wrong — but stare at it for a second and notice what it quietly assumes. It starts in the middle of the story. There's already a searchable index sitting there, ready to be queried, already chunked into the right pieces, already converted into vectors. Where did that index come from?\n\nA wiki page, a PDF, a support runbook — none of those arrive pre-chunked and pre-vectorized. Somebody, at some earlier point, had to turn raw documents into the tidy structure the query path reads from. That earlier work runs on a completely different clock than the query — ahead of time, in the background, with no user waiting on it. That offline work is the actual subject of this module, and most RAG postmortems never look at it.\n\nHere's the trap that makes it worth slowing down for. When a RAG system misbehaves, the instinct is to blame the part everyone can see — the retriever, the prompt, the model's reasoning. Picture two engineers staring at the same complaint, \"the assistant won't surface this document,\" both equally confident: one wants to retune the retriever, the other wants to check something upstream entirely. Who's right? By the end of this module that question has a one-sentence answer, because you'll have walked the offline pipeline stage by stage and watched, concretely, how each stage can fail on its own terms — including two failure modes, buried in embedding and indexing, that look exactly like \"the document just isn't there\" even while the document is sitting in the index the entire time, untouched by anything resembling a parsing or deletion bug. After that we'll deal with the harder fact that documents don't hold still once they're indexed, and what a pipeline has to do in response.\n\nNo need to rush. First: why there are two separate pipelines at all. Then: each stage of the offline one, in order, with its own specific way of breaking. Finally: what happens when a source document changes after it's already been indexed.",
    scenario: "The vendor-compliance RAG assistant has been live for eight months, built over a corpus of PDFs the legal team collects from every vendor — things like the \"Aegis Vendor Security Policy,\" now on its third revision (v3), ingested the week the system launched. Two things happen in the same week, and at first they read like the same bug wearing two faces.\n\nMonday: the platform team finishes migrating the whole embedding layer to a newer model — better multilingual coverage, the ticket says. The migration script re-embeds every document in the corpus, working through them in batches by document ID, writing new vectors over the old ones as it goes. It's supposed to finish overnight, but it hits a provider rate limit two-thirds of the way through and quietly stops — nothing crashes, nothing alerts. \"Aegis Vendor Security Policy,\" queued near the very end of the migration’s batch order — an accident of internal document ID, not urgency or alphabetization — never gets touched. Its chunks are still sitting in the index as vectors from the old model. A compliance analyst then asks the assistant a routine question about the vendor's policy. Her query gets embedded with the new model. Take a second before reading on: what do you expect happens when that new-model query vector gets compared, via cosine similarity, to an old-model chunk vector sitting a few rows away in the very same index?\n\nThursday: the vendor sends a corrected version, v4, fixing a clause the old one got wrong. It goes through ingestion cleanly — parsed, chunked, embedded with the new model, written to the index, no errors logged anywhere. An engineer queries for it thirty seconds later to confirm the update landed. Nothing comes back. He starts drafting a bug report: \"upsert appears to have silently failed.\"\n\nBoth complaints reach you as the same sentence — \"the assistant won't surface this document\" — about the very same document family. Before you touch the retriever: are Monday's and Thursday's failures actually the same problem showing up twice, or two different pipeline stages breaking independently in the same week? And for Thursday specifically — is \"the upsert silently failed\" really the right diagnosis, or is there a separate mechanism, specific to how the index stage treats freshly written vectors, that explains what he's seeing without assuming anything went wrong at all?",
    explanation: [
      "Start with the version of this system that doesn't split into two pipelines — everything happens live, at query time. A question arrives, and only then does the system go fetch the relevant document, parse it, chunk it, embed the chunks, and search among them, all before it can answer. Try to picture running that for every single query, on a corpus of fifty thousand documents. Parsing alone can take seconds per document; embedding a full corpus's worth of chunks against an API can take minutes to hours. A user is sitting there, waiting for a response, while all of that happens. That's not a slow chatbot, that's a chatbot that times out. The moment you notice that the expensive, corpus-wide work (parsing, chunking, embedding everything) and the cheap, per-query work (embedding one short question, searching an already-built index) don't have to happen at the same time, the two-pipeline design stops being a design choice and starts being close to inevitable: do the expensive work once, ahead of time, offline — call that **ingestion** — and leave the query path with almost nothing to do but search and generate.",
      { type: "illustration", label: "Two clocks: ingestion vs. query", content: `OFFLINE INGESTION PIPELINE   (runs ahead of time, over batches of documents)
 ┌───────┐   ┌─────────────┐   ┌────────────────────┐   ┌───────┐   ┌───────┐   ┌───────┐
 │ Parse │ → │ Clean/Dedup │ → │ Extract metadata    │ → │ Chunk │ → │ Embed │ → │ Index │
 └───────┘   └─────────────┘   └────────────────────┘   └───────┘   └───────┘   └───────┘
  PDF/HTML     strip boiler-     source, section,          split      vector     ANN write
  → raw text   plate, dedupe     timestamp, ACLs per        into      per        + periodic
               near-duplicates   chunk                      chunks    chunk      refresh

               clock: minutes to hours per batch — nobody is waiting on it

--------------------------------------------------------------------------------------------
ONLINE QUERY PATH   (runs once per request, a user IS waiting)
 ┌─────────────┐   ┌────────────┐   ┌───────────────────────────┐
 │ Embed query │ → │ Search the │ → │ Generate answer from the  │
 │             │   │ index      │   │ retrieved chunks          │
 └─────────────┘   └────────────┘   └───────────────────────────┘

               clock: milliseconds to a couple of seconds — a person is watching a spinner` },
      "Now walk the offline pipeline stage by stage, because \"ingestion\" isn't one operation, it's six, and each one has its own way of quietly going wrong. Take the **Aegis Vendor Security Policy**, a fifteen-page PDF the compliance team uploaded the week this system launched, as a document to follow through all six.\n\n**Parse** comes first: turn the PDF's bytes into text a model can read. The Aegis policy has a two-column layout for its clause tables and a repeated header/footer with the page number and a confidentiality stamp on every page. A naive parser reads the page left-to-right in raster order instead of down each column first — so a sentence that starts in the left column at row three finishes, nonsensically, with whatever happens to sit in the right column at row three. Feed that scrambled text downstream and every later stage inherits the damage; there's no stage after Parse that can un-scramble a sentence it never saw parsed correctly.",
      "**Clean & dedup** comes right after parsing, and it's easy to underrate. Every page of the Aegis PDF carried that repeated header and footer — if it isn't stripped, \"Page 7 of 15 — CONFIDENTIAL\" ends up embedded as if it were policy content, diluting every chunk it touches. Dedup matters for a different reason: legal re-uploaded a near-identical draft of the policy two days before finalizing it, differing only in a comment in the margin. Without a dedup step, the corpus now holds two almost-identical Aegis chunks, and retrieval has to waste a slot on the redundant one instead of surfacing something actually different.",
      "**Extract metadata** is the stage that attaches, per chunk, the facts a chunk can't infer about itself: which document it came from, which section, when it was last updated, and — for a corpus like this one — who's allowed to see it. The Aegis policy is marked confidential, restricted to the compliance team's access group. If that ACL is extracted once for the whole document but the chunker later splits the document and the ACL tag doesn't get copied onto every resulting chunk, you get a genuinely dangerous failure: a chunk of confidential vendor security detail becomes retrievable by anyone whose query happens to match it, because at query time nothing in the vector search itself knows to check permissions — filtering by ACL has to happen using metadata that was supposed to be attached back at ingestion time, and in this failure, wasn't.",
      "**Chunk** splits the cleaned, tagged text into the pieces that actually get embedded and searched. The Aegis policy has a clause that reads \"vendor must rotate encryption keys every 90 days, except\" — and the exception, sitting in the next sentence, is exactly the kind of thing a fixed-size chunk boundary can slice away from the rule it modifies. Split there and you get one chunk asserting an absolute rule and another, disconnected chunk holding its only exception; retrieval can easily return the first without the second. That's why chunking schemes lean on overlap between consecutive chunks — it isn't padding, it's insurance against cutting a rule away from its own caveat.",
      "Now for the two stages that, on the surface, look the most mechanical — and are exactly where the sneakiest failures live, because when they misbehave, nothing about the document's *text* or any earlier stage did anything wrong.\n\n**Embed** turns each chunk into a vector, positioned so that semantically similar text lands at nearby coordinates. Here's where to slow down and predict: what happens if a corpus's embedding model gets swapped for a newer version, but the re-embedding job only gets partway through the corpus before something interrupts it — a rate limit, a deploy, a canceled job — leaving some chunks re-embedded with the new model and the rest still holding vectors from the old one? A query embedded with the new model is now being compared, via cosine similarity, against a mix of both. Different embedding models place semantically similar text at different coordinates in entirely different, non-comparable geometric spaces; there is no reason a \"close\" score between a new-model query and an old-model chunk means anything at all. Whichever chunks were never re-embedded don't get retrieved, or get retrieved at random — not because they're missing, not because they were parsed badly, but because part of the index is speaking one geometric language and part is speaking another, and nobody told the similarity function.",
      "**Index** is the last stage: writing each chunk's vector into the data structure the query path actually searches — typically an approximate-nearest-neighbor structure, not a flat table. Most of these structures don't make a freshly written vector searchable the instant it's written. For throughput reasons, writes get buffered and only periodically merged, or \"refreshed,\" into the searchable structure — a refresh interval most vector stores expose as a tunable setting, precisely because it trades write throughput against how quickly new data becomes visible, often adjustable from single-digit seconds to a few minutes depending on how the store is configured. So a chunk can be fully parsed, cleanly embedded, and successfully written — no errors anywhere in the pipeline — and still not show up in a search run moments later, simply because the index hasn't refreshed yet. Same visible symptom as the embed-version problem — a chunk that's provably in the pipeline doesn't show up in search — but a completely different mechanism, and a completely different fix: waiting (or forcing a refresh) fixes a lag; re-embedding fixes a version mismatch, and refreshing sooner does nothing for a version mismatch it was never the cause of.",
      "Six stages, six distinct ways to fail, and notice none of them required the document to change. Now bring in the complication that does: documents keep changing after they've been indexed. The Aegis policy is currently on v3 in the index, but legal has already sent a v4. What does the pipeline do with that?",
      "Three cases, and they call for three different actions, not one generic \"re-run ingestion.\" If a document is **edited** in place — same identity, updated content — the fix is an *upsert by document ID*: re-run that one document through Parse, Clean/dedup, Extract metadata, Chunk, Embed, and Index, and overwrite its old chunks with the new ones. If a document is **deleted** outright — the vendor relationship ends, the policy is withdrawn — its chunks need to be *removed* from the index; leaving them in place is how a retiring policy keeps getting cited as current. And if a document is **superseded** — Aegis v4 formally replaces v3, but the two might carry different document IDs in the vendor's own numbering — the correct move is both: *upsert the v4 chunks* (the same embed-and-write mechanism just described) *and explicitly delete the superseded v3 chunks*, because upserting v4 under a new ID doesn't automatically make v3 disappear. Skip that explicit delete, and both versions sit in the index side by side, contradicting each other whenever a query happens to retrieve both.",
      { type: "illustration", label: "Cost of one changed document: full rebuild vs. incremental upsert", content: `Corpus: 50,000 documents, already parsed, chunked, embedded, and indexed.
One document (the Aegis policy) changes: v3 → v4, about 10 affected chunks.

FULL REBUILD                              INCREMENTAL UPSERT
re-parse all 50,000 documents             re-parse the 1 changed document
re-clean/dedup all of them                re-clean/dedup it
re-extract metadata for all of them       re-extract its metadata
re-chunk the entire corpus                re-chunk its ~10 chunks
re-embed the entire corpus                re-embed its ~10 chunks
re-write the entire index                 upsert its ~10 vectors by doc id,
                                           delete any of its old (v3) chunks

cost ∝ size of the ENTIRE corpus          cost ∝ size of the ONE changed document` },
      "That cost gap is why almost no production ingestion pipeline does full rebuilds on every change — it's incremental upsert by default, full rebuild reserved for schema changes or a wholesale embedding-model migration (the very kind of migration that, run carelessly, is what leaves a corpus half in one embedding space and half in another).\n\nWhich brings the two pipelines back together at the end. A team sets a **freshness SLA** — how long it's acceptable for a document change to take before it's reflected in what the assistant can retrieve — and that number determines how the ingestion pipeline gets built: batched hourly, event-triggered on every edit, or somewhere between. But notice the asymmetry that's been running underneath every stage in this module. The offline pipeline can afford to be slow, retried, and expensive per document, because nothing about it makes a user wait. The online query path can't afford any of that, because a user is always waiting on it. So the entire design instinct, from Parse through Index through incremental re-indexing, is the same instinct restated six different ways: push every expensive operation you possibly can back into ingestion time, and leave the query path with as close to nothing left to do as possible.",
    ],
    keyPoints: [
      "**A RAG system has two pipelines at different times: the offline ingestion/indexing pipeline (background, ahead of time) and the online query path (per request, latency-critical).** Retrieval quality is capped by ingestion quality — the query path can only retrieve what ingestion wrote, in the form it wrote it.",
      "**Ingestion stages, each a distinct decision: parse → clean/dedup → extract metadata → chunk → embed → index.** Parsing is the most underestimated (PDF multi-column layouts, HTML boilerplate, tables/figures → garbage-out poisons everything downstream). Clean/dedup removes near-duplicates that would flood retrieval.",
      "**Metadata extraction is high-leverage: source (citation), section, timestamp (freshness/staleness), and ACLs/permissions.** Metadata lets retrieval FILTER before it ranks; skipping ACLs at ingestion is how a RAG system leaks docs a user can't see — you can't bolt permissions on at query time if the index doesn't carry them.",
      "**Incremental re-indexing keeps the index fresh without full rebuilds by making the unit of work the doc, not the corpus.** On edit: upsert by stable doc id (re-embed only that doc's chunks). On delete: actively remove its chunks (or a deleted doc keeps being cited). On supersession: upsert v4, delete v3. Cost ∝ size of change, not corpus size. Full rebuilds are for embedding-model/schema migrations only.",
      "**Freshness SLAs and cost/latency asymmetry are design levers.** The SLA (minutes vs. nightly) drives event-driven vs. batch ingestion and must be elicited, not assumed. Ingestion is throughput-bound and off the critical path, retrieval is latency-bound per query — so push expensive work (rich parsing, metadata, per-chunk summaries) to ingestion; you pay once and amortize over every query.",
    ],
    recap: [
      "**Two pipelines:** offline ingestion (parse → clean/dedup → metadata → chunk → embed → index, background) vs online query path (embed → search → generate, per request). Retrieval quality is CAPPED by ingestion quality.",
      "**Parse is the most underestimated stage** — PDF columns, HTML boilerplate, tables → garbage-out poisons everything; retrieval can't recover a mangled parse.",
      "**Metadata (source, section, timestamp, ACLs) lets retrieval filter before ranking** — skipping ACLs at ingestion leaks docs a user can't see; permissions can't be bolted on at query time.",
      "**Incremental re-index, keyed by doc id:** edit → upsert that doc's chunks; delete → remove them (else it keeps being cited); supersession → upsert v4 + delete v3. Cost ∝ change, not corpus — full rebuild = model/schema migration only.",
      "**Freshness SLA (minutes vs nightly) drives event-driven vs batch.** Ingestion is off the critical path — push expensive work (rich parse, metadata, per-chunk summaries) there: pay once, amortize over every query.",
    ],
    mcqs: [
      {
        question: "An engineer edits one paragraph in a single 40-page runbook, and your nightly job re-embeds all 12,000 documents from scratch to pick up the change — a six-hour, expensive rebuild. What is the correct design, and what mechanism makes it work?",
        options: [
          "Increase the embedding batch size so the full rebuild runs faster; full rebuilds stay unavoidable for any doc change",
          "Switch to a faster embedding model so re-embedding all twelve thousand docs becomes cheap enough to run nightly",
          "Incremental re-indexing by doc id: upsert only the edited doc's chunks, so cost tracks the change, not the corpus",
          "Cache query results at the application layer so index staleness no longer matters at all to any single user",
        ],
        correct: 2,
        explanation: "Option 3 is correct: the fix is incremental re-indexing, where the unit of work is the document rather than the whole corpus. A stable doc-id → chunk-ids mapping lets you re-parse, re-chunk, and re-embed only the edited doc's chunks and upsert them in place, leaving all other chunks untouched — so the cost of a one-paragraph edit is proportional to that doc's ~30 chunks, not to re-embedding all 12,000 documents. Full rebuilds are reserved for embedding-model or schema migrations. Option 1 accepts the wrong premise (that any change forces a full rebuild) and only makes the wasteful path faster. Option 2 also re-embeds the entire corpus; a faster model reduces the constant but keeps cost proportional to corpus size. Option 4 doesn't address freshness at all — a cache over a stale/rebuilt index doesn't change how updates are ingested.",
      },
      {
        question: "During ingestion your team skips extracting per-document ACL/permission metadata, planning to 'add permissions later at query time.' Why is this a problem, and what does it reveal about the ingestion pipeline?",
        options: [
          "It's fine — permissions are purely a query-time concern and can always be applied well after retrieval happens",
          "Skipping ACL metadata only affects citation quality, not the system's actual access control at all",
          "Attaching ACLs slows the embedding step, so omitting them at ingestion improves throughput with no real downside",
          "Retrieval only filters on metadata the index carries, so missing ACLs let a user see docs they shouldn't see",
        ],
        correct: 3,
        explanation: "Option 4 is correct: retrieval can only filter on metadata the index actually carries, so if ingestion never attaches per-doc ACLs, there is no field for the retriever to filter on and it can return chunks from documents the user has no right to see — a data-leak. This is why metadata extraction (source for citation, timestamp for freshness, ACLs for access) is a first-class ingestion stage: retrieval filters before it ranks, and you cannot bolt permissions on at query time against an index that doesn't hold them. Option 1 is exactly the dangerous misconception — permissions must be enforceable at retrieval, which requires the data at ingestion. Option 2 understates the impact; the failure is access control, not citations. Option 3 is wrong — the issue is a security/correctness gap, not a throughput tradeoff.",
      },
      {
        question: "Your RAG demo works, but a PDF with a two-column layout returns garbled text and a deleted wiki page is still being cited. A colleague wants to fix both by tuning the retriever (better embeddings, higher top-k). Which two statements correctly diagnose where these bugs actually live?",
        options: [
          "The garbled PDF is a parse-stage failure from the linearized layout; no retrieval tuning fixes a mangled parse",
          "The still-cited deleted page is a missing delete step in re-indexing — chunks never removed via doc-id map",
          "The garbled PDF is a retrieval bug, since a better embedding model would have understood the interleaved column text correctly",
          "These are unavoidable limitations of RAG systems in general, and no pipeline redesign can meaningfully address either one",
        ],
        correct: [0, 1],
        explanation: "Options 1 and 2 are both correct: retrieval quality is capped by ingestion quality — the query path can only surface what ingestion put in the index and in the form it put it there. A two-column PDF read left-to-right interleaves columns into nonsense at the parse stage, and no amount of better embeddings or higher top-k reconstructs meaning from garbled text; the fix is structure-aware parsing (option 1). The still-cited deleted page is a freshness failure in incremental re-indexing: on delete, the doc's chunks must be actively removed via the doc-id → chunk-ids mapping, and nobody wrote that step (option 2). Option 3 misattributes a parse-stage problem to the retriever — embeddings operate on whatever text the parser produced, garbled or not. Option 4 is defeatist — these are well-understood, fixable ingestion concerns.",
      },
    ],
    takeaway: "A RAG system has two pipelines running at different times: the online query path (embed → search → generate, per request, latency-critical) and the offline ingestion pipeline (parse → clean/dedup → extract metadata → chunk → embed → index, in the background) — and retrieval quality is capped by ingestion quality, because the query path can only ever surface what ingestion wrote and how it wrote it. Parsing is the most underestimated stage (multi-column PDFs, HTML boilerplate, tables), and metadata extraction (source, section, timestamp, and especially ACLs) is high-leverage because retrieval filters before it ranks and can't enforce permissions the index doesn't carry. Keep the index fresh with incremental re-indexing keyed by stable doc id — upsert on edit, delete on removal, upsert-plus-delete on supersession — so cost scales with the change, not the corpus, reserving full rebuilds for embedding-model/schema migrations. Finally, use the two pipelines' cost/latency asymmetry as a lever: push expensive work (rich parsing, metadata, per-chunk summaries) into off-critical-path ingestion, where you pay once and amortize it over every future query, and let the freshness SLA (minutes vs. nightly) drive event-driven vs. batch ingestion.",
  },
};
