// L1/L2 depth for attention, quantization, transformers. Spread into PREP_QUESTIONS.
export const Q_CORE_DEEPEN = [
  // ==================== ATTENTION L1 (Deep) ====================
  {
    id: "attention-l1-1",
    topic: "attention",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "In scaled dot-product attention, the scores QK^T are divided by √d before the softmax. What is d, and why is the division there?",
    options: [
      "d is the sequence length; dividing keeps the scores summing to 1 before softmax.",
      "d is the dimension of each attention head's key/query vectors; dividing counteracts the growth of the dot-product variance so softmax gradients don't vanish.",
      "d is the number of heads; dividing averages the contribution of each head.",
      "d is the batch size; dividing normalizes across examples for stable training.",
    ],
    correct: 1,
    keywords: [],
    explanation: "The dot product of two random vectors of dimension d_head has variance that scales with d_head. Without scaling, scores grow large in magnitude, pushing softmax into saturated regions where gradients are near zero. Dividing by √d_head keeps the variance ~1, keeping softmax in a well-conditioned regime. The scaling constant is √(head_dim), not the model dimension or sequence length.",
    trap: "A weaker candidate says d is the sequence length or model dimension. It is specifically the per-head key/query dimension (d_model / n_heads). Using d_model would over-scale in multi-head attention.",
  },
  {
    id: "attention-l1-2",
    topic: "attention",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "What is the correct order of operations for a single scaled dot-product attention computation?",
    options: [
      "softmax(Q + K) then multiply by V",
      "(QK^T / √d), then softmax over the key axis, then multiply by V",
      "(QV^T / √d), then softmax, then multiply by K",
      "softmax(V), then multiply by QK^T",
    ],
    correct: 1,
    keywords: [],
    explanation: "Attention(Q,K,V) = softmax(QK^T / √d) V. You compute pairwise query-key similarity scores, scale them, normalize with softmax along the key dimension to get attention weights that sum to 1 per query, then take a weighted sum of the value vectors. V carries the actual content that gets aggregated; Q and K only produce the weights.",
    trap: "Confusing which matrices produce weights vs content. Q and K produce the routing weights; V is the payload that gets mixed. Applying softmax to V or mixing Q with V breaks the mechanism.",
  },
  {
    id: "attention-l1-3",
    topic: "attention",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "Why use multi-head attention instead of one large single-head attention with the same total dimension?",
    options: [
      "Multiple heads reduce the total parameter count compared to one head.",
      "Each head attends in a different learned subspace, letting the model jointly capture several relationship types (e.g., syntax and coreference) at different positions.",
      "Multiple heads eliminate the need for positional encodings.",
      "A single head cannot be parallelized on a GPU, but multiple heads can.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Multi-head attention projects Q/K/V into h lower-dimensional subspaces, runs attention in each, and concatenates. A single softmax tends to collapse onto one dominant pattern per query; splitting into heads lets the model represent multiple distinct relationships simultaneously (different heads specialize in positional, syntactic, or semantic patterns). Total dimension and roughly the parameter count stay comparable.",
    trap: "Claiming multi-head is cheaper in parameters. It is roughly parameter-neutral vs a single head of the same total width; the benefit is representational diversity, not cost.",
  },
  {
    id: "attention-l1-4",
    topic: "attention",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "How is causal (autoregressive) masking implemented in decoder self-attention, and what does it enforce?",
    options: [
      "Value vectors for future tokens are zeroed after the softmax, so they contribute nothing.",
      "Scores for positions j > i are set to -inf before softmax, so token i can attend only to itself and earlier tokens.",
      "Future tokens are physically removed from the sequence before the layer runs.",
      "The query for each future token is set to zero, blocking its contribution.",
    ],
    correct: 1,
    keywords: [],
    explanation: "A causal mask adds -inf to the pre-softmax scores at positions strictly to the right of the current query. After softmax those weights become 0, so position i attends only to positions <= i. This preserves the autoregressive property (no leakage from future tokens) required for next-token prediction and keeps training consistent with inference.",
    trap: "A weaker answer zeros values after softmax. If you mask after softmax, the remaining weights no longer sum to 1 and the distribution is distorted. The mask must be applied to scores before softmax.",
  },
  {
    id: "attention-l1-5",
    topic: "attention",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "text",
    question: "Explain why vanilla self-attention has O(n^2) cost in sequence length n, naming both the compute and memory bottleneck.",
    options: [],
    correct: 0,
    keywords: ["n^2", "quadratic", "sequence length", "QK^T", "attention matrix", "memory", "compute", "pairwise", "scores"],
    explanation: "Every query attends to every key, producing an n×n attention score matrix. Computing QK^T is O(n^2 · d) FLOPs, and materializing/storing that n×n matrix (plus the softmax) is O(n^2) memory. Both compute and the intermediate memory footprint grow quadratically with sequence length, which is why long-context attention is expensive and motivates Flash Attention and sparse variants.",
    trap: "Only citing compute and forgetting that the n×n matrix itself is the memory wall — the memory blowup is often the binding constraint on long context, and it's what Flash Attention specifically attacks.",
  },

  // ==================== ATTENTION L2 (Cross-concept) ====================
  {
    id: "attention-l2-1",
    topic: "attention",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "How do Multi-Head (MHA), Multi-Query (MQA), and Grouped-Query (GQA) attention differ, and what is the primary tradeoff?",
    options: [
      "They differ in the number of query heads; MQA has the most query heads for higher quality.",
      "MHA gives each head its own K/V; MQA shares one K/V across all heads; GQA shares K/V within groups — trading quality for a smaller KV-cache and faster decoding.",
      "MQA and GQA increase KV-cache size to improve accuracy at long context.",
      "GQA removes the value matrix entirely to save memory.",
    ],
    correct: 1,
    keywords: [],
    explanation: "All three keep multiple query heads. The difference is how many distinct K/V projections exist: MHA = one K/V per head (largest KV-cache), MQA = a single shared K/V for all heads (smallest cache, some quality loss), GQA = K/V shared within groups of query heads (a middle ground). Fewer K/V heads shrink the KV-cache, which is the dominant memory cost during autoregressive decoding, speeding up inference at long context. GQA is the common production choice (e.g., Llama 2/3) because it recovers most MHA quality at a fraction of the cache.",
    trap: "A weaker candidate thinks MQA/GQA reduce query heads or grow the cache. They reduce the number of key/value heads; query heads are unchanged. The lever is KV-cache size vs quality.",
  },
  {
    id: "attention-l2-2",
    topic: "attention",
    tier: "L2",
    difficulty: "hard",
    gated: true,
    type: "mcq",
    question: "Flash Attention is faster than vanilla attention primarily because it:",
    options: [
      "Uses an approximate softmax that skips low-probability keys, reducing FLOPs.",
      "Is IO-aware: it tiles the computation and uses online softmax so it never materializes the full n×n matrix in slow HBM, cutting memory traffic — the FLOPs are essentially the same.",
      "Replaces the n×n attention matrix with a low-rank approximation.",
      "Runs attention in lower precision (INT8) to reduce compute.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Flash Attention computes exact attention. Its speedup comes from being IO-aware: it processes Q/K/V in blocks in fast on-chip SRAM, uses an online (streaming) softmax to avoid ever writing the full n×n scores to HBM, and thereby slashes memory-bandwidth traffic — which is the real bottleneck on modern GPUs. Total FLOPs are roughly unchanged; it wins by moving less data and using less memory (linear in n for the materialized state).",
    trap: "A common misconception is that Flash Attention is an approximation or reduces FLOPs. It is exact and roughly FLOP-neutral — the gain is fewer HBM reads/writes (IO-awareness), not fewer arithmetic operations.",
  },
  {
    id: "attention-l2-3",
    topic: "attention",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "How do RoPE, learned absolute, and ALiBi positional schemes differ in how they inject position?",
    options: [
      "All three add a learned vector to the token embedding at the input layer.",
      "Learned absolute adds position embeddings at the input; RoPE rotates Q and K by position-dependent angles inside attention (relative, extrapolates better); ALiBi adds a distance-based linear bias to attention scores.",
      "RoPE and ALiBi both store a lookup table of absolute position vectors.",
      "ALiBi rotates the value vectors while RoPE biases the softmax.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Learned absolute embeddings add a trainable per-position vector at the input and struggle to extrapolate past the trained length. RoPE applies a position-dependent rotation to the query and key vectors, so the dot product depends on relative position — this generalizes to longer contexts better and is the modern default (Llama, etc.). ALiBi adds a static linear penalty proportional to key-query distance directly to the attention scores, enabling length extrapolation with no learned position parameters.",
    trap: "Saying RoPE rotates values or adds vectors at the input. RoPE rotates Q and K (not V) inside attention, giving a relative-position dependence in the dot product — that's why it extrapolates.",
  },
  {
    id: "attention-l2-4",
    topic: "attention",
    tier: "L2",
    difficulty: "hard",
    gated: true,
    type: "mcq",
    question: "What is the core tradeoff of sliding-window / sparse attention versus full attention?",
    options: [
      "Sparse attention is always more accurate because it ignores irrelevant tokens.",
      "Sparse/sliding-window attention drops the cost from O(n^2) toward roughly O(n·w), enabling long context, but each token only directly sees a limited window — long-range dependencies must propagate through stacked layers.",
      "Sliding-window attention increases memory but improves exact recall of distant tokens.",
      "Full attention and sliding-window attention have identical cost; the only difference is training stability.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Full attention is O(n^2) but every token can directly attend to every other. Sliding-window (local) attention restricts each token to a window of size w, reducing cost to about O(n·w) and enabling much longer sequences (used in Mistral, Longformer). The cost is that distant tokens are only reachable indirectly — information must hop across layers — so pure local attention can weaken exact long-range recall unless combined with global tokens or full-attention layers.",
    trap: "Assuming sparse attention improves accuracy by filtering noise. It's a compute/memory-for-reach tradeoff: you gain length but each token loses direct global visibility, which can hurt long-range tasks.",
  },
  {
    id: "attention-l2-5",
    topic: "attention",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "text",
    question: "Contrast self-attention and cross-attention: where do Q, K, and V come from in each, and give a use case for cross-attention.",
    options: [],
    correct: 0,
    keywords: ["self-attention", "cross-attention", "same sequence", "encoder", "decoder", "query", "key", "value", "different sequence", "translation"],
    explanation: "In self-attention, Q, K, and V all come from the same sequence — each token attends to other tokens in its own sequence. In cross-attention, Q comes from one sequence (e.g., the decoder's current tokens) while K and V come from another (e.g., the encoder's output). Cross-attention is how an encoder-decoder model conditions generation on the source — e.g., a translation decoder attending to the encoded source sentence, or a diffusion model attending to a text prompt.",
    trap: "Saying cross-attention just means attending across positions. The defining feature is that Q comes from a different sequence than K/V — that's what lets a decoder condition on encoder output.",
  },

  // ==================== QUANTIZATION L1 (Deep) ====================
  {
    id: "quantization-l1-1",
    topic: "quantization",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "What fundamentally distinguishes Post-Training Quantization (PTQ) from Quantization-Aware Training (QAT)?",
    options: [
      "PTQ quantizes weights after training with no gradient updates (fast, cheap); QAT simulates quantization during training so the model learns to be robust to it (costlier, higher accuracy).",
      "PTQ requires the full training set and many GPU-days; QAT is a one-shot calibration step.",
      "PTQ only works for INT4; QAT only works for INT8.",
      "QAT quantizes activations while PTQ quantizes only weights.",
    ],
    correct: 0,
    keywords: [],
    explanation: "PTQ takes a trained model and converts weights (and often activations) to low precision using a small calibration set, with no backprop — it's fast and needs little data, but accuracy can drop at very low bit-widths. QAT inserts fake-quantization ops during training/fine-tuning so gradients flow through the rounding (via a straight-through estimator) and the model adapts its weights to the quantization grid, typically recovering more accuracy at aggressive bit-widths, at the cost of a training loop.",
    trap: "Reversing the two: QAT is the expensive, higher-accuracy training-time method; PTQ is the cheap, calibration-only method. A weaker candidate says PTQ needs the full training set — it only needs a small calibration set.",
  },
  {
    id: "quantization-l1-2",
    topic: "quantization",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "Per-tensor vs per-channel quantization: what's the difference and why does per-channel usually help?",
    options: [
      "Per-tensor uses a separate scale per output channel; per-channel uses one global scale.",
      "Per-tensor uses one scale/zero-point for the whole tensor; per-channel uses a scale per channel (e.g., per output feature), better fitting channels with very different value ranges and reducing error.",
      "Per-channel quantization requires QAT while per-tensor works with PTQ only.",
      "They are identical for weights and differ only for activations.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Per-tensor quantization applies a single scale (and zero-point) to an entire weight tensor. Per-channel gives each channel (typically each output feature/column) its own scale. Because different channels often have very different magnitude ranges, a single global scale wastes resolution on some channels and clips others; per-channel scales fit each channel's range, materially lowering quantization error — this is standard for weights. Activations are frequently kept per-tensor for hardware simplicity.",
    trap: "Thinking finer granularity is free. Per-channel is more accurate but adds a scale vector and some overhead; it's typically applied to weights, while activations often stay per-tensor for kernel efficiency.",
  },
  {
    id: "quantization-l1-3",
    topic: "quantization",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "GPTQ and AWQ are both weight-only PTQ methods. What's the key idea behind each?",
    options: [
      "GPTQ retrains the whole model; AWQ prunes redundant weights before quantizing.",
      "GPTQ uses second-order (Hessian-based) error compensation to quantize weights column-by-column while minimizing output error; AWQ is activation-aware, scaling/protecting the weight channels that matter most for salient activations.",
      "GPTQ and AWQ both require the full training dataset and QAT.",
      "AWQ quantizes activations to INT4 while GPTQ leaves activations in FP16.",
    ],
    correct: 1,
    keywords: [],
    explanation: "GPTQ (an OBQ/OBS-style method) uses approximate second-order information (the Hessian of the layer's reconstruction error) to quantize weights one column at a time, updating the remaining weights to compensate for the error introduced — minimizing output distortion. AWQ observes that a small fraction of weight channels are salient because they interact with large-magnitude activations; it applies per-channel scaling to protect those channels before rounding, preserving accuracy without backprop. Both are calibration-based, weight-only PTQ.",
    trap: "Calling either a retraining/QAT method or an activation quantizer. Both quantize weights only, via calibration — GPTQ's signature is the Hessian-based compensation; AWQ's is activation-awareness.",
  },
  {
    id: "quantization-l1-4",
    topic: "quantization",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "Why is quantizing the KV-cache attractive for LLM inference, and what's the main risk?",
    options: [
      "The KV-cache is negligible in size, so quantization only helps compute, not memory.",
      "At long context the KV-cache dominates memory and grows with sequence length; quantizing it to INT8/INT4 frees memory and bandwidth, but keys/values have outliers so aggressive quantization can degrade generation quality.",
      "KV-cache quantization is lossless because caches store integers already.",
      "Quantizing the KV-cache only helps prefill, not decoding.",
    ],
    correct: 1,
    keywords: [],
    explanation: "During autoregressive decoding, the KV-cache stores keys and values for all previous tokens and grows linearly with context length and batch size, often becoming the dominant memory and bandwidth cost — larger than the weights at long context. Quantizing it to INT8 or INT4 directly reduces that footprint and improves throughput. The risk is that key/value activations can contain outliers, so overly aggressive (especially per-tensor INT4) quantization can hurt output quality; per-channel or per-token schemes mitigate this.",
    trap: "Underestimating the KV-cache. At long context it dominates memory, not the weights — that's exactly why cache quantization is a high-leverage lever, but its outlier sensitivity makes it riskier than weight quantization.",
  },
  {
    id: "quantization-l1-5",
    topic: "quantization",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "text",
    question: "Why are activations harder to quantize than weights in transformers, and what problem do outlier features cause?",
    options: [],
    correct: 0,
    keywords: ["outliers", "activations", "dynamic range", "clip", "scale", "per-channel", "per-token", "runtime", "accuracy"],
    explanation: "Weights are fixed after training and have relatively well-behaved, static distributions, so their scales can be chosen offline. Activations are input-dependent and, in large transformers, develop persistent large-magnitude outlier features in specific channels. A single scale that covers those outliers wastes precision on the many normal values (or clipping the outliers destroys important signal), so naive per-tensor INT8 activation quantization can sharply drop accuracy. Solutions include per-channel/per-token scales, keeping outlier dimensions in higher precision (e.g., LLM.int8()), or migrating the scale into the weights (SmoothQuant).",
    trap: "Treating weights and activations as equally easy. Activation outliers and runtime dynamic range are the crux — this is why many schemes quantize weights aggressively but keep activations at higher precision or use special outlier handling.",
  },

  // ==================== QUANTIZATION L2 (Cross-concept) ====================
  {
    id: "quantization-l2-1",
    topic: "quantization",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "Quantization, distillation, and pruning are three compression levers. How do they differ conceptually?",
    options: [
      "They are interchangeable names for reducing precision of weights.",
      "Quantization lowers numeric precision of the same architecture; pruning removes weights/structures (sparsity); distillation trains a smaller student to mimic a larger teacher — they attack different axes and can be combined.",
      "Distillation and pruning both reduce bit-width, while quantization removes neurons.",
      "Only quantization reduces memory; pruning and distillation only speed up training.",
    ],
    correct: 1,
    keywords: [],
    explanation: "These are orthogonal compression axes. Quantization keeps the architecture but stores/computes in fewer bits (FP16 to INT8/INT4). Pruning removes parameters or whole structures (heads, channels), inducing sparsity. Distillation trains a separate, smaller model (the student) to reproduce a larger teacher's behavior. Because they operate on different dimensions — precision, sparsity, and model size/knowledge transfer — they compose (e.g., a distilled model that is then quantized and pruned).",
    trap: "Collapsing them into one idea. A strong answer stresses they are orthogonal and stackable: quantization = precision, pruning = sparsity/structure removal, distillation = smaller model learning from a bigger one.",
  },
  {
    id: "quantization-l2-2",
    topic: "quantization",
    tier: "L2",
    difficulty: "hard",
    gated: true,
    type: "mcq",
    question: "When would you choose QAT over PTQ despite QAT's higher cost?",
    options: [
      "Whenever the model is larger than 1B parameters, since PTQ never works above that size.",
      "When you need very low bit-widths (e.g., INT4 or lower) or the model is small/sensitive and PTQ's accuracy drop is unacceptable — QAT lets the model adapt to the quantization grid and recover accuracy.",
      "When you have no training data or compute budget, since QAT is the cheaper option.",
      "Only for encoder-only models; decoder-only models must use PTQ.",
    ],
    correct: 1,
    keywords: [],
    explanation: "PTQ is the default because it's cheap and usually fine at INT8 for large models. You reach for QAT when PTQ's accuracy loss is too large — typically at aggressive bit-widths (INT4 and below) or with smaller/more sensitive models where the quantization error can't be absorbed. QAT simulates quantization during fine-tuning so the weights adapt to the discrete grid, recovering accuracy, but it needs training data, compute, and a training loop.",
    trap: "Thinking bigger models need QAT more. Larger models are often more robust to PTQ; it's aggressive bit-widths and smaller/sensitive models that most justify QAT's extra cost.",
  },
  {
    id: "quantization-l2-3",
    topic: "quantization",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "GPTQ, AWQ, and bitsandbytes (NF4/LLM.int8()) are all popular. What's the practical distinction?",
    options: [
      "All three do QAT with full backprop and differ only in file format.",
      "GPTQ and AWQ are calibration-based weight-only PTQ producing INT4/INT3 weights for fast inference; bitsandbytes offers on-the-fly quantization (LLM.int8() with outlier handling, and NF4 for QLoRA fine-tuning) with no calibration pass.",
      "bitsandbytes requires a Hessian calibration step while GPTQ needs none.",
      "AWQ and bitsandbytes both quantize only activations, GPTQ only weights.",
    ],
    correct: 1,
    keywords: [],
    explanation: "GPTQ (Hessian-based) and AWQ (activation-aware) are offline, calibration-based, weight-only PTQ methods that produce low-bit (often 4-bit) weights optimized for fast inference kernels. bitsandbytes provides more turnkey quantization: LLM.int8() keeps outlier dimensions in FP16 and does mixed-precision matmul with no calibration, and NF4 (a 4-bit normal-float format) is the backbone of QLoRA for memory-efficient fine-tuning. The practical tradeoff is calibration effort and inference kernel speed (GPTQ/AWQ) vs zero-calibration convenience and fine-tuning support (bitsandbytes).",
    trap: "Assuming they're interchangeable. GPTQ/AWQ optimize a calibrated 4-bit model for inference speed; bitsandbytes' NF4 is the go-to for QLoRA fine-tuning and LLM.int8() prioritizes accuracy via outlier handling — different jobs.",
  },
  {
    id: "quantization-l2-4",
    topic: "quantization",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "How do FP8, INT8, and INT4 compare for LLM serving in terms of accuracy vs throughput?",
    options: [
      "INT4 is always the best choice because it maximizes both accuracy and throughput.",
      "FP8 keeps a floating exponent (wide dynamic range, good for activations, needs modern GPUs); INT8 is a strong accuracy/throughput default; INT4 roughly halves memory again vs INT8 for max throughput but with the largest accuracy risk.",
      "FP8 and INT8 have identical dynamic range; only the bit count differs.",
      "INT4 has a wider dynamic range than FP8 because integers span more values.",
    ],
    correct: 1,
    keywords: [],
    explanation: "FP8 (e.g., E4M3/E5M2) retains an exponent, giving a much wider dynamic range than INT8 at the same bit-width, which helps with outlier-heavy activations — but it needs hardware support (H100-class). INT8 is the workhorse: near-FP16 accuracy with big throughput/memory gains. INT4 roughly halves memory again and boosts throughput further, but its coarse grid makes it the most accuracy-sensitive, often requiring GPTQ/AWQ-style methods and careful outlier handling. The trend is more bits = safer accuracy, fewer bits = more throughput/less memory.",
    trap: "Assuming lower bit-width is strictly better. There's a real accuracy cliff at INT4, and integer formats lack FP8's dynamic range — so INT4 needs the most care and FP8 wins where activation outliers matter.",
  },
  {
    id: "quantization-l2-5",
    topic: "quantization",
    tier: "L2",
    difficulty: "hard",
    gated: true,
    type: "text",
    question: "Name two situations where quantization tends to hurt disproportionately, and explain why.",
    options: [],
    correct: 0,
    keywords: ["small models", "reasoning", "chain-of-thought", "low bit", "error accumulation", "redundancy", "outliers", "long generation"],
    explanation: "Two common cases: (1) Small models — they have less redundancy/over-parameterization to absorb quantization noise, so the same bit-width that barely dents a 70B model can noticeably degrade a 1B model. (2) Reasoning / long chain-of-thought tasks — quantization error compounds across many autoregressive steps, and a single early wrong token can derail a multi-step solution, so tasks requiring precise long generation (math, coding, multi-hop reasoning) show larger drops than short-answer tasks. Outlier-heavy activations at very low bit-widths also amplify both effects.",
    trap: "Answering only 'accuracy drops a bit.' The nuance is that small models lack redundancy to absorb error, and reasoning tasks accumulate error over long generations — quantization is not uniformly cheap across model sizes and task types.",
  },

  // ==================== TRANSFORMERS L1 (Deep) ====================
  {
    id: "transformers-l1-1",
    topic: "transformers",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "Encoder, decoder, and encoder-decoder transformers differ primarily in which attention they use. Which mapping is correct?",
    options: [
      "Encoder = causal self-attention; decoder = bidirectional self-attention; enc-dec = neither.",
      "Encoder = bidirectional self-attention (sees whole input); decoder = causal self-attention (sees only past); enc-dec = encoder bidirectional plus decoder with causal self-attention and cross-attention to the encoder.",
      "All three use only cross-attention; the difference is depth.",
      "Encoder and decoder are identical; only the loss function differs.",
    ],
    correct: 1,
    keywords: [],
    explanation: "An encoder uses bidirectional self-attention — every token sees the whole input — making it ideal for understanding tasks (BERT). A decoder uses causal (masked) self-attention so each token sees only itself and earlier tokens, enabling autoregressive generation (GPT). An encoder-decoder (T5, original Transformer) runs a bidirectional encoder over the source, then a decoder that does causal self-attention over its own outputs plus cross-attention into the encoder's representations — the classic setup for conditional generation like translation.",
    trap: "Flipping encoder and decoder masking. Encoder = bidirectional (full context), decoder = causal (past only). Enc-dec adds cross-attention on top of the decoder's causal self-attention.",
  },
  {
    id: "transformers-l1-2",
    topic: "transformers",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "Pre-norm vs post-norm transformers: what's the difference and why do most modern LLMs use pre-norm?",
    options: [
      "Pre-norm removes LayerNorm entirely; post-norm applies it twice per block.",
      "Post-norm applies LayerNorm after the residual add; pre-norm applies LayerNorm inside the residual branch (before the sublayer), which keeps a clean identity path and stabilizes gradients, making very deep models trainable without heavy warmup.",
      "Pre-norm and post-norm are identical in gradient flow; the choice is purely cosmetic.",
      "Post-norm is used in modern LLMs because it converges faster and is more stable at depth.",
    ],
    correct: 1,
    keywords: [],
    explanation: "In post-norm (original Transformer), the block is x -> Sublayer -> Add -> LayerNorm, so normalization sits on the residual stream and can disrupt the identity path, often needing careful learning-rate warmup at depth. In pre-norm, LayerNorm is applied to the input of each sublayer (x -> LN -> Sublayer -> Add), leaving an unnormalized identity/residual highway from input to output. This keeps gradient magnitudes stable through many layers, so deep models (GPT-2/3, most modern LLMs) train more reliably with pre-norm.",
    trap: "Saying post-norm is more stable at depth. It's the opposite — pre-norm's clean residual path is what makes very deep transformers trainable, which is why it's the modern default.",
  },
  {
    id: "transformers-l1-3",
    topic: "transformers",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "What is the role of the position-wise feed-forward network (FFN) in a transformer block, and why is it typically ~4× the model width?",
    options: [
      "The FFN mixes information across tokens, replacing the need for attention.",
      "The FFN applies a per-token nonlinear transformation (usually expanding to ~4×d then projecting back), adding capacity and nonlinearity; attention mixes across tokens, the FFN processes each token's representation independently.",
      "The FFN normalizes activations, which is why it needs extra width.",
      "The FFN's width is arbitrary and has no effect on capacity.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Attention handles cross-token mixing (routing information between positions), but it's largely linear in the values. The FFN is applied independently and identically to each token: a linear up-projection to a larger hidden dimension (commonly 4×d_model), a nonlinearity (ReLU/GELU/SwiGLU), then a down-projection. This expansion gives the model most of its nonlinear representational capacity and holds a large share of the parameters; the 4× ratio is an empirically strong capacity/compute tradeoff.",
    trap: "Thinking the FFN mixes tokens or replaces attention. It's strictly per-token (position-wise); attention does the cross-token communication, the FFN does per-token nonlinear feature transformation.",
  },
  {
    id: "transformers-l1-4",
    topic: "transformers",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "mcq",
    question: "Why do transformers require explicit positional information, unlike RNNs?",
    options: [
      "Transformers process tokens sequentially, so position is implicit and encoding is redundant.",
      "Self-attention is permutation-equivariant — it treats the input as a set with no inherent order — so without positional encodings the model can't distinguish token order, which is essential for language.",
      "Positional encodings exist only to speed up training and don't affect correctness.",
      "RNNs also need positional encodings; transformers just add more of them.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Self-attention computes weighted sums over all tokens with no built-in notion of order — permuting the inputs permutes the outputs identically (permutation-equivariant). So 'the cat sat' and 'sat cat the' would be indistinguishable without extra signal. Positional encodings (absolute learned, sinusoidal, RoPE, ALiBi) inject order into the representation. RNNs get order for free because they consume tokens one at a time, updating a hidden state sequentially.",
    trap: "Claiming transformers process sequentially like RNNs. They process all positions in parallel with no ordering prior — that parallelism is why position must be added explicitly.",
  },
  {
    id: "transformers-l1-5",
    topic: "transformers",
    tier: "L1",
    difficulty: "medium",
    gated: false,
    type: "text",
    question: "In a typical decoder-only LLM, roughly how are parameters distributed between attention and FFN layers, and where does most of the count sit?",
    options: [],
    correct: 0,
    keywords: ["FFN", "feed-forward", "two-thirds", "majority", "4x", "attention", "projections", "MLP", "parameters"],
    explanation: "Within each transformer block, the FFN (up- and down-projection, ~4×d each way) typically holds roughly two-thirds of the block's parameters, while the attention projections (Q, K, V, and output, each ~d×d) hold about one-third. So across the network the FFN/MLP layers dominate the parameter count. Embedding and output (unembedding) matrices also contribute, especially in smaller models with large vocabularies. This is why MoE targets the FFN — that's where the parameters (and much of the compute) live.",
    trap: "Assuming attention holds most parameters because it's the 'signature' mechanism. The FFN blocks hold the majority (~2/3 per block) — which is exactly why sparse-MoE and quantization efforts focus there.",
  },

  // ==================== TRANSFORMERS L2 (Cross-concept) ====================
  {
    id: "transformers-l2-1",
    topic: "transformers",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "What are the two core advantages of transformers over RNNs/LSTMs for sequence modeling?",
    options: [
      "Transformers use fewer parameters and always need less memory than LSTMs.",
      "Parallelism (all positions processed at once during training, vs RNN's sequential dependency) and direct O(1)-path long-range modeling (any token attends to any other, vs signal degrading through many recurrent steps).",
      "Transformers are inherently causal so they avoid the need for masking that LSTMs require.",
      "LSTMs cannot model language at all, whereas transformers can.",
    ],
    correct: 1,
    keywords: [],
    explanation: "RNNs/LSTMs must process tokens one step at a time, so training can't parallelize across the time dimension, and long-range dependencies must survive many sequential updates where gradients vanish/explode. Transformers process all positions in parallel (huge training throughput on GPUs) and give every token a direct attention path to every other token (constant path length), so long-range dependencies are modeled directly rather than propagated. The costs are O(n^2) attention and no built-in order (needs positional encodings).",
    trap: "Citing parameter efficiency. Transformers aren't inherently smaller; their wins are training parallelism and direct long-range paths, at the price of quadratic attention.",
  },
  {
    id: "transformers-l2-2",
    topic: "transformers",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "BERT and GPT are both transformers. What's the fundamental architectural/objective difference?",
    options: [
      "BERT is a decoder trained with next-token prediction; GPT is an encoder trained with masked language modeling.",
      "BERT is an encoder with bidirectional attention trained on masked language modeling (fill-in-the-blank, great for understanding); GPT is a decoder with causal attention trained on next-token prediction (great for generation).",
      "They use identical attention; only the tokenizer differs.",
      "BERT can generate text autoregressively while GPT can only classify.",
    ],
    correct: 1,
    keywords: [],
    explanation: "BERT is encoder-only with bidirectional self-attention, pretrained by masking random tokens and predicting them (masked language modeling) — every token sees full left-and-right context, which is ideal for classification, extraction, and understanding, but it isn't a natural generator. GPT is decoder-only with causal self-attention, pretrained to predict the next token — this left-to-right objective makes it a natural autoregressive generator. The masked-vs-causal attention and MLM-vs-next-token objective are the core split.",
    trap: "Swapping the two. BERT = encoder/bidirectional/MLM (understanding); GPT = decoder/causal/next-token (generation). BERT is not a natural autoregressive generator.",
  },
  {
    id: "transformers-l2-3",
    topic: "transformers",
    tier: "L2",
    difficulty: "hard",
    gated: true,
    type: "mcq",
    question: "When would you choose encoder-only vs decoder-only vs encoder-decoder for a task?",
    options: [
      "Always use encoder-decoder; the other two are obsolete.",
      "Encoder-only for understanding/embedding/classification (e.g., BERT for NLU); decoder-only for open-ended generation and general-purpose LLMs (GPT); encoder-decoder for conditional seq-to-seq where input and output differ (translation, summarization, T5).",
      "Decoder-only for classification; encoder-only for generation; enc-dec for embeddings.",
      "The choice is arbitrary since all three have identical capabilities.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Encoder-only models produce rich bidirectional representations, best for understanding tasks: classification, NER, retrieval embeddings. Decoder-only models are autoregressive generators and have become the default for general-purpose LLMs because a single next-token objective scales and handles many tasks via prompting. Encoder-decoder shines when there's a distinct source to condition on and a separately structured target — classic seq-to-seq like translation and summarization (T5, BART), where cross-attention lets the decoder attend to a fully-encoded source.",
    trap: "Defaulting to enc-dec for everything. Modern general LLMs are decoder-only; enc-dec is chosen specifically for conditional seq-to-seq with a distinct input to encode, and encoder-only for embeddings/understanding.",
  },
  {
    id: "transformers-l2-4",
    topic: "transformers",
    tier: "L2",
    difficulty: "hard",
    gated: false,
    type: "mcq",
    question: "Why do transformers scale better than CNNs/RNNs for large-scale language modeling?",
    options: [
      "Transformers have hard inductive biases that constrain them to language, making them efficient.",
      "Their weak inductive bias plus a uniform, parallelizable architecture lets them exploit huge data and compute, following smooth scaling laws where loss predictably falls as params/data/compute grow — CNNs' locality bias and RNNs' sequential bottleneck don't scale as gracefully.",
      "Transformers require far less data than CNNs to reach the same accuracy.",
      "CNNs cannot be trained on GPUs, so transformers win by default.",
    ],
    correct: 1,
    keywords: [],
    explanation: "Transformers have relatively weak, general inductive biases (mostly permutation-equivariant attention) and a highly uniform, GPU-parallel structure. With enough data and compute, this generality becomes a strength: performance follows smooth, predictable scaling laws (loss decreasing as a power law in parameters, data, and compute). CNNs bake in locality/translation biases (great with limited data but a ceiling for arbitrary long-range language structure), and RNNs' sequential recurrence bottlenecks both throughput and long-range signal — so both scale less gracefully than transformers at frontier scale.",
    trap: "Assuming stronger inductive bias is always better. At small data it can be, but transformers' weak bias + parallelism is exactly what lets them convert massive compute/data into predictable gains via scaling laws.",
  },
  {
    id: "transformers-l2-5",
    topic: "transformers",
    tier: "L2",
    difficulty: "hard",
    gated: true,
    type: "mcq",
    question: "Dense FFN vs Mixture-of-Experts (MoE) FFN: what's the tradeoff?",
    options: [
      "MoE activates all experts for every token, so it uses more compute than a dense FFN of equal size.",
      "MoE replaces the dense FFN with many expert FFNs but routes each token to only a few (top-k), so total parameters grow massively while per-token FLOPs stay near-constant — more capacity for similar inference compute, at the cost of routing complexity, load-balancing, and memory.",
      "Dense FFNs have more parameters but MoE always has higher accuracy for free.",
      "MoE and dense FFNs are computationally identical; MoE only changes the tokenizer.",
    ],
    correct: 1,
    keywords: [],
    explanation: "A dense FFN uses all its parameters for every token. MoE swaps in a set of expert FFNs plus a router that sends each token to only its top-k experts (often 1-2 of many). This decouples total parameter count from per-token compute: you get far more capacity (billions more params) while activating only a fraction per token, so inference FLOPs stay close to a much smaller dense model. The costs are routing/gating complexity, the need for load balancing so experts are used evenly, higher total memory (all experts must be resident), and trickier training/serving.",
    trap: "Thinking MoE runs all experts. It routes to top-k only — that's the whole point: huge parameter capacity at near-constant active compute, traded against routing, load-balancing, and memory overhead.",
  },
];
