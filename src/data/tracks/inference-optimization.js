// GSL premium-niche track — Inference Optimization & Serving / On-device LLMs.
// RUNNER_DATA fragment. Spread into src/data/foundationsRunnerData.js via ...RUNNER_INFERENCE_OPT.
// Distinct from the existing "production" gym (cost/latency/observability) and the foundations
// quantization/kv-cache/speculative modules (conceptual): this track is the SERVING-INTERNALS niche —
// prefill/decode mechanics, continuous batching, PagedAttention, kernel/parallelism stacks, on-device.
// Keep the export name RUNNER_INFERENCE_OPT. Additive only.

export const RUNNER_INFERENCE_OPT = {
  "infra-prefill-decode": {
    depthTier: "deep",
    interviewWeight: "high",
    groundUp: "Let's start from something you've felt but probably never named. When you chat with a large language model, there are really *two* waits, not one. First there's the pause before **anything** appears — the model gathering itself before its first word. Then there's the pace at which the rest of the answer *streams out*, word by word. Those two sensations are different enough that it's worth asking a simple question: are they even the same kind of work underneath?\n\nThey aren't — and that's the quiet secret this whole module turns on. An LLM answers your prompt in **two physically distinct phases**, and those phases have *opposite* bottlenecks. One is hungry for raw arithmetic; the other is starved for memory bandwidth. Mix them up, and you can pour money into making inference \"faster\" in a way that changes nothing a user actually feels.\n\nWe'll build this up gently and from the ground: what really happens when the model reads your prompt, why producing the *next* token is a fundamentally different job from reading the prompt, and how a single idea — arithmetic intensity — explains the entire split. Take your time with it. By the end, you'll be able to look at a profiler showing a GPU sitting nearly idle *while everything feels slow* and explain exactly why in one clean sentence.",
    scenario: "Now let's put all of that to work on a real one. A team ships a 13B chat model on a single A100. The p50 time-to-first-token is a snappy 180ms, but users complain the model 'types slowly' — each subsequent token takes ~40ms, so a 400-token answer takes 16 seconds to stream out. A well-meaning engineer proposes buying a faster GPU with more FLOPS. The profiler shows the GPU at 90% compute utilization during the first 180ms, then dropping to **8% compute utilization** for the rest of the request while memory bandwidth pegs at 95%. Take a moment before reading on: given what we just built, what does that profile tell you? Here's the reasoning, step by step. The first 180ms is prefill — a big parallel matmul over the whole prompt — so compute is pegged; that's why TTFT is fine. Then the phase flips to decode, one token at a time, and the very same GPU falls to 8% compute while memory bandwidth saturates — the textbook signature of a memory-bandwidth bottleneck. And notice how cleanly that kills the FLOPS proposal: a faster-FLOPS GPU speeds up a resource that was already idle during the slow part. The two numbers users feel — first-token delay and per-token delay — come from two physically different bottlenecks, so the real levers are higher HBM bandwidth or amortizing the weight stream across many tokens (batching, speculative decoding), never more FLOPS.",
    explanation: [
      "LLM inference is **not one workload — it is two**, and they have opposite bottlenecks. Every request runs a **prefill** phase followed by a **decode** phase, and almost every serving optimization you'll ever discuss attacks one or the other specifically.\n\n**Prefill** processes the entire prompt at once. All the prompt tokens are already known, so the model runs them through every layer in **one big parallel forward pass** — a batched matmul over the whole sequence. This produces the first output token and, crucially, fills the **KV cache** — the running memory of every past token's key and value vectors, so attention never has to recompute them on later steps — for every prompt position. ==Prefill is compute-bound: you have a large matrix multiply and the tensor cores are the bottleneck.==\n\n**Decode** then generates the rest of the answer **one token at a time**. Token N cannot be computed until token N-1 exists, because N-1 is part of the input that produces N. So decode is *sequential by construction* — one full forward pass through every layer to produce exactly one token, then repeat.",
      "The reason decode feels slow is **arithmetic intensity** — the ratio of FLOPs performed to bytes moved from memory.\n\nIn a single-stream decode step you process **one** token. To do that, the GPU must stream the *entire* weight matrix of every layer from HBM (High-Bandwidth Memory — the GPU's onboard RAM) into the compute units — the same ~26GB of fp16 weights for a 13B model — and do a tiny amount of math with it (one token's worth of matmuls). You move gigabytes to do megaflops. ==The tensor cores sit idle waiting on memory; you are memory-bandwidth-bound, not compute-bound.==\n\nPrefill is the opposite. Processing a 512-token prompt reuses each streamed weight across 512 positions, so the arithmetic-to-bytes ratio is high and the tensor cores stay busy. This is the general shape you'll see on any profiler pointed at LLM inference: **prefill saturates FLOPs, decode saturates memory bandwidth.**",
      { type: "illustration", label: "Why one profiler shows two totally different bottlenecks", content: `13B model, fp16 weights ≈ 26 GB. A100: ~312 TFLOP/s (bf16), ~2.0 TB/s HBM.

PREFILL (512-token prompt, one pass):
  streams 26 GB weights ONCE, reuses across 512 positions
  FLOPs ≈ 2 × 13e9 × 512  ≈ 1.3e13   → ~43 ms of pure compute
  bytes ≈ 26 GB           → ~13 ms of pure memory
  compute time > memory time  ⇒ COMPUTE-BOUND  (tensor cores ~90% busy)
  → governs TIME-TO-FIRST-TOKEN (TTFT)

DECODE (1 token per step):
  streams 26 GB weights to produce ONE token
  FLOPs ≈ 2 × 13e9 × 1    ≈ 2.6e10   → ~0.08 ms of pure compute
  bytes ≈ 26 GB           → ~13 ms of pure memory
  memory time >> compute time  ⇒ MEMORY-BANDWIDTH-BOUND  (cores <1% busy — 0.08 / 13 ≈ 0.6%; real profiler
  overhead, attention/softmax ops, and imperfect overlap push the measured figure a bit higher in practice)
  → governs INTER-TOKEN LATENCY / TPOT (time-per-output-token)

Same weights, same GPU — the phase decides the bottleneck.

(These are theoretical floors from raw FLOPs/bytes math alone. Measured numbers in a real deployment run several times higher than these floors — kernel-launch overhead, attention/softmax cost, and framework overhead all stack on top — but the qualitative split holds regardless of exactly how much overhead a given stack adds: prefill stays compute-heavy, decode stays memory-heavy.)` },
      "This split is why the two latency numbers in every serving SLA are physically distinct.\n\n**TTFT (time-to-first-token)** is dominated by prefill: how long until the user sees anything. It scales with *prompt length* (more prompt tokens = bigger prefill matmul) and is a **compute** problem.\n\n**TPOT (time-per-output-token)**, also called inter-token latency, is dominated by decode: how fast tokens stream after the first. It is roughly constant per token, set by how long it takes to stream the weights once, and is a **memory-bandwidth** problem. ==A 400-token answer's total latency ≈ TTFT + 400 × TPOT== — so long generations live or die on TPOT.\n\nPicture a short-prompt, long-answer request: a 20ms TTFT and a 12ms TPOT over a 200-token reply put total latency at roughly 20 + 200×12 ≈ 2,420ms — dominated almost entirely by TPOT. Flip to a long-prompt, short-answer request and the ratio flips too: TTFT can dwarf the handful of TPOT steps that follow. Which term dominates depends entirely on the shape of the request — exactly why serving teams report TTFT and TPOT separately instead of blending them into one number.",
      "Recall the **KV cache** from a moment ago (the running memory of every past token's key and value vectors) — it's the object that ties both phases together and drives most of the memory story.\n\nDuring prefill, attention for each token needs the keys and values of every *earlier* token. Rather than recompute them every decode step, the model **caches** the keys and values for all past positions — that's the KV cache. Each new decoded token reads the whole KV cache (to attend over history) and appends its own K and V. ==So decode streams weights *plus* a KV cache that grows one entry per token, per layer, per attention head.==\n\nKV-cache size ≈ `2 × layers × hidden_dim × seq_len × batch × bytes`. Plug in the same 13B model (40 layers, hidden_dim 5120, fp16 → 2 bytes) at a 2,048-token context and batch of 1: `2 × 40 × 5120 × 2048 × 1 × 2 ≈ 1.68 GB` for one sequence. Now raise concurrency to 16 in-flight sequences at that same context length and the cache alone is **~27 GB** — already bigger than the 26 GB the weights themselves occupy. It grows **linearly with sequence length and concurrency**, and at long contexts or high concurrency it can rival or exceed the weights in HBM. This is why the KV cache is the #1 memory constraint in serving — and why it becomes part of the per-step memory traffic that makes decode bandwidth-bound.",
      "The payoff of this framing is that it **tells you which optimization to reach for.** Every serving technique targets prefill or decode specifically:\n\n**Decode is memory-bound with idle compute** → so you have spare FLOPs to spend. That's the door for (a) **continuous batching** — process many requests' decode steps together so one weight-stream serves N tokens, amortizing the bandwidth cost; and (b) **speculative decoding** — let a small draft model propose several tokens so the big model verifies them in one memory-bound pass (covered in the foundations Speculative Decoding module).\n\n**Prefill is compute-bound and scales with prompt length** → so you attack it with **prompt/prefix caching** (reuse KV for shared system prompts), **chunked prefill** (break a long prompt into pieces so it doesn't monopolize the GPU), and attention kernels that avoid ever materializing the full attention matrix in HBM, computing it in fused on-chip blocks instead (FlashAttention).\n\n==If you can't name whether a technique helps prefill or decode, you don't understand it yet.== That is the entire value of this mental model: it converts a vague 'make inference faster' into a targeted decision about a specific bottleneck.",
    ],
    keyPoints: [
      "**Inference is two workloads with opposite bottlenecks.** Prefill processes the whole prompt in one parallel pass (compute-bound, sets TTFT). Decode generates one token at a time, sequentially (memory-bandwidth-bound, sets inter-token latency).",
      "**Decode is memory-bound because of low arithmetic intensity.** Each step streams the entire weight matrix from HBM (the GPU's high-bandwidth memory) to produce a single token — gigabytes moved, megaflops done — so tensor cores idle while memory bandwidth pegs.",
      "**The two SLA numbers are physically distinct.** TTFT scales with prompt length and is a compute problem; TPOT is roughly constant per token and is a bandwidth problem. Total latency ≈ TTFT + (tokens × TPOT), so long answers are dominated by TPOT.",
      "**The KV cache is the memory linchpin.** It caches past keys/values so attention isn't recomputed each step, grows linearly with sequence length and concurrency, and adds to the per-step memory traffic — often rivaling the weights at long context.",
      "**The framing dictates the fix.** Decode's idle compute is what makes continuous batching and speculative decoding work; prefill's compute cost is what prefix caching and chunked prefill attack. Name the phase before you name the optimization.",
    ],
    recap: [
      "**Two phases, opposite bottlenecks**: prefill → whole prompt in parallel, compute-bound, sets TTFT. Decode → one token at a time, memory-bandwidth-bound, sets TPOT.",
      "**Decode's low arithmetic intensity**: streams ~all weights from HBM per single token → memory-bound, not compute-bound; more FLOPS doesn't fix it.",
      "**Latency ≈ TTFT + tokens × TPOT.** TTFT scales with prompt length (compute); TPOT near-constant per token (bandwidth) → long generations live on TPOT.",
      "**KV cache** stores past keys/values to skip recompute; grows linearly with seq_len × concurrency → can exceed weight memory at long context, driving decode's per-step traffic.",
      "**Match fix to phase**: decode's idle compute → continuous batching + speculative decoding; prefill's compute cost → prefix caching + chunked prefill + FlashAttention.",
    ],
    mcqs: [
      {
        question: "A 13B model streams tokens slowly (40ms/token) even though the profiler shows the GPU at only 8% compute utilization during generation, with HBM bandwidth at 95%. An engineer wants to buy a GPU with 2× the FLOPS. Why won't that meaningfully speed up the streaming?",
        options: [
          "Decode is compute-bound just like prefill, so the 8% utilization figure reflects a small matmul size rather than a true memory limit; more FLOPS would directly cut per-token compute time and speed up token streaming.",
          "The GPU is thermally throttling under sustained load, dropping clocks to roughly 60% of rated speed regardless of workload type; a higher-FLOPS card paired with better liquid cooling would restore full token-generation throughput.",
          "Decode has low arithmetic intensity: each step streams the entire weight matrix from HBM to produce one token, so tensor cores idle on memory while bandwidth saturates — added FLOPS don't fix a bottleneck that was never compute.",
          "The KV cache has overflowed the 80GB HBM budget and is being swapped to system RAM over PCIe at every decode step, adding roughly 3x round-trip latency, so the slowdown comes from PCIe transfer time rather than weight streaming.",
        ],
        correct: 2,
        explanation: "Option C is correct: decode has low arithmetic intensity — it moves ~all the model weights from HBM to compute a single token, doing a tiny amount of math. The 8%-compute / 95%-bandwidth profile is the textbook signature of a memory-bandwidth bottleneck, so adding FLOPS speeds up an idle resource; the real levers are higher HBM bandwidth or amortizing the weight stream across many tokens (batching, speculative decoding). Option A misapplies prefill's compute-bound reasoning to decode, which is the wrong phase — decode's bottleneck is memory, not matmul size. Option B invents thermal throttling; the profile described (steady 8%/95%, not degrading clocks) is the fragmentation-free signature of a bandwidth bottleneck, not throttling. Option D invents KV-cache offload to system RAM, which isn't indicated by the given profile and is a separate failure mode entirely.",
      },
      {
        question: "A serving team reports two different latency numbers: TTFT is 180ms and TPOT is 40ms/token. A product manager asks why they can't just report 'one latency number.' What is the most accurate explanation of why these are physically distinct?",
        options: [
          "TTFT reflects prefill, a compute-bound pass over the whole prompt scaling with prompt length; TPOT reflects decode, a memory-bandwidth-bound per-token step, roughly constant. Latency is TTFT plus tokens times TPOT.",
          "TTFT and TPOT both come from the same decode loop; TTFT is simply the first decode step's latency averaged with network overhead, so the two numbers are really one metric measured twice under different names.",
          "TTFT measures time spent queued in the scheduler before the GPU picks up the job; TPOT measures the GPU's per-token compute cost once scheduled. The two diverge because of queueing delay, not model architecture.",
          "TTFT is higher because retrieval calls, tool invocations, or a vector lookup typically run for 100-200ms before generation starts; TPOT reflects pure token generation cost once those external calls have already finished.",
        ],
        correct: 0,
        explanation: "Option A is correct: prefill and decode are separate phases with different bottlenecks. TTFT reflects prefill — a compute-bound batched matmul over the entire prompt, so it grows with prompt length. TPOT reflects decode — a memory-bandwidth-bound step that streams the weights to emit one token, roughly constant regardless of how many tokens came before. Because a response is TTFT once plus TPOT per generated token, the two numbers capture genuinely different physics. Option B wrongly folds TTFT into the decode loop; TTFT is a prefill-phase measurement, architecturally distinct from decode. Option C invents a queueing explanation; the split described in the scenario is architectural (compute vs bandwidth bound), not a scheduling artifact. Option D assumes RAG/tool-calling context that isn't part of this scenario — the first-token cost here is prefill compute, not external calls.",
      },
      {
        question: "Decode leaves the GPU's tensor cores mostly idle because it's memory-bandwidth-bound. Select the TWO techniques that directly convert that idle compute into higher effective throughput.",
        options: [
          "Continuous batching: process many requests' decode steps together so one weight-stream from HBM produces a token for every request in the batch, turning idle compute into throughput for extra sequences.",
          "Speculative decoding: a small draft model proposes several tokens ahead, and the large model verifies them all in one memory-bound pass, converting idle compute into parallel verification work.",
          "Raising the sampling temperature to around 1.2: this increases the diversity of generated tokens and empirically lets the vLLM scheduler pack roughly 30% more sequences per batch without hurting output quality.",
          "Disabling the KV cache and recomputing full self-attention from scratch every decode step frees that reserved HBM region for other tensors, which directly raises tensor-core utilization to near 100% throughout generation.",
        ],
        correct: [0, 1],
        explanation: "Options A and B are correct: both exploit decode's idle tensor cores. Continuous batching processes many requests' decode steps in one weight-stream, so the same memory trip now produces N tokens instead of 1, using the spare compute on extra sequences. Speculative decoding has a small draft model propose several tokens, which the large model then verifies in a single memory-bound pass — turning what would be several sequential idle-compute steps into one step with more math per byte moved. Option C is wrong — temperature controls sampling randomness, not how many sequences the scheduler can batch or how compute is utilized. Option D is backwards — disabling the KV cache forces recomputing all past keys/values every step, which increases both compute and memory traffic rather than freeing headroom.",
      },
    ],
    takeaway: "LLM inference is two workloads: prefill processes the whole prompt in one compute-bound pass (sets TTFT, scales with prompt length), and decode generates tokens one at a time in a memory-bandwidth-bound loop (sets TPOT, roughly constant per token). Decode streams ~all the weights from HBM to emit a single token, so it's bandwidth-bound with idle compute — which is exactly why more FLOPS doesn't fix slow streaming, and why continuous batching and speculative decoding exist to fill that idle compute. Name the phase before you name the optimization.",
  },

  "infra-batching-throughput": {
    depthTier: "deep",
    interviewWeight: "high",
    groundUp: "Let's pick up exactly where the last module left us, because it handed us a gift. We learned that generating tokens one at a time leaves the GPU's math units almost entirely idle — the chip spends its time hauling weights out of memory to produce a single token, and the tensor cores mostly sit and wait. Sit with that for a second: **idle compute is free throughput you already paid for and are throwing on the floor.**\n\nSo here's the natural question that runs this whole module. If the expensive part is streaming the weights once, and the cheap part (the actual math) is barely being used, could we make *one* trip to memory serve *many* requests at the same time? Give the GPU eight users' next-tokens to compute from the same weight-stream instead of one, and the costly memory trip is suddenly amortized across eight tokens. That idea is **batching**, and it's the single biggest throughput lever in modern serving.\n\nBut — and this is where it gets interesting — the obvious way to batch turns out to be quietly broken for LLMs, in a way that's invisible until you watch a short request get trapped behind a long one. We'll build it up the honest way: start with the naive batch, watch precisely how it fails, and let each fix grow out of the failure before it. No rush. By the end you'll be able to explain, in one sentence, why a short answer can take just as long to come back as a long one, when they're stuck in the same batch.",
    scenario: "Now let's put all of that to work on a real one. A team self-hosts a 7B model behind an API. Traffic is bursty and output lengths vary wildly — some users ask a yes/no question (5 tokens out), others want a full essay (800 tokens out). The first implementation serves one request at a time: throughput is a dismal 40 tokens/sec aggregate and the GPU sits at 12% utilization, yet requests still queue and p99 latency is terrible. The second attempt batches 8 requests together statically — and now a user's 5-token answer takes 20 seconds because it's stuck behind an 800-token essay in the same batch. Take a moment before reading on: given what we built, why does each attempt fail, and what fixes both? Here's the reasoning, step by step. Serving one-at-a-time wastes decode's idle compute — that's the 12% utilization and the low throughput. Static batching recovers some of it but marches lockstep until the *slowest* member finishes, so the 5-token answer is done in 5 steps yet can't be returned for ~795 more — classic head-of-line blocking from variable, unknown output lengths. Notice the fix falls straight out of the diagnosis: continuous (in-flight) batching schedules per decode step, so finished sequences drop out and free their KV memory immediately while new requests join mid-flight — no request waits on another. The cost you pay is a throughput-vs-latency dial (bigger batches raise tail latency) and the need for chunked prefill so one giant prompt doesn't stall everyone, all ultimately capped by KV-cache memory.",
    explanation: [
      "Start from the throughput problem the previous module set up: **single-stream decode is memory-bandwidth-bound with idle compute.** Serving one request at a time streams the entire weight matrix from HBM to produce a single token — you pay the full memory cost for one token of output. The tensor cores are ~90% idle. ==That idle compute is free throughput you're throwing away.==\n\nThe fix is to make one weight-stream do more work: process **many requests' decode steps in the same forward pass.** If 8 requests each want their next token, you can compute all 8 next-tokens by streaming the weights *once* and doing 8× the (cheap) matmul work — turning a memory-bound step that produced 1 token into one that produces 8. Throughput rises roughly linearly with batch size until you re-saturate compute or run out of KV-cache memory.",
      "The naive way to batch is **static batching**: collect N requests, run them together from start to finish, return all N when done. It captures some of the win but has a fatal flaw for LLMs — **variable output length.**\n\nUnlike a fixed-size image classifier, LLM requests finish at different, *unknown-in-advance* times: one hits its stop token after 3 decode steps, another runs 500. In static batching the whole batch marches lockstep until the **slowest** member finishes. ==Every request that finished early keeps occupying its slot — burning GPU cycles on padding and holding KV-cache memory — until the longest generation in the batch completes.== That's classic head-of-line blocking — a short request trapped behind a long one, waiting on nothing but shared occupancy in the batch.\n\nStatic batching also can't admit new requests mid-flight: a request that arrives one step after the batch launches waits for the entire batch to drain.",
      { type: "illustration", label: "Static vs continuous batching over decode steps", content: `Four requests. A wants 2 tokens, B wants 5, C wants 3, D wants 2.
(each cell = one decode step for that slot; · = wasted/padded slot)

STATIC BATCHING (lockstep, batch finishes when slowest B finishes):
  step:   1  2  3  4  5
  A:      A  A  ·  ·  ·     A done at step 2, then idles 3 steps
  B:      B  B  B  B  B
  C:      C  C  C  ·  ·     C done at step 3, then idles 2 steps
  D:      D  D  ·  ·  ·     D done at step 2, then idles 3 steps
  → 12 useful tokens, 8 wasted (of 20 slot-steps total) ⇒ 40% wasted. A/D wait for B.

CONTINUOUS (IN-FLIGHT) BATCHING (finished slots freed, new work admitted):
  step:   1  2  3  4  5
  A:      A  A  E  E  E     A finished → E (new request) admitted mid-flight
  B:      B  B  B  B  B
  C:      C  C  C  F  F     C finished → F admitted
  D:      D  D  E' F' ...   D finished → slot reused immediately
  → no request waits on B; GPU stays saturated; slots never idle.` },
      "**Continuous (in-flight) batching** is the fix, and it's the default in vLLM and TGI. The batch is managed at the granularity of a **single decode step**, not a whole request.\n\nEach step, the scheduler assembles the set of currently-active sequences and runs one decode step for all of them together. When a sequence emits its stop token, it **drops out immediately** and its slot (and KV memory) is freed. When a new request arrives, it can **join the running batch** on the very next step — the batch composition changes continuously. ==No request waits for another to finish; the GPU never idles on padding.== This is the single biggest throughput lever in modern LLM serving, often 5–20× over naive single-stream, precisely because it converts decode's idle compute into useful work while eliminating head-of-line blocking.",
      "Continuous batching creates a scheduling wrinkle: **prefill and decode compete for the GPU.** A newly-admitted request must first run its (compute-heavy) prefill before it can join the (memory-bound) decode loop. If a user submits a 4,000-token prompt, its prefill is a big matmul that, run monolithically, **stalls every in-flight decode** for hundreds of milliseconds — spiking everyone's inter-token latency.\n\n**Chunked prefill** solves this: split the long prompt's prefill into fixed-size chunks and interleave those chunks with ongoing decode steps, so a long prompt makes steady progress without monopolizing the GPU. Combined with **priority scheduling** (e.g. favor decode to protect streaming smoothness, or cap the prefill budget per step), this keeps TTFT (time-to-first-token) for new requests and TPOT (time-per-output-token, the inter-token gap) for existing ones both bounded. ==Continuous batching without chunked prefill still lets one giant prompt wreck tail latency.==",
      "The whole thing is a **throughput-versus-latency dial**, and staff-level judgment is knowing where to set it. Bigger effective batches raise throughput (more tokens/sec, lower cost per token) but push up **tail latency**: more sequences per step means each step does more work and takes longer, so per-token latency creeps up, and admission delays grow when the batch is full.\n\nThe tuning knobs are **max batch size / max num sequences**, **max tokens per step** (the prefill+decode budget), and how aggressively you admit new work versus protect in-flight requests. ==For an interactive chat SLA you cap batch size to protect p99 TPOT; for an offline bulk-generation job you crank batch size to the KV-memory limit and don't care about tail latency.== And the ceiling on batch size is almost always **KV-cache memory**, not compute — which is exactly the constraint the next module (PagedAttention) is built to relax.",
    ],
    keyPoints: [
      "**Batching turns decode's idle compute into throughput.** One HBM weight-stream can produce next-tokens for many requests at once, so a memory-bound step that made 1 token now makes N — throughput rises ~linearly with batch size until compute re-saturates or KV memory runs out.",
      "**Static batching suffers head-of-line blocking.** LLM outputs finish at different, unknown times; a static batch marches lockstep until the slowest member ends, so early finishers idle in padded slots and new requests can't join mid-batch.",
      "**Continuous (in-flight) batching schedules per decode step.** Finished sequences drop out and free their KV memory instantly; new requests join the running batch on the next step. This is the core 5–20× throughput unlock in vLLM/TGI, with no request waiting on another.",
      "**Chunked prefill protects tail latency.** A long prompt's prefill is compute-heavy and can stall all in-flight decode; splitting it into chunks and interleaving with decode keeps both TTFT (new) and TPOT (existing) bounded. Priority scheduling tunes the balance.",
      "**It's a throughput-vs-latency dial capped by KV memory.** Bigger batches mean lower cost/token but higher tail latency; you cap batch size for interactive SLAs and max it for offline jobs. The batch-size ceiling is usually KV-cache memory, not compute.",
    ],
    recap: [
      "**Single-stream decode wastes idle compute** (memory-bound) → batching makes one weight-stream serve N requests' next-tokens, throughput ~linear in batch size.",
      "**Static batching → head-of-line blocking**: unknown, variable output lengths force lockstep until the slowest member finishes; early finishers idle, no mid-flight joins.",
      "**Continuous (in-flight) batching** schedules per decode step: finished sequences drop out, free KV memory instantly; new requests join next step. vLLM/TGI default, ~5–20× over naive.",
      "**Chunked prefill + priority scheduling**: split a long prompt's prefill into chunks interleaved with decode → bounds both TTFT and TPOT instead of stalling everyone.",
      "**Throughput-vs-latency dial, capped by KV memory**: bigger batch → lower cost/token, worse tail latency. Cap for interactive SLAs, max for offline; KV-cache (not compute) is the usual ceiling — PagedAttention's target.",
    ],
    mcqs: [
      {
        question: "A team static-batches 6 requests of very different output lengths. A user's 8-token reply takes 14 seconds because it's stuck behind a 600-token generation in the same batch. Select the TWO accurate statements about the cause and the fix.",
        options: [
          "Static batching runs every request in lockstep, so the batch can't return until its slowest member finishes — a short request is held hostage by a long one, purely a scheduling artifact, not a GPU speed limit.",
          "The 600-token request allocates KV blocks in 64-token pages instead of the default 16-token pages, forcing the paging allocator to periodically defragment and stall the whole batch until that reallocation completes.",
          "Continuous (in-flight) batching fixes it by scheduling per decode step: a finished sequence's slot and KV memory free immediately, so short or new requests never wait behind a long one.",
          "Increasing the static batch size from 6 to 48 requests would fix it, since spreading the 600-token generation's cost across 8x more concurrent slots statistically shortens every individual request's average wait.",
        ],
        correct: [0, 2],
        explanation: "Options A and C are correct: LLM requests finish at different, unpredictable times, and a static batch advances in lockstep until its slowest member (the 600-token generation) completes — so the 8-token reply is done in 8 steps but can't be returned or freed for ~592 more, which is head-of-line blocking purely from the batching structure. Continuous batching fixes it at single-decode-step granularity: a finished sequence exits immediately, freeing its slot and KV memory, and new or short requests join the running batch on the next step instead of waiting. Option B invents a KV-block-size mismatch; block size is a fixed serving parameter, not something that varies per request length and stalls a batch. Option D makes things worse, not better — a bigger static batch just means more chances of an even-longer member to wait behind, not a statistical improvement.",
      },
      {
        question: "In a continuously-batched vLLM server, a user submits a 4,000-token prompt and suddenly every other in-flight request's inter-token latency spikes for a few hundred milliseconds. What's happening, and what's the standard mitigation?",
        options: [
          "The long prompt overflowed the model's 8,192-token max context window, causing the server to silently truncate the oldest turns and reprocess every other active request's context from scratch to keep the batch aligned.",
          "The scheduler evicted other requests' KV cache blocks to make room for the new prompt's reservation, and those requests are now recomputing their entire attention history from the beginning of their sequence.",
          "The GPU's power limiter detected the sudden compute spike and throttled core clocks to roughly 70% of rated speed for a few hundred milliseconds, uniformly slowing every kernel currently running on the device.",
          "The new request's prefill is a large compute-bound matmul that stalls in-flight decode when run as one monolithic pass; chunked prefill splits it into pieces interleaved with decode so no single step blocks everyone.",
        ],
        correct: 3,
        explanation: "Option D is correct: a newly admitted request must run its prefill before it can join the decode loop, and prefill is compute-heavy and scales with prompt length. A 4,000-token prefill run as one monolithic step occupies the GPU long enough that every in-flight decode step is delayed — hence the synchronized latency spike. Chunked prefill breaks the prompt's prefill into fixed-size pieces and interleaves them with decode steps, bounding how long any single step blocks decode. Option A invents context-window overflow and cross-request truncation, which doesn't happen from a long-but-valid prompt. Option B describes KV eviction, a separate memory-pressure failure mode, not what a single large prefill causes on its own. Option C invents thermal/power throttling; the symptom described is a scheduling stall tied to prefill size, not a device-wide clock change.",
      },
      {
        question: "An engineer running continuous batching wants to raise throughput and keeps increasing the max batch size. Throughput climbs, then plateaus and OOM errors appear — well before compute utilization maxes out. What is the binding constraint, and what's the correct framing?",
        options: [
          "The batch size hit the CPU core count that manages request scheduling and tokenization threads; adding more CPU cores or a faster scheduler process would allow the server to admit more concurrent sequences.",
          "KV-cache memory is the ceiling: each sequence holds KV storage that grows with its length, so the count that fits is limited by leftover HBM after weights, not compute — the constraint PagedAttention relaxes.",
          "Throughput is capped by network bandwidth to clients, since each additional concurrent sequence adds another streaming connection that competes with the others for the server's shared outbound network interface.",
          "The model's 13-billion parameter count directly limits batch size, since larger models require proportionally larger batch sizes to keep the tensor cores compute-efficient and avoid wasting matmul throughput.",
        ],
        correct: 1,
        explanation: "Option B is correct: in continuous batching each active sequence holds a KV cache proportional to its sequence length, and all those caches must coexist in HBM alongside the weights. So the practical batch-size ceiling is KV-cache memory, which is why OOM appears before compute saturates. Recognizing that memory (not FLOPs) is the binding constraint is the setup for PagedAttention, which packs KV memory efficiently to fit more concurrent sequences per GPU. Option A confuses GPU batch scheduling with CPU core count, which doesn't gate GPU memory capacity. Option C invents a network bottleneck unrelated to on-GPU batch capacity — token output bandwidth is negligible compared to GPU memory pressure. Option D is backwards — the weights are a fixed cost; what actually scales with batch size and causes the OOM is the per-sequence KV cache, not the parameter count.",
      },
    ],
    takeaway: "Batching converts decode's idle compute into throughput by making one HBM weight-stream serve many requests' next-tokens at once. Static batching wastes most of that gain to head-of-line blocking because LLM outputs finish at different, unknown times. Continuous (in-flight) batching — the vLLM/TGI default — schedules per decode step so finished sequences exit and free KV memory immediately while new requests join mid-flight, delivering the core 5–20× throughput unlock; chunked prefill keeps a long prompt from stalling everyone. It's a throughput-vs-latency dial whose ceiling is almost always KV-cache memory, not compute — which is exactly what PagedAttention exists to relax.",
  },

  "infra-paged-attention-kv": {
    depthTier: "deep",
    interviewWeight: "high",
    groundUp: "Let's carry forward the one fact the last module pinned down: what limits how many requests you can serve at once is **not** the GPU's math — it's memory, specifically the KV cache. So the game becomes simple to state: fit more KV caches into the same HBM (the GPU's high-bandwidth memory). And here's the puzzle we'll unravel — even when the arithmetic says a card should hold far more concurrent conversations than it actually does, with most of the memory somehow 'used' but not actually holding anything. Where did it go?\n\nThe answer is one of the most satisfying ideas in all of systems, because it isn't new at all. It's a fifty-year-old operating-systems trick — **virtual memory and paging** — quietly reborn to solve an LLM problem. The reason your card fills up isn't that KV caches are big; it's that the *naive way of storing them* leaves huge pockets of reserved-but-empty space, the same fragmentation that plagued early memory allocators.\n\nWe'll build it from the ground up: first the arithmetic of what a KV cache actually costs per token, then exactly how the obvious contiguous storage wastes most of it, and finally how paging recovers nearly all of it — and, as a bonus, lets requests *share* memory they have in common. Take your time. By the end, a card that OOMs while looking half-empty will make complete sense.",
    scenario: "Now let's put all of that to work on a real one. A continuously-batched server should, by the memory math, fit ~40 concurrent 2K-context requests on an 80GB A100 after weights. In practice it OOMs at ~14 concurrent requests, and nvidia-smi shows 30GB of VRAM 'used' while active KV caches only account for ~12GB. Average sequences are short, yet most of the card is unusable. Take a moment before reading on: given what we built, where is the memory going? Here's the reasoning, step by step. Naive serving hands each request one contiguous block sized to the *maximum* possible sequence length, so a short request that generates 100 tokens still holds a 2,048-token reservation — internal fragmentation — and the come-and-go of variable lengths leaves gaps too small to reuse — external fragmentation. That gap between 12GB live and 30GB 'used' *is* the fragmentation; it's why allocation fails at 14 while the card looks half-full. Notice the fix falls straight out of the diagnosis: PagedAttention stores KV in small fixed-size blocks mapped by a per-request block table, allocated on demand and non-contiguous, cutting waste to at most one partial block per sequence and eliminating external fragmentation entirely. That reclaimed memory feeds a bigger continuous batch (~2–4× throughput at equal latency in vLLM), and because blocks are addressable, requests sharing a system prompt can point at the same physical blocks via copy-on-write.",
    explanation: [
      "The previous module ended on the real ceiling: **KV-cache memory, not compute, caps how many requests you can batch.** So the game is fitting more KV caches in HBM. The problem is that the *naive* way of storing the KV cache wastes most of the memory you paid for.\n\nFirst, the arithmetic. Per token, the KV cache stores a key and a value in **every layer, for every attention head** (or KV head under grouped-query attention). ==KV bytes ≈ `2 (K and V) × layers × kv_heads × head_dim × bytes_per_elem`, per token.== For a 13B-class model at fp16 that's on the order of ~0.5–1 MB per token; multiply by sequence length and by the number of concurrent requests and the KV cache quickly becomes the dominant consumer of VRAM — which is why storing it efficiently is the whole ballgame.",
      "Naive serving stores each request's KV cache in **one contiguous block of memory sized to the maximum possible sequence length.** That creates two classic memory-management pathologies at once.\n\n**Internal fragmentation (the big one):** you reserve, say, 2,048 tokens of KV space up front, but the request only generates 100 tokens — so ~95% of that reservation sits allocated-but-empty for the request's whole lifetime. You can't lend it to another request because it's earmarked. ==Every request pre-pays for its worst case, so the card fills with reserved-but-unused KV space.==\n\n**External fragmentation:** requests of different lengths come and go, leaving oddly-sized gaps between live contiguous blocks. A new request needs a contiguous run big enough for its max length; even if the *total* free memory is plenty, no single gap is large enough, so allocation fails. That's the shape of the puzzle this module opened with: memory reported as 'used' that isn't actually holding KV data — the rest is fragmentation waste.",
      "**PagedAttention** (the idea vLLM is built on) fixes this by borrowing the oldest trick in operating systems: **virtual memory and paging.**\n\nInstead of one contiguous per-request block, the KV cache is split into small **fixed-size blocks** (e.g. 16 tokens' worth of KV each). A request's logical sequence of tokens is mapped to a set of physical blocks through a **block table** — exactly like an OS page table mapping virtual pages to physical frames. ==The physical blocks a request uses need not be contiguous; they're allocated from a shared pool on demand, one block at a time as the sequence grows.==\n\nThe attention kernel is made 'paged-aware': it gathers the K/V it needs by following the block table rather than assuming a flat contiguous array. The result: you allocate KV memory in small increments only as tokens are actually produced, so internal fragmentation drops to at most one partially-filled block per sequence (a few percent), and external fragmentation vanishes because every block is the same size and interchangeable.",
      { type: "illustration", label: "Contiguous max-length reservation vs paged blocks", content: `NAIVE CONTIGUOUS (reserve max_len = 2048 per request):
  req A: [■■■■□□□□□□□□□□□□□□□□□□□□□□]  used 100 / reserved 2048  → ~95% wasted
  req B: [■■■■■■□□□□□□□□□□□□□□□□□□□□]  used 300 / reserved 2048  → ~85% wasted
  free memory is fragmented into gaps too small for a new 2048-block reservation
  → OOM long before the card is actually full.

PAGEDATTENTION (fixed 16-token blocks + per-request block table):
  block pool (physical):  [b0][b1][b2][b3][b4][b5][b6][b7] ... (shared)
  req A block table → [b0, b3]          (2 blocks = up to 32 tokens, grows on demand)
  req B block table → [b1, b2, b5, b7]  (blocks need NOT be contiguous)
  → allocate a block only when a sequence fills the previous one;
    waste ≤ one partial block per request (a few %), no external fragmentation.

Shared prefix (copy-on-write): system prompt lives in blocks [b0,b1];
  many requests' block tables point at [b0,b1] → stored ONCE, not per request.` },
      "The payoff is **large and directly measured in concurrency.** Cutting KV waste — typically ~60–80% under naive contiguous reservation — down to a few percent lets you pack **several times more concurrent sequences** onto the same GPU — the original vLLM paper reports up to ~2–4× higher throughput than prior systems at the same latency, essentially all of it from reclaimed KV memory feeding a bigger continuous batch. ==PagedAttention is *what makes continuous batching memory-efficient at scale* — the two are complementary: batching wants more sequences, paging lets the memory hold them.==\n\nPaging also unlocks **sharing.** Because blocks are addressable and reference-countable, multiple requests can point their block tables at the **same physical blocks** for a shared prefix — a common system prompt, a few-shot preamble, or the branches of a beam search / parallel samples. The shared blocks are stored **once** and use **copy-on-write**: the moment one request needs to diverge (write a different token), it gets its own copy of just that block. For a workload where every request carries the same 500-token system prompt, prefix sharing alone means N requests store that shared prefix's KV once instead of N times — real, measurable memory back.",
      "PagedAttention is one lever on the KV budget; production systems stack it with two others when memory is *still* tight.\n\n**KV-cache quantization** (int8 or FP8 KV) halves or quarters KV bytes per token — orthogonal to weight quantization, and covered in the foundations Quantization module. Paged blocks make this clean: you just store lower-precision K/V in each block.\n\n**KV eviction / windowing** drops or offloads KV for tokens unlikely to matter — e.g. sliding-window attention keeps only the last W tokens' KV, and 'H2O'-style heavy-hitter policies keep the tokens attention actually attends to. And under memory pressure, a scheduler can **preempt** a running request: page its KV out to CPU (or recompute it later) to make room, then swap it back — the serving analog of OS page-out. ==The unifying idea is that once the KV cache is paged, it becomes a managed resource you can quantize, share, evict, and swap — instead of a rigid contiguous reservation you can only over-provision.==",
    ],
    keyPoints: [
      "**KV bytes per token ≈ 2 × layers × kv_heads × head_dim × bytes**, accumulated over sequence length and concurrency. It's the dominant VRAM consumer in serving, so how you *store* it determines how many requests fit — not just how big it is.",
      "**Naive contiguous storage wastes most KV memory.** Reserving max-sequence-length per request causes internal fragmentation (short requests hold huge empty reservations) and external fragmentation (variable-length requests leave gaps too small to reuse) — you OOM with the card half-empty.",
      "**PagedAttention applies OS paging to the KV cache.** Fixed-size blocks + a per-request block table map logical tokens to non-contiguous physical blocks allocated on demand, cutting waste to ≤ one partial block per sequence and eliminating external fragmentation.",
      "**Paging is what makes continuous batching scale.** Reclaimed KV memory feeds a larger batch, yielding ~2–4× throughput at the same latency in vLLM — the two techniques are complementary, not alternatives.",
      "**Addressable blocks enable prefix sharing (copy-on-write).** A shared system prompt, few-shot preamble, or beam/parallel-sample branch is stored once; a request copies only the block it diverges on — huge savings when every request carries the same prefix.",
      "**Paging turns KV into a managed resource.** Once blocked, KV can also be quantized (int8/FP8 KV), windowed/evicted (sliding-window, heavy-hitter), or preempted/swapped to CPU under pressure — layered levers on the same memory budget.",
    ],
    recap: [
      "**KV cache = the serving memory ceiling**: bytes/token ≈ 2 × layers × kv_heads × head_dim × bytes, × seq_len × concurrency — storage efficiency decides concurrency.",
      "**Naive contiguous max-length reservation fragments memory**: internal (short requests hold empty reservations) + external (gaps too small to reuse) → OOM while the card looks half-used.",
      "**PagedAttention = OS paging for KV**: fixed-size blocks + block table map logical→physical, non-contiguous, on demand → waste ≤ one partial block/request, no external fragmentation.",
      "**Complements continuous batching**: reclaimed KV memory → bigger batch → ~2–4× throughput at equal latency in vLLM.",
      "**Prefix sharing via copy-on-write**: shared system prompt/few-shot/beam branches stored once, copied only on divergence.",
      "**KV becomes a managed resource**: stack quantization (int8/FP8), windowing/eviction, CPU swap/preemption on top of paging when memory is still tight.",
    ],
    mcqs: [
      {
        question: "A continuously-batched server OOMs at ~14 concurrent short requests even though active KV caches only account for ~12GB of an 80GB card and nvidia-smi shows ~30GB 'used'. What is the primary cause under naive contiguous KV storage?",
        options: [
          "The model weights are being duplicated once per active request as a per-connection safety copy to prevent race conditions during concurrent decode, which explains most of the gap between live KV and 'used' memory.",
          "Fragmentation: naive storage reserves a block sized to the max sequence length, so short requests hold empty reservations, and variable lengths leave gaps too small to reuse — 'used' memory is reservation.",
          "A memory leak in the sampling loop is accumulating logit tensors and attention-score buffers across steps instead of releasing them after each token is sampled, gradually consuming VRAM the allocator never reclaims.",
          "The reported 12GB undercounts the true KV size because the monitoring tool excludes the value vectors and only counts keys, so the real KV footprint including values already matches the full 30GB 'used' figure.",
        ],
        correct: 1,
        explanation: "Option B is correct: naive serving gives each request one contiguous block sized to the maximum possible sequence length. A short request that generates 100 tokens still holds a 2,048-token reservation — internal fragmentation — and the come-and-go of variable-length requests leaves gaps too small for the next contiguous reservation — external fragmentation. So VRAM shows large 'used' while actual live KV is small, and allocation fails early. Option A is impossible — weights are a single fixed allocation, not duplicated per request. Option C invents a sampling-loop leak; the symptom (used ≫ live KV, OOM at low concurrency) is the fragmentation signature, not unbounded monotonic growth. Option D contradicts the stated numbers — the question states active KV genuinely accounts for ~12GB, so the 30GB figure is reservation waste, not undercounted live usage.",
      },
      {
        question: "Select the TWO statements that correctly describe how PagedAttention eliminates KV-cache fragmentation.",
        options: [
          "It splits the KV cache into small fixed-size blocks and maps a request's logical tokens to physical blocks via a per-request block table, similar to OS virtual-memory paging.",
          "It compresses each token's KV vectors with a gzip-style entropy coder tuned to a 4:1 ratio before storing them in HBM, shrinking the footprint independent of the allocation structure.",
          "It moves the entire KV cache to CPU RAM between every decode step over PCIe and streams only the needed 16-token portion back to the GPU on demand each time attention runs.",
          "Physical blocks are allocated on demand from a shared pool and need not be contiguous, so waste is bounded to one partial block per sequence, and external fragmentation disappears.",
        ],
        correct: [0, 3],
        explanation: "Options A and D are correct: PagedAttention borrows OS paging. The KV cache becomes fixed-size blocks (e.g. 16 tokens each); a per-request block table maps the logical token sequence to physical blocks drawn from a shared pool. Because those blocks are allocated on demand, non-contiguous, and uniform in size, internal waste is bounded to the single partially-filled last block per request, and external fragmentation disappears since any free block fits any request. Option B confuses paging with generic byte-level compression — the win here is allocation structure, not entropy coding of the KV values. Option C describes CPU offload/swap, a separate, optional lever for memory pressure; PagedAttention's core mechanism keeps KV on the GPU in paged blocks rather than shuttling it to CPU every step.",
      },
      {
        question: "A serving workload sends thousands of requests that all share the same 500-token system prompt but then diverge. Which PagedAttention capability directly reduces KV memory here, and how does it stay correct when requests diverge?",
        options: [
          "Nothing built into PagedAttention helps here; each request must still store its own full private copy of the shared 500-token system prompt's entire KV cache in separate physical blocks.",
          "KV quantization automatically detects byte-identical prompt token sequences across requests and deduplicates their storage at the block level using a content hash.",
          "Prefix sharing with copy-on-write: block tables from many requests point at the same physical blocks holding the shared prompt; a request copies a block only when it diverges.",
          "Continuous batching merges all requests sharing a prompt into one internal sequence, so the prompt tokens are processed and stored only a single time for the whole batch.",
        ],
        correct: 2,
        explanation: "Option C is correct: paged blocks are addressable and reference-countable, so multiple requests can share the physical blocks that hold a common prefix (the 500-token system prompt) — stored a single time instead of per request. Correctness on divergence is handled by copy-on-write: the first time a request needs to write into a shared block, it copies just that block and updates its own block table, leaving other requests' view intact. Option A is the pre-paging status quo this feature fixes, not a description of what happens with it enabled. Option B conflates two orthogonal levers — quantization lowers precision per element but has no mechanism for detecting or deduplicating identical prompts. Option D is wrong — continuous batching schedules distinct sequences together per step but does not merge them into one shared-storage sequence; prefix sharing via block tables is the actual mechanism that dedupes the prompt.",
      },
    ],
    takeaway: "KV-cache memory, not compute, caps serving concurrency — and naive contiguous, max-length KV storage wastes most of it to internal and external fragmentation, so you OOM with the card half-empty. PagedAttention applies OS-style paging: fixed-size KV blocks mapped by a per-request block table, allocated on demand and non-contiguous, cutting waste to ≤ one partial block per sequence. That reclaimed memory feeds a larger continuous batch (~2–4× throughput at equal latency in vLLM), and addressable blocks enable copy-on-write prefix sharing for common system prompts and beam branches. Once paged, the KV cache becomes a managed resource you can also quantize, window/evict, and swap to CPU under pressure.",
  },

  "infra-serving-stacks": {
    depthTier: "deep",
    interviewWeight: "high",
    groundUp: "The last module turned the KV cache into a managed resource — paged, quantized, evicted, swapped, its waste squeezed down to a few percent. But paging memory more efficiently doesn't shrink the weights themselves, and that's exactly the wall this module starts at: a constraint you can't argue your way out of. Some models are simply too big for a single GPU — a 70B model at fp16 is 140GB of weights, and the biggest single card holds 80GB. So before you pick any fancy serving software, you face a more basic question: *how do you split one model across several GPUs?* Everything else in this module is downstream of that answer.\n\nAnd it turns out there isn't one way to split — there are three, and they trade off against each other. You can slice each layer's matrices across GPUs, or hand different layers to different GPUs, or (for Mixture-of-Experts) scatter the experts around. Each choice buys you something and costs you something: latency, communication, the ability to scale across machines. Getting this right is most of what separates a real answer from \"just use vLLM.\"\n\nWe'll build it from the ground up: first the three ways to split a model and when each wins, then what actually distinguishes the serving stacks (it's kernels and operational cost, not features), then the handful of kernel tricks that explain where latency really comes from, and finally the infrastructure realities — cold starts, autoscaling, build-vs-buy — that decide whether the thing survives contact with real production traffic. Take your time; by the end you'll be able to defend a whole serving architecture on a whiteboard, mechanics and all.",
    scenario: "Now let's put all of that to work on a real one. You must self-host a Llama-3 70B model behind a strict SLA: p99 TTFT under 400ms, p99 inter-token latency under 50ms, at 500 concurrent users. It won't fit on one 80GB GPU at fp16 (140GB of weights). Take a moment before reading on: given what we built, how do you place the model, choose a stack, and defend where the latency comes from? Here's the reasoning, step by step. The model doesn't fit on one card, so parallelism comes first — and because the SLA is tight on inter-token latency, tensor parallelism (TP=8 on an 8×A100 node) is the call: it shards each layer's matrices so the model fits *and* all 8 GPUs work the same token together, with the per-layer all-reduce cheap over in-node NVLink; you'd add pipeline parallelism across nodes only if one node's memory couldn't hold it. Then the stack choice is really about kernels and ops cost, not features: vLLM if you want the pragmatic high-throughput default, TensorRT-LLM if you need the lowest latency from a compiled fused engine (and can pay the per-model engine-build tax), Triton if you're orchestrating a fleet. The latency itself lives in HBM traffic and launch overhead — which is why FlashAttention, kernel fusion, and CUDA graphs matter. Finally, wrap it in leading-signal autoscaling with warm replicas, because cold starts load 140GB of weights and reactive scaling arrives too late.",
    explanation: [
      "First, the model doesn't fit on one card, so the foundational decision is **parallelism** — *how* you split a 70B model across GPUs. There are three axes and they compose.\n\n**Tensor parallelism (TP):** split each layer's weight matrices *across* GPUs — every GPU holds a slice of every layer and they collaborate on each matmul. This is the go-to for fitting a model that's too big for one card and for cutting latency, because all GPUs work on the *same* token simultaneously. The cost: an **all-reduce communication** across GPUs on every layer, so TP wants fast interconnect (NVLink) and usually stays within one node (TP=8 across 8 GPUs). ==TP reduces latency but is communication-heavy — it lives inside a node.==\n\n**Pipeline parallelism (PP):** split the model's *layers* across GPUs — GPU0 holds layers 1–20, GPU1 holds 21–40, etc. A token flows through the pipeline stage by stage. Communication is cheap (just hand off activations between stages), so PP scales *across* nodes — but naively it creates a 'pipeline bubble' (idle stages) unless you keep many microbatches in flight. ==PP scales across nodes cheaply but adds pipeline latency.==",
      "**Expert parallelism (EP)** is the third axis, for **Mixture-of-Experts** models (covered conceptually in the foundations MoE module): distribute the experts across GPUs, and route each token to its selected experts, which may live on other GPUs — an all-to-all communication. It's how you serve a model with a huge total parameter count but a small *active* count per token.\n\nThe practical recipe for a 70B dense model: ==use **tensor parallelism within a node** to fit it and get low latency; add **pipeline parallelism across nodes** only when the model or context is too big for a single node's aggregate memory.== As a general rule of thumb, one 8-GPU node with TP=8 (or TP=4 on a smaller 4-GPU node) comfortably holds a 70B model at fp16/fp8 with room for KV cache, and a tight inter-token SLA typically favors TP's same-token parallelism over PP's cross-stage handoffs.",
      { type: "illustration", label: "Tensor vs pipeline vs expert parallelism", content: `A 70B model, 80 layers, split across GPUs three different ways:

TENSOR PARALLELISM (TP=4): split each layer's matrices across 4 GPUs
  layer L weights  →  [G0 slice][G1 slice][G2 slice][G3 slice]
  all 4 GPUs work the SAME token together; all-reduce EVERY layer
  → best for latency + fitting big models; needs NVLink; stays in-node.

PIPELINE PARALLELISM (PP=4): split the 80 LAYERS across 4 GPUs
  G0: layers 1-20 → G1: 21-40 → G2: 41-60 → G3: 61-80
  token flows stage→stage; cheap comms → scales ACROSS nodes
  → risk: pipeline 'bubble' (idle stages) without enough microbatches.

EXPERT PARALLELISM (EP): for MoE — spread experts across GPUs
  token routed to its top-k experts (may be on other GPUs); all-to-all comm
  → serves huge total params with small ACTIVE params per token.

Real 70B recipe: TP within a node (fit + latency), PP across nodes only if needed.` },
      "With the model placed, the **stack choice** is really a choice about *kernels and operational cost.* All three stacks do continuous batching and PagedAttention-style KV management now; they differ in how hard they push the GPU and how much work they demand of you.\n\n**vLLM:** the pragmatic default. It originated PagedAttention, has excellent continuous batching, wide model coverage, and is easy to stand up. Throughput is excellent; latency is very good but not absolute-best. ==Low operational cost — you're serving in an afternoon.==\n\n**TensorRT-LLM:** NVIDIA's stack that **compiles** the model into a fused, hardware-specific engine ahead of time. It squeezes the lowest latency of the three by aggressively fusing kernels, using CUDA graphs, and exploiting FP8 on Hopper — but you pay for it: you build an engine per model/precision/GPU/batch-shape, tuning is finicky, and iteration is slow. ==Highest performance, highest operational cost.==\n\n**Triton Inference Server:** not primarily an LLM kernel engine but an **orchestration layer** — it serves many models (often *with* a TensorRT-LLM or vLLM backend), handles dynamic batching, versioning, multi-model routing, and ensembles. ==You reach for Triton when you're running a fleet of models, not tuning one.==\n\nTwo more names worth being able to define on sight, since a head-to-head comparison will put them right next to these three: **TGI** (Hugging Face's **Text Generation Inference** server) is another easy-to-stand-up option in vLLM's spirit — a single container with wide model coverage, a reasonable default when you want something running fast. **SGLang** is a serving framework built around **RadixAttention** — a KV-cache-reuse technique that organizes cached prefixes in a **radix tree** so any requests sharing a prefix (a system prompt, a few-shot preamble, branches of the same generation) automatically share that cached KV instead of recomputing it, without you hand-wiring prefix caching yourself. That makes SGLang a strong pick for workloads with heavy prefix overlap or that need richer control flow — parallel generation, structured constraints, agent-style branching.",
      "The kernel-level tricks are *why* TensorRT-LLM can win on latency, and they're worth naming because they apply everywhere.\n\n**FlashAttention** is the biggest one and it's an **IO-awareness** result, not a math change. Standard attention materializes the full N×N attention-score matrix in HBM — for long sequences that's huge memory traffic, and attention is memory-bound. FlashAttention computes attention in **tiles that stay in fast on-chip SRAM**, never writing the giant score matrix to HBM (it uses an online-softmax trick to accumulate without storing all scores). ==Same output, far fewer HBM reads/writes → big speedup and much lower memory, especially at long context.==\n\n**Kernel fusion** merges many small ops (e.g. a matmul + bias + activation, or the whole attention block) into one GPU kernel, so intermediate results stay in registers/SRAM instead of round-tripping through HBM — again attacking memory traffic, the real bottleneck. **CUDA graphs** capture a whole repeated decode step as one launchable graph, eliminating per-op CPU launch overhead that otherwise dominates when each op is tiny (which it is in decode). All three exist because decode is memory-and-launch-bound, so cutting HBM traffic and launch overhead is where the latency lives.",
      "Finally, wrap the stack in the **serving-infra decisions** that determine cost and reliability at real production scale.\n\n**Autoscaling** is hard for LLMs because of **cold starts**: spinning up a new replica means pulling 140GB of weights and loading them onto the GPU — tens of seconds to minutes. ==So you scale on a leading signal (queue depth / tokens-in-flight, not CPU) and keep warm replicas or a warm pool, because reactive scaling arrives too late for a traffic spike.== You also right-size GPUs to the SLA (H100 for tight latency, A100/L40S for cost), and separate prefill-heavy from decode-heavy traffic when it pays (disaggregated serving splits prefill and decode onto different GPU pools so a burst of long prompts doesn't wreck decode latency).\n\nAnd the honest top-level decision — **build vs buy** — belongs to the Production track: a managed API removes all of this operational surface (parallelism, engine builds, autoscaling, cold starts) at a per-token premium; self-hosting wins on cost at high, steady volume, on data control, and when you need a model or configuration no API offers. ==Stack selection is an SLA + hardware + team-capability decision, not a 'which is best' decision.==",
    ],
    keyPoints: [
      "**Parallelism has three composable axes.** Tensor parallelism splits each layer's matrices across GPUs (low latency, all-reduce every layer, stays in-node); pipeline parallelism splits layers across GPUs (cheap comms, scales across nodes, risks pipeline bubbles); expert parallelism distributes MoE experts (all-to-all routing).",
      "**The 70B recipe: TP within a node, PP across nodes only if needed.** Tensor parallelism within a single node fits a 70B model and gives low inter-token latency because all GPUs work the same token; add PP across nodes only when one node's memory is insufficient.",
      "**Stack choice is about kernels and operational cost, not features.** vLLM = easy, high-throughput default (low ops cost); TensorRT-LLM = compiled fused engine, lowest latency, highest ops cost (per-model engine builds); Triton = multi-model orchestration layer, often fronting a vLLM/TRT-LLM backend.",
      "**Kernel wins are IO-awareness wins.** FlashAttention tiles attention in on-chip SRAM and never writes the N×N score matrix to HBM (same output, far less memory traffic); kernel fusion keeps intermediates off HBM; CUDA graphs remove per-op launch overhead — all attacking decode's memory/launch bottleneck.",
      "**LLM autoscaling is dominated by cold starts.** Loading 140GB of weights takes tens of seconds, so scale on a leading signal (queue depth/tokens-in-flight) and keep warm replicas; reactive CPU-based scaling arrives too late. Disaggregating prefill and decode protects tail latency under bursty prompts.",
      "**Stack selection is SLA + hardware + team, and build-vs-buy is upstream.** A managed API removes parallelism/engine/autoscaling/cold-start surface at a per-token premium; self-hosting wins on high steady volume, data control, and unavailable configurations (see the Production track).",
    ],
    recap: [
      "**Three parallelism axes** (compose): tensor → split matrices in a layer, low latency, all-reduce/layer, in-node. Pipeline → split layers across GPUs, cheap comms, cross-node, bubble risk. Expert → spread MoE experts, all-to-all.",
      "**70B recipe**: TP within a node → fit + cut inter-token latency; add PP across nodes only when memory demands it.",
      "**Stacks differ on kernels + ops cost**: vLLM (easy, high throughput, low ops) vs TensorRT-LLM (compiled fused engine, lowest latency, high ops — per-model builds) vs Triton (multi-model orchestration, fronts vLLM/TRT-LLM).",
      "**Kernels = IO-awareness**: FlashAttention tiles attention in SRAM, skips the HBM N×N matrix; fusion keeps intermediates off HBM; CUDA graphs cut launch overhead — all attack decode's memory/launch bottleneck.",
      "**Infra**: autoscale on leading signals (queue depth), keep warm replicas (cold starts load the full 140GB); disaggregate prefill/decode for tail latency; right-size GPUs to the SLA.",
      "**Selection = SLA + hardware + team; build-vs-buy is upstream**: managed API removes ops surface at a per-token premium, self-host wins on steady volume/data control/custom configs.",
    ],
    mcqs: [
      {
        question: "You must serve a 70B model (140GB fp16) under a strict p99 inter-token-latency SLA on a single 8×A100 node. Which parallelism strategy fits the model AND best serves the latency target, and why?",
        options: [
          "Pipeline parallelism across the 8 GPUs: splitting the 80 layers into 8 sequential stages lets each GPU specialize on a narrow slice of the network, which reduces the per-token compute each device performs, roughly quartering step time.",
          "Tensor parallelism (TP=8) within the node: it shards each layer's matrices across all 8 GPUs so the model fits, and since all GPUs work on the SAME token together, the per-layer all-reduce over NVLink minimizes inter-token latency.",
          "Data parallelism: run 8 full copies of the model, one per GPU, so requests load-balance across replicas and no single GPU becomes a throughput bottleneck.",
          "Expert parallelism: distribute portions of the dense 70B model across GPUs as if they were routed experts, since it is the most memory-efficient way to split any transformer of this size.",
        ],
        correct: 1,
        explanation: "Option B is correct: tensor parallelism shards each layer's matrices across the 8 GPUs, which both fits the 140GB model (each GPU holds ~1/8 of the weights) and minimizes latency because all 8 GPUs collaborate on the same token in parallel — the per-layer all-reduce is cheap over the node's NVLink. That's the standard low-latency recipe for a big dense model on one node. Option A is worse for latency — pipeline parallelism makes a token traverse stages sequentially and risks pipeline bubbles; its main advantage (cheap cross-node comms) isn't needed within a single node. Option C is impossible here — data parallelism needs each GPU to hold a FULL 140GB copy, which doesn't fit on an 80GB card. Option D misapplies expert parallelism, which is a Mixture-of-Experts-specific mechanism (routing tokens to a subset of experts); a dense 70B model has no experts to distribute.",
      },
      {
        question: "An interviewer asks why FlashAttention is faster than a standard attention implementation. What is the correct core explanation?",
        options: [
          "It uses a mathematically approximate attention that skips low-weight tokens below a fixed 0.01 threshold, trading a small amount of accuracy for real speed gains on long input sequences overall.",
          "It replaces attention with a linear-time kernel approximation of the softmax function, so total cost grows linearly with sequence length instead of quadratically at long context.",
          "It's IO-aware: standard attention writes the N×N score matrix to HBM, while FlashAttention tiles the computation in SRAM with online softmax, never materializing that matrix in memory.",
          "It offloads the softmax normalization step to the CPU cores so that step can run concurrently with the GPU's matmul work on the following attention head's computation.",
        ],
        correct: 2,
        explanation: "Option C is correct: FlashAttention is an exact algorithm whose speedup comes from IO-awareness, not approximation. Standard attention materializes the N×N attention-score matrix in HBM, and since attention is memory-bandwidth-bound, that traffic dominates. FlashAttention tiles the computation so each tile stays in on-chip SRAM and uses online softmax to accumulate results without ever writing the full score matrix to HBM — producing identical output with dramatically less HBM traffic, with the benefit growing at long sequence lengths. Options A and B describe genuinely different techniques — sparse or linear attention — which trade accuracy or use approximations; FlashAttention is mathematically exact. Option D invents CPU offload of softmax; FlashAttention is entirely a GPU kernel restructuring, not a CPU/GPU split.",
      },
      {
        question: "Your self-hosted LLM service autoscales replicas reactively on GPU utilization. During a traffic spike, latency blows past SLA for ~90 seconds before new replicas help. Select the TWO accurate statements about the root cause and the correct fix.",
        options: [
          "Cold starts are the root cause: bringing a new replica online means loading 140GB of weights onto the GPU, taking tens of seconds — by the time utilization crosses its threshold, the SLA is already violated.",
          "Switching the load balancer from round-robin to a least-connections routing policy would eliminate the delay entirely, since spreading requests more evenly among the existing replicas closes the full 90-second gap.",
          "The correct fix is to scale on a leading signal such as queue depth or tokens-in-flight and keep warm replicas ready, so capacity exists before the queue backs up rather than reacting after utilization spikes.",
          "Upgrading every replica to a larger GPU would remove the need to scale out at all, since one bigger instance can absorb a full traffic spike without ever provisioning additional replicas at all.",
        ],
        correct: [0, 2],
        explanation: "Options A and C are correct together: the 90-second gap is the LLM cold-start problem — a new replica must pull and load 140GB of weights onto the GPU before it can serve, so by the time reactive utilization-based scaling triggers, the spike has already violated the SLA. The fix follows directly from that diagnosis: scale on a leading indicator (queue depth or tokens-in-flight, which rises before utilization saturates) and maintain warm replicas so capacity is ready ahead of demand, not after. Option B is a distractor — load-balancer routing policy among existing replicas doesn't create or remove the weight-loading cold-start delay for a new one coming online. Option D doesn't fix the timing problem either — a bigger GPU still has to load its full set of weights before it's usable, so it reacts just as late to the same utilization-based trigger.",
      },
    ],
    takeaway: "Serving a model too big for one GPU is first a parallelism decision: tensor parallelism (split matrices in a layer, low latency, all-reduce per layer, in-node) fits a 70B model and hits tight inter-token SLAs, with pipeline parallelism added across nodes only when memory demands and expert parallelism reserved for MoE. The stack choice — vLLM (easy, high-throughput default) vs TensorRT-LLM (compiled fused engine, lowest latency, high ops cost) vs Triton (multi-model orchestration) — is really about kernels and operational cost; FlashAttention, fusion, and CUDA graphs win by cutting HBM traffic and launch overhead, the true decode bottleneck. Wrap it in leading-signal autoscaling with warm replicas (because cold starts load the full 140GB) and treat build-vs-buy as an SLA + hardware + team decision, not a 'which is best' one.",
  },

  "infra-edge-ondevice": {
    depthTier: "deep",
    interviewWeight: "medium",
    groundUp: "Let's pick up exactly where the last module left off. Every lever we just built — tensor/pipeline/expert parallelism, vLLM/TensorRT-LLM/Triton, FlashAttention-class kernels, leading-signal autoscaling — assumes a datacenter: multiple GPUs, NVLink between them, and a rack's worth of power and cooling standing behind the request.\n\nNow shrink the target as far as it goes: a single phone. There's no second GPU to shard a 70B model across, no interconnect to all-reduce over, and — this is the part that changes everything — no separate pool of VRAM at all. The model, the OS, and every other app share one small pot of RAM, maybe 8GB total. Pause here: if none of the parallelism or serving-stack tricks from the last four modules survive that shrink, what's actually left to make an LLM run at all?\n\nThe honest answer is that the datacenter recipe doesn't transport — but one idea from much earlier in the curriculum turns out to carry the whole weight: quantization. Not as a nice-to-have speed optimization this time, but as the thing standing between an app that launches and one that doesn't. We'll build it from the ground up: what actually fits in a phone's memory budget, why that arithmetic forces a specific bit-width, what software makes a phone capable of running a transformer at all, and when the honest engineering answer is to not run on-device in the first place. Take your time — by the end you'll be able to watch a 7B model crash on launch and explain exactly why, in one line of arithmetic.",
    scenario: "Now let's put all of that to work on a real one. Product wants an LLM assistant that runs fully ON-DEVICE — a phone or laptop, no server round-trip — for privacy (data never leaves the device), offline use, and zero per-query cost. The target hardware is a phone with 8GB of unified RAM and no datacenter GPU. Someone tries to load a 7B model at fp16 and the app crashes on launch. Take a moment before reading on: given what we just built, why did it crash, and what's the minimal change that makes it launch without gutting quality? Here's the reasoning, step by step. By `params × bytes_per_param`, 7B at fp16 is `7e9 × 2 = 14GB` of weights — bigger than the entire 8GB device, so the app never even gets to run a matmul; it fails to allocate. int8 halves that to 7GB, which technically fits inside 8GB but leaves next to nothing for the OS, the app, and the KV cache — still unstable. int4 gets weights to ~3.5GB, leaving real headroom, which is why on-device inference standardizes on 4-bit with an outlier-aware scheme like NF4 to hold quality at that bit-width. That's the launch fix. The harder ask — quantify what fits and defend when to run locally versus escalate — is a model-selection and architecture question on top of the bit-width one: pick the smallest model (often distilled) that clears the task's quality bar for the budget, run it through an edge runtime that maps onto the phone's best accelerator, and route only the queries it can't handle well — hard reasoning, long context — out to a frontier cloud model.",
    explanation: [
      "The first thing to internalize is that **the datacenter recipe does not transport to a phone.** Everything the previous modules assumed is gone: there's no 80GB HBM, no A100, no NVLink, and — most importantly — a brutal **memory and bandwidth budget.** A phone has ~8GB of *unified* RAM shared between the OS, the app, and the model, with memory bandwidth an order of magnitude below a datacenter GPU, plus **thermal and battery** ceilings that throttle sustained compute. ==On the edge, memory is the wall and quantization is the ladder over it — it's not an optimization, it's the enabling technology.==\n\nRecall the same phase physics: decode is memory-bandwidth-bound. On a phone the bandwidth is low, so tokens-per-second is directly limited by how fast you can stream the (quantized) weights from RAM. Smaller weights don't just fit — they generate *faster*.",
      "The core arithmetic is the same `params × bytes_per_param` from the Quantization module, and it decides everything on-device.\n\nA 7B model at fp16 is `7e9 × 2 = 14GB` of weights — larger than the entire 8GB device, so it can't load (that's the crash). ==Edge inference is therefore a quantization problem first:== `int8 → 7GB` (technically fits but leaves no headroom for the OS, the app, or the KV cache), and `int4 → ~3.5GB` (fits with room to spare for KV cache and the application). This is why on-device work standardizes on **int4** — often 4-bit with tricks like the NF4 non-uniform grid or k-quant mixed-bit schemes — as the sweet spot: aggressive enough to fit and stream fast, careful enough (via outlier-aware methods) to hold quality.",
      { type: "illustration", label: "The on-device memory budget: what actually fits in 8GB", content: `Phone: 8 GB unified RAM (OS + app + model share it). Usable for model ≈ 5-6 GB.

7B model weights by precision:
  fp16 (2 B/param)   → 14.0 GB  ✗ exceeds the whole device — crash on load
  int8 (1 B/param)   →  7.0 GB  ✗ fits weights but no room for OS/app/KV → unstable
  int4 (0.5 B/param) →  3.5 GB  ✓ fits with ~2 GB left for KV cache + app

Match the MODEL to the budget instead of forcing a 7B:
  3B int4  → ~1.5 GB   comfortable, fast, good for on-device assistants
  1B int4  → ~0.5 GB   very fast, tight quality — routing / simple tasks
  8B int4  → ~4.0 GB   near the ceiling; viable on 8-12 GB devices

Edge inference = (int4 quantization) + (small/distilled model) + (edge runtime).` },
      "Beyond weight bits, **model selection and distillation** are the other half of fitting the budget. Rather than force a 7B down to a marginal size, you often pick a **small but capable** model in the 1–8B range purpose-built for edge, or a **distilled** model — a small student trained to mimic a large teacher (the foundations Distillation module) — which packs more capability per parameter than an equally-small model trained from scratch. ==The edge design question is 'what's the smallest model that clears the quality bar for THIS task,' not 'how small can I squeeze the big model.'== Task-specific fine-tunes of a small base frequently beat a heavily-quantized giant on the actual on-device workload while fitting comfortably.",
      "You also need an **edge runtime** — datacenter stacks (vLLM/TensorRT-LLM) don't run here. The canonical ones:\n\n**llama.cpp / GGUF:** a portable C/C++ inference engine and its quantized model format (GGUF), with a family of 'k-quant' schemes (Q4_K_M, etc.). It runs on CPU, mobile, and consumer GPUs, and is the de-facto standard for local LLMs. **MLX** is Apple's framework tuned for **Apple Silicon's unified memory**, exploiting the shared CPU/GPU/Neural-Engine memory so weights aren't copied across a PCIe bus. **ONNX Runtime** and vendor stacks (Qualcomm, MediaTek) target mobile **NPUs** — dedicated neural accelerators that do quantized matmuls at far better performance-per-watt than the CPU, which matters for battery and thermals. ==The runtime's job is to map quantized weights onto whatever accelerator the device has (NPU > GPU > CPU) and to keep everything in unified memory to avoid copies.==",
      "Finally, the honest architecture is usually **hybrid, not purely local.** On-device inference is *cheaper, private, and offline*, but a 1–8B int4 model genuinely can't match a frontier cloud model on hard reasoning, long context, or breadth. The production pattern is a **router**: run the small on-device model for the common, simple, privacy-sensitive, or offline cases, and **escalate to the cloud** for queries it can't handle well. ==You decide per-query where to run based on difficulty, privacy, connectivity, and cost.== The routing signal can be a confidence estimate, a small classifier, or explicit task type. This gives you the privacy/latency/cost wins of on-device for the bulk of traffic while preserving frontier quality for the tail — and it's the same build-vs-buy tradeoff from the Production track, just resolved *per request* instead of once for the whole product.",
    ],
    keyPoints: [
      "**On the edge, memory is the wall and quantization is the enabling technology, not an optimization.** A phone has ~8GB shared unified RAM, low bandwidth, and thermal/battery limits — so weight size decides both what fits and how fast it decodes.",
      "**The `params × bytes` math forces int4.** 7B fp16 = 14GB (crashes an 8GB device); int8 = 7GB (fits but no headroom for OS/app/KV); int4 = ~3.5GB (fits with room for KV cache) — which is why on-device standardizes on 4-bit (NF4/k-quant) with outlier-aware methods to hold quality.",
      "**Match the model to the budget instead of squeezing a giant.** Pick a small capable model (1–8B) or a distilled student that packs more capability per parameter; the question is the smallest model that clears the task's quality bar, not how small you can crush the big one.",
      "**Edge runtimes map quantized weights onto device accelerators.** llama.cpp/GGUF (portable, de-facto standard), MLX (Apple Silicon unified memory), ONNX Runtime / vendor NPU stacks — preferring NPU > GPU > CPU for performance-per-watt and keeping weights in unified memory to avoid copies.",
      "**The real architecture is hybrid with a router.** Run the small local model for common/simple/private/offline queries and escalate hard ones to a frontier cloud model; decide per-query on difficulty, privacy, connectivity, and cost — build-vs-buy resolved per request rather than once.",
    ],
    recap: [
      "**Datacenter recipe doesn't transport**: ~8GB shared unified RAM, low bandwidth, thermal/battery limits → memory is the wall, quantization is the ladder over it (enabling tech, not optimization).",
      "**`params × bytes` decides what fits**: 7B fp16 = 14GB (crash) → int8 = 7GB (no headroom) → int4 = ~3.5GB (fits + KV room) → on-device standardizes on int4 (NF4/k-quant) with outlier-aware methods.",
      "**Match model to budget**: small capable models (1–8B) or distilled students beat a heavily-quantized giant on the actual task while fitting comfortably.",
      "**Edge runtimes**: llama.cpp/GGUF (portable standard), MLX (Apple Silicon unified memory), ONNX Runtime/NPU vendor stacks — prefer NPU > GPU > CPU, keep weights in unified memory to avoid copies.",
      "**Hybrid + router**: small model on-device for common/private/offline/cheap; escalate hard queries to a frontier cloud model — per-query routing on difficulty/privacy/connectivity/cost.",
    ],
    mcqs: [
      {
        question: "An app tries to load a 7B model at fp16 on an 8GB phone and crashes on launch. What's the direct cause, and what's the minimal change that makes on-device inference viable while preserving reasoning quality?",
        options: [
          "The phone's CPU is too slow to execute a 7B model's matmuls in real time at any batch size; the only real fix is waiting roughly two full hardware generations for a faster mobile chip to ship.",
          "fp16 tensors aren't supported by mobile operating systems at all; converting the model to fp32, the actual mobile-native format, resolves the launch crash.",
          "The model needs a live network connection to stream additional transformer layers on demand; enabling background data access permission fixes the crash.",
          "7B fp16 weights are 14GB, bigger than the 8GB device. Quantizing to int4 (~3.5GB) fits with KV-cache headroom, and an outlier-aware scheme like NF4 preserves quality naive rounding loses.",
        ],
        correct: 3,
        explanation: "Option D is correct: by `params × bytes_per_param`, 7B fp16 is 14GB of weights, which exceeds the phone's total 8GB of unified RAM, so it can't even load. Quantizing to int4 brings weights to ~3.5GB, fitting with room for the OS, app, and KV cache — and using an outlier-aware 4-bit scheme (NF4 or llama.cpp k-quants) preserves quality that naive rounding would destroy at low bit-width. Option A misdiagnoses the crash as a speed problem when it's an out-of-memory allocation failure — the app never gets to run any matmuls. Option B is backwards — fp32 is LARGER (4 bytes/param = 28GB), making the problem far worse, and fp16 isn't 'unsupported' on mobile. Option C invents on-demand layer downloading, which defeats the whole offline/private premise and doesn't address the memory allocation failure at launch.",
      },
      {
        question: "A team needs the best possible quality on a specific on-device task within a 4GB memory budget. Which approach is most sound?",
        options: [
          "Choose the smallest model clearing the task's quality bar — a small capable base (1-8B) or a distilled/fine-tuned student, quantized to int4.",
          "Take the largest available model, such as a 70B parameter model, and quantize it all the way down to int2 so its raw scale still fits inside the 4GB memory budget.",
          "Always default to a 7B model at int4 regardless of task difficulty, since 7B has become the fixed industry-standard configuration for on-device LLM deployment everywhere.",
          "Run a 70B model entirely in the cloud and cache every possible response locally so the device itself never needs to perform any on-device inference at all.",
        ],
        correct: 0,
        explanation: "Option A is correct: the edge design principle is to match the model to the budget and the task — pick the smallest model (a small capable base or a distilled student, which delivers more capability per parameter than an equally-small from-scratch model) that meets the quality bar, quantized to int4. That reliably beats forcing a giant model down to a destructive bit-width. Option B is the anti-pattern — int2 degrades quality severely even with smart methods, and a 70B at int2 would be both low-quality and still unwieldy. Option C over-generalizes; the right model size depends on the task's difficulty and the budget, not a fixed 7B. Option D breaks the offline requirement — you can't cache all possible responses to open-ended queries, and pre-caching responses isn't on-device inference at all.",
      },
      {
        question: "A product ships a 3B int4 model on-device but users hit occasional hard reasoning and long-context queries the small model handles poorly. Select the TWO components of the standard production fix that preserves on-device benefits for most traffic while covering the hard tail.",
        options: [
          "Upgrade the on-device model's precision to int8 across the board, since higher precision alone closes the entire capability gap with frontier cloud models.",
          "Route the common, simple, privacy-sensitive, or offline queries to the on-device model, keeping most traffic local for its cost, privacy, and offline benefits.",
          "Escalate the queries the small model handles poorly — hard reasoning, long context — to a frontier cloud model, deciding per request based on difficulty and cost.",
          "Pre-generate and bundle answers to every single anticipated hard query directly inside the app itself so the device never needs to make a single network call at runtime at all.",
        ],
        correct: [1, 2],
        explanation: "Options B and C are correct together: the production pattern is a hybrid router that runs the small local model for the bulk of traffic (common, simple, privacy-sensitive, offline) and escalates only the queries it can't handle well to a frontier cloud model, choosing per request based on difficulty, privacy, connectivity, and cost. Together these keep the privacy, latency, and zero-cost benefits for most requests while preserving frontier quality for the hard tail. Option A is wrong on its own — int8 makes a 3B model slightly better numerically but doesn't turn it into a frontier-quality model; capability is bounded by scale, not just precision. Option D is impossible for open-ended queries — you can't enumerate and bundle answers to unbounded hard reasoning questions, and it defeats the assistant's purpose.",
      },
    ],
    takeaway: "On-device LLM inference is fundamentally a memory problem, and the datacenter recipe doesn't transport: an 8GB phone has shared unified RAM, low bandwidth, and thermal limits, so weight size dictates both what fits and how fast it decodes. The `params × bytes` math forces int4 (7B fp16 = 14GB crashes; int4 = ~3.5GB fits) with outlier-aware 4-bit schemes to hold quality, paired with picking the smallest model — often a distilled student — that clears the task bar, run on an edge runtime (llama.cpp/GGUF, MLX, ONNX/NPU) that maps quantized weights onto the device's best accelerator in unified memory. The honest architecture is hybrid: a router keeps common, private, offline queries local and escalates the hard tail to a frontier cloud model — build-vs-buy resolved per request.",
  },
};
