// Agent-topic RUNNER_DATA — CORE group (spread into foundationsRunnerData.js).
// Gives the Agent Lab modules the same teaching template (scenario/explanation/
// keyPoints/recap/mcqs) as every other Foundations module; the interactive
// component still renders as the hands-on section. Keep export name RUNNER_AGENT_CORE.
export const RUNNER_AGENT_CORE = {
  "agent-react": {
    depthTier: "core",
    interviewWeight: "high",
    groundUp: "Let's start with what a plain language model actually *is*, stripped of hype: it's a single function. Text goes in, text comes out, and that's the whole transaction. Ask it \"what's the status of order #4471?\" and it will happily write a fluent, confident answer — but it never *looked anything up*. It can't. It has no hands. It's guessing from patterns, and when it guesses wrong, it sounds exactly as sure as when it's right.\n\nSo here's the puzzle: how do you take a thing that can only *talk* and turn it into a thing that can *act* — look up the real order, notice the real status, and only then answer? Pause on that, because the answer is simpler than it sounds. You let the model do the one thing it's good at (writing out what it wants to do next), then you — the surrounding program — actually *do* that thing in the real world and hand the true result back. Then it writes its next step against reality instead of a guess.\n\nThat back-and-forth loop is called **ReAct** (Reason + Act), and it's the single most important pattern under essentially every AI agent you'll ever build. We'll take it one slot at a time — who thinks, who acts, and, crucially, *who is allowed to report what actually happened* — because that last question turns out to be where real agents quietly break.",
    scenario: "Now let's put all of that to work on a real one. You ship a support agent that can call a `refund` tool and a `lookup_order` tool. In staging it works. In production it starts issuing refunds for orders it never looked up, and the transcript shows the model wrote 'Observation: order eligible for refund' with its OWN reasoning — the tool was never called. Take a moment before reading on: given the three slots you just learned, which one was violated? Here's the reasoning, step by step. The model is only ever allowed to own the Thought and the Action; the *runtime* owns the Observation, because the Observation is supposed to be a real fact from a real tool run. Here the model wrote text straight into the Observation slot — a fact nobody fetched — and a real refund fired off a hallucination. Notice what that tells you about the fix: this isn't a 'the model lied' problem you solve by scolding it, it's a structural hole. The loop failed to treat the Action as a hard stop point, so the decoder ran straight past the tool call into a made-up Observation. Enforce the stop, and the fabricated fact has nowhere to live.",
    explanation: [
      "Start from the one thing a language model can actually do: a single forward pass turns a prompt into text, and that is the entire transaction. There is no side channel to the world inside that pass — the model cannot look anything up, cannot run code, cannot check a claim against a database. **This means** every fact it emits is reconstructed from patterns in its weights, so the moment a task depends on something the model does not already know (a live order status, a current price), a plain call can only *guess* — and it guesses in the same confident voice it uses for things it does know.\n\n**Therefore**, to make such a model act rather than merely talk, you cannot change the forward pass — you have to wrap a loop around it that lets the model's text *drive* real actions and then feeds the real results back in. That is exactly what **ReAct (Reason + Act)** is: it interleaves two things the model is already good at — writing out reasoning, and emitting structured actions — into a loop that touches the world between steps.\n\n**So** the loop resolves into exactly three roles, repeated until the model emits a final answer:\n\n- **Thought** — the model writes its reasoning as plain text: what it knows, what it still needs, what it will do next.\n- **Action** — the model emits a structured tool call (name + arguments), then *stops generating*.\n- **Observation** — the *runtime*, not the model, executes the tool and feeds the real result back into the context.\n\n**Which means** the single most important invariant falls straight out of *who has access to the world*: only the runtime can run a tool, so only the runtime can produce a truthful Observation. ==The model produces Thoughts and Actions; the runtime produces Observations. The model must never write its own Observations.==",
      "Why interleave reasoning and acting at all, rather than plan everything up front? Because the model does not know the answer to `lookup_order(#4471)` until the tool runs. A pure-planning agent commits to a full plan against facts it is *guessing*; a ReAct agent takes one step, sees the real result, and lets that result reshape the next Thought. **The reasoning is conditioned on observations that did not exist when the reasoning started.** That grounding is what lets ReAct recover from a 404, a wrong assumption, or an empty search — it simply reasons about the observation and tries again.\n\nThe explicit Thought text is not decoration. It is the model externalising its plan token-by-token so the *next* Action is chosen against a written-out rationale rather than an implicit one. Empirically this both improves action selection and gives you a debuggable trace: when the agent misbehaves, the Thought tells you what it *believed*, which localises the fault.",
      { type: "illustration", label: "One ReAct loop — grounded vs fabricated observation", content: `PROMPT: "Is order #4471 eligible for a refund?"

Thought:     I need the order status before I can judge eligibility.
Action:      lookup_order({ id: 4471 })
                 |
                 v   <-- RUNTIME executes the tool, not the model
Observation: { status: "delivered", days_since: 47, policy_window: 30 }   <-- REAL
                 |
                 v
Thought:     Delivered 47 days ago; policy window is 30 days. Not eligible.
Action:      final_answer("Not eligible — outside the 30-day window.")

  --- THE PRODUCTION BUG (fabricated observation) ---

Thought:     The customer seems upset, they probably qualify.
Observation: "order eligible for refund"        <-- MODEL WROTE THIS. No tool ran.
Action:      refund({ id: 4471 })                <-- real action on a hallucinated fact

  The loop failed at the Observation boundary: the model was allowed to
  emit text into the slot the runtime was supposed to own.` },
      "So how do you *stop* the model from writing its own Observations? You do not ask it politely — you enforce it structurally. The runtime treats an Action as a **generation stop point**: the model emits the tool call and the decoder halts. Nothing after the Action token comes from the model on that turn. The runtime executes the tool, appends the *real* result as the Observation, and only then resumes generation. With modern function-calling APIs this is native — the model returns a `tool_calls` object and the turn *ends*; there is no channel for it to also produce an observation. ==The staging-vs-production bug in the scenario is almost always a home-grown ReAct loop that parsed free-text and forgot to stop generation at the Action, letting the model run straight through into a made-up Observation.==",
      "The loop needs three guardrails or it fails in predictable ways. **First, a max-step limit.** Nothing in the pattern guarantees termination — a model that keeps deciding it needs 'one more search' will loop until it exhausts the budget. A hard cap (e.g. 8–12 steps) with a graceful 'I could not complete this' is mandatory. **Second, reliable tool outputs.** ReAct's grounding is only as good as its Observations; a tool that returns ambiguous or malformed output corrupts the very facts the reasoning is conditioned on. **Third, precise tool schemas.** The model chooses Actions from tool descriptions; a vague schema produces wrong-tool and wrong-argument Actions (this is exactly why tool design is its own module).",
      "The three failure modes map cleanly onto the three roles, and this is the debugging payoff. A **bad Thought** is a planning failure — the model reasoned to the wrong next step (wrong tool chosen for the right reason, or a flawed decomposition). A **bad Action** is an interface failure — right intent, malformed call (wrong tool, wrong arguments, hallucinated tool name). A **bad Observation** is an execution/parsing failure — the tool errored, returned junk, or (worst case) the model was allowed to fabricate it. ==When an agent misbehaves, the first question is never 'is the model dumb' — it is 'which of the three slots is wrong,' because each has a different fix: better prompt/planning, better schema, or better tool reliability + a hard stop at the Action boundary.==",
      "ReAct is the architectural primitive under essentially every production agent framework — LangGraph, the OpenAI Agents SDK, Google's ADK all implement this loop or a close variant (some add explicit planning or reflection steps, but the Thought→Action→Observation spine remains). Understanding it at the level of *who owns each slot* and *where generation stops* is what separates diagnosing an agent in minutes from guessing for hours. The interactive lets you step through a real trace and inspect what the model is actually doing at each stage — use it to internalise the Observation-ownership boundary, since that is the invariant most home-grown loops violate.",
    ],
    keyPoints: [
      "**ReAct interleaves Reason and Act into a loop:** Thought (reasoning as text) → Action (structured tool call) → Observation (real tool result) → repeat until final answer. This turns a one-shot LLM call into a grounded agent.",
      "**The model owns Thought and Action; the RUNTIME owns Observation.** The model must never write its own Observation — that is a fabricated fact driving a real action, the classic production failure.",
      "**Interleaving beats up-front planning** because the model can't know a tool's result until it runs. Each Thought is conditioned on Observations that didn't exist when planning began — that grounding is what lets ReAct recover from errors.",
      "**The Action is a generation stop point.** Modern function-calling ends the turn at the tool call; home-grown loops that parse free text and forget to stop are what let the model run straight into a made-up Observation.",
      "**Three mandatory guardrails:** a max-step limit (nothing guarantees termination), reliable tool outputs (grounding is only as good as Observations), and precise tool schemas (Actions are chosen from descriptions).",
      "**Failure localises to a slot:** bad Thought = planning fault (fix prompt), bad Action = interface fault (fix schema), bad Observation = execution/fabrication fault (fix tool reliability + stop at Action). Knowing which slot failed is the whole debugging win.",
    ],
    recap: [
      "**ReAct = Thought → Action → Observation**, looped until a final answer — the primitive that turns an LLM into an agent.",
      "**Model owns Thought + Action; runtime owns Observation.** A fabricated Observation is the #1 production bug.",
      "**Grounding via interleaving:** each Thought conditions on a real prior Observation — recovers from a 404 or wrong guess.",
      "**Action = a generation stop point** (native in function-calling) — nothing lets the model also write the Observation.",
      "**Guardrails:** max-step cap, reliable tool outputs, precise schemas. Debug by naming which of the three slots failed.",
    ],
    mcqs: [
      {
        question: "In a ReAct loop, an agent's transcript shows a line 'Observation: order is eligible for refund' immediately followed by a real refund action — but the order-lookup tool was never actually invoked. What went wrong architecturally?",
        options: [
          "The model's reasoning was too weak here — a substantially larger model would likely avoid this specific mistake",
          "The tool's JSON schema had six parameters, so the model silently skipped the call to save on generated tokens",
          "The runtime let the model write into the Observation slot instead of halting at the Action for the tool to fill it",
          "The max-step limit was set to fifty, letting the loop run many extra turns until it drifted into a fabricated state",
        ],
        correct: 2,
        explanation: "Option 3 is correct: the invariant of ReAct is that the model owns Thought and Action while the runtime owns Observation. Here the model was allowed to write its own Observation because the loop did not treat the Action as a hard generation stop point — so a hallucinated 'fact' drove a real action. This is the classic home-grown-loop bug (parsing free text without halting at the tool call). Option 1 is wrong because model size does not fix a structural hole; even a strong model will happily fill an Observation slot left open to it. Option 2 is wrong because parameter count does not cause fabricated observations, and the model did not 'skip to save tokens' — it emitted an observation it was structurally permitted to emit. Option 4 is wrong because a high max-step limit governs how many loops run, not whether the Observation boundary is enforced; the fault is at the Action/Observation boundary, not step count.",
      },
      {
        question: "Why does ReAct interleave reasoning and acting step-by-step rather than have the model produce a complete plan up front and then execute it? Which two statements are correct?",
        options: [
          "The model cannot know a tool call's result until the tool runs, so a full plan up front commits to guessed facts",
          "Each Thought is conditioned on real Observations absent when planning began, so the loop recovers from a 404 or wrong guess",
          "Interleaving uses roughly forty percent fewer total tokens than producing one complete plan and then executing it end to end",
          "Interleaving removes the need for tool schemas, since the model picks each action dynamically with no fixed contract at all",
        ],
        correct: [0, 1],
        explanation: "Options 1 and 2 are both correct: a tool's output is unknown until it actually runs, so an up-front plan necessarily commits to guessed facts (option 1) — whereas ReAct lets each new, real Observation reshape the next Thought, which is exactly how the loop recovers from a 404, an empty search, or a wrong assumption (option 2). Option 3 is wrong — interleaving is not primarily a token-saving technique; it typically uses more tokens via repeated context, and its value is correctness through grounding, not efficiency. Option 4 is wrong — ReAct still depends heavily on precise tool schemas; dynamic action selection makes good schemas more important, not unnecessary.",
      },
    ],
    takeaway: "ReAct turns a one-shot LLM call into an agent by looping Thought → Action → Observation, where the model owns Thoughts and Actions and the runtime owns Observations. Interleaving grounds each reasoning step in real tool results; enforce it by stopping generation at the Action, and guard it with a max-step cap, reliable tools, and precise schemas. Debug by asking which of the three slots failed.",
  },
  "agent-tool-design": {
    depthTier: "core",
    interviewWeight: "high",
    groundUp: "Let's start with a question that quietly decides whether your agent works: when a model reaches for a tool, what is it actually *looking at* to make that choice? Not your code — it never sees your code. It sees a little text card called a **tool schema**: the tool's name, a one-line description of what it does, and a list of the arguments it takes. That card, and only that card, is everything the model knows about the tool.\n\nSit with how much that changes things. If you name a tool `search` and describe it as \"Search for things,\" you haven't given the model a bad implementation — the code underneath might be flawless. You've given it a bad *card*. It has no way to know what the tool searches, when it's the right choice, or when it absolutely shouldn't be used. So it does the natural thing: it fires the vague tool at everything, including questions it should have just answered itself.\n\nHere's the reassuring part, and the whole spirit of this module: because the model's entire view of a tool is that little text card, most agent reliability problems that *look* like \"the model is dumb\" are really \"the card was underspecified\" — and you fix them by editing text, not by retraining anything. We'll walk through each field of the card — name, description, parameters, even the shape of the errors it returns — and see how a single well-written line changes whether, when, and how the model calls.",
    scenario: "Now let's put all of that to work on a real one. Your HR agent has a tool named `search` described as 'Search for things.' Users report it answering benefits questions with fabricated policy numbers, and calling `search` for questions it should answer from general knowledge. The model is fine; you didn't change it. The regression came from adding three more tools last sprint, which made 'search for things' ambiguous against them. Take a moment before reading on: given that the schema is the model's entire view of the tool, where does the fix have to live — and what single line would do the most work? Here's the reasoning, step by step. You can't touch the model, and you don't need to: the over-triggering comes straight from a description that says nothing about *what* is searched, *when* to use it, or — most importantly — *when not to*. Notice that the highest-leverage change isn't a new tool or a bigger model, it's one negative clause in the description ('do NOT use for general knowledge') plus a specific name that identifies the corpus, so the model stops treating a vague 'search for things' as a catch-all and stops colliding with the three tools you added last sprint.",
    explanation: [
      "Begin from a single constraint about how a model calls a tool at all: it never sees your code. All it receives is a **tool schema** — a name, a description, and a typed parameter list — and it must choose its next action purely from that text. **Because** that text is the model's entire view of the tool, the schema is not documentation sitting beside the real thing; it *is* the interface through which your design intent reaches the model's behaviour. **Which means** if the model chooses badly, the flawless code underneath is irrelevant — the model never read it. ==So the overwhelming majority of agent reliability problems that look like 'the model is dumb' are actually 'the schema underspecified the tool,' and they are fixed by editing text, not weights.==\n\n**This forces** a clean taxonomy of what can go wrong, because a tool call has only three degrees of freedom and each traces back to the schema: the model calls the **wrong tool** (name/description didn't disambiguate), passes **wrong arguments** (parameter descriptions didn't constrain), or calls **at the wrong time** (no when-to-use / when-not-to-use guidance). **Therefore** every failure this module walks through is a schema-authoring failure, and the rest of this module walks each field of the schema in the order the model reads it.",
      "**The description is where the model decides *whether* to call the tool.** A description like 'Search for things' tells it nothing about what corpus is searched, so the model treats it as a catch-all and fires it for everything — including questions it should answer from parametric knowledge. The fix is to make the description carry three things: *what* the tool does over *what data*, *when to use it*, and explicitly *when NOT to use it*. That negative guidance is the single highest-leverage line in a schema — it's what stops the over-triggering in the scenario.",
      { type: "illustration", label: "Same tool, weak vs strong schema", content: `WEAK — the model has no basis to choose or constrain the call:
{
  "name": "search",
  "description": "Search for things",
  "parameters": { "query": { "type": "string" } }
}
  -> fires for everything (no when/when-not), sends vague queries
     ("expenses"), can't anticipate the response shape.

STRONG — every field steers behaviour:
{
  "name": "search_internal_kb",
  "description": "Search the internal HR & policy knowledge base.
     USE for company policies, benefits, leave, expenses.
     Do NOT use for general knowledge or real-time data.",
  "parameters": {
    "query": {
      "type": "string",
      "description": "Precise topic, e.g. 'remote meal expense
         policy 2024' — not 'expenses'."
    },
    "top_k": {
      "type": "integer",
      "description": "3 for most queries, 5 for complex multi-part.",
      "default": 3
    }
  }
}
  -> specific name disambiguates; 'Do NOT' stops over-calling;
     query hint forces precise retrieval; top_k default is sane.` },
      "**Parameter design is where the *arguments* come from.** Each parameter needs a type and a description that constrains what a good value looks like — a `query` field with the hint 'be precise, e.g. remote meal expense policy 2024 not expenses' improves retrieval, because the model imitates the example. Prefer **enums over free strings** wherever the value space is closed (a `status` param as an enum of `open|pending|closed` can't be hallucinated the way a free string can). Give **sane defaults** so the model can omit params it's unsure about rather than inventing them. And keep the parameter *count* low: every extra required argument is another thing the model can get wrong.",
      "**Granularity is an architectural choice, not a detail.** Too coarse — one mega-tool `manage_account(action, ...)` that does ten things via a mode flag — forces the model to reason about a huge branching argument space and fails at picking the right mode. Too fine — fifty micro-tools — floods the model's selection space so it can't tell them apart (this is exactly the scenario's regression: adding tools made an ambiguous name collide). The rule of thumb: **one tool = one clear verb over one clear noun**, named so specifically that its purpose is unmistakable next to its neighbours. If two tools could plausibly answer the same request, at least one is misnamed or mis-scoped.",
      "**Error handling is part of the schema's contract, because the model reasons over what a tool returns.** A tool that throws an opaque stack trace or a bare `null` gives the ReAct loop a useless Observation — the model can't tell 'no results' from 'the service is down' from 'bad argument,' so it either gives up or retries blindly. Well-designed tools return **structured, actionable errors**: `{ error: 'not_found', message: 'No order #4471', suggestion: 'verify the order id' }`. That turns a dead end into a recoverable step — the model reads the suggestion and self-corrects. ==Design tool outputs (success AND failure) as messages *to the model*, not as internal logs.==",
      "The **Model Context Protocol (MCP)** standardises this whole interface. Instead of re-writing a bespoke tool integration for every LLM client, you expose tools once from an MCP *server* and any MCP-speaking client can call them — the schema/description/parameter grammar above is exactly what MCP formalises. The design principles don't change; MCP just makes a well-authored tool reusable across frameworks. The interactive lets you diff a weak schema against a strong one field-by-field and step through sequential/parallel/conditional calling patterns — use it to feel how a single description line changes when and whether the model calls at all.",
    ],
    keyPoints: [
      "**The schema is the model's entire view of a tool** — name, description, typed params. It's a specification interface, not docs; most 'the model is dumb' failures are actually underspecified schemas fixed by editing text, not weights.",
      "**The description decides WHETHER to call.** Include what/over-what-data, when to use, and explicitly when NOT to use. The negative 'Do NOT use for…' line is the highest-leverage line for stopping over-triggering.",
      "**Parameter descriptions decide the ARGUMENTS.** Constrain with concrete examples, prefer enums over free strings for closed value spaces, give sane defaults, and minimise required-param count.",
      "**Granularity: one tool = one clear verb over one clear noun.** Mega-tools with mode flags overload the argument space; too many micro-tools collide on selection. If two tools could answer the same request, one is misnamed.",
      "**Errors are messages to the model, not logs.** Return structured, actionable errors ({error, message, suggestion}) so the ReAct loop can distinguish not-found from down from bad-arg and self-correct instead of failing blindly.",
      "**MCP standardises the tool interface** so a well-authored tool is reusable across any client. The design principles are unchanged — MCP just formalises the schema grammar and makes tools portable.",
    ],
    recap: [
      "**Schema = the model's only view of a tool** — reliability lives in schema authoring, not model weights.",
      "**Description decides WHETHER to call** — the highest-leverage line is an explicit 'Do NOT use for…' clause.",
      "**Parameters decide the ARGUMENTS** — constrain with examples, prefer enums, give defaults, keep count low.",
      "**Granularity: one verb, one noun** — avoid mode-flag mega-tools and colliding micro-tools alike.",
      "**Structured, actionable errors** let the agent recover; MCP makes a well-authored schema portable across clients.",
    ],
    mcqs: [
      {
        question: "An HR agent's `search` tool (described only as 'Search for things') keeps getting called for general-knowledge questions it should answer directly, and returns fabricated policy numbers. What is the single highest-leverage schema change to stop the over-triggering?",
        options: [
          "Rename the tool to search_v2 so the model treats it as a fresh capability carrying none of the old baggage",
          "Raise the model's sampling temperature so it explores non-tool answers more often instead of defaulting to search",
          "Bolt on ten more optional parameters so the model has extra knobs to try to specify its intent more precisely",
          "Add explicit use-for and do-not-use-for lines to the description plus a name that names the actual corpus",
        ],
        correct: 3,
        explanation: "Option 4 is correct: the description is where the model decides whether to call a tool at all, and the missing piece is negative guidance — an explicit 'do NOT use for general knowledge' clause plus a specific name that identifies the corpus. That directly stops the model from treating a vague 'search for things' as a catch-all. Option 1 is wrong because a version-bumped name still carries no when/when-not semantics; the model has no more basis to choose correctly. Option 2 is wrong because temperature governs sampling randomness, not tool-selection policy; raising it makes behaviour less reliable, not more, and does nothing about the missing description guidance. Option 3 is wrong because more parameters expand the space of things the model can get wrong; the fix is clearer routing guidance in the description, not more knobs.",
      },
      {
        question: "A tool currently returns a bare `null` when an order isn't found. In a ReAct agent, why is returning a structured error like `{ error: 'not_found', message: 'No order #4471', suggestion: 'verify the order id' }` materially better? Which two statements are correct?",
        options: [
          "A structured error lets the model tell not-found apart from service-down and bad-argument, not one opaque null",
          "The suggestion field gives the model something concrete to act on, turning a dead end into a recoverable next step",
          "It shaves the average token count of every single call by roughly a third, noticeably lowering the cost of running the agent",
          "It stops the underlying tool from ever failing again in production, since the errors are formatted instead of simply raised",
        ],
        correct: [0, 1],
        explanation: "Options 1 and 2 are both correct: in ReAct the tool's return value becomes the Observation the model reasons over, so a structured error must distinguish failure types (not-found vs down vs bad-argument) that a bare null collapses into one ambiguous signal (option 1) — and a concrete 'suggestion' field gives the model something to act on, turning a dead end into a recoverable step rather than a blind retry or a give-up (option 2). Option 3 is wrong — the benefit is recoverability and correctness, not token savings; structured errors are usually longer than a bare null. Option 4 is wrong — error formatting doesn't prevent failures, it makes failures legible to the model.",
      },
    ],
    takeaway: "A tool's schema — name, description, typed params — is the model's entire view of it, so reliability is authored there, not in the model. Put when/when-NOT guidance in the description (decides whether to call), constrain arguments with examples/enums/defaults, keep one tool = one verb over one noun, and return structured actionable errors so the agent can recover. MCP standardises this interface so good tools are reusable across clients.",
  },
  "agent-memory-foundations": {
    depthTier: "core",
    interviewWeight: "high",
    groundUp: "Here's a fact that sounds impossible the first time you hear it: a language model has **no memory at all**. None. Every time you call it, it wakes up with total amnesia, reads whatever text you handed it *this instant*, produces a reply, and forgets everything the moment it finishes. The 'conversation' you seem to be having with a chatbot is an illusion your application maintains by quietly re-sending the whole transcript on every single turn.\n\nSit with that for a second, because it flips the usual question inside out. The interesting puzzle is not \"how does a model remember?\" — it *can't*. The puzzle is: given a thing with no memory, how do you *engineer* the appearance of one, cheaply enough to run all day and reliably enough that it doesn't contradict itself an hour in? That turns out to have several different answers, each with its own price tag — and the whole art is picking the right one for the right kind of information.\n\nDon't worry if 'context window', 'retrieval', and 'fine-tuning' currently feel like a jumble of buzzwords. We'll build them up one at a time, starting from that single hard fact — the model is stateless — and everything else will follow as a *consequence* of working around it. By the end the coding agent that mysteriously starts contradicting itself after twenty turns will look completely inevitable, and you'll know exactly which lever fixes it.",
    scenario: "Now let's put all of that to work on a real one. Your coding agent runs beautifully for 20 turns, then starts contradicting decisions it made earlier *in the same session*, re-reading files it already read, and finally throws a context-length exception. A teammate shrugs and says 'just fine-tune it on the conversation.' Take a moment before reading on: given what you now know about *where* memory lives, which layer failed, and why is fine-tuning aimed at the wrong one? Here's the reasoning, step by step. The context-length exception isn't a bug — it's the working-memory scratchpad hitting its hard token cap, the *expected* behaviour of a finite buffer used as if it were infinite. The contradictions and the re-reads are the same story one level up: important information (an earlier decision, a file already seen) fell *out* of the live context, or was never managed to stay in it, so on this turn the model simply cannot attend to it. Notice what that rules out: nothing here is a reasoning failure the model could 'try harder' to fix, and nothing is repaired by baking the transcript into weights — the conversation changes every turn, and weights can't be edited per turn. The failure is working-memory *management*, so the fix lives there: summarize the old turns, retrieve the relevant ones on demand, and keep decisions and constraints inside the scratchpad on purpose.",
    explanation: [
      "Start from the single hard constraint that everything else is a workaround for: a transformer has **no persistent state between calls**. Each forward pass sees only the tokens in the prompt *right now* and nothing from any previous call. **Because** of that, 'memory' cannot be a property of the model — it has to be an engineering pattern layered on top of a fundamentally stateless function. **This means** the word 'memory' is doing too much work: there are several distinct mechanisms hiding under it, and they differ sharply in capacity, persistence, and cost, so treating them as one thing is the root cause of most scaling failures.\n\n**So** the first and most important split is **working (short-term) memory vs long-term memory**, and it falls straight out of the constraint. Working memory is the one place the model can actually *attend*: the **context window itself** — the tokens currently in the prompt. It's the scratchpad, everything live this turn. Long-term memory is, by definition, everything that has to *outlive* a single call — conversation history you re-inject, facts kept in an external store and retrieved on demand, or knowledge baked into the weights. **Which means** long-term memory is never 'live' on its own: to be used at all, it must be pulled back *into* working memory first.",
      "**The context window is a scratchpad, and scratchpads have three brutal properties.** It is *finite* (a fixed token budget — 128k, 200k, whatever the model gives you); it is *volatile* (nothing in it survives once you start a new call unless you re-send it); and it is *the most expensive memory you have*, because you pay per token, every turn, for everything you keep in it. ==The scenario's context-length exception is not a bug — it's the scratchpad filling up, which is the *expected* behaviour of a finite working memory used as if it were infinite.==\n\nThis reframes the coding agent's failures. Contradicting earlier decisions and re-reading files aren't reasoning failures — they're symptoms of important information falling *out* of the working set (or never being managed within it). The question is never 'why is the model forgetting' but 'how am I managing what stays in the scratchpad.'",
      { type: "illustration", label: "Where 'memory' lives, and the cost/persistence of each", content: `                         PERSISTENCE     COST TO USE       CAPACITY
WORKING (in-context)
  the prompt tokens        this call only  $$$ per-token      tiny (token budget)
  = the scratchpad         volatile        EVERY turn        128k-ish, hard cap

LONG-TERM (outlives the context)
  conversation history     re-injected     $$ (re-send it)   grows unbounded
  external store + RAG      durable         $ (retrieve k)    effectively infinite
  weights (fine-tune)      permanent        ~free at infer    fixed, baked in

Reading the table:
  * Only working memory is 'live' — the model attends to it directly.
  * Everything long-term must be RE-INJECTED into working memory to be used.
  * As a session grows you MUST decide what to keep in the scratchpad,
    what to summarise, and what to push to a store and retrieve on demand.` },
      "Because working memory is finite and expensive, agents that run past a demo need a **context-management strategy** — a policy for what stays raw, what gets compressed, and what gets evicted. The main levers: **truncation** (drop the oldest turns — cheap, but you lose them), **summarization** (compress older turns into a short synopsis and keep the synopsis in context — you preserve the gist at a fraction of the tokens), and **retrieval** (push the full history to an external store and pull back only the k chunks relevant to the current turn). Real systems combine these: keep the last few turns verbatim, a rolling summary of everything before that, and retrieve specifics on demand.",
      "**Summarization is the workhorse and it has a sharp trade-off.** Compressing 30 turns into a 200-token synopsis keeps you under budget, but summarization is *lossy* — the detail you drop is gone from working memory. If the coding agent summarised away 'we decided NOT to use library X for reason Y,' it will happily re-propose X later. So the skill is choosing *what* to preserve: decisions, constraints, and commitments are high-value to keep; verbose intermediate output is safe to compress. ==A summary is a bet about what future turns will need; a bad summary manifests as an agent that contradicts its own earlier reasoning.==",
      "**Retrieval is how you get 'effectively infinite' memory without paying to hold it in context.** You store history/facts in an external store (typically embedded into a vector store), and each turn you embed the current query, pull the top-k most relevant chunks, and inject *only those* into the scratchpad. This is the pattern that scales: the store grows without bound, but per-turn context stays small and cheap. The failure mode moves, though — now it's a **retrieval** problem: pull the wrong chunks and the model reasons over irrelevant context; pull too few and it's missing the fact it needed. 'The agent gave a wrong answer' becomes 'did working-memory management fail, or did retrieval fetch the wrong thing?' — two different fixes.",
      "This is why the teammate's 'just fine-tune it' is the wrong lever. **Fine-tuning writes information into the weights — permanent, near-free at inference, but static.** It's right for stable skills and style, catastrophically wrong for information that changes every turn: you cannot fine-tune 'the user just said their name is Priya' into the weights mid-conversation, and you shouldn't fine-tune facts that change frequently because the weights can't be edited per-turn. Session-scoped, fast-changing information belongs in working memory and long-term stores, not in the weights. ==Matching the mechanism to the information's change-rate and lifespan is the entire discipline.== The interactive lets you compare each memory type's capacity, persistence, and cost side-by-side — use it to build the reflex of asking 'which memory type is this, and is it the right one' before reaching for any single lever.",
    ],
    keyPoints: [
      "**Transformers are stateless between calls** — everything an agent 'remembers' is an engineering pattern on top of that. 'Memory' is not one thing; the first split is working (in-context) vs long-term (outlives the context).",
      "**Working memory = the context window = a scratchpad** that is finite (hard token cap), volatile (gone unless re-sent), and the most expensive memory (paid per token, every turn). Context-length errors are the scratchpad filling up, not a bug.",
      "**Long-term memory must be re-injected to be used.** Conversation history, external stores + retrieval, and weights all live outside the live context; only working memory is directly attended to.",
      "**Scaling past a demo requires a context-management strategy:** truncation (drop oldest), summarization (compress older turns into a synopsis), and retrieval (store everything, pull back top-k). Real systems combine all three.",
      "**Summarization is lossy — it's a bet about what future turns need.** Preserve decisions/constraints/commitments; compress verbose intermediate output. A bad summary shows up as an agent contradicting its own earlier reasoning.",
      "**Match the mechanism to the information's change-rate:** fast-changing/session-scoped facts belong in working memory + stores, not the weights. Fine-tuning is for stable skills/style, never for per-turn facts.",
    ],
    recap: [
      "**LLMs are stateless — all memory is layered on.** First split: working (in-context) vs long-term (outside it).",
      "**Working memory = the context window** — finite, volatile, per-token-expensive. Context-overflow = scratchpad full.",
      "**Long-term memory must be re-injected** into working memory to be used — nothing outside the prompt is live.",
      "**Context management = truncation + summarization + retrieval.** Summaries are lossy bets: keep decisions, drop verbosity.",
      "**Fine-tuning suits stable skills, never per-turn facts** — match the mechanism to the info's change-rate.",
    ],
    mcqs: [
      {
        question: "A coding agent runs fine for 20 turns, then starts contradicting earlier decisions and eventually throws a context-length exception. A teammate says 'let's fine-tune it on the conversation.' Which two statements give the correct diagnosis and lever?",
        options: [
          "The context window is a paid, volatile scratchpad, so the exception is it filling as facts fall out of live context",
          "The fix is a context strategy: summarize old turns, retrieve relevant chunks on demand, truncate what's safe to lose",
          "Fine-tuning the model nightly on that day's full transcript teaches it to remember and is the correct fix for this case",
          "The model's weights silently degraded partway through the run, so restarting the model process resolves the behavior",
        ],
        correct: [0, 1],
        explanation: "Options 1 and 2 are both correct: the context window is working memory — finite, volatile, and paid per token — so a context-length exception is the scratchpad filling, and contradictions come from important information falling out of (or never being managed within) the live context (option 1). The right lever is therefore a context-management strategy: summarize older turns, retrieve relevant chunks on demand, and truncate what's safe to lose (option 2). Option 3 is wrong because fine-tuning writes into weights permanently and near-free at inference but is static; you cannot bake per-turn, session-scoped facts into weights mid-conversation. Option 4 is wrong because weights don't corrupt mid-session — the model is stateless and behaving as expected under a full context.",
      },
      {
        question: "Your agent uses summarization to stay under its token budget: it compresses older turns into a short synopsis. It later re-proposes a library the team had explicitly rejected earlier in the session. What does this reveal about summarization?",
        options: [
          "Summarization should never be used at all here; only full raw-history truncation is safe for an agent like this one",
          "The retrieval store fetched the wrong chunks this turn, which is the only explanation the transcript supports",
          "Summarization is a lossy bet about future needs, and it dropped the high-value do-not-use-library-X decision",
          "The model hallucinated the re-proposal from nothing; summarization played no role in what happened here",
        ],
        correct: 2,
        explanation: "Option 3 is correct: summarization compresses many tokens into few and is inherently lossy, so it functions as a bet about what future turns will need. Decisions, constraints, and commitments (like 'do NOT use library X for reason Y') are high-value and must be preserved; when a summary drops them, the agent loses the basis for its earlier reasoning and re-proposes the rejected option. Option 1 is wrong because truncation drops the oldest turns entirely and is generally worse at preserving important old decisions than a well-chosen summary; the fix is summarising better, not abandoning it. Option 2 is wrong because the agent here relies on summarization for history, not retrieval; misattributing it to retrieval points at the wrong subsystem. Option 4 is wrong because this isn't free-floating hallucination — the model simply no longer has the decision in its working memory because the summary evicted it.",
      },
    ],
    takeaway: "Transformers are stateless, so all agent memory is layered on: working memory is the context window — a finite, volatile, per-token-expensive scratchpad — and long-term memory (history, external stores, weights) must be re-injected to be used. Scaling past a demo means a context-management strategy (truncate/summarize/retrieve); summaries are lossy bets, so preserve decisions and constraints. Match the mechanism to the info's change-rate: fine-tuning for stable skills, never for per-turn facts. Next: the Agent Memory · Libraries module picks up the long-term side of this split and shows you the production-hardened libraries (Mem0, Zep, LangMem, MemGPT/Letta) that implement it, plus a further distinction — episodic vs semantic memory — you'll need once a naive vector store isn't enough.",
  },
  "agent-memory-libraries": {
    depthTier: "core",
    interviewWeight: "medium",
    groundUp: "You've given your agent a reasoning loop and well-designed tools, and you know the fundamental split from the memory foundations module: the context window is live but finite, and anything that must outlast a single call has to be stored elsewhere and re-injected to be used. So here's the puzzle. A user tells you their timezone. Weeks later they move and mention the new one. You stored every message the obvious way — converting each into numbers that capture its meaning, so you can later fetch the few stored entries most similar to a question. That meaning-based lookup is **semantic retrieval**, and the database that does it is a **vector store**. Now ask 'what timezone is this user in?' and it returns both answers, old and new, ranked by similarity. Nothing in the system knows that one fact replaced the other.\n\nThat isn't a retrieval bug — it's the ceiling of what similarity search can do. A vector store is storage plus lookup, nothing more: no notion of time, identity, change, or importance. And long-term memory turns out to come in two kinds that need different machinery. Records of specific past events — what a user asked about weeks ago — are **episodic memory**: inherently temporal, scoped to a user. Distilled durable facts — standing preferences, things true about a user until they change — are **semantic memory**: stored once, updated when they change. A flat pile of embedded chunks fails both at once, duplicating the durable facts and jumbling the timeline.\n\nThe missing machinery does four specific jobs: distilling raw turns into structured facts instead of transcript (**extraction and consolidation**), letting a new fact supersede a contradicted old one (**conflict resolution**), stamping memories with when they happened so recency can count (**temporal awareness**), and expiring stale or low-value entries so the store stays sharp (**TTL**, time-to-live, paired with importance scoring). You could build all of that yourself — and rediscover each of those four jobs the hard way — but production-hardened **memory libraries** already package it. **Mem0** and **LangMem** focus on cross-session recall and personalization; **Zep** adds a time-aware knowledge graph for when relationships between facts matter; **MemGPT/Letta** treats the context window like an operating system's paged memory, deciding for itself what to swap in and out.\n\nThis module turns that landscape into a decision procedure: an escalation ladder that starts with no memory system at all, adds a library only when cross-session recall or personalization demands it, and reaches for a custom vector store only when access control, bespoke metadata filtering, or scale forces your hand. The interactive lets you inspect six memory types with implementation snippets, compare the libraries head-to-head, and run a decision wizard against your own constraints. By the end you'll be able to say when a memory library earns its place versus building your own — and name exactly what it adds that raw storage never will.",
    scenario: "You need your assistant to remember, across sessions, that a user is vegetarian, prefers metric units, and works in EST — and to recall a fact they mentioned three weeks ago. Your first instinct is to build a custom vector store: embed every message, retrieve top-k each turn. Two sprints in you're hand-rolling TTL expiry, dedup, and conflict resolution (the user changed their timezone) — all of which Mem0 or Zep already handle. The interview question underneath: when do you reach for a memory library versus build your own, and what do these libraries actually do that a raw vector store doesn't?",
    explanation: [
      "The foundations module gave you the base split: **working memory** (the context window — live, finite, expensive) vs **long-term memory** (everything that has to outlive a single call, and must be re-injected to be used). This module is about the *implementation* layer for that long-term side: the production-hardened libraries — **Mem0, Zep, LangMem, MemGPT/Letta**, and custom vector stores — that turn 'long-term memory' from a concept into a running system, plus a finer split *within* long-term memory — episodic vs semantic, introduced in a moment — that the choice of library has to handle well. ==The recurring mistake is spending sprints building a custom memory solution when a library already solves the hard cases — TTL expiry, memory consolidation, conflict resolution — that a naive vector store does not.==\n\nStart from what a *raw* vector store gives you and what it does not. A vector store gives you **semantic retrieval**: embed text, store the vectors, and at query time pull the top-k most similar chunks. That is genuinely useful — it's the substrate under long-term memory. But it is only *storage + similarity search*. It has no notion of *time*, *identity*, *change*, or *importance*.",
      "That gap is exactly where the scenario bites, and it maps to the two long-term memory kinds you must implement differently. **Episodic memory** = specific past events/conversations ('three weeks ago the user asked about X') — inherently *temporal and user-scoped*. **Semantic memory** = distilled durable facts about the user or world ('is vegetarian', 'prefers metric') — inherently *deduplicated and updatable*. A raw vector store stores both as undifferentiated chunks and will happily return a stale timezone next to the corrected one, because it has no mechanism to know one *supersedes* the other.",
      { type: "illustration", label: "Raw vector store vs a memory library — the missing machinery", content: `USER over three weeks says:
  turn 3   "I'm in PST"
  turn 40  "actually I moved, I'm EST now"
  turn 41  "I'm vegetarian"        turn 60  "I'm vegetarian"  (again)

RAW VECTOR STORE (embed + top-k):
  stores 4 chunks. Query 'what timezone?' -> returns BOTH
  "I'm in PST" and "I'm EST now" ranked by similarity.
  Stores the vegetarian fact TWICE. No idea one supersedes another.
     missing: time awareness, conflict resolution, dedup, TTL, importance

MEMORY LIBRARY (Mem0 / Zep / LangMem):
  extract fact  -> {timezone: EST}   (CONFLICT RESOLUTION: EST supersedes PST)
  extract fact  -> {diet: vegetarian} stored ONCE  (DEDUP / consolidation)
  each memory carries a timestamp + recency + importance (EPISODIC ordering)
  stale/low-value memories expire  (TTL) so the store doesn't bloat
     you get: an evolving USER PROFILE, not a pile of raw chunks` },
      "So what do the libraries actually *do* on top of a vector store? Four things a naive store lacks. **(1) Extraction / consolidation** — they distill raw turns into structured memories ('user is vegetarian') rather than storing every message verbatim, so the store holds facts, not transcript. **(2) Conflict resolution** — when a new fact contradicts an old one (EST supersedes PST), they update rather than accumulate. **(3) Temporal awareness** — memories carry timestamps and recency so 'three weeks ago' is answerable and recent facts can outrank old ones. **(4) Lifecycle / TTL + importance** — low-value or stale memories expire so the store doesn't bloat and retrieval stays sharp. These are the exact features you found yourself hand-rolling.",
      "The libraries differentiate on positioning, and knowing the shape matters more than memorising the table. **Mem0** and **LangMem** are the go-to for cross-session recall and user personalization — extract-and-consolidate a durable user profile with minimal setup. **Zep** adds a temporal knowledge-graph model, strong when relationships between facts and time ordering matter. **MemGPT/Letta** take a different tack: an OS-inspired architecture that treats the context window like paged memory, *itself* deciding what to swap in and out of the limited context — memory-management-as-agent, useful when you want the system to autonomously manage its own working set. A **custom vector store** remains the right call when you need access control, custom metadata filtering, or scale beyond what the managed libraries support.",
      "The decision procedure is a short escalation, and it's the answer to the interview question. **Start with in-context only** — no memory library at all — because it's the cheapest and simplest, and many agents never need more. **Add Mem0 or LangMem** the moment you need cross-session recall or per-user personalization (the scenario's vegetarian/metric/EST profile is exactly this trigger). **Reach for Zep** when temporal/relational structure between facts is load-bearing. **Roll your own vector store only** when access control, bespoke metadata filtering, or scale forces it. ==Building custom *first* is the anti-pattern — you'll re-implement TTL, dedup, and conflict resolution badly.==",
      "The trap to avoid is over-engineering memory before you've felt the pain that justifies it — and its mirror, under-engineering by dumping everything into a raw vector store and being surprised when it returns contradictory or stale facts. Library choice also shapes architecture in ways that are hard to refactor later (a temporal knowledge graph vs a flat profile store are not swap-compatible), so the choice deserves real thought at design time. The interactive lets you inspect the six memory types with implementation snippets, compare the libraries head-to-head, and run a decision wizard against your own session-length / personalization / latency constraints — use the wizard to pressure-test that escalation ladder on a concrete use case.",
    ],
    keyPoints: [
      "**This is the implementation layer for the memory types:** the production libraries (Mem0, Zep, LangMem, MemGPT/Letta) + custom vector stores that realise long-term memory. The anti-pattern is hand-building what a library already solves.",
      "**A raw vector store is only storage + similarity search** — it has no notion of time, identity, change, or importance, so it returns stale facts (old timezone) next to corrected ones and stores duplicates.",
      "**Libraries add four things a naive store lacks:** extraction/consolidation (facts not transcript), conflict resolution (new supersedes old), temporal awareness (timestamps/recency), and lifecycle/TTL + importance (expiry so the store doesn't bloat).",
      "**Episodic (temporal, event-specific) and semantic (durable, deduplicated facts) memory need different handling** — the reason a flat vector store fails at both simultaneously.",
      "**Positioning:** Mem0/LangMem = cross-session recall + personalization; Zep = temporal knowledge-graph relations; MemGPT/Letta = OS-style context paging (memory-management-as-agent); custom store = access control / metadata / scale.",
      "**Escalation, not custom-first:** start in-context only → add Mem0/LangMem for cross-session recall → Zep for temporal/relational structure → custom vector store only for access control, metadata filtering, or scale.",
    ],
    recap: [
      "**Memory libraries are the implementation layer** for long-term memory — don't hand-roll what they already solve.",
      "**A raw vector store = storage + top-k only:** no time, identity, change, or importance — returns stale/duplicate facts.",
      "**Libraries add extraction, conflict resolution, temporal awareness, and TTL/importance** — an evolving profile, not a chunk pile.",
      "**Match the tool:** Mem0/LangMem for recall + personalization, Zep for temporal graphs, MemGPT/Letta for context paging, custom for access/scale.",
      "**Escalate, don't over-build:** in-context → Mem0/LangMem → Zep → custom store only when forced — custom-first is the anti-pattern.",
    ],
    mcqs: [
      {
        question: "You store every user message in a raw vector store and retrieve top-k each turn. A user who said 'I'm in PST' three weeks ago later says 'I moved, I'm EST now.' When you query 'what's the user's timezone?', the store returns both. Which two statements correctly explain why, and what memory libraries like Mem0/Zep add to fix it?",
        options: [
          "A raw vector store has no notion of time or conflict, so it returns both the old and the new fact by similarity",
          "Libraries add extraction and conflict resolution atop raw storage, so a new fact like EST supersedes the old PST one",
          "The embedding model currently in use is undersized; a larger embedding model would return only the corrected timezone",
          "Retrieval's top-k value was set far too high; dropping it all the way down to one permanently fixes every staleness case",
        ],
        correct: [0, 1],
        explanation: "Options 1 and 2 are both correct: a raw vector store gives you semantic retrieval but no notion of time, identity, change, or importance, so both the old and corrected timezone are stored and returned by similarity (option 1). Memory libraries sit on top and add exactly the missing machinery — they extract structured facts, resolve conflicts so a new fact supersedes an old one, attach timestamps/recency, and expire stale entries, turning a pile of chunks into an evolving profile (option 2). Option 3 is wrong because a bigger embedding model improves similarity quality, not the absence of conflict resolution; both facts are semantically relevant, so it would still return both. Option 4 is wrong because k=1 might return either the stale or the fresh fact by similarity — it has no way to know which supersedes — so it doesn't fix staleness reliably.",
      },
      {
        question: "For a new assistant that needs cross-session personalization (remember a user is vegetarian, prefers metric), what does the recommended escalation say about reaching for a memory solution?",
        options: [
          "Start in-context only, then add Mem0 or LangMem for recall, then Zep for temporal structure, custom store last",
          "Always reach for a custom vector store first, since every managed library is little more than a toy by comparison",
          "Fine-tune the user's stated preferences straight into the model weights for zero-latency recall on every later turn",
          "Default to MemGPT or Letta for every single case, since its context-paging design outranks the other approaches always",
        ],
        correct: 0,
        explanation: "Option 1 is correct: the escalation is start simple and add capability only when a real need appears — in-context first (cheapest), then Mem0/LangMem for cross-session recall and personalization (exactly this use case's trigger), then Zep when temporal/relational structure is load-bearing, and a custom vector store only when access control, bespoke metadata filtering, or scale forces it. Option 2 inverts the guidance: building custom first is the anti-pattern because you re-implement TTL, dedup, and conflict resolution badly. Option 3 is wrong because per-user preferences change and are session/user-scoped; fine-tuning bakes static info into weights and can't be updated per user or per turn. Option 4 is wrong because MemGPT/Letta targets autonomous context paging, not a universal answer; matching the tool to the need (here, personalization) points at Mem0/LangMem, not paging.",
      },
    ],
    takeaway: "Memory libraries are the implementation layer for long-term memory: a raw vector store is only storage + top-k similarity, with no sense of time, identity, change, or importance, so it returns stale and duplicate facts. Mem0/Zep/LangMem/MemGPT add extraction, conflict resolution, temporal awareness, and TTL. Escalate rather than over-build — in-context → Mem0/LangMem for recall+personalization → Zep for temporal graphs → custom store only for access control or scale.",
  },
};
